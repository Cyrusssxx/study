# 408-quiz 细节总结

> 聚焦"为什么这么做 / 踩过什么坑 / 怎么验证"，供后续迭代快速对齐。
> 关联：`项目总结.md`（架构全貌）、`记忆总结.md`（约定与坑）、`最近改动总结_20260829.md`（按提交流水）。

---

## A. 数据单源与序列化（最容易炸的点）

### A.1 真源分层
- 题库/笔记/导图/打卡表**只**在 `pwa/data/`；`data/questions/*` 是 `tools/sync_questions.py` 从真源生成的构建产物，**已 .gitignore，禁止手改**。
- 上游管线（parse_pdf / add_chapters / fmt_code_questions 等）直接写 `pwa/data`。改数据前先确认你动的是真源不是镜像。

### A.2 序列化格式逐文件不同（血泪）
改 JSON **绝不能**随手 `json.dump` 重写——每个文件缩进/换行/编码约定不同，整库 diff 会爆炸。
- `algo_notes.json`：`indent=1`
- `ds_daka.json`：`indent=2` + **CRLF** + **无尾换行**
- 分隔符统一 `separators=(',', ': ')`

**正确做法**：先 `json.loads` 读入 → 改内存对象 → **只改目标字段** → 用该文件原始参数 `json.dumps` 后字节回比原文件，确认只有预期行变化再写回。本次打卡表纯图化（`6922ade`）即如此：剥掉 `content`/`answer` 字段后，用 `indent=2`+CRLF 重序列化，字节从 150113 → 36621，仅删字段、不动其它排版。

---

## B. Service Worker 三层缓存（离线部署的生命线）

### B.1 为什么是三层
| 层 | 触发更新 | 风险 |
|---|---|---|
| APP（外壳） | 页面/JS/CSS 变动 | 低频 |
| DATA（数据） | 题库/笔记变动 | 中频 |
| LAZY（懒加载图+algo_notes） | 仅图/讲义变动 | **改它时前两层哈希都不变** |

**致命坑**：懒加载资源不在 APP/DATA 清单里。若 fetch 路由先判 APP/DATA 命名空间，懒加载资源会永久命中旧缓存——**你修的图永远到不了用户设备**。本次（`58b7a14`）正是发现 `algo_notes.json` + 5 个 figs 目录（990 文件/90MB）游离在两层之外，才加 `LAZY_PREFIXES` + `LAZY_VER` 第三层。

### B.2 路由判定顺序
懒加载前缀必须用 `path.includes('/'+prefix)` **优先判定**，且前缀不能用 `endsWith`（路径可能是 `/data/ds_figs/x.jpg`，`endsWith('ds_figs/')` 会漏）。`activate` 阶段清 `quiz408-lazy-*` 旧命名空间。

### B.3 加新懒加载资源
`sw.js` 的 `LAZY_PREFIXES` 加一行（文件写全路径、目录写带尾斜杠），`build_sw.py` 自动展开目录并纳入哈希。**不要手改版本号**。

---

## C. 打卡表纯图化（2026-08-29，commit `6922ade`）

### C.1 决策与理由
用户："数据结构打卡表上的题目和答案也可以删了，有教材图片。"

- 删前校验：62 题**无一例外**都有 `figs.content` + `figs.answer`（无图题 0），删文字不会变空白卡。
- 删掉全部 `content`/`answer` 文本（61273 字），仅留 `priority`/`source`/`kaodian`/`tasks`/`figs`。体积 150→36 KB（-75.6%）。
- 图片 162 张 6.9 MB 全在 `daka_figs/`，未动；`warmFigureCache` 照旧预热，离线不破图。

### C.2 渲染兼容改造（`daka.html` `renderCard`）
- **旧**：无文字时套「（解答见教材）」占位 → 纯噪声（图才是内容）。
- **新**：文字为空时不再塞占位，直接渲染教材原图；**图+文字都缺才给占位兜底**（防异常数据空白卡）。
- 旧数据若带回 `content`/`answer` 仍兼容：文字排在图前。

### C.3 连带影响（已处理）
- `tools/build_ds_code.py` 靠 `ds_daka.json` 题干文字做关键词匹配生成 `ds_code.json`，删文字后必然落空。原报错「未找到 2009 代码题」会误导成漏题 → 改为直接点明真实原因（`a230395`）。已验证脚本 exit 1 早退、**不写** `ds_code.json`。
- `tools/fix_ds_chars.py` 有 `if 'content' in q` 守卫，字段没了自动空转，安全。
- `search.html` 只索引刷题题库、不含打卡表，无影响 → 打卡表题干自此不可搜索/复制，符合"看图打卡"初衷。

### C.4 验证
`_test_daka_render.js`：62 题逐题跑真实 `renderCard`，162 个图片引用全部命中 `daka_figs` 真实文件、占位兜底 0 触发、旧数据兼容路径正常。

---

## D. 收藏清理接口（2026-08-28 ~ 29，`09a5e02` / `62eb514`）

### D.1 两个接口，作用范围不同
| 接口 | 删除范围 | 保留 |
|---|---|---|
| `/api/favorites/reset/<科目>` | `progress` + `wrong` | `favorites` 收藏本身 |
| `/api/favorites/reset-wrong/<科目>` | 仅 `wrong`（错题本） | `progress` 作答记录 + `favorites` 收藏 |

两者共用一段删除循环，但 key 路径不同：`progress` 用 autoIncrement 的 `r.pk`，`wrong`/`favorites`/`notes` 用 `question_id`。**改错是静默的**——删不掉或误删页面都看不出来，所以必须有回归测试。

### D.2 计数口径的坑（用户原话："不是收藏里所有的错题都清理"）
初版计数用了 `include_resolved=true`，导致按钮显示 5 而错题本只显示 3 → 用户以为清理范围不对。
**修正**：计数去掉该参数，与错题本实际显示一致（只看未掌握）。
**刻意的 asymmetry**：计数 = 仅未掌握；删除 = 全部 wrong 行（含已掌握）。原因：若只删未掌握，已掌握的错题会被 30 天复习机制重新加回，清理失效。

### D.3 验证
`_test_fav_wrong_reset.js`（jsdom + fake-indexeddb，29 断言）：覆盖两接口作用范围、key 路径、收藏不被误删。`api()` 返回 `{ok,status,json:async()=>obj}`，**取值必须先 `await (await api(...)).json()`**，直接读属性得 `undefined`。

---

## E. 右下角悬浮按钮避让（2026-08-28，`28f403b`）

### E.1 旧逻辑是死的
旧：`querySelector('.ol-nav-fab')` + `.bg-fab-shift` 类仅在 ≤900px 生效。问题：
1. 笔记页"回到开头"按钮 `.notes-top-btn` **根本不在选择器名单**里；
2. `.bg-fab-shift` 类从未定义桌面效果（写了等于没写）；
3. `querySelector` 忽略 `display:none`，移动端隐藏的按钮仍会被算作碰撞。

### E.2 新逻辑
`pwa/js/background.js` 真实**矩形相交测试**：`FAB_SEL` 名单 + 参考矩形由 CSS 常量（`right:20`/`bottom:20`）反推（**不能读 fab 当前 `bottom`，否则抬过一次再测会误判无碰撞**），`shiftFab()` 实测上移留 `FAB_GAP=12px`；scroll/resize 用 rAF 节流重测。

### E.3 铁律
**新增任何右下角 fixed 按钮，必须补进 `FAB_SEL`**，否则会被 🎨 压住。现有名单：`.notes-top-btn`（回到开头）、`.ol-nav-fab`（☰，≤900px）、`.dati-nav-fab`（☰ 定位）。

### E.4 验证
`_test_bg_fab_shift.js`（jsdom，13 场景）：注意 jsdom 默认 `innerWidth=1024`，**必须在 `beforeParse` 里 redefine**，否则几何断言全错且症状是"该触发的都返回空"；`@media` 不保证按 innerWidth 求值，场景用样式覆盖造。

---

## F. 算法讲义表格修复（2026-08-28，`3d15d17` / `58b7a14`）

- 蓝色链接根因：`.ol-nav-link` 类**从未定义任何 CSS**，继承浏览器默认 `a` 蓝色。重排时仿 os 笔记骨架（notes-header/layout/toc/toc-toggle/content/top-btn）即消除。
- PDF 文本层缺陷：空表头列被丢弃、单元格跨视觉行拆裂 → 表格右移一列。审计 33 段 pipe，**规则**：N-1 短行只会出现在 row 0 → 安全左补一个空 `<th>`。`algo.html` `tableBlock()` 据此左补。
- 用【一休】算法讲义 PDF 还原 3 张损坏表（ch1 复杂度、ch4 例题遍历、ch2 二分模板对比），items 391→380、表格 10→11、原文降级段 3→0。
- 验证 `_test_algo_render.js`（33 断言）：章节卡/标题层级/表格左补/真题例题卡/目录。

---

## G. git 与提交纪律（反复踩的坑）

- **严禁 `git add -A`**：历史 `core.autocrlf=true` + 无 `.gitattributes` 曾让整库显示 M（CRLF 噪声）。已加 `.gitattributes`（`* text=auto eol=lf`），提交一律**显式路径 stage**。
- 远程改 SSH 后沙箱 `.git/refs` 只读，`git status` 误报"领先 N"；**推送成功以 `git ls-remote origin main` 实时查询为准**。
- 每次任务完成默认 commit + push（用户约定），push 后用 `git ls-remote` 核实远程 HEAD 一致再回报。

---

## H. 当前未提交的工作树（非本次文档范围，提醒）

`git status` 显示还有若干**其它会话遗留**的未提交改动，与本次文档互不牵连，本次仅 stage 文档与记忆文件：
- 已改未提交：`CHANGELOG.md`、`add_chapters.py`、若干 `archive/` 脚本删除标记等
- 未跟踪：36 项（含 `data/`/`docs/`/`archive/` 等）

如需清理这些遗留改动，单独告诉我，我会按显式路径逐一确认再提交。
