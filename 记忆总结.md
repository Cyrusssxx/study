# 记忆总结（408-quiz 项目）

> 由 `.workbuddy/memory/MEMORY.md` + 各日更日志（2025-08-25 ~ 2026-08-29）浓缩而成。
> 这是"约定、坑、决策"的可迁移底稿；逐日流水见 `.workbuddy/memory/2026-*.md`。
> 关联文档：`项目总结.md`、`细节总结.md`。

---

## 一、项目定位与真源

- **PWA 为唯一真源**：桌面 exe（PyWebView）已于 2026-08-14 废弃删除，`pwa/` 是 canonical。双端镜像（static/templates）已消除。
- **题库单源**（2026-08-14）：`pwa/data/{cn,co,ds,ds_daka,os}.json` 为真源；`data/questions/*` 改为 `tools/sync_questions.py` 生成的构建产物（.gitignore，禁手改）。上游管线均写 `pwa/data`。
- **笔记单源**（2026-08-14）：`pwa/data/notes/{cn,co,ds,os}_notes.json` 为真源；原 `data/notes/` 归档至 `archive/notes_source/`（其中 `cn_notes.json` 两边语义完全不同，先归档保留未硬删）。
- **打卡表纯图化**（2026-08-29，`6922ade`）：`ds_daka.json` 62 题文字字段已删，仅留元数据 + `figs`；`daka_figs/` 162 图未动。改 `daka.html`/`ds_daka.json` 必跑 `_test_daka_render.js`。

---

## 二、不可踩的坑（硬约束）

### 2.1 序列化格式逐文件不同
改 JSON **绝不能**随手重序列化——每个文件缩进/换行/编码约定不同，整库 diff 爆炸。
- `algo_notes.json` = `indent=1`
- `ds_daka.json` = `indent=2` + **CRLF** + 无尾换行
- 分隔符统一 `separators=(',', ': ')`
- 正确姿势：`json.loads` → 改内存对象 → 只改目标字段 → 字节回比确认再写回。

### 2.2 SW 三层缓存
- `APP_VER`（外壳，644KB）/ `DATA_VER`（数据，5.2MB）/ `LAZY_VER`（懒加载图+algo_notes，90MB），由 `tools/build_sw.py` 按内容 SHA-256 自动注入，**不手改版本号**；`.githooks/pre-commit` 提交前自动重建。
- 懒加载资源不在 APP/DATA 清单，**改它时前两层哈希都不变**；fetch 路由对懒加载前缀用 `path.includes('/'+prefix)` **优先判定**（不能 `endsWith`）。新增懒加载资源在 `LAZY_PREFIXES` 加一行。

### 2.3 git 纪律
- **严禁 `git add -A`**：历史 CRLF 噪声（`core.autocrlf` + 无 `.gitattributes`），已加 `.gitattributes`（`* text=auto eol=lf`），提交一律**显式路径 stage**。
- 远程改 SSH 后沙箱 `.git/refs` 只读，`git status` 误报"领先 N"；**推送成功以 `git ls-remote origin main` 实时查询为准**。
- `NUL` 是 Windows 空设备误报（之前 `test -e NUL` 永远"存在"），磁盘上无真实 NUL 文件，无需处理。

### 2.4 题库 number/id 全局连续唯一
插入/删除题后必须把插入点之后所有题的 `number` 与 `id` **同步顺延**，否则与后续章节题号冲突（曾因只顺延单节导致 2.4 死锁节 272/273 撞号，后改全库 `number>=插入点` 统一 +N）。

### 2.5 教材核对资源
王道 2027 四科 PDF 在 `D:\ai code\408教材`；PDF 文本/表格提取用托管 `pymupdf`（避免沙箱 venv 建不全，直接对托管 python 装包）。

---

## 三、前端交互约定

### 3.1 右下角悬浮按钮避让（`pwa/js/background.js`）
- 真实**矩形相交测试**：`FAB_SEL` 名单 + 参考矩形由 CSS 常量（`right:20`/`bottom:20`）反推（**不能读 fab 当前 `bottom`**，否则抬过一次再测误判无碰撞），`shiftFab()` 实测上移留 `FAB_GAP=12px`；scroll/resize 用 rAF 节流。
- **铁律**：新增任何右下角 fixed 按钮，必须补进 `FAB_SEL`，否则被 🎨 压住。现有：`.notes-top-btn`（回到开头）、`.ol-nav-fab`（☰，≤900px）、`.dati-nav-fab`（☰ 定位）。
- 验证 `_test_bg_fab_shift.js`（13 场景，jsdom）：`innerWidth` 须在 `beforeParse` redefine，否则几何断言全错。

### 3.2 各页右下角按钮
笔记/讲义页 `.notes-top-btn`「回到开头」（滚动 >400px 才出现）；导图/刷题/打卡页 `.ol-nav-fab`「☰」（仅 ≤900px）；大题页 `.dati-nav-fab`「☰ 定位」。

---

## 四、后端接口与回归测试

### 4.1 收藏清理两接口（`pwa/js/backend.js`）
| 接口 | 删除 | 保留 |
|---|---|---|
| `/api/favorites/reset/<科目>` | `progress` + `wrong` | `favorites` |
| `/api/favorites/reset-wrong/<科目>` | 仅 `wrong` | `progress` + `favorites` |

- 共用删除循环但 key 路径不同：`progress` 用 `r.pk`（autoIncrement），`wrong`/`favorites`/`notes` 用 `question_id`。**静默删不掉/误删**，必须回归。
- 计数口径：计数 = 仅未掌握（与错题本一致）；删除 = 全部 wrong 行（含已掌握，否则被 30 天复习机制重加）。
- 验证 `_test_fav_wrong_reset.js`（29 断言，jsdom + fake-indexeddb）：`api()` 取值必须先 `await (await api(...)).json()`。

### 4.2 讲义版式回归
`_test_algo_render.js`（33 断言，node + stub DOM，`eval()` 跑 `algo.html` 内联脚本）：章节卡/标题层级/表格左补/真题例题卡/目录。

### 4.3 打卡表回归
`_test_daka_render.js`：62 题逐题跑真实 `renderCard`，图片命中 + 占位兜底计数。

---

## 五、仓库结构约定

- 根目录 `.py` 仅保留 13 个活跃管线脚本（config/parse_pdf/add_chapters/generate_answers/opt_os_format/extract_daka/extract_daka_figs/fmt_code_questions/validate_js/dump_questions/fill_missing_answers/map_sections/add_images_only）。
- 一次性/QA 脚本归档：`archive/scripts/`（93）、`archive/scratch/`（`_*_*.py` + 调试产物）、`archive/backups_questions/`。找回旧脚本去 archive/ 翻。
- `tools/`：build.py / build_sw.py / build_meta.py / build_ds_code.py / sync_questions.py / *_verify.py / extract_*_figs.py / notes_to_map.py / fix_ds_chars.py / _ds_lib.py 等构建与校验脚本。

---

## 六、逐日关键节点（摘要）

| 日期 | 关键产出 |
|---|---|
| 2025-08-25 | 坏图修复 + 题图点击放大 |
| 2026-08-14 | 废弃 exe、PWA 设唯一真源；题库/笔记单源；单源 BUILD 期哈希；pre-commit 钩子；OS 答案全量核对 |
| 2026-08-17 | OS/DS/CN 答案复核；笔记细粒度跳转；图表补全（v2 提取）；知乎代码真题抓取 |
| 2026-08-18 | 18 道代码真题落地 + 10 轮残损/导航/排版修复；打卡页图慢空白修复 |
| 2026-08-20 | OS PV 总结 + 思维导图（os_map）+ 卡片流改造 + 首页性能 |
| 2026-08-21 | 自定义快捷键（鼠标侧键）、侧栏收起、三级目录全局推广、示意图升 SVG |
| 2026-08-22 | 全站页面状态持久化（刷新回到上次题 / 打卡进度） |
| 2026-08-23 | （日志较空，小修） |
| 2026-08-24 | 综合打磨轮 |
| 2026-08-25 | OS 题库 580 题勘误；笔记必看图（21 张王道原图）贴正文旁；磁盘调度对比表修复 |
| 2026-08-28 | 算法讲义重排 + 表格修复 + SW 第三缓存层 |
| 2026-08-29 | 🎨 撞位修复；清空收藏错题 + 计数口径修正；打卡表纯图化 |

---

## 七、待办 / 开放项

- `archive/notes_source/` 中 `cn_notes.json` 两边语义完全不同（data 32 段 vs pwa 116 段、0 重叠），若确认 pwa 为最终版可硬删该归档。
- 工作树仍有其它会话遗留的未提交改动（`CHANGELOG.md`、`add_chapters.py`、`archive/` 脚本删除标记、未跟踪 36 项），与文档改动互不牵连，需单独确认提交。
- 考虑把"jsdom + fake-indexeddb 在 node 跑 PWA 后端"的测试范式沉淀为可复用 skill（已在 MEMORY 记录，本次未建）。
