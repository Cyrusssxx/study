# -*- coding: utf-8 -*-
"""
enrich_os_notes.py — OS 线上考点笔记「题库考点补充」块详化
把 os_notes.json 里过于简洁的补充块替换为"通俗原理 + 机制 + 例题"详细版。
用法: python tools/enrich_os_notes.py [--dry]
"""
import json
import sys

PATH = "pwa/data/notes/os_notes.json"
NEW = {}

NEW["1.1.3 操作系统的特征"] = """<h4>📌 题库考点补充</h4><ul><li><b>并发 vs 并行（为什么区分的核心）</b>：单 CPU 同一<b>瞬间</b>只能执行一个程序，但 OS 让多个程序<b>交替执行</b>——<b>并发</b> = “同一<b>时间间隔</b>内（宏观上）有多个程序都在推进”（微观轮流）；<b>并行</b> = “同一<b>时刻</b>真的同时在执行”（需要多 CPU/多核）（Q5）。</li><li>所以并发是“<b>逻辑上同时</b>”，并行是“<b>物理上同时</b>”；单核能并发不能并行。</li></ul>"""

NEW["2.2.6 多处理机调度"] = """<h4>📌 题库考点补充</h4><ul><li><b>多处理机调度为什么多了两个目标（原理）</b>：多个 CPU 时，除了单机的周转/响应目标，还要让各 CPU<b>忙得均匀</b>（<b>负载平衡</b>——否则有的核闲着有的堵死），以及尽量让进程<b>在同一个核上接着跑</b>（<b>处理器亲和性</b>——利用该核 Cache 中的现场数据，换核要重新加载 Cache，更慢）（Q195）。</li><li>两者要平衡：完全负载均衡可能牺牲亲和性。</li></ul>"""

NEW["2.1.6 进程的通信"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么进程不能直接访问对方地址空间（原理）</b>：进程有<b>独立的虚拟地址空间</b>，一个进程的地址在另一个进程里<b>可能是空的或指向别处</b>——直接访问会破坏隔离（一个进程崩溃不该弄坏别的进程）→ 必须通过 OS 提供的<b>通信机制</b>（管道/消息/共享存储/信号量等）中介（Q88）。</li><li><b>全局变量为什么不跨进程</b>：每个进程有自己的地址空间副本，改一个进程的全局变量<b>不影响其它进程</b>（那是“全局”于本进程，不全局于系统；线程才共享全局变量）（Q128）。</li></ul>"""

NEW["2.2.2 进程调度"] = """<h4>📌 题库考点补充</h4><ul><li><b>三级调度各管什么（为什么分三级）</b>：<ul><li><b>高级调度（作业调度）</b>：把外存的后备作业<b>调入内存并创建进程</b>、放入就绪队列（决定“谁进入系统”）（Q161）；</li><li><b>中级调度（内存调度）</b>：把暂时不运行的进程<b>挂起到外存</b>、必要时再调回——目的 = <b>节省内存</b>、缓解资源紧张（Q160）；</li><li><b>低级调度（进程调度）</b>：从就绪队列选一个上 CPU（最频繁）。</li></ul></li></ul>"""

NEW["5.2.1 设备独立性软件"] = """<h4>📌 题库考点补充</h4><ul><li><b>设备独立性是什么（为什么重要）</b>：用户（应用）只用<b>逻辑设备名</b>（如“打印机”抽象名），不直接指定物理设备；OS 负责<b>逻辑设备 → 物理设备</b>的映射（Q571、Q607、Q613 2020）。</li><li><b>好处</b>：换了物理设备（如换型号、换厂家）<b>只要换设备驱动程序、应用不用改</b>——用户程序与硬件解耦，也便于设备分配（用逻辑名分配）。</li></ul>"""

NEW["5.2.6 I/O操作举例"] = """<h4>📌 题库考点补充</h4><ul><li><b>中断服务程序的执行顺序（为什么）</b>：<b>关中断</b>（防止处理中被新中断打断、现场被破坏）→ <b>保护现场</b>（保存寄存器等——断点 PC/PSW 已由硬件在响应时保存，软件再保存其余）→ <b>中断事件处理</b>（真正干活）→ <b>恢复现场</b>（还原寄存器）→ <b>中断返回</b>（回到被中断的地方继续）（Q652 2010）。</li><li>注意：<b>“保存断点（PC）”由硬件（中断隐指令）完成</b>，服务程序软件部分只保存通用寄存器等其余现场。</li></ul>"""

NEW["4.3.3 虚拟文件系统（VFS）"] = """<h4>📌 题库考点补充</h4><ul><li><b>VFS 解决什么问题（为什么需要）</b>：一台机器可能同时挂载 ext4、NTFS、FAT、网络文件系统等<b>多种文件系统</b>，接口各不相同 → VFS 在它们之上统一抽象出<b>四个对象：超级块（文件系统）、索引节点（文件）、目录项、文件对象</b>，让系统调用（open/read…）<b>用同一套接口访问任何文件系统</b>（本地+网络，Q538、Q544 2025）。</li><li>相当于“文件系统的系统调用接口层”，具体差异由下层各文件系统实现。</li></ul>"""

NEW["4.1.3 文件的逻辑结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>索引文件的访问（为什么）</b>：文件记录<b>不按顺序存放</b>，要按关键字查到某条记录必须先<b>查索引表</b>（索引项=关键字+记录指针）拿到记录的位置，再读记录——所以“直接读记录”<b>必须先查索引表</b>（Q455）。</li><li><b>顺序文件＋索引 = 索引顺序文件</b>：先按关键字建<b>稀疏索引</b>（每组一条），查找 = 先折半/顺序查索引定位到小组，再组内顺序找 → 10000 条记录、最好分组 √10000=100 → 平均 <b>100 次</b>（Q456）。</li></ul>"""

NEW["4.2.3 目录结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>目录检索为什么和“盘块数”有关（原理）</b>：查找文件 = 逐个读目录项 FCB 比较文件名。FCB 64B、每盘块 1KB → 每块装 16 个 FCB；3200 项 → <b>3200/16 = 200 块</b>，顺序线性检索平均要读 <b>100 块</b>（Q465）。</li><li><b>用 inode 就快（为什么）</b>：inode 16B/1KB → 每块 64 个；但是!! 目录中放的是<b>文件名+inode 号</b>（不是整个 inode 拷进目录），检索只需比较短文件名 → 3200 项适合同理算 <b>26 次</b>（Q466）——把大而全的 FCB 拆成“小目录项+inode”，检索 I/O 量大幅下降（见 4.2.2）。</li></ul>"""

NEW["1.1.1 操作系统的概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>OS 管什么、不管什么（边界）</b>：OS 管理<b>计算机资源</b>——硬件资源（CPU、内存、I/O 设备）＋软件资源（<b>文件</b>等）（Q1）；<b>不是</b>OS 管理对象：<b>源程序内容</b>（那是用户文件的数据，OS 只管它的存储与管理，不解释内容）和<b>编译器</b>（那是应用程序，Q2、Q3）。</li><li>OS 基本功能 = <b>控制与管理系统内各种资源</b>（正确处理多个程序的资源竞争）（Q4）。</li></ul>"""

NEW["1.2.4 实时操作系统"] = """<h4>📌 题库考点补充</h4><ul><li><b>实时系统为什么“不追求资源利用率”（原理）</b>：它的第一目标是<b>在规定时间内处理完外部事件</b>（硬实时必须在截止期内、软实时尽量）——为保证“一定响应得了”，往往<b>预留资源</b>、宁可机器闲一些，因此资源利用率反而不是优化目标（Q16、Q17、Q26）。</li><li>应用：航空订票（对响应时间有硬要求）、机床控制、股票交易（Q18）；区分：分时追求公平响应、实时追求按时响应。</li></ul>"""

NEW["5.2.3 设备分配与回收"] = """<h4>📌 题库考点补充</h4><ul><li><b>独占设备为什么静态分配（原理）</b>：独占设备（打印机）<b>一次只能被一个进程用</b>，如果动态分配、中途出事会乱 → <b>在作业/进程运行的开始就一次分配、结束才回收</b>（静态分配，Q599）；</li><li><b>共享设备（磁盘）动态分配</b>：多个进程可轮流/同时访问，按需申请即可。</li><li><b>分配算法</b>：FCFS、最高优先级（Q578）；分配时要考虑：设备类型、进程权限、设备占用状态、逻辑→物理映射（Q615 2023）。</li></ul>"""

NEW["2.5.2 死锁预防"] = """<h4>📌 题库考点补充</h4><ul><li><b>预防 = 破坏 4 个必要条件中的 1 个（为什么）</b>：死锁 4 条件（互斥、占有并等待、不可剥夺、循环等待），只要<b>破除其一</b>就不会死锁：<ul><li>破坏“<b>占有并等待</b>”→ <b>一次分配</b>（申请时把所有要的资源一次拿齐，否则一个都不拿，Q277、Q281）；</li><li>破坏“<b>循环等待</b>”→ <b>资源有序分配</b>（给资源编号，只能按递增序申请，谁也不会互相等成环，Q280、Q287、Q295）；</li><li>破坏“<b>不可剥夺</b>”→ 资源可剥夺（进程占有的资源可被系统强行收回，Q286）；</li><li><b>“互斥”最难破坏</b>——很多资源天生不能同时共享（打印机），只能在技术上模拟（Q290）。</li></ul></li></ul>"""

NEW["3.2.7 内存映射文件"] = """<h4>📌 题库考点补充</h4><ul><li><b>内存映射文件的原理（为什么高效）</b>：把磁盘文件的内容<b>映射到进程的虚拟地址空间</b>，对文件的操作变成<b>对内存的读写</b>（读写按需换页调入，不再用 read/write 系统调用）；修改内存 OS 会自动<b>写回文件</b>（Q426、Q451 2025）。</li><li><b>多进程映射同一文件 → 天然共享内存 IPC</b>（多个进程看到同一份内容，可互斥协作）。</li><li><b>最少页框数由指令寻址方式决定</b>：一条指令及其操作数可能跨多个页，出错时要在页框内存放“指令+立即数/间接地址涉及的页” → 取决于寻址方式（Q450 2025）。</li></ul>"""

NEW["4.1.1 文件的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>逻辑结构 vs 物理结构（关键区分）</b>：<b>逻辑结构</b> = 用户（程序）看到的组织形式：无结构（流式，字节流）或有结构（顺序/索引/索引顺序文件，按记录），让用户方便访问（Q453）；<b>物理结构</b> = OS 在磁盘上的实际存放方式：连续/链接/索引，<b>由 OS 决定、对用户透明</b>（Q454、Q476）——同一个逻辑文件可对应不同物理组织。</li><li>UNIX 把 <b>I/O 设备当“特殊文件”</b>：统一了设备和文件的接口（Q452）。</li></ul>"""

NEW["1.2.3 分时操作系统"] = """<h4>📌 题库考点补充</h4><ul><li><b>分时系统的核心（为什么时间片）</b>：把 CPU 时间切成<b>时间片</b>轮流分给每个用户（进程）——每个用户都感觉自己独占机器（Q19、Q21）。</li><li><b>时间片一定时，用户越多、轮一圈越久 → 响应时间越长</b>（Q22）；保证响应 ≤2s、100 个进程 → 时间片 ≤ 2s/100 = <b>20ms</b>（Q23）。</li><li>改善响应：<b>优先级 + 非抢占</b>（高优先级用户进程可插队，Q20）。</li></ul>"""

NEW["5.3.2 磁盘的管理"] = """<h4>📌 题库考点补充</h4><ul><li><b>三次格式化的区别（为什么越做越高）</b>：<ul><li><b>低级格式化（物理格式化）</b>：把盘面划分成<b>磁道、扇区</b>，并写入扇区控制信息（地址、校验等）（Q625）；</li><li><b>分区</b>：把磁盘分成几个逻辑盘区；</li><li><b>逻辑格式化（高级格式化）</b>：在分区上<b>创建文件系统</b>——写根目录、空闲块数据结构（如位图）等（Q624、Q646）→ <b>引导扇区（自举记录）也是此刻产生的</b>（Q624 2017）。</li></ul></li></ul>"""

NEW["2.3.1 同步与互斥的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么共享变量会出错（原理）</b>：“x=x+1”在机器上其实分<b>读、加、写</b>三步；两个进程同时执行就会<b>交错</b>——可能都读旧值、都写同一个值 → 丢失一次加 1。例：x 初值 1，A 和 B 都做 x±1 → 结果可能 0/1/2（取决于交错顺序，Q266 2011）。</li><li><b>对策 = 互斥</b>：对共享变量的“读改写”操作包成<b>临界区</b>，同一时刻只允许一个进程进入（信号量/锁实现）→ x+=1 与 x+=2 必须互斥（Q267 2016）。</li><li>真题：PV 组合计算 x、y、z=5,12,9（Q263）；x=2 的可行执行序列 2 个（Q270 2018）。</li></ul>"""

NEW["4.2.8 文件共享"] = """<h4>📌 题库考点补充</h4><ul><li><b>硬链接 vs 符号链接（为什么引用计数不一样）</b>：<ul><li><b>硬链接</b>：多个目录项<b>指向同一个 inode</b>（共享文件体），每多一个硬链接 <b>inode 引用计数 +1</b>；删一个链接只是计数 −1，计数为 0 才真正删除文件（Q499、Q500、Q507、Q517）；</li><li><b>符号链接（快捷方式）</b>：目录项存的是<b>指向另一个文件的路径字符串</b>，是一个独立的“薄文件”——<b>引用计数不变</b>；源文件删了符号链接就失效。</li></ul></li><li><b>多进程共享同一文件时，系统打开文件表只有一项</b>（该文件的打开记录全局唯一，每个进程的打开文件描述项再各自指向它，Q520 2020）。</li></ul>"""

NEW["2.1.5 进程控制"] = """<h4>📌 题库考点补充</h4><ul><li><b>创建进程的四步（为什么“分配 CPU”不在其中）</b>：①<b>申请空白 PCB</b>（进程的唯一标志）；②<b>分配内存</b>（装入程序/数据）；③<b>初始化 PCB</b>（填入标识、状态=就绪）；④<b>入就绪队列</b>（Q94、Q133、Q154 2021）——至于<b>谁上 CPU 是调度器的事</b>，创建只保证“可被调度”，不负责立刻运行。</li><li><b>什么时候创建新进程</b>：用户登录、高级调度选择作业、响应用户请求（交互）、程序启动（Q96、Q144 2010）。</li></ul>"""

NEW["5.3.4 固态硬盘"] = """<h4>📌 题库考点补充</h4><ul><li><b>SSD 为什么比磁盘快但有磨损（原理）</b>：SSD 用<b>闪存</b>、无机械部件 → 随机读写都比磁盘快得多；但闪存的存储单元<b>擦写次数有限</b>（会老化）→ <b>易磨损</b>（Q640）。</li><li><b>磨损均衡的目的 = 延长寿命</b>（不是加速！）：把擦写分散到所有块上，避免总写同一块提前报废（Q642）；<b>静态磨损均衡优于动态</b>（连冷数据也参与搬移，更均衡，Q641）。</li><li>文件系统为机械盘和 SSD 都提供的 = <b>确定盘块大小</b>（Q651 2025）。</li></ul>"""

NEW["2.1.3 进程的内存映像"] = """<h4>📌 题库考点补充</h4><ul><li><b>进程映像 = PCB + 程序段 + 数据段</b>；<b>PCB 是进程存在的唯一标志</b>（没有 PCB 就没有进程，Q87、Q97、Q110）：进程 vs 程序根本区别 = <b>动态（有生命周期、在推进）vs 静态（磁盘上的一堆代码，Q89）</b>。</li><li><b>线程相对进程</b>：线程更轻（只含 PC/寄存器/栈等少量状态）→ <b>创建切换开销小、并发度高</b>；但<b>线程不能脱离进程存在</b>（必须属于某进程）；同进程线程<b>共享地址空间</b> → 共享数据时<b>仍需同步互斥</b>（Q121、Q123、Q124）。</li></ul>"""

NEW["4.2.6 文件的物理结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>文件保护靠什么（为什么两层）</b>：访问文件是否被允许 = <b>用户拥有的访问权限 × 文件本身的属性</b>共同决定（Q497、Q503）；保护分<b>系统级（登录注册）/目录级/用户级/文件级</b>四层（Q504）。</li><li><b>权限位设计</b>：权限位 = 用户类别数 × 权限种类数（如 4 类用户×5 种权限 = <b>20 位</b>，Q516 2017）；常用手段：口令、存取控制表（ACL）、用户权限表（Q502）。</li></ul>"""

NEW["1.1.2 操作系统的功能和目标"] = """<h4>📌 题库考点补充</h4><ul><li><b>什么是系统调用（为什么需要）</b>：<b>系统调用 = OS 提供给编程人员的程序接口</b>——用户程序要求 OS 服务（打开文件、申请内存、发消息…）的唯一正规途径（Q7、Q8、Q11 2010），<b>只能通过用户程序间接使用</b>（不能直接调用 OS 内部函数，Q6）。</li><li><b>库函数 ≠ 系统调用</b>：printf 等库函数<b>在用户态执行</b>、可跨平台；<b>并非所有库函数底层都依赖系统调用</b>（如纯计算函数不用）（Q10、Q44）。</li><li>OS 本身是资源管理器，<b>不供用户直接编程</b>（用户编程通过系统调用与库函数），Q9。</li></ul>"""

NEW["3.1.3 基本分页存储管理"] = """<h4>📌 题库考点补充</h4><ul><li><b>分页为什么（原理）</b>：把每个进程的地址空间固定切成<b>相等大小的页</b>、内存切成同样大小的<b>页框</b>，页可以<b>不连续地</b>装入任意页框 → <b>消除外部碎片</b>（只可能剩 <b>页内少量内部碎片</b>，Q347、Q357）。</li><li><b>地址转换</b>：逻辑地址 = 页号+页内偏移 → 查<b>页表</b>（由 OS 建立、驻留内存、首地址存<b>页表基址寄存器</b>）得页框号 → 拼出物理地址；转换由<b>硬件（MMU）</b>完成（Q343、Q348、Q349、Q361、Q366）。</li><li><b>关键</b>：页是“OS 感知”的分配单位（Q347）；主存访问粒度仍是字节/字（Q359）。</li></ul>"""

NEW["4.3.1 文件系统布局"] = """<h4>📌 题库考点补充</h4><ul><li><b>逻辑文件系统的职责</b>：按名存取（目录管理、文件名→文件描述符 fd 的解析）、存储保护（Q528）。</li><li><b>文件数量上限 = inode 数量</b>（每个文件一个 inode；inode 号 n 位 → 最多 2ⁿ 个文件，Q519 2020：4B inode 号 → 2³²）。</li><li><b>引导过程</b>：磁盘第一个扇区 = <b>主引导扇区</b>（MBR 主引导程序 + 分区表 + 结束标志，Q535）；文件区管理目标 = <b>提高存储利用率</b>（Q532）。</li></ul>"""

NEW["3.2.1 虚拟内存的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>虚存为何可行（原理）</b>：程序运行时<b>一段时间只访问一小部分</b>（局部性原理，Q397、Q416）→ 不必全部装入内存，用到的页才调入 → 特征 = <b>多次性、对换性、离散性</b>（非一次性全部装入，Q396、Q398、Q424）。</li><li><b>虚存容量怎么定</b>：<b>由地址位数决定</b>（如 32 位地址最多 4GB 虚空间），实际上受<b>内外存容量之和</b>限制（Q408、Q435 2012）；必须基于<b>离散分配</b>（连续分配无法“部分在内存部分在外存”，Q435）。</li><li>请求分页 vs 基本分页的区别 = <b>不必全部装入</b>、缺页才按需调入（Q398、Q424）。</li></ul>"""

NEW["5.1.3 I/O软件层次结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>四层 I/O 软件（自顶向下）</b>：<b>用户级 I/O</b>（库函数/系统调用接口）→ <b>设备无关软件</b>（通用逻辑：设备名映射、缓冲、分配）→ <b>设备驱动程序</b>（与硬件打交道的细节：写寄存器、发命令）→ <b>中断处理程序</b>（响应设备中断）（Q568 2011、Q569 2012、Q560、Q561）。</li><li><b>分工</b>：<b>翻译系统调用参数、缓冲区管理等通用工作 → 设备无关软件</b>（Q560）；<b>写设备寄存器/磁盘参数计算 → 设备驱动程序</b>（Q561、Q610 2013）；键盘中断处理程序最先拿到输入（Q567 2010）。</li></ul>"""

NEW["2.3.4 信号量"] = """<h4>📌 题库考点补充</h4><ul><li><b>PV 操作（为什么是低级原语）</b>：P/V 是<b>进程通信的原语</b>——原语 = 不可中断地一次做完（要么全做要么不做），否则检查与改变计数值之间被插入别的进程就乱套（Q228、Q230、Q239）。</li><li><b>语义</b>：<b>P（wait）</b>：S 减 1，若 S&lt;0 则<b>阻塞</b>（进等待队列，Q229）；<b>V（signal）</b>：S 加 1，若 S≤0 则唤醒一个等待进程进<b>就绪</b>态（Q232）；<b>互斥信号量初值 1</b>、同步信号量按资源数定（Q233、Q234）；<b>S&lt;0 时 |S| = 等待进程数</b>（Q219、Q241、Q249、Q252、Q264 2010）。</li></ul>"""

NEW["5.2.4 SPOOLing技术（假脱机技术）"] = """<h4>📌 题库考点补充</h4><ul><li><b>SPOOLing 把独占设备“变成”共享（原理）</b>：打印机是<b>独占设备</b>（一次一个进程用），SPOOLing 在磁盘上开<b>输入井/输出井</b>模拟设备——进程打印时<b>先把内容高速写入磁盘输出井</b>（很快），由后台“缓输出程序”再慢慢打印 → 对用户进程来说打印机像“可同时用”的共享设备，<b>提高独占设备利用率</b>（Q589、Q590、Q591、Q593、Q596、Q598、Q612 2016）。</li><li>进程实际“分到”的是<b>外存区（虚拟设备）</b>而非真设备（Q594）；<b>不需要外围计算机</b>（不再是脱机批处理时代，Q592）；本质 = <b>以空间换时间</b>（Q597）＋把独占变共享。</li></ul>"""

NEW["1.3.4 系统调用"] = """<h4>📌 题库考点补充</h4><ul><li><b>系统调用完整过程（为什么用户态→内核态）</b>：①用户程序<b>传参</b>（参数放寄存器/压栈）；②执行<b>陷入（访管）指令</b>——该指令在<b>用户态执行</b>，触发中断 → CPU 切换到<b>内核态</b>；③内核按系统调用号执行对应的<b>服务例程</b>；④返回用户态，继续下一条（Q55 2017、Q62 2022、Q57 2019）。“陷入”是用户态到内核态的<b>唯一合法途径</b>（与 1.3.2 呼应）。</li><li><b>硬件/软件分工</b>：<b>保存断点与 PSW 由硬件</b>（中断隐指令）完成，<b>保存通用寄存器由 OS</b>（软件）完成（Q52 2015、Q58 2020、Q62 2022）；<b>子程序调用不用保存 PSW</b>（同特权级、无模式切换，Q49 2012）。</li></ul>"""

NEW["5.3.1 磁盘"] = """<h4>📌 题库考点补充</h4><ul><li><b>磁盘存取时间三部分里寻道最大（为什么）</b>：<b>寻道时间</b>（磁头移到目标磁道，机械移动、最慢）＞＜b&gt;旋转延迟</b>（等目标扇区转到磁头下）＞<b>传输时间</b>（传送数据）→ 影响最大的 = <b>寻道时间</b>（Q623、Q627），所以磁盘调度算法都围绕<b>减少寻道</b>（Q620）。</li><li><b>读写单位 = 块</b>（磁盘以块为单位与主存交换，Q622）；计算真题：3000 转/分 10 扇区 → 旋转一周 20ms、一个扇区 <b>2ms</b>（Q634）；7200 转/分 160 扇区 → 传输率 <b>9600KB/s</b>（Q635）；磁道访问序列计算（Q631、Q632）。</li></ul>"""

NEW["5.2.5 设备驱动程序接口"] = """<h4>📌 题库考点补充</h4><ul><li><b>设备驱动程序的职责边界</b>：它是<b>唯一直接控制硬件的软件</b>——接收上层命令并转成设备命令、查设备状态、<b>启动设备</b>、处理设备相关中断、磁盘调度、计算磁盘柱面/磁头/扇区号（Q602、Q603、Q604、Q610 2013、Q614 2022）；<b>数据分析与缓冲不是它的活</b>（那是设备无关软件/缓冲管理层，Q603）；键盘中断服务例程把数据放<b>内核缓冲区</b>（Q616 2024）。</li><li><b>为什么与 I/O 控制方式有关</b>：程序控制/中断/DMA 三种方式下驱动要做的事完全不同（Q614 2022）。</li></ul>"""

NEW["2.3.5 经典同步问题"] = """<h4>📌 题库考点补充</h4><ul><li><b>三大经典问题的关键手法（为什么）</b>：<ul><li><b>生产者-消费者</b>：既要<b>互斥</b>（缓冲区同一时刻一人动）又要<b>同步</b>（空了别取、满了别放）；用两个信号量（空位/满位）+ 互斥锁（Q256–Q260）。</li><li><b>读者-写者</b>：读者之间<b>可同时</b>读（读数不冲突）、写者与任何人<b>互斥</b>（Q261）。</li><li><b>哲学家就餐</b>：加一个<b>seat 信号量 = n−1（4）</b>，保证最多 4 人同时拿叉子 → 必有一人能拿到两把 → <b>破坏循环等待防死锁</b>（Q262、Q275、Q306）。</li></ul></li><li><b>管程</b>：把同步机制<b>封装成编程语言结构</b>，内部<b>不止互斥</b>还有条件变量（wait/signal），每次只允许一个进程进入（Q269 2016、Q271 2018、Q244、Q245）。</li></ul>"""

NEW["2.5.1 死锁的概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>避免 vs 预防 vs 检测（为什么分层）</b>：<b>预防</b>=破坏必要条件（资源利用率低）；<b>避免</b>=<b>银行家算法</b>，每次分配前判断是否进入<b>不安全状态</b>，是则不给（Q279、Q302、Q303）——安全状态<b>必无死锁</b>，不安全状态<b>未必</b>死锁（可能碰巧避开，Q291、Q310 2013）；银行家需知道<b>最大需求量</b>、且<b>不限制申请顺序</b>（Q312 2015）。</li><li><b>检测与解除</b>：用<b>资源分配图</b>检测——<b>有环未必死锁</b>（环上资源可被不相关进程释放），<b>单资源类有环必死锁</b>（Q289、Q294）；死锁定理即检测算法（Q300）；解除 = <b>撤销进程 / 剥夺资源</b>（Q285、Q286、Q304、Q315 2019）。</li></ul>"""

NEW["3.2.4 页面置换算法"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么 OPT 最优实现不了（原理）</b>：<b>OPT 淘汰“最远将来才用”的页</b>——需要预知未来访问，现实中不可能，只作理论下界；<b>LRU</b> 用“最近最久未用”近似（基于局部性，不 Belady）；<b>FIFO</b> 先进先出、简单但可能 <b>Belady 异常</b>（页框增多缺页反而增加，Q407、Q423、Q429、Q438）；<b>CLOCK（NRU）</b> 用访问位近似 LRU（Q405、Q441、Q445）。</li><li><b>缺页次数下限 = 不同页号数</b>（每页至少缺一次，Q406）；工作集 = 窗口内最近 k 次访问的<b>去重页集</b>（Q442 2016）；驻留集 ⊆ 内存里的页、工作集 ⊆ 驻留集（Q419）。</li></ul>"""

# ============ 主逻辑 ============
def main():
    dry = "--dry" in sys.argv
    raw = open(PATH, "rb").read()
    d = json.loads(raw.decode("utf-8"))
    done, appended, missing = [], [], []
    for c in d["chapters"]:
        for s in c["sections"]:
            sk = "§" + s["section"].strip()
            if sk in NEW:
                h = s.get("html") or ""
                j = h.find("📌")
                if j >= 0:
                    k = h.rfind("<h4>", 0, j)
                    s["html"] = h[: (k if k >= 0 else j)] + NEW[sk]
                    done.append(sk)
                else:
                    s["html"] = h + NEW[sk]
                    appended.append(sk)
            for ss in s.get("subsections", []):
                sec = ss["section"].strip()
                if sec not in NEW:
                    continue
                h = ss.get("html") or ""
                j = h.find("📌")
                if j >= 0:
                    k = h.rfind("<h4>", 0, j)
                    ss["html"] = h[: (k if k >= 0 else j)] + NEW[sec]
                    done.append(sec)
                else:
                    ss["html"] = h + NEW[sec]
                    appended.append(sec)
    for k in NEW:
        if k not in done and k not in appended:
            missing.append(k)
    print(f"替换 {len(done)} 节, 追加 {len(appended)} 节, 未命中 {len(missing)}")
    if missing:
        for m in missing:
            print("  [未命中]", m)
        if not dry:
            sys.exit(1)
    if dry:
        print("dry-run 结束, 未落盘")
        return
    out = (json.dumps(d, ensure_ascii=False, indent=2) + "\n").encode("utf-8")
    open(PATH, "wb").write(out)
    d2 = json.load(open(PATH, encoding="utf-8"))
    n = 0
    for c in d2["chapters"]:
        for s in c["sections"]:
            if "📌 题库考点补充" in (s.get("html") or ""):
                n += 1
            for ss in s.get("subsections", []):
                if "📌 题库考点补充" in (ss.get("html") or ""):
                    n += 1
    print(f"写入完成, 含补充块的 节+小节 总数 = {n}")

if __name__ == "__main__":
    main()