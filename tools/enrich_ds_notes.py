# -*- coding: utf-8 -*-
"""
enrich_ds_notes.py — 数据结构线上考点笔记「题库考点补充」块详化
把 ds_notes.json 里过于简洁的补充块替换为"通俗原理 + 机制 + 例题"详细版。
支持小节(subsections)与节级(sections, 键名加 § 前缀)。
用法: python tools/enrich_ds_notes.py [--dry]
"""
import json
import sys

PATH = "pwa/data/notes/ds_notes.json"
NEW = {}

NEW["§8.2 插入排序"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么比较次数看“正序程度”（原理）</b>：直接插入排序每趟把第 i 个元素<b>从后往前</b>与前面已有序的 i−1 个元素比较、找到插入位置。若序列已接近正序，每个元素“走两步就到了”，比较很少；恰为逆序时每个元素都要比到底（Q518）。</li><li>结论：<b>序列越接近正序，直接插入越快</b>（最好 O(n)、最坏 O(n²)），这是它“适合基本有序序列”的原因。</li></ul>"""

NEW["2.2.1 顺序表的定义"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么扩容要“整体复制”（原理）</b>：顺序表的元素<b>必须占用一段连续的存储空间</b>，而当前那块空间后面可能已被别的数据占用，无法原地扩大 → 只能<b>另申请一块更大的连续空间（n+m 个单元）</b>，把原 n 个元素<b>整体复制</b>过去，再释放旧空间（Q39）。</li><li>代价：扩容是 O(n) 操作且可能“搬家”，所以顺序表适合“提前估好容量”的场景。</li></ul>"""

NEW["§1.1 数据结构的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么既要存元素也要存关系（原理）</b>：光有“一堆值”无法表达结构——比如所有元素都是 1，但顺序表、树、图它们的意义完全不同。存储结构必须用<b>物理手段</b>（顺序的相邻性、链式的指针、散列的哈希函数）把<b>元素之间的逻辑关系</b>也“装”进去（Q5）。</li><li>例：线性表存“前后继关系”→ 顺序存储用下标相邻、链式存储用 next 指针。</li></ul>"""

NEW["§5.1 树的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>树边数的规律（为什么）</b>：除根外每个结点有且只有<b>一条“父→子”的边</b>把它连上来 → 任意树 <b>边数 = 结点数 − 1</b>（n 结点树有 n−1 条边）。森林是多棵树 → <b>总边数 = 总结点数 − 树的棵数</b>（Q189 2016：15 边、25 结点 → 25−15=<b>10</b> 棵树）。</li></ul>"""

NEW["§6.2 图的存储及基本操作"] = """<h4>📌 题库考点补充</h4><ul><li><b>邻接表各操作的时间复杂度（为什么）</b>：<ul><li><b>建表 O(n+e)</b>：建立 n 个顶点表结点 + 读 e 条边各插入一次（头插 O(1)）→ 总共 O(n+e)（Q334）；</li><li><b>删除某顶点及其关联边 O(n+e)</b>：既要删顶点表、又要扫它的邻接表删边，还可能扫其它顶点表找入边 → O(n+e)（Q335）；</li><li><b>求某顶点入度 O(n+e)</b>：邻接表只存“出边”，求入度要<b>遍历全部边</b>统计指向该顶点的次数 → O(n+e)（Q336）。</li></ul></li></ul>"""

NEW["§1.2 算法和算法评价"] = """<h4>📌 题库考点补充</h4><ul><li><b>O(n²) 的真实含义</b>：表示执行时间<b>随问题规模 n 的增长按 n² 的比例增长</b>（渐进意义），问题规模仍是 n（Q8）；不是“等于 n²”，而是“与 n² 同阶”。</li><li><b>空间 O(1)</b> = 算法所需<b>辅助空间（临时变量等）与问题规模 n 无关</b>（是个常数）——≠“不需要任何空间”（算法运行总要占主存存数据和少量变量，Q9）。</li></ul>"""

NEW["3.3.1 栈在括号匹配中的应用"] = """<h4>📌 题库考点补充</h4><ul><li><b>括号匹配为什么用栈（原理）</b>：匹配规则是“<b>最后遇到的开括号先闭</b>”（后进先出）——遇到左括号入栈，遇到右括号时取栈顶与它配对，正好是栈的特性（Q151 2025）。</li><li><b>栈容量与嵌套深度</b>：n 层嵌套需要栈至少能存 n 个左括号，容量 3 处理不了 4 层嵌套。</li><li>易错：①递归<b>不一定</b>要用栈实现（单向/尾递归可迭代）；②<b>出栈次序不能仅由入栈次序确定</b>（还取决于何时出栈）；③栈是<b>一端</b>操作（不是两端）（Q148 2017）。</li></ul>"""

NEW["5.4.1 树的存储结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>孩子兄弟表示法（为什么找双亲要遍历）</b>：每个结点只存“<b>第一个孩子 + 下一个兄弟</b>”两个指针，沿着“最左孩子→右兄弟”可以遍历整棵树，但<b>没有双亲指针</b> → 找某结点的双亲必须<b>从根开始遍历 O(n)</b>（Q274）。</li><li>二叉链表表示树：<b>左指针为空的结点 = 叶子</b>（没有孩子）（Q270）；由此可推森林与二叉树的相互转换（见 5.4.2）。</li></ul>"""

NEW["8.2.2 折半插入排序"] = """<h4>📌 题库考点补充</h4><ul><li><b>折半插入改进了什么、没改进什么（原理）</b>：插入排序分为<b>“找位”与“移动”</b>两步。折半插入用<b>折半查找</b>找插入位置 → <b>比较次数降到 O(nlog₂n)</b>（且与序列初态无关，总是这么多）；但<b>移动元素</b>的代价没变（还是一个个挪）→ 总时间仍 <b>O(n²)</b>（Q526、Q530 2012）。</li><li>所以它和直接插入<b>只可能在比较次数上不同</b>，时间复杂度同阶。</li></ul>"""

NEW["8.1.1 排序的定义"] = """<h4>📌 题库考点补充</h4><ul><li><b>三个基础概念</b>：<ul><li><b>稳定性</b>：关键字<b>相同</b>的元素排序后<b>相对次序不变</b> → 稳定排序；稳定性是排序算法的性质，<b>与算法优劣无关</b>（快的可能不稳定、慢的可以稳定，Q512、Q513）。</li><li><b>拓扑排序不是“排序”</b>：它是对<b>有向无环图</b>求“线性次序”，对象是顶点关系、不是关键字序列，故<b>不属于内部排序</b>（Q511）。</li><li>链表上也可排序（链式存储的插入/归并等），并非只能数组（Q513）。</li></ul></li></ul>"""

NEW["3.4.2 数组的存储结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>二维数组按行优先的地址公式（为什么）</b>：行优先 = 先把第 0 行存满再存第 1 行。元素 a[i][j] 前面有 <b>i 行 × 列数 + j 个</b>元素，每个占 L 字节 → <code>LOC(i,j) = 起始地址 + (i×列数 + j)×L</code>（Q155：A[8][5]、9×10、L=3 → 8×10+5=85 → SA+85×3=<b>SA+255</b>；Q156、Q166 2021）。</li><li>关键是<b>列数</b>是数组的定义维数，不是 i 下标。</li></ul>"""

NEW["6.2.1 邻接矩阵法"] = """<h4>📌 题库考点补充</h4><ul><li><b>邻接矩阵怎么读出入度（为什么）</b>：A[i][j]=1 表示“i→j 有边”。<b>出度 = 第 i 行</b>（i 发出去几条）、<b>入度 = 第 j 列</b>（指向 j 的几条）；带权图则数“非∞且非 0”的项（Q326、Q327、Q340 2013）。</li><li><b>A² 的妙用</b>：A²[i][j] = 从 i 到 j <b>长度为 2 的路径数</b>（途经一个中间顶点，矩阵乘法累加，Q329）——同理 A^k 为长度为 k 的路径数。</li></ul>"""

NEW["2.2.2 顺序表上基本操作的实现"] = """<h4>📌 题库考点补充</h4><ul><li><b>怎么选存储结构（看要哪些操作）</b>：<ul><li>经常<b>“按序号存取第 i 个元素/前驱后继”</b>、或“按任意序号存取 + 只在末尾插删”→ 选<b>顺序表</b>（随机存取 O(1)，Q32、Q33）；</li><li>经常<b>在中间插删</b> → 选链表（O(1) 但找位置 O(n)）。</li></ul></li><li><b>随机存取 vs 顺序存取</b>：顺序表支持<b>随机存取</b>（按下标直接定位，下标即地址）<b>也支持顺序存取</b>；链表只能<b>顺序存取</b>（必须从头沿指针找，Q41）。</li></ul>"""

NEW["6.2.4 邻接多重表"] = """<h4>📌 题库考点补充</h4><ul><li><b>四种存储结构各用于什么图（为什么）</b>：<ul><li><b>邻接多重表 = 无向图专用</b>：每条边只存一个边结点、被两个顶点共享，避免邻接表的边重复存储，方便删边/求度（Q338）；</li><li><b>十字链表 = 有向图专用</b>：每个边结点同时挂进“出边链表”与“入边链表”，一条边同时表达弧尾、弧头（Q339）；</li><li>邻接表有向/无向都能用；邻接矩阵也都可以。</li></ul></li><li>邻接多重表的度：统计<b>边结点中 ivex/jvex 之一等于该顶点</b>的个数（Q341 2024）。</li></ul>"""

NEW["§3.3 栈和队列的应用"] = """<h4>📌 题库考点补充</h4><ul><li><b>栈的应用为什么“后进先出”合适</b>：<b>递归</b>（后调用的先返回）、<b>表达式求值/括号匹配</b>（运算符与括号的后到先处理）、<b>函数调用</b>（嵌套调用的返回地址后进先出）——都是“最后进入的先生成结果/先结束”（Q132、Q134、Q141）。</li><li><b>队列的应用为什么“先进先出”合适</b>：<b>缓冲区/打印缓冲</b>（先到先服务）、<b>FIFO 页面置换</b>（最早进入的最老先被换出）、<b>层次遍历、BFS</b>（按层/按距离远近依次处理）（Q143 2009）。</li></ul>"""

NEW["2.1.1 线性表的定义"] = """<h4>📌 题库考点补充</h4><ul><li><b>线性表的定义拆解（为什么这么定义）</b>：n 个<b>相同数据类型</b>元素的<b>有限序列</b>——①同类型（才能连续存放/统一处理）；②有限（不能无穷）；③序列（有序性：有先后次序）（Q24、Q25）。</li><li><b>前驱/后继的唯一性</b>：除首元素外每个元素有<b>唯一直接前驱</b>、除尾元素外有<b>唯一直接后继</b>；<b>单元素表</b>的那一个元素既无前驱也无后继（Q26、Q27）。</li><li>注意区分：树/图也有“前后关系”但不是线性次序（每个结点可有多个后继）。</li></ul>"""

NEW["7.2.1 顺序查找"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么顺序查找最通用</b>：它只要求元素<b>可比较</b>，对存储结构无要求——顺序表、链表都可以（Q407、Q488）；有序无序都可以（无序只能用顺序查找）。</li><li><b>ASL = (n+1)/2 为什么与有序无关</b>：等概率下成功查找的平均比较次数只取决于<b>元素所在位置</b>（第 i 个要比较 i 次），与关键字是否有序无关（Q408、Q409）。</li><li>概率不等时 <code>ASL = Σ pᵢ·i</code>（把概率大的放前面更省）（Q410）。</li></ul>"""

NEW["7.3.3 红黑树"] = """<h4>📌 题库考点补充</h4><ul><li><b>红黑树为什么“近似平衡”（原理）</b>：在 BST 基础上给每个结点加红/黑颜色，用<b>五条性质</b>约束：①结点非红即黑；②根黑；③叶（外部结点）黑；④红结点的两个孩子必黑（不能连续两个红）；⑤任一结点到其各叶子的路径含<b>相同数目的黑结点</b>——性质④⑤保证<b>最长路径 ≤ 2×最短路径</b>，高度 ≤ 2log₂(n+1)（Q447）。</li><li><b>插入/删除的调整</b>：通过<b>变色 + 旋转</b>恢复性质；插入默认染红，冲突时看<b>叔结点</b>颜色决定变色还是旋转（叔红→变色；叔黑→旋转，Q451）；<b>删除的调整可能超过 2 次旋转</b>（说“至多 2 次”是错的，Q448）。</li><li><b>与 AVL 比</b>：红黑树“牺牲一点平衡性换更少旋转”→ <b>插入/删除更快但查询略慢</b>（AVL 查询效率一般更高，Q447）；全黑结点 → 满二叉树（Q449）。</li><li>真题：插入 1~7 → <b>3 个红结点</b>（Q450）。</li></ul>"""

NEW["8.3.1 冒泡排序"] = """<h4>📌 题库考点补充</h4><ul><li><b>冒泡为什么“一趟定一个”（原理）</b>：每趟把相邻元素两两比较、大（小）者后移，<b>一趟结束最大（最小）元素像气泡一样“浮”到最末端</b>——每趟至少把一个元素送到最终位置（Q517、Q540）。</li><li><b>比较/交换次数</b>：逆序时每趟都比到底、需 n−1 趟且交换最多；正序时一趟比较 n−1 次即结束（<b>可加标志提前结束</b>，Q534）；比较次数 = 序列的<b>逆序数</b>（升序排倒序的 5 元素 = C(5,2)=15 次，Q535）。</li><li>双向冒泡（来回扫）可减少趟数（Q542）。</li></ul>"""

NEW["8.4.1 简单选择排序"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么比较次数与初态无关（原理）</b>：每趟必须在<b>剩余元素中找最小</b>——不管序列原来怎样，第 1 趟都要比 n−1 次、第 2 趟 n−2 次…总比较次数恒为 <b>n(n−1)/2</b>，与初始次序<b>无关</b>；但<b>移动很少</b>（每趟最多换 1 次，共 O(n)）→ 最坏情况下移动次数最少（比其他 O(n²) 排序强，Q555、Q561、Q615 2025）。</li><li><b>取前 k 个最小</b>用<b>堆排序</b>最快（不用排全序，O(n+k·log₂n)）（Q556）。</li></ul>"""

NEW["1.2.1 算法的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>算法五大特性（为什么是这五个）</b>：<b>有穷性</b>（必须在有限步内结束）、<b>确定性</b>（每步含义明确无歧义）、<b>可行性</b>（能由基本运算实现）、<b>输入</b>（0 个或多个）、<b>输出</b>（至少 1 个）——“可维护性/可读性/可靠性”是<b>好代码</b>的要求，不是算法的定义特性（Q6）。</li><li><b>算法评价</b>：综合<b>时间效率、空间效率</b>＋正确性、可读性、健壮性；时间与空间<b>可互换</b>（“空间换时间”是常见手段，如预计算查表）（Q7）。</li></ul>"""

NEW["4.2.2 串的模式匹配算法——KMP算法"] = """<h4>📌 题库考点补充</h4><ul><li><b>KMP 为什么快（核心原理）</b>：失配时<b>主串指针 i 不回溯</b>，而是把模式串“滑动”到已匹配部分的<b>最长相等前后缀</b>位置继续比（<code>j=next[j]</code>）——已经比较过的字符不重比，时间复杂度从 O(mn) 降到 <b>O(m+n)</b>（Q169、Q170、Q171、Q172、Q177 2015）。</li><li><b>next 数组求法</b>：求模式串每位的“最长相等前后缀长度”整体右移一位、低位补 −1，再全体 +1（Q173）。<b>nextval（改进）</b>再压缩连续相同字符的 next，比较次数更少。</li><li><b>失配滑动距离 = j − nextval[j]</b>（Q176、Q179 2024）；真题：主串 <code>aabaaaba</code>、模式 <code>aaab</code> → KMP <b>9 次</b>、nextval <b>7 次</b>（Q174、Q175）。</li></ul>"""

NEW["3.2.4 双端队列"] = """<h4>📌 题库考点补充</h4><ul><li><b>双端队列为什么分“输入受限/输出受限”（原理）</b>：真正的双端队列两端都能进出、太灵活；考题常退化为<b>一端只能进（输入受限）</b>或<b>一端只能出（输出受限）</b>，两端各“1 种受限”得到 2 种退化形态（Q126），判断某序列是否可由某种受限双端队列得到。</li><li>判断方法：先确定哪端固定、哪端可变，然后手工模拟出入队过程，出现冲突即不可能（Q127 2010：输出受限时 <code>d,b,c,a,e</code> 不可能；Q131 2021 同型）。</li></ul>"""

NEW["3.3.3 栈在递归中的应用"] = """<h4>📌 题库考点补充</h4><ul><li><b>递归为什么用栈（原理）</b>：递归调用是嵌套的——外层的局部变量与返回点要先保存，内层返回后再恢复 → <b>“后调用的先返回”正是栈的后进先出</b>，所以<b>函数局部变量用栈存储</b>（Q140）。</li><li><b>递归一定慢吗？</b>：递归的代价 = <b>重复计算</b>（同一子问题算多次，如朴素斐波那契）+ 栈开销 → 效率<b>低于</b>等价的非递归（Q139）。</li><li><b>消除递归不一定用栈</b>：<b>单向递归/尾递归</b>可直接改成<b>迭代循环</b>（问题规模单调缩小，无需保存现场，Q142）；系统栈自底到顶 = 调用顺序（Q146 2015：main→S(1)→S(0)）。</li></ul>"""

NEW["§6.4 图的应用"] = """<h4>📌 题库考点补充</h4><ul><li><b>拓扑序列存在 ⟺ 无环（为什么）</b>：拓扑排序每次删一个<b>入度为 0</b>的顶点；若有环，环上的顶点入度永远 ≥1 删不掉 → <b>有环则无拓扑序列</b>（Q370、Q374：含顶点数&gt;1 的强连通分量即有环）。</li><li><b>入度为 0 的顶点用栈或队列均可</b>（顺序不同 → 序列可能不同，Q372）；拓扑唯一 ≠ 入度出度≤1，也<b>不唯一确定图</b>（Q372、Q373、Q380）；强连通图（顶点&gt;1）必有环 → 不能拓扑排序（Q373）。</li></ul>"""

NEW["8.2.1 直接插入排序"] = """<h4>📌 题库考点补充</h4><ul><li><b>直接插入的比较次数（为什么）</b>：每趟把第 i 个元素插入前 i−1 个有序元素中：<b>正序</b>时每个只要比 1 次（共 n−1）、<b>逆序</b>时每个都要比到头（共 <b>n(n−1)/2</b>）→ <b>序列基本有序时效率最高 O(n)</b>（Q514、Q515、Q516、Q602）。</li><li><b>“两趟后前 3 个元素局部有序”</b>：插入排序保证前缀有序（Q517）；但最后一趟开始前<b>有的元素可能还没动过</b>（它不是每趟定一个最终位置的排序，Q519）。</li></ul>"""

NEW["§7.2 顺序查找和折半查找"] = """<h4>📌 题库考点补充</h4><ul><li><b>折半查找的前提（为什么）</b>：必须<b>顺序存储＋有序</b>——因为要用下标直接跳到 mid（链表没法 O(1) 取中，静态链表也不行，Q411、Q430 2024、Q488）。</li><li><b>判定树是平衡二叉树</b>：每次取中是折半 → 判定树左右子树高度差 ≤1，且<b>中序即元素顺序</b>（Q413）；查找长度 ≤ ⌈log₂(n+1)⌉（Q418、Q419）。</li><li><b>不是总比顺序查找快</b>：查第一个元素时顺序查找 1 次就中（折半至少 ⌈log₂(n+1)⌉ 次，Q412）；最坏 O(n) 与 BST 最坏不同（Q414、Q421）。</li></ul>"""

NEW["5.2.2 二叉树的存储结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>二叉链表空指针为什么是 n+1（原理）</b>：n 个结点的二叉链表有 <b>2n 个指针域</b>，真正被用（指向孩子）的有 n−1 个（每个非根结点被一个指针指着）→ 空指针 = 2n − (n−1) = <b>n+1</b>（Q207）。推广：三叉链表（加 parent）3n 域、2(n−1) 被用 → 空 n+2（Q208）；三叉树三叉链表 4n 域、3(n−1) 用 → 空 n+3 变体按题算（Q212）。</li><li><b>顺序存储的浪费</b>：必须按<b>完全二叉树的下标规则</b>填充，高度 5 的任意二叉树最坏要 <b>2⁵−1=31</b> 个单元（Q217 2020）；某空结点若“后面还有非空后代”则该数组<b>不能还原</b>成二叉树（Q219 2025）。</li></ul>"""

NEW["5.5.2 并查集"] = """<h4>📌 题库考点补充</h4><ul><li><b>并查集为什么用“双亲表示法”（原理）</b>：它只需要回答“<b>两个元素是否在同一集合</b>”——把每个集合组织成一棵树、<b>根作为集合代表</b>，每个结点只存<b>双亲指针</b>（双亲表示法），判断同集合 = <b>Find 两者的根是否相同</b>（Q291）。</li><li><b>两个操作</b>：<b>Find(x)</b> 沿双亲找根；<b>Union(x,y)</b> 把一棵树的根接上另一棵的根，<b>“小树并大树”</b>（按规模/秩合并）控制树高（Q292、Q293）。</li><li><b>用途</b>：Kruskal 求 MST 判环、判断无向图连通性、动态连通性；<b>最坏 O(n)</b>（链状树、未路径压缩，Q294）。</li></ul>"""

NEW["8.7.5 最佳归并树"] = """<h4>📌 题库考点补充</h4><ul><li><b>最佳归并树是什么（为什么仿哈夫曼）</b>：外部排序的归并趟数固定后看不出差别，但<b>各归并段的长度不同</b>——把短的段先合、长的段后合，总读写量最小 → 就是<b>带权路径和最小的 k 叉树（哈夫曼思想）</b>（Q627）。</li><li><b>k 阶归并树度 k 结点数 = (m−1)/(k−1)</b>（m 个叶/初始段，Q628）；不够满叉时<b>补虚段</b>（权 0 的假叶子）凑满（Q629 2013、Q631 2019：120 段 12 路 → 补 2 个）。</li><li>WPL 计算即归并总代价（Q629：2,3,4,5,6,7 三叉 → <b>46</b>）。</li></ul>"""

NEW["2.3.3 双链表"] = """<h4>📌 题库考点补充</h4><ul><li><b>双链表插删为什么是 4 个指针（原理）</b>：每个结点有 next 和 prior 两个指针，插入/删除要同时维护<b>两个方向的链</b>：q 在 p 前插 → 改 q 的 next、q 的 prior、p 的 prior、p 前驱的 next <b>共 4 个指针域</b>；删除同理（Q55–Q58、Q72 2016）。</li><li><b>关键：先连后断</b>——先让新结点/被删结点的前后指针接好，再断开旧链，<b>防止断链丢失结点</b>（Q56）。</li><li><b>首尾复杂度</b>：循环双链表删尾 O(1)（尾结点 prior 直接定位）；非循环双链表删尾要遍历 <b>O(n)</b>；删首都是 O(1)（Q68）。</li></ul>"""

NEW["§6.3 图的遍历"] = """<h4>📌 题库考点补充</h4><ul><li><b>DFS/BFS 为什么叫这个名字（原理）</b>：<b>DFS 深挖</b>——沿着一条路走到黑再回头（栈/递归实现，类似树先序）；<b>BFS 层层扩散</b>——先访问所有一步可达、再两步（队列实现，类似树层序）（Q342、Q350）。</li><li><b>复杂度</b>：邻接表 O(n+e)（每个顶点看一次、每条边看一次）、邻接矩阵 O(n²)（扫矩阵，Q345、Q347、Q356 2012）。</li><li><b>妙用</b>：DFS 调用次数 = 连通分量数（每次从新起点启动 DFS 就是一个新分量，Q344）；DFS 检测到<b>回边即有环</b>（Q353、Q370）；BFS 可求无权图单源最短路径（层数即距离，Q342、Q405 2023）；BFS 生成树高度 ≤ DFS（Q355）。</li></ul>"""

NEW["7.2.3 分块查找"] = """<h4>📌 题库考点补充</h4><ul><li><b>分块查找的组织（原理）</b>：<b>块间有序、块内无序</b>——先按关键字值把线性表分成若干块（后一块所有元素都大于前一块），只对<b>每块的最大（最小）关键字建索引表</b>；查找先在索引表（可顺序/折半）定位到块，再在块内顺序查找（Q422）。</li><li><b>最理想块长 = √n</b>（指数表与块内查找成本均衡，Q423：2500 → 50；Q431 2025：400 → 20）。</li><li><b>ASL 公式</b>：块内顺序 + 索引顺序 ≈ (n/b+1)/2 + (b+1)/2（Q424、Q425）。</li></ul>"""

NEW["6.3.1 广度优先搜索"] = """<h4>📌 题库考点补充</h4><ul><li><b>BFS 为什么用队列（原理）</b>：BFS 的核心是“<b>先来先访问</b>”——访问一个顶点后，把它的未访问邻居“排队”，队头的先处理 → 队列先进先出正合适；每个顶点<b>入队恰好 1 次</b>（访问过就不再入队，用 visited 标记）（Q346）。</li><li><b>BFS 与最短路径</b>：无权图（或各边权相等）中 BFS <b>第一次访问到某顶点时经过的边数就是最短路径长度</b>（按层扩散的自然结果，Q342、Q405 2023）。</li><li>DFS 遍历序列不唯一（邻接表次序相关）；非强连通图不一定需 2 次 BFS（看起始点，Q343）。</li></ul>"""

NEW["3.3.2 栈在表达式求值中的应用"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么中缀转后缀（原理）</b>：后缀（逆波兰）表达式<b>无括号无优先级、只需从左到右扫一遍</b>，机器求值极简单；转换规则 = 运算符按其<b>优先级与结合性</b>处理，<b>优先级低的不能压过优先级高的</b>（所以用栈暂存运算符）（Q133、Q150 2024）。</li><li>手算技巧：<b>括号法</b>（把每个子表达式运算符提到括号右）或<b>表达式二叉树后序遍历</b>：<code>a*(b+c)-d → abc+*d-</code>。</li><li><b>运算符栈最大深度</b> = 栈中同时存了多少个待处理运算符（Q144 2012：<code>a+b-a*((c+d)/e-f)+g</code> 最大 <b>5</b>；Q145 2014：扫到 f 时栈 <code>+(-*</code>）。</li></ul>"""

NEW["§8.7 外部排序"] = """<h4>📌 题库考点补充</h4><ul><li><b>内/外排序的本质区别</b> = <b>是否涉及内外存数据交换</b>（内存放不下才到磁盘上归并，Q617）；大数据文件（10TB）只能用<b>归并排序</b>（其它排序需要随机访问+内存，Q630 2016）。</li><li><b>时间大头 = 内外存交换</b>（磁盘 I/O 比运算慢千倍，Q618）→ 优化的方向都是<b>减少归并趟数/读写次数</b>。</li><li><b>置换-选择排序</b>：用内存最多能生成<b>平均为传统 2 倍长的初始归并段</b>（减小段数），且各段<b>不等长</b>（Q618、Q623、Q624）；I/O 缓冲区是内部归并的工作区（Q621）。</li></ul>"""

# ============ 主逻辑 ============
def main():
    dry = "--dry" in sys.argv
    raw = open(PATH, "rb").read()
    d = json.loads(raw.decode("utf-8"))
    done, appended, missing = [], [], []
    for c in d["chapters"]:
        for s in c["sections"]:
            sk = "§" + s["section"].strip()
            if sk in NEW:
                h = s.get("html") or ""
                j = h.find("📌")
                if j >= 0:
                    k = h.rfind("<h4>", 0, j)
                    s["html"] = h[: (k if k >= 0 else j)] + NEW[sk]
                    done.append(sk)
                else:
                    s["html"] = h + NEW[sk]
                    appended.append(sk)
            for ss in s.get("subsections", []):
                sec = ss["section"].strip()
                if sec not in NEW:
                    continue
                h = ss.get("html") or ""
                j = h.find("📌")
                if j >= 0:
                    k = h.rfind("<h4>", 0, j)
                    ss["html"] = h[: (k if k >= 0 else j)] + NEW[sec]
                    done.append(sec)
                else:
                    ss["html"] = h + NEW[sec]
                    appended.append(sec)
    for k in NEW:
        if k not in done and k not in appended:
            missing.append(k)
    print(f"替换 {len(done)} 节, 追加 {len(appended)} 节, 未命中 {len(missing)}")
    if missing:
        for m in missing:
            print("  [未命中]", m)
        if not dry:
            sys.exit(1)
    if dry:
        print("dry-run 结束, 未落盘")
        return
    out = (json.dumps(d, ensure_ascii=False, indent=2) + "\n").encode("utf-8")
    open(PATH, "wb").write(out)
    d2 = json.load(open(PATH, encoding="utf-8"))
    n = 0
    for c in d2["chapters"]:
        for s in c["sections"]:
            if "📌 题库考点补充" in (s.get("html") or ""):
                n += 1
            for ss in s.get("subsections", []):
                if "📌 题库考点补充" in (ss.get("html") or ""):
                    n += 1
    print(f"写入完成, 含补充块的 节+小节 总数 = {n}")

if __name__ == "__main__":
    main()