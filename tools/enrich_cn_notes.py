# -*- coding: utf-8 -*-
"""
enrich_cn_notes.py — 计网线上考点笔记「题库考点补充」块详化
把 cn_notes.json 里过于简洁的补充块替换为"通俗原理 + 机制 + 例题"详细版。
幂等:按小节名精确替换 📌 块(或在小节末尾追加),可重复运行。
用法:
  python tools/enrich_cn_notes.py --dry   # 只看将被替换的小节数
  python tools/enrich_cn_notes.py         # 落盘
写入格式:ensure_ascii=False, indent=2, 尾换行(与 cn_notes.json 原格式一致)。
"""
import json
import re
import sys

PATH = "pwa/data/notes/cn_notes.json"

# ============ 新补充块(按小节名精确匹配) ============
NEW = {}

NEW["3.4.1 流量控制与滑动窗口机制"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么要流量控制（通俗引入）</b>：发送方发得太快、接收方来不及收，缓冲区就会溢出丢帧。解决办法是由<b>接收方告诉发送方“你最多还能发多少”</b>——这个数就是<b>接收窗口（滑动窗口大小）</b>，发送方据此调整自己的发送窗口，这就是流量控制（Q138：实现措施=接收方向发送方反馈信息；<b>不是</b>“加大缓冲区/减小发送速率/加快接收速度”）。</li><li><b>为什么还要序号＋确认＋超时重传（可靠传输）</b>：只控制速度还不够，帧还可能<b>丢失或出错</b>。于是给每帧编<b>序号</b>（让接收方辨认顺序、识别重复帧），接收方每收到正确帧就回<b>确认 ACK</b>，发送方若在<b>计时器超时</b>前没收到 ACK 就重传。序号＋确认＋超时重传三件套构成<b>可靠传输</b>，链路层可靠传输正是靠序号与确认实现的（Q108、Q123、Q124）。</li><li><b>滑动窗口怎么“滑”</b>：发送方只允许发送落在<b>发送窗口内</b>的帧；每收到一个 ACK，窗口就向前滑动，同时把已确认的帧“放出”窗口。接收方只接收落在<b>接收窗口内</b>的帧（Q129–Q151 相关）。</li><li><b>GBN 与 SR 对比（必背）</b>：<ul><li><b>后退 N 帧 GBN</b>：<b>发送窗口 ≤ 2ⁿ−1、接收窗口 = 1</b>；<b>累积确认</b>（ACK n 表示“n 及以前全部收到”）；某帧出错时接收方<b>丢弃该帧及之后所有帧</b>，发送方超时后<b>从出错帧起全部重传</b>。优点：只需一个计时器、确认开销小；缺点：出错时浪费带宽。例（Q142 2009 真题）：已收 0/2/3 号确认→最大确认 3→4/5/6/7 全部重传。</li><li><b>选择重传 SR</b>：<b>发送窗口 = 接收窗口 ≤ 2ⁿ⁻¹</b>；<b>逐帧独立确认</b>；接收方<b>可缓存失序帧</b>，只重传真正出错的那一帧。优点：出错只重传 1 帧；缺点：每帧独立计时/确认，开销大。例（Q143 2011 真题）：仅收到 1 号确认、0/2 号超时→只重传 0/2 两帧。</li></ul></li><li><b>窗口上限为什么这么定（原理）</b>：序号只有 n 位、共 2ⁿ 个编号。GBN 若发送窗口取满 2ⁿ，当 n 帧全部发出且都未确认时，下一轮发出的帧序号与上一轮完全相同，接收方分不清是重传还是新帧→故 <b>W_T ≤ 2ⁿ−1</b>（Q134）。SR 因接收方还要缓存失序帧，收发窗口之和不能超过 2ⁿ→<b>W ≤ 2ⁿ⁻¹</b>（Q131、Q136、Q148 2019 真题：3 比特、W_T=5→W_R≤3；Q151 2024 真题 SR 窗口=4）。</li><li><b>按序 vs 失序接收（连续 ARQ 的核心区别）</b>：GBN 接收窗口=1→必须<b>按序</b>接收；SR 接收窗口&gt;1→可<b>先收下失序帧</b>（序号仍在窗口内）。“接收方可以不按序接收”<b>只对 SR 成立</b>（Q128）。</li><li><b>利用率与计算</b>：<code>U = (发送窗口 × 帧发送时延) / (帧发送时延 + RTT + ACK 发送时延)</code>。窗口够大时 GBN/SR 的 U 可达 100%（Q140）。由 U 反推可选协议：例 20kb/s、数据帧与确认帧各 2000B（发送时延均 800ms）、RTT=1400ms、要求 U&gt;80%→发送窗口 x&gt;3；3 比特序号下 GBN=2³−1=7、SR=2²=4，<b>两者皆可</b>（Q137）。求“利用率最高时序号位数”：以最短帧长算窗口下限（Q139、Q144、Q145、Q146、Q147、Q149）。</li></ul>"""

NEW["3.4.2 可靠传输机制"] = """<h4>📌 题库考点补充</h4><ul><li><b>停止-等待（通俗版）</b>：发送方<b>发一帧就停下来</b>等 ACK，收到再发下一帧。发送/接收窗口均=1，任何时刻只有 1 帧在途。因为大量时间花在“等”，<b>信道利用率 U 不可能达到 100%</b>；链路越长（RTT 越大）浪费越严重，所以只适合 RTT 很小的信道（Q121、Q122、Q140）。</li><li><b>帧丢失怎么办（死锁与超时）</b>：若数据帧在途中丢失，接收方永远不会回 ACK，发送方若一直等就会<b>死锁</b>。解决办法：发送方设<b>计时器</b>，超时未收到 ACK 就重传（Q123、Q127）。同理 ACK 丢失也会触发超时重传，接收方靠<b>帧编号</b>识别重复帧并丢弃（Q108、Q124）。</li><li><b>GBN 累积确认 → 判断重传几帧（高频计算）</b>：GBN 的 ACK n 表示“<b>n 及以前所有帧都正确收到</b>”，一个 ACK 覆盖了它之前的所有确认。例：发送方已发出 0~6 号，超时时只收到对 1、2、4 号的确认→最大确认是 4→4 号及以前全部成功，只需重传 <b>5、6 共 2 帧</b>（Q130）。注意：收到 1/2/4 三个确认，不等于 3 号错了——4 号确认本身就隐含 3 号已到。</li></ul>"""

NEW["4.2.3 网络地址转换NAT"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么需要 NAT</b>：IPv4 地址只有 32 位、数量有限，而局域网内往往有成百上千台主机。NAT 让<b>整个内网共用少数（甚至 1 个）公网 IP</b> 访问因特网，缓解地址耗尽（Q312、Q344）。</li><li><b>工作原理（通俗版）</b>：内网主机（如 192.168.x.x）发往外网的报文，经过 NAT 路由器时<b>源 IP 被改写为路由器的公网 IP</b>，同时建立“内网 IP:端口 → 公网端口”映射条目；外网返回的报文按映射表反向把<b>目的 IP/端口改回内网地址</b>再投递（Q312–Q319）。</li><li><b>NAPT（端口多路复用）</b>：用<b>端口号区分不同内网主机</b>，实现多台内网主机共用一个公网 IP（Q344）。</li><li><b>效果与易错点</b>：外部看不到内网拓扑，内网主机对外表现为同一公网 IP（Q314 真题）；NAT 屏蔽了内网真实地址，外网主动访问内网主机很困难，需 NAT 穿透（中继/打洞）。</li><li><b>2023 真题（Q346）</b>：主机经 NAT 通信后，报文的<b>源 IP 被改为路由器外接口的全球 IP</b>——NAT 只改 IP/端口，链路层 MAC 不变。</li></ul>"""

NEW["4.2.5 地址解析协议（ARP）"] = """<h4>📌 题库考点补充</h4><ul><li><b>ARP 解决什么问题</b>：IP 分组最终要在链路上交给<b>同网段</b>的下一跳设备，而链路层靠 MAC 地址寻址。ARP 完成<b>IP 地址 → MAC 地址</b>的解析（Q332 2012 真题）。</li><li><b>工作过程（通俗版）</b>：先查本机 <b>ARP 缓存表</b>；没有则向本网段<b>广播 ARP 请求</b>（“谁的 IP 是 192.168.1.1？报上 MAC”），目标主机<b>单播回 ARP 回答</b>，发送方把映射写入缓存（Q335–Q337）。</li><li><b>缓存表</b>：动态（自动学习、有过期时间）+ 静态（手工配置）。</li><li><b>ARP 不跨网段</b>：目的 IP 不在本网段时，主机把分组交给<b>默认网关（路由器）</b>，用 ARP 解析<b>网关的 MAC</b>而非目的主机的 MAC（Q326–Q337）。</li><li><b>层级争议</b>：TCP/IP 体系中 ARP 归<b>网络层</b>（服务于 IP），OSI 中归<b>数据链路层</b>；考题一般按“网络层协议”判（Q330–Q334）。ARP 报文直接封装在以太网帧中（Q335）。</li></ul>"""

NEW["4.2.7 网际控制报文协议ICMP"] = """<h4>📌 题库考点补充</h4><ul><li><b>ICMP 干什么</b>：IP 只负责尽力转发、<b>不报告差错</b>。ICMP 是<b>网络层的差错报告与询问机制</b>，封装在 IP 数据报中（<b>协议号 1</b>），报告 IP 分组在传送中的差错（Q326、Q328、Q333）。</li><li><b>两类报文</b>：<ul><li><b>差错报告</b>：终点不可达（含“需要分片但 DF=1”）、源点抑制（已过时）、超时（TTL=0）、参数问题、改变路由/重定向（Q326、Q346–Q349、Q351–Q353）。</li><li><b>询问</b>：回送请求/回答（ping）、时间戳请求/回答（Q346–Q356）。</li></ul></li><li><b>TTL 超时场景</b>：TTL=0→路由器丢弃并回 ICMP 超时报文；分组超过 MTU 且 DF=1→回 ICMP 终点不可达（需分片）（Q351–Q353）。</li><li><b>易错</b>：ICMP 差错报文<b>不再嵌套 ICMP 差错</b>；ICMP 是网络层协议——直接为 ICMP 提供服务的是 IP（Q333 2012 真题）。ping 与 traceroute 的应用见 4.3.1。</li></ul>"""

NEW["1.1.1 计算机网络的概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>定义（通俗版）</b>：计算机网络是把<b>多台自治的计算机</b>通过通信设备<b>互联</b>起来、加上相应软件形成的<b>集合体</b>，目的是资源共享与信息交换（三要素：自治计算机＋互连＋集合体）。</li><li><b>为什么强调“自治”</b>：每台计算机都有<b>独立的硬件与操作系统</b>、能独立运行——这正是它与“多机系统”（共享一套硬件/OS）、“分布式系统”（对用户表现为一台机器）的本质区别：<b>网络是松耦合的自治机集合，多机/分布式系统不是网络</b>（Q1、Q3）。</li></ul>"""

NEW["1.1.3 计算机网络的功能"] = """<h4>📌 题库考点补充</h4><ul><li><b>三大功能</b>：①<b>数据通信</b>（最基本）；②<b>资源共享</b>（硬件/软件/数据共享，省成本）；③<b>分布式处理</b>（把大任务分给多台机器并行完成，提高可靠性——某台故障可由冗余机器顶上——与整体效率）（Q2）。</li><li><b>易错</b>：网络让各计算机<b>协作</b>而非“相互独立”——“使各计算机相对独立”是<b>错误</b>表述（Q2）。</li></ul>"""

NEW["1.1.5 计算机网络的分类"] = """<h4>📌 题库考点补充</h4><ul><li><b>按交换技术分：数据报 vs 虚电路</b>。数据报（无连接）：每个分组<b>独立选路</b>、走不同的路径、<b>不保证按序到达</b>、也不需要先建立连接——像寄信，每封信单独走（Q7）。虚电路（面向连接）：先建立<b>逻辑连接</b>，所有分组走同一条路径、按序到达——像打电话，先拨号后通话。</li><li><b>易错</b>：“数据报只适合短报文”是<b>误解</b>——数据报适合突发性、交互式通信，长短都行（Q7）。</li></ul>"""

NEW["1.2.1 计算机网络分层结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么分层</b>：各层独立（改一层不影响其他层）、职责清晰、便于标准化与排错。分层的目的=提供标准语言、定义标准界面、增加功能独立性；<b>不包括</b>“定义功能的具体执行方法”（实现细节留给各层协议，Q18、Q47）。</li><li><b>封装（通俗版）</b>：数据从上往下传时，每层给上层的数据<b>加自己的控制信息（首部，有的还加尾部）</b>：物理层传输比特流、<b>不封装</b>；数据链路层加<b>帧首+帧尾</b>（定界）；网络层加<b>IP 地址（第三层地址）</b>；传输层加<b>端口号</b>（Q33、Q39）。</li></ul>"""

NEW["2.1.1 基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>信道 vs 电路</b>：信道是传送信号的通道，一条可双向通信的电路往往含<b>两个信道</b>（发送信道＋接收信道）；调制=把<b>数字数据</b>变成<b>模拟信号</b>（如电话线传数据）。</li><li><b>波特率 vs 比特率（核心公式）</b>：<code>比特率 = 波特率 × log₂V</code>，V=每个码元的有效离散值个数。即<b>每个码元携带 log₂V 比特</b>：16 态码元=4 比特/码元→比特率=波特率×4（Q56、Q62、Q64、Q70、Q71、Q73、Q74）。</li><li><b>传输方式</b>：计算机内部（距离短、并行走线成本可接受）用<b>并行</b>；长距离线缆昂贵→用<b>串行</b>（Q58）。</li></ul>"""

NEW["2.2.2 物理层接口的特性"] = """<h4>📌 题库考点补充</h4><ul><li>物理层接口<b>四特性</b>（Q92–Q95），一句话记忆：<b>“形状—电压—含义—顺序”</b>：<ul><li><b>机械特性</b>：接口的<b>形状、尺寸、引脚数</b>（能插上吗）</li><li><b>电气特性</b>：<b>电压范围</b>（如 +10~+15V 表示 0，多少伏算 0/1）</li><li><b>功能特性</b>：某根线<b>某电平表示什么意义</b>（如数据线高电平=1）</li><li><b>过程/规程特性</b>：<b>事件发生的先后顺序</b>（如先建立连接→再传数据→最后释放）</li><li>真题（2018 Q95）：描述“引脚高电平时含义”→<b>功能特性</b>；“物理地址”属<b>数据链路层</b>，不在物理层。</li></ul></li></ul>"""

NEW["2.3.1 中继器"] = """<h4>📌 题库考点补充</h4><ul><li><b>中继器为什么是“再生”而不是“放大”</b>：数字信号经过长距离传输会<b>衰减并变形</b>（波形畸变）；简单放大会把<b>噪声一起放大</b>，越放越糟。再生=把收到的信号<b>恢复成标准波形</b>再发出（放大＋整形），所以中继器用于<b>数字信号</b>延长距离（Q96、Q97）。</li><li><b>用法与限制</b>：可连接<b>不同介质但同协议</b>的局域网（Q101）；串联个数受<b>5-4-3 规则</b>限制（最多 5 段、4 个中继器、3 个带主机段，Q104）；以太网粗缆（10Base-5）<b>每段 ≤500m</b>，超过必须分段、段间用中继器连接（Q98）。</li></ul>"""

NEW["2.3.2 集线器"] = """<h4>📌 题库考点补充</h4><ul><li><b>集线器本质</b>：一个<b>多端口的中继器</b>，所有端口共享同一条总线——物理拓扑是<b>星形</b>，逻辑上仍是<b>总线网</b>（Q99）。</li><li><b>域与带宽</b>：所有端口<b>同属一个冲突域、也同属一个广播域</b>（Q100）；平均带宽=总带宽÷主机数（Q102）；收到数据从<b>除入端口外所有端口广播</b>（Q103）。</li><li><b>串联限制</b>：中继器/集线器串联个数受 5-4-3 规则限制，<b>并非无限制</b>（Q104）。</li></ul>"""

NEW["3.2.3 零比特填充法"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么插 0（原理）</b>：HDLC 用 01111110 作帧定界符，若数据里恰好出现连续的 6 个 1，接收方会<b>误认为帧结束</b>。解决：发送方在数据中<b>连续 5 个 1 后插入一个 0</b>；接收方看到“连续 5 个 1 后跟 0”就把这个 0 <b>删掉</b>，还原数据（Q111）。</li><li>零比特填充面向<b>比特流</b>（可传任意位组合），与字节填充（面向字节、插转义字符）对应。大纲已删但题仍在，须会判断填充/还原结果。</li></ul>"""

NEW["3.3.1 检错编码"] = """<h4>📌 题库考点补充</h4><ul><li><b>奇偶校验（原理与局限）</b>：在 k 位数据后加 1 位，使整个码字中 1 的个数为偶数（偶校验）或奇数（奇校验）。接收方数 1 的个数判对错。局限：<b>只能检奇数个比特错</b>——偶数个比特错会“互相抵消”，1 的个数奇偶性不变而<b>漏检</b>；且只能检错<b>不能纠错</b>（Q113、Q114）。</li><li><b>CRC 循环冗余校验（原理，必考）</b>：收发双方<b>事先约定生成多项式 G(x)</b>（最高次=r，对应 r 位余数）。发送：①数据左移 r 位补 0；②用 G(x) 做<b>模 2 除法（异或）</b>得 r 位余数 FCS；③发送“数据＋FCS”。接收：用同一 G(x) 去除整个帧，<b>余数为 0 → 无错</b>，否则有错（Q117、Q118、Q119）。CRC 可检出<b>所有单比特错</b>与绝大多数突发错。</li><li><b>码距规律（高频）</b>：码距 dmin → 最多检 d−1 位错、纠 ⌊(d−1)/2⌋ 位错。2025 真题 dmin=4 → 检 3 纠 1（Q120）。</li><li><b>差错控制“既有检错也有纠错”</b>：数据链路层既可用检错编码也可用纠错编码——<b>奇偶检验码</b>只检错不纠错且检不出偶数错；<b>CRC</b> 检错（能检所有单比特错）；<b>海明码</b>可纠正 1 位错（Q112）。</li></ul>"""

NEW["3.3.2 纠错编码"] = """<h4>📌 题库考点补充</h4><ul><li><b>海明码原理</b>：在 k 位信息位中插入 r 位校验位（<code>2ʳ ≥ k+r+1</code>），使整个码字具备<b>纠 1 位错</b>的能力（Q115、Q116）。校验位放在<b>2 的幂次位置</b>（1、2、4、8…），每个校验位负责一组比特，出错时各校验组的结果组合成<b>出错位号</b>，直接指出哪一位错了、把它取反即可纠正。</li><li><b>纠错 vs 检错能力</b>：纠正 d 位错需要码距 ≥ 2d+1（海明码 d=1 → 码距 ≥3，能纠 1 位）；只检错则码距 ≥ d+1 即可（Q115）。</li><li>例：k=10 位信息 → r=4（2⁴=16 ≥ 10+4+1=15），10 位数据需 4 位校验位（Q116）。</li></ul>"""

NEW["3.5.1 信道划分介质访问控制"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么“划分”就不冲突</b>：把信道在<b>频率（FDM）、时间（TDM）、码型（CDM）</b>上分开，各路信号各占各的，互不干扰——静态划分<b>不会发生冲突</b>（与随机访问的 CSMA 相对，Q158、Q171 2013 真题）。</li><li><b>FDM 频分</b>：不同频率同时传；<b>TDM 时分</b>：不同时间片轮流传（同步 TDM 固定时隙、统计 TDM 动态分配）；<b>CDM 码分</b>：同一时刻同频，靠<b>正交码片</b>区分用户（Q152–Q157）。</li><li><b>TDM 要点</b>：介质位速率<b>&gt; 单个信号位速率</b>（Q155）；计算机网络多用 TDM（适合数字信号、可逐级再生，Q156）。</li><li><b>CDMA 计算</b>：各站码片两两<b>正交（规格化内积=0）</b>；接收方把收到的混合信号与目标站码片求内积，得 1/0/−1 判数据（Q169、Q170、Q172 2014 真题）。</li></ul>"""

NEW["3.5.3 轮询访问：令牌传递协议"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么令牌环无冲突</b>：信道中只有一个<b>令牌</b>（特殊帧）在循环传递，<b>同一时刻只有持有令牌的站能发送</b>，其它站只能转发——天然无冲突（Q163–Q168）。</li><li><b>优缺点</b>：负载<b>重时利用率高</b>（CSMA/CD 负载重时冲突反而加剧）；每个站发完必须释放令牌、令牌持有时间有限，保证公平；半双工。</li></ul>"""

NEW["3.7.1 广域网的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>WAN 与 LAN 差异在哪</b>：主要不在距离/速率，而在<b>协议与技术</b>——WAN 覆盖广大地理范围，用<b>分组交换＋存储转发＋网状拓扑</b>（冗余路径、可靠性高），不像 LAN 靠广播介质（Q222–Q226）。</li><li><b>互连</b>：LAN 与 WAN（或两个 LAN）互连用<b>路由器</b>（网络层设备）。</li></ul>"""

NEW["3.7.2 点对点协议"] = """<h4>📌 题库考点补充</h4><ul><li><b>PPP 是什么</b>：数据链路层的<b>点对点</b>协议（如拨号上网、专线），<b>面向字节</b>，支持多种网络层协议（Q227–Q233）。</li><li><b>组帧（两种填充）</b>：<b>异步</b>链路用<b>字符填充</b>（插转义字符 0x7D）；<b>同步</b>链路用<b>零比特填充</b>（连续 5 个 1 插 0）——目的都是防止数据里出现帧定界符而被误切。</li><li><b>工作阶段</b>：<b>LCP</b>（链路控制协议，协商链路参数）→ <b>NCP</b>（网络控制协议，配置网络层如 IP）。</li><li><b>服务特点</b>：提供<b>有连接的不可靠</b>服务（有编号帧机制但不做差错重传）；<b>HDLC 不属于 TCP/IP 协议族</b>（PPP 才是拨号/专线的 TCP/IP 数据链路协议）。</li></ul>"""

NEW["3.8.3 共享式以太网"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么“共享”=低效</b>：所有站点竞争同一信道，同一时刻只能一对通信（半双工），冲突频发。<b>设备层级与域</b>（核心记忆）：<ul><li>集线器/中继器=物理层：<b>不分割冲突域也不分割广播域</b>（Q234、Q237、Q241）</li><li>网桥/交换机=数据链路层：<b>分割冲突域、不分割广播域</b>（Q235、Q237、Q240、Q242）</li><li>路由器=网络层：<b>冲突域、广播域都分割</b></li></ul></li></ul>"""

NEW["4.1.3 网络层提供的两种服务"] = """<h4>📌 题库考点补充</h4><ul><li><b>虚电路（面向连接） vs 数据报（无连接）</b>：虚电路先建立<b>逻辑连接</b>，分组走同一路径、按序到达、路由器简单（像打电话）；数据报每个分组<b>独立选路</b>、可能乱序、路由器需查表转发（像寄信）。IP 采用<b>数据报＋尽最大努力交付</b>（Q257–Q262）。</li><li><b>为什么 IP 首部要校验而数据不校验</b>：路由器每跳都要读首部（源/目的 IP、TTL…），首部出错会错误转发——所以<b>检验和只验首部</b>；数据可靠性交给上层（传输层），网络层不管（Q275–Q279）。</li><li><b>每跳 TTL 都减 1</b> → 每跳首部内容都在变 → 每跳必须<b>重新计算首部检验和</b>（Q275–Q279）。</li></ul>"""

NEW["4.5.1 多播的概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>三种发送方式</b>：<b>单播</b>=一对一（发送 N 份数据给 N 个接收者，浪费带宽）；<b>广播</b>=一对全体（发给网内所有主机）；<b>多播（组播）</b>=一对多中的“一组”（只发 1 份，路径上的路由器<b>按需复制</b>给多个接收者）→ <b>节省带宽、时延小</b>（Q384）。</li><li><b>路由器行为</b>：多播路由器<b>可以从多个接口转发</b>同一分组；单播只从一个接口转出（Q384）。</li></ul>"""

NEW["4.5.4 IGMP与多播路由协议"] = """<h4>📌 题库考点补充</h4><ul><li><b>IGMP 干什么</b>：让路由器知道<b>本网段内有哪些主机属于哪个多播组</b>（组成员关系管理），属<b>网络层</b>协议（Q385）。</li><li><b>多播路由与单播路由的差别</b>：避免多播路由环路用的是<b>构造多播转发树</b>（以源为根生成树，树中无环）；<b>水平分割</b>是距离向量协议（RIP）用来<b>防无穷计数</b>的，不是多播专用（Q385）。</li></ul>"""

NEW["4.6.1 移动IP的概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么需要两个地址</b>：主机漫游到外地网后，IP 地址不能变（否则通信对端找不到它），但数据又必须能送到当前所在地。于是给移动节点两个地址（Q389）：<ul><li><b>归属地址（主地址）</b>：永远不变的“身份”，对通信对端可见</li><li><b>转交地址（辅地址）</b>：在外地网<b>临时</b>获得的地址，<b>随位置动态改变</b>，用于把数据实际送达</li></ul></li></ul>"""

NEW["4.6.2 移动IP通信过程"] = """<h4>📌 题库考点补充</h4><ul><li><b>工作流程</b>：<b>代理发现 → 注册（向归属代理报转交地址） → 隧道转发 → 注销</b>（Q388）。</li><li><b>收/发不对称（核心考点）</b>：他主机→移动节点：数据先按<b>归属地址</b>送到<b>归属代理</b>，归属代理再把分组<b>隧道</b>（IP-in-IP 封装）送给外地代理，最后交给移动节点（Q390）；移动节点→他主机：源仍写<b>归属地址</b>，可<b>直接经外地代理发出</b>，不用绕回归属代理。</li><li><b>结论</b>：移动节点到外地后<b>能直接发、不能直接收</b>（收必须经归属代理隧道转发）（Q390）。</li></ul>"""

NEW["4.7.2 路由器的组成和功能"] = """<h4>📌 题库考点补充</h4><ul><li><b>路由器两大功能</b>：①<b>路由选择</b>（控制面）——由<b>路由选择处理机</b>运行<b>路由选择协议</b>、构造/更新<b>路由表</b>（Q400），转发表内容由<b>路由算法</b>决定（Q399）；②<b>分组转发</b>（数据面）——按表把分组从输入端口转到输出端口。</li><li><b>路由器工作在几层</b>：实现<b>物理层＋数据链路层＋网络层</b>功能（不含传输层/应用层，Q395）；转发一个分组要经<b>物理层→数据链路层→网络层→数据链路层→物理层</b>五层处理（Q402）。</li><li><b>处理时延对比（常考）</b>：路由器<b>处理时延最大</b>（软件解析 IP 首部）：交换机（硬件查表）几十 μs ≪ 路由器几千 μs ≪ 集线器最小（Q404、Q408）。</li></ul>"""

NEW["4.7.3 路由表与分组转发"] = """<h4>📌 题库考点补充</h4><ul><li><b>路由器的内部结构</b>：<b>路由选择部分</b>（路由选择处理机＋路由选择协议＋路由表）＋<b>分组转发部分</b>（输入端口＋交换结构＋输出端口）（Q401、Q403）。</li><li><b>表项内容</b>：目的网络＋下一跳＋<b>接口标识符</b>；转发按<b>接口标识符</b>确定从哪个输出端口发出（Q406）。</li><li><b>转发规则</b>：表项全不匹配→走<b>默认路由 0.0.0.0/0.0.0.0</b>（Q405）；路由器<b>只按 IP 地址转发</b>（不按 MAC，MAC 是链路层的事）（Q396）。</li></ul>"""

NEW["5.1.1 传输层的功能"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么叫“端到端”</b>：网络层只管<b>主机到主机</b>（把分组送到目的主机），但主机里可能有几十个进程，到底交给谁？传输层提供<b>进程到进程</b>的逻辑通信（用端口号找到具体进程），这就是“端到端”（Q412–Q415）。</li><li><b>传输层提供</b>：透明传输（对上层隐藏网络细节）、差错控制、流量控制、复用/分用（Q412）。</li></ul>"""

NEW["5.1.3 无连接服务与面向连接服务"] = """<h4>📌 题库考点补充</h4><ul><li><b>两类服务（通俗版）</b>：<b>面向连接（TCP）</b>像打电话——先<b>建立连接</b>，然后按序传，最后<b>释放连接</b>，保证<b>可靠＋按序交付</b>，但开销大、效率低；<b>无连接（UDP）</b>像寄信——不管到达顺序、不保证不丢（Q414、Q421、Q439）。</li><li><b>“可靠”的标志</b>：<b>使用确认机制</b>确保数据不丢失（Q416）。</li><li><b>UDP 的特征</b>：无连接、不可靠、<b>无流量控制、无拥塞控制</b>；可靠性必须由<b>应用层</b>负责（Q421–Q422、Q427）。UDP 比 IP 多提供的是<b>端口/复用分用功能</b>（找到具体进程）（Q426）。</li></ul>"""

NEW["5.3.4 TCP可靠传输"] = """<h4>📌 题库考点补充</h4><ul><li><b>TCP 可靠传输靠什么</b>：<b>序号＋确认＋超时重传＋检验和</b>——每个字节一个序号，接收方回“期望的下一个序号”，发送方超时重传；首部校验失败的分段被丢弃并触发重传（Q442–Q443、Q449、Q460）。</li><li><b>效率真题（2021 Q493）</b>：12B 应用层数据：UDP 效率 12/20=<b>60%</b>，TCP 效率 12/32=<b>37.5%</b>（TCP 首部 20B 比 UDP 8B 大，小报文下差距明显）。</li></ul>"""

NEW["5.3.5 TCP流量控制"] = """<h4>📌 题库考点补充</h4><ul><li><b>TCP 流量控制原理</b>：发送窗口 = <code>min(自己的拥塞窗口 cwnd, 对方通告的接收窗口 rwnd)</code>（Q451、Q454）；接收方在<b>首部窗口字段</b>里通告自己还能收多少字节（=自己的接收窗口，Q447），发送方据此限速——与链路层“接收方告诉发送方还能发多少”是同一个思想，只是 TCP 的单位是<b>字节</b>（Q448、Q467）。</li><li><b>零窗口死锁（Q452）</b>：接收方窗口减到 0 后不再收数据，若此时<b>通告零窗口的报文丢失</b>，发送方在等窗口、接收方在等数据→双方互等死锁。解决：发送方启动<b>持续计时器</b>，超时发<b>零窗口探测报文</b>，迫使接收方重报窗口大小。</li></ul>"""

NEW["6.1.2 对等模型（P2P）"] = """<h4>📌 题库考点补充</h4><ul><li><b>P2P 是什么</b>：每个节点既是客户（请求）又是服务器（提供），<b>节点对等、直接通信</b>；它是构建在 IP 网络之上的<b>逻辑覆盖网络</b>——节点间的“对等连接”是逻辑上的，<b>不改变物理拓扑</b>（Q504）。</li><li><b>为什么比 C/S 分发快</b>：可以<b>同时从多个节点并行下载</b>不同分块（Q505）；C/S 下所有请求都压向单一服务器。</li></ul>"""

NEW["6.2.1 层次域名空间"] = """<h4>📌 题库考点补充</h4><ul><li><b>域名结构（从右往左读）</b>：<code>主机名.三级域.二级域.顶级域.</code>——最右边是<b>顶级域</b>（com/edu/cn…），越往左级别越低、越具体；根在最高层（不写出来）。如 <code>www.abc.com</code>：顶级 com → 二级 abc → 三级 www（Q508–Q509）。</li><li><b>DNS 是什么</b>：基于<b>C/S 的分布式数据库</b>，完成<b>域名 ↔ IP</b> 映射（Q508–Q509）。<b>易错</b>：NAT（内→外 IP 翻译）<b>不属于 DNS</b>（Q509）。</li></ul>"""

NEW["6.5.1 WWW的概念与组成"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么访问网页第一步是 DNS</b>：URL 里写的是<b>域名</b>（如 www.abc.com），而 TCP 连接需要<b>IP 地址</b>——所以必须先做<b>域名解析（DNS）</b>拿到 IP，才能与服务器建立 <b>TCP(80)</b> 连接（Q547–Q549）。</li><li><b>URL 的作用</b>：<b>唯一标识</b>因特网上的一个文档/资源（Q550）；WWW = 网页 + URL + HTTP（Q547 相关）。</li></ul>"""

NEW["1.1.6 计算机网络的性能指标"] = """<h4>📌 题库考点补充</h4><ul><li><b>时延的四部分</b>：<b>发送时延</b>=分组大小÷发送速率（排队的“口子”多宽）、<b>传播时延</b>=距离÷信号速率（路多长）、排队时延（缓存里等）、处理时延（路由器解析）。发送时延与带宽有关、传播时延与距离有关，两者不能混（Q10 相关）。</li><li><b>端到端速率为什么取最小值（瓶颈）</b>：数据要经过链路和两端设备，<b>最慢的一环决定整体速率</b>——如 100Mb/s 链路 + 1Gb/s 与 10Mb/s 网卡 → 实际最高 <b>10Mb/s</b>（Q9）。</li><li><b>时延等式反解（求分组大小）</b>：令发送时延=传播时延 → <code>分组大小 = 带宽 × 传播时延</code>。例：100km、信号 10⁸m/s → 传播时延 1ms，20Mb/s × 1ms = <b>20Kb</b>（Q10）。</li></ul>"""

NEW["2.2.1 双绞线、同轴电缆、光纤与无线传输介质"] = """<h4>📌 题库考点补充</h4><ul><li><b>双绞线</b>：两根线<b>绞合</b>的目的=抵消相互电磁干扰（Q84）；加<b>屏蔽层</b>进一步提高抗干扰（Q85）。</li><li><b>同轴电缆</b>：外层屏蔽好 → 高带宽＋抗噪，故比双绞线快（Q87）；传统同轴以太网=<b>半双工</b>（Q86）。</li><li><b>光纤（为什么抗电磁干扰）</b>：光在玻璃纤芯内靠<b>全反射</b>传播（多模，Q89），或近乎沿直线传播（单模，纤芯直径≈光波长，Q90）；光信号<b>不导电、不受电磁干扰/噪声影响</b>（Q88）。</li><li><b>卫星通信</b>：覆盖广、易广播/多址，但<b>受气候影响大、误码率较高</b>、时延大、费用高（Q91）。</li></ul>"""

NEW["4.3.1 IPv6的特点"] = """<h4>📌 题库考点补充</h4><ul><li><b>IPv6 首部为什么“简化”</b>：固定 <b>40B</b> 基本首部、无校验和、无分片字段（分片只由源主机做）、无选项（改放扩展首部）——路由器<b>处理更简单更快</b>，转发性能提升（Q354、Q357–Q363）。</li><li><b>ICMPv6</b>：ICMP 差错报文<b>不再嵌套 ICMP 差错</b>（避免差错风暴）（Q354–Q356）。</li><li><b>应用</b>：<b>ping 是应用层进程、使用 ICMP 回送报文</b>；<b>traceroute 用 ICMP 超时报文</b>逐跳探测路径（Q354–Q356）。</li></ul>"""

NEW["4.3.2 IPv6数据报的基本首部"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么 IPv6 首部没有检验和</b>：链路层（以太网 FCS）已做差错检测，IPv4 首部检验和每跳都要重算、纯属浪费；去掉后路由器<b>不用再算检验和</b>，转发更快（Q357–Q363）。</li><li><b>IPv6 分片只能由源主机做</b>，中间路由器不分片（Q363）；IPv4 中间路由器也可分片（DF=0 时）（对比记忆）。</li><li><b>ICMPv6 差错场景</b>：TTL=0 → 路由器丢弃并回 ICMP 超时报文；分组超过链路 MTU 且 DF=1 → 回 ICMP 终点不可达（需分片）（Q351–Q353、Q355）。</li></ul>"""

NEW["4.4.4 开放最短路径优先协议"] = """<h4>📌 题库考点补充</h4><ul><li><b>OSPF 为什么快（链路状态原理）</b>：每个路由器通过<b>洪泛 LSA</b> 把“我的直连链路及代价”告诉全网，于是<b>每个路由器都掌握全网拓扑</b>，再用 <b>Dijkstra</b> 算出到所有目的的最短路径树——拓扑变了只需重新洪泛＋重算，<b>收敛快</b>（Q372、Q379）。</li><li><b>封装</b>：直接封装在 <b>IP</b> 中（协议号 89，网络层协议，不用 TCP/UDP）（Q371–Q374、Q376–Q382）。</li><li><b>通信机制</b>：用 <b>Hello</b> 报文维持邻居关系（周期检测邻居存活）；<b>仅网络变化时</b>才洪泛 LSA（平时不周期性交换整表，与 RIP 的 30s 周期交换整表相反）。</li><li><b>划分区域</b>：主要目的=<b>减少洪泛通信量</b>（LSA 只在区域内洪泛）；区域类型：主干区域(area 0)、ABR 区域边界、BR 主干、ASBR 自治系统边界；主干路由器<b>可兼作</b> ABR（Q371–Q374、Q376–Q382）。</li></ul>"""

NEW["4.4.5 边界网关协议"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么要 BGP、而且跑在 TCP 上</b>：AS（自治系统）之间互不信任、策略复杂，路由信息必须<b>可靠</b>送达——所以 BGP 用 <b>TCP（179 端口，应用层协议）</b>承载（Q382）；域内（IGP）用 RIP/OSPF、域间（EGP）用 BGP（Q364、Q376）。</li><li><b>路径向量算法</b>：BGP 交换的不是“下一跳”，而是<b>到达某网络要经过的 AS 序列（路径）</b>——拿到邻居的 AS 序列就知道这条路会不会成环（自己已在序列中就丢弃），所以天然<b>无环</b>（Q375）。</li><li><b>目标</b>：只求<b>无环的“较好”路由</b>（还要满足 AS 间的策略），<b>不追求全局最优</b>（与 RIP/OSPF 不同）（Q364、Q375–Q382）。</li></ul>"""

NEW["4.5.2 IP多播地址"] = """<h4>📌 题库考点补充</h4><ul><li><b>多播 IP 与多播 MAC 的映射</b>：多播 IP = <b>D 类 224.0.0.0–239.255.255.255</b>；对应的以太网多播 MAC = <b>01-00-5E-00-00-00 ~ 01-00-5E-7F-FF-FF</b>。映射规则：取 D 类 IP 的<b>后 23 位</b>填入 MAC 后 23 位（前 5 位丢弃）——因为以太网多播地址空间有限，只能覆盖 23 位（Q386）。</li><li><b>多播地址只能做目的地址</b>，不能做源地址（Q387）。</li></ul>"""

NEW["5.2.2 UDP检验"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么用“反码求和”</b>：把首部+数据按 <b>2B（16 位）</b>对齐，全部 16 位字<b>二进制反码求和</b>（溢出的进位<b>回卷</b>加到低位），最后<b>取反</b>作为检验和——这样接收端把“收到的检验和＋原数据”一起反码求和，<b>全 1 表示无错</b>，检错能力强（Q429、Q437 2024 真题反码回卷计算）。</li><li><b>覆盖范围</b>：<b>伪首部（含源/目的 IP，仅用于计算、不传送不上交，Q430）＋ UDP 首部＋数据</b>——所以 UDP 能顺带发现 IP 地址错误（Q429）。</li><li><b>可选</b>：不计算检验和则全置 0；计算结果恰为 0 时填<b>全 1</b>（避免与“未计算”混淆）（Q429–Q430）。</li></ul>"""

NEW["5.3.1 TCP的特点"] = """<h4>📌 题库考点补充</h4><ul><li><b>TCP 是“字节流”协议</b>：TCP 把应用层数据看成<b>一串连续的字节</b>，每个字节一个序号，按字节序号管理（不按报文段边界）——发送方按 MSS 切分、接收方按序号重组（Q442–Q443、Q449）。</li><li><b>特点速记</b>：<b>面向连接、一对一、全双工、可靠、字节流</b>，<b>不支持广播/多播</b>；比 UDP 开销大，对错误/超时分组<b>强制重传</b>、负责分段与重组（Q442–Q443）。</li><li><b>四次挥手（为什么是四次）</b>：TCP 全双工，两个方向各自独立关闭——A 发 FIN 只表示<b>A 不再发数据</b>（但仍可接收），B 回 ACK 确认后，B 再发自己的 FIN，A 回 ACK——所以是四次（Q455、Q461、Q462、Q464）。</li></ul>"""

NEW["6.1.1 客户/服务器模型（C/S）"] = """<h4>📌 题库考点补充</h4><ul><li><b>C/S 角色（通俗版）</b>：客户=<b>主动发起请求</b>的一方，服务器=<b>被动等待并提供服务</b>的一方；客户/服务器是<b>角色</b>（不是硬件也不是软件，一台机器可同时是 A 服务的服务器和 B 服务的客户）（Q500–Q503）。</li><li><b>为什么客户要事先知道服务器地址</b>：服务器的地址是<b>公开固定</b>的（客户来找它）；客户地址未知且每次可能不同，所以服务器从客户发来的<b>报文里获取客户地址</b>（Q501）。</li><li><b>易错</b>：连接建立后服务器<b>也可主动发数据</b>（说“服务器只响应”是错的，Q502）；客户<b>面向用户</b>、服务器<b>面向任务</b>（Q503）；<b>客户之间不能直接通信</b>（必须经服务器中转，Q505 2019 真题）。</li></ul>"""

# ============ 主逻辑 ============
def main():
    dry = "--dry" in sys.argv
    raw = open(PATH, "rb").read()
    d = json.loads(raw.decode("utf-8"))

    done, appended, missing = [], [], []
    for c in d["chapters"]:
        for s in c["sections"]:
            for ss in s.get("subsections", []):
                sec = ss["section"].strip()
                if sec not in NEW:
                    continue
                h = ss.get("html") or ""
                new = NEW[sec]
                j = h.find("📌")
                if j >= 0:
                    # 回溯到最近的 <h4> 起始,避免残留半个 <h4> 标签
                    k = h.rfind("<h4>", 0, j)
                    start = k if k >= 0 else j
                    ss["html"] = h[:start] + new
                    done.append(sec)
                else:
                    ss["html"] = h + new
                    appended.append(sec)

    for k in NEW:
        if k not in done and k not in appended:
            missing.append(k)

    print(f"替换 {len(done)} 节, 追加 {len(appended)} 节, 未命中 {len(missing)}")
    if missing:
        for m in missing:
            print("  [未命中]", m)
        if not dry:
            sys.exit(1)

    if dry:
        print("dry-run 结束, 未落盘")
        return

    out = (json.dumps(d, ensure_ascii=False, indent=2) + "\n").encode("utf-8")
    open(PATH, "wb").write(out)

    # 校验:重读并检查每个新块
    d2 = json.load(open(PATH, encoding="utf-8"))
    n = 0
    for c in d2["chapters"]:
        for s in c["sections"]:
            for ss in s.get("subsections", []):
                if "📌 题库考点补充" in (ss.get("html") or ""):
                    n += 1
    print(f"写入完成, 含补充块的小节总数 = {n}")

if __name__ == "__main__":
    main()
