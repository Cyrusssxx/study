# -*- coding: utf-8 -*-
"""
enrich_co_notes.py — 计组线上考点笔记「题库考点补充」块详化
把 co_notes.json 里过于简洁的补充块替换为"通俗原理 + 机制 + 例题"详细版。
支持小节(subsections)与节级(sections, 键名加 § 前缀)。
用法: python tools/enrich_co_notes.py [--dry]
写入格式: ensure_ascii=False, indent=2, 尾换行(与 co_notes.json 一致)。
"""
import json
import sys

PATH = "pwa/data/notes/co_notes.json"
NEW = {}

NEW["2.2.2 定点数的移位运算"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么移位运算重要</b>：算术左移 1 位 = 乘 2、算术右移 1 位 = 除 2，一条指令完成，比真正乘除法快得多，编译器常用“移位＋加减”优化乘以常数（Q78 相关）。</li><li><b>三种移位（核心）</b>：<ul><li><b>算术左移</b>：数据各位左移、<b>低位补 0</b>、最高位移出；若移出的最高位与<b>新符号位不同 → 溢出</b>（结果超出表示范围）。</li><li><b>算术右移</b>：各位右移、<b>高位补符号位</b>——负数右移补 1、正数补 0，保证符号不变（右移即除 2 取整）。</li><li><b>逻辑移位</b>：不分符号，<b>左右都补 0</b>；逻辑左移与算术左移结果相同（仅右移不同）。</li></ul></li><li><b>溢出的根本原因 = 机器字长有限</b>：移位或运算产生的进位/移出位<b>没有地方保存</b>，只能丢弃，一旦影响符号位就溢出（Q77）。</li></ul>"""

NEW["4.1.3 定长操作码指令格式"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么至少 8 位（原理）</b>：操作码用来<b>区分不同指令</b>，k 位操作码最多给出 2^k 种编码。200 条指令 → 需要 <code>2^k ≥ 200</code>：2⁷=128 &lt; 200 ≤ 2⁸=256 → <b>k≥8 位</b>（⌈log₂200⌉=8，Q284）。</li><li>定长操作码的缺点：指令字短时地址码空间被操作码占用，指令条数越多越挤——引出扩展操作码（见 4.1.4）。</li></ul>"""

NEW["7.1.1 I/O系统的组成"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么外设不能直接挂总线（通俗版）</b>：外设<b>千差万别</b>——速度慢（键盘/打印机 vs CPU）、数据格式不同（串行 vs 并行）、电平/时序不同。直接连总线的话 CPU 要适配所有设备，无法实现。</li><li><b>设备控制器（I/O 接口）的作用</b>：夹在<b>总线与设备之间“翻译+缓冲”</b>：①接收 CPU 命令并译码；②数据缓冲（速度匹配）；③格式/电平转换（并行↔串行）；④向 CPU 报告设备状态（就绪/忙/错）（Q528）。</li><li>连接关系：<b>CPU/主存 → 系统总线 → 设备控制器 → 设备</b>（Q528）。</li></ul>"""

NEW["3.1.2 主存储器的组成和基本操作"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么 MAR/MDR 位数与总线挂钩（原理）</b>：<b>MAR（地址寄存器）</b>存“要访问哪个单元”，要能覆盖<b>全部可寻址单元</b>→位数 = 地址总线宽度 = ⌈log₂(可寻址单元数)⌉；<b>MDR（数据寄存器）</b>存“读出的数据/要写入的数据”→位数 = 数据总线宽度（CPU 一次能传的位数）（Q155）。</li><li>例：64K×8 位主存 → MAR 16 位（2¹⁶=64K）、MDR 8 位；位扩展/字扩展后 MAR 位数由<b>总地址空间</b>决定（见 3.3.2 的 MAR 真题）。</li></ul>"""

NEW["3.6.5 虚拟存储器与Cache的比较"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么 TLB 和 Cache 都用 SRAM（原理）</b>：它们都处于 CPU 访问的<b>最热路径</b>，必须最快——SRAM 是速度最快的存储器件（无需刷新），所以都选它；代价是贵、容量小，只能做“小表”。</li><li><b>命中率为什么都靠局部性</b>：TLB（页表快表）缓存最近用过的页表项、Cache 缓存最近用过的块，都依赖“刚用过还要用/相邻还要用”的局部性原理；<b>缺失后都要回到主存</b>取（Cache 缺→访主存取块，TLB 缺→访主存取页表项）——这就是两者的相似性（Q273 2020）。</li><li><b>区别</b>：Cache 由硬件全自动管理；TLB 是页表的副本、内容由 OS 维护。</li></ul>"""

NEW["3.1.3 存储器的层次化结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么多级存储（通俗版）</b>：速度快的存储贵、容量大的存储慢，没有任何一种存储器能同时“快、大、便宜”→用层次结构：<b>寄存器 → Cache → 主存 → 外存</b>，容量逐级增大、速度逐级变慢、价格逐级下降（Q153、Q157）。</li><li><b>透明性（核心考点）</b>：主存-外存层次的调度（虚拟存储）对<b>应用级程序员透明</b>（他不关心数据在不在内存，由 OS 管）；Cache-主存层次对<b>程序员完全透明</b>（硬件自动管理）。</li><li><b>CPU 与 Cache 交换、也可与主存直接交换</b>：Cache 命中的读走 Cache；主存写回/直写、以及 Cache 缺失且无法填充时（如 DMA）CPU 可直接访主存（Q158）。</li></ul>"""

NEW["5.6.5 高级流水线技术"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么按序流动只有 RAW（原理）</b>：流水线按序发射、按序完成时，指令间依赖只有“<b>后面的指令要读前面指令刚写的结果</b>”（RAW，写后读）——因为后一条还没写完、不可能反过来被前面读。只有<b>乱序</b>（后完成的先写）才会出现 WAR（写后读改先写后读错）与 WAW（写后写）。（Q455）</li><li><b>流水段不是越多越好</b>：每多一段就要在段间加<b>流水寄存器</b>（保存中间结果）——段数过多时寄存器开销大增、控制复杂、且段间固定延时占比例上升，<b>吞吐率不再提升甚至下降</b>（Q453）。</li></ul>"""

NEW["6.2.2 总线定时"] = """<h4>📌 题库考点补充</h4><ul><li><b>异步通信的三种握手（按可靠性/速度递进）</b>：异步无公共时钟，靠<b>请求（主→从）—应答（从→主）</b>信号配合：<ul><li><b>不互锁</b>：发请求不等应答就走（最快、最不可靠——对方可能没反应）</li><li><b>半互锁</b>：请求后等应答才撤销请求，但应答不要求确认（中速）</li><li><b>全互锁</b>：请求→应答→撤销请求→应答撤销，步步确认（最慢、最可靠）</li></ul>故速度 <b>不互锁 &gt; 半互锁 &gt; 全互锁</b>，可靠性相反（Q515、Q522 2015）。</li></ul>"""

NEW["4.3.1 常用汇编指令介绍"] = """<h4>📌 题库考点补充</h4><ul><li><b>相对转移目标怎么算（原理）</b>：x86 的偏移量是<b>相对“下一条指令首地址”</b>的（取指后 PC 已指向下一条）：<code>目标 = 取指后 PC + 偏移量</code>。</li><li>真题（Q332）：jle 指令共 <b>2 字节</b>（操作码 1B＋偏移 1B），偏移=0dH，指令首地址 804846cH → 取指后 PC = 804846cH+2 = 804846eH → 目标 = <b>804846eH+0dH = 804847bH</b>。注意不能把偏移加到“当前指令地址”上。</li></ul>"""

NEW["5.3.4 单总线结构的数据通路"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么 ALU 要暂存器（原理）</b>：单总线结构下<b>同一时刻总线上只能传一个数</b>。ALU 两个输入若都直连总线：先送 A，再送 B 时 A 就被覆盖了——所以<b>一个输入直连总线、另一个输入先锁进暂存器（如 Y 寄存器）</b>固定住（Q383）。</li><li><b>输出为什么也要经暂存器</b>：ALU 结果直接回总线会与正在送入的输入<b>形成反馈干扰</b>运算（结果还没稳定就又被当作输入），所以输出先存暂存器（如 Z），稳定后再送总线/寄存器。</li><li>总结：单总线 ALU = <b>输入一暂存＋输出一暂存</b>，代价是多一个时钟周期，但省总线。</li></ul>"""

NEW["4.4.3 CISC 和 RISC 的比较"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么 CISC 配微程序控制器（原理）</b>：CISC 指令功能强、变长、寻址方式多→每条指令要做的操作复杂且各不相同，若用硬布线（组合逻辑）直接生成控制信号，电路极其庞大且难改；用<b>微程序（控制存储器 ROM 存微指令序列）</b>实现则简洁、易设计修改（Q342）。</li><li><b>CISC 优缺点</b>：方便汇编程序员编程（一条指令干一件事）、代码紧凑；但<b>指令格式种类多不利于编译优化</b>（编译器难以统一调度），且复杂指令执行慢。</li><li>对照：RISC 指令规整少，用<b>硬布线</b>控制器、普遍流水线（见 §4.4）。</li></ul>"""

NEW["3.5.6 Cache容量的计算"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么存储系统总容量不是简单相加（原理）</b>：Cache 是<b>主存部分内容的副本</b>，不增加信息量，所以“主存容量＋Cache 容量”没有意义——系统实际可寻址容量 = 主存容量（Q224）。</li><li><b>缺失时两处传送的单位不同</b>：<b>主存→CPU 传“字”</b>（CPU 要哪个字立刻给它，速度优先）；<b>主存→Cache 传“块”</b>（一次填一整块，利用空间局部性为后续访问做准备）（Q225）。</li></ul>"""

NEW["5.2.2 CPU的时序控制"] = """<h4>📌 题库考点补充</h4><ul><li><b>多周期 CPU 的特点（为什么）</b>：把每条指令拆成取指→译码→执行→访存→写回等步骤，<b>每步一个时钟周期</b>：<ul><li>各指令<b>使用的时钟周期数可不同</b>（乘法比加法步骤多、周期数多）</li><li>但<b>每个时钟周期的长度均匀</b>（时钟源统一，周期长度固定）</li></ul></li><li><b>取指与译码是每条指令必经</b>（否则无法知道要做什么）；而<b>取数/写回不一定访存</b>——只有访存类指令才走到访存周期（如加法只读写寄存器、不需要访存）（Q376、Q377）。</li></ul>"""

NEW["4.3.4 过程调用的机器级表示"] = """<h4>📌 题库考点补充</h4><ul><li><b>过程调用为什么这个顺序（逐步讲）</b>：<ul><li><b>传实参</b>：调用者把参数放进约定寄存器/压栈（被调者按约定取）；</li><li><b>保存返回地址并跳转</b>：CALL 指令把“调用后要回到的下一条指令地址”（PC 当前值）压栈，再跳进被调者入口——不保存就回不去；</li><li><b>保存现场、分配局部空间</b>：被调者开头把要用的寄存器压栈（免得覆盖调用者数据），再给局部变量腾空间（栈帧）；</li><li><b>执行函数体</b>：完成功能；</li><li><b>恢复现场、释放空间</b>：还原寄存器、撤销栈帧；</li><li><b>取返回地址返回</b>：RET 从栈弹出返回地址送 PC，回到调用点继续。</li></ul>顺序即“先替被调者铺路、干完再还路”（②③①⑥⑤④，Q338）。</li></ul>"""

NEW["5.7.2 硬件多线程的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>超线程怎么“一个核当两个用”（原理）</b>：单核内复制<b>线程上下文</b>（PC、寄存器组等状态），使核心能同时保存<b>两个线程的执行状态</b>——某个线程访存等待（流水线空转）时，另一线程的指令<b>立刻顶上占用执行部件</b>，提高部件利用率（Q476、Q477）。</li><li><b>为什么性能≠双核</b>：两个线程<b>共享执行单元</b>（ALU/访存部件），争用资源时只能一个挂起等另一个，所以超线程的加速比远低于双核。</li><li><b>需要芯片组/OS/应用支持</b>才有效（OS 要把线程认成“逻辑核”来调度）。</li></ul>"""

NEW["3.2.2 非易失性存储器"] = """<h4>📌 题库考点补充</h4><ul><li><b>Flash 为什么断电不丢（原理）</b>：每个存储单元是一个<b>浮栅 MOS 管</b>，电荷注入浮栅后即使断电也<b>被绝缘层困住不泄漏</b>——有电荷=0、无电荷=1（或反之），因此<b>非易失</b>（Q175、Q190 2012）。</li><li><b>为什么写慢于读（结构决定的）</b>：写数据前必须先<b>擦除整块</b>（把块内全部单元复位），所以写（擦+编程）明显慢于读；读速度接近 RAM、写接近 ROM → <b>读写不对称</b>。</li><li>用于 U 盘、SSD、BIOS 芯片；克服“写慢/擦块”靠磨损均衡与块管理（SSD 控制器）。</li></ul>"""

NEW["§5.4 控制器的功能和工作原理"] = """<h4>📌 题库考点补充</h4><ul><li><b>控制器两大实现方式（为什么这样选）</b>：<ul><li><b>硬布线（组合逻辑）控制器</b>：用门电路直接组合出“操作码→控制信号”的电路。<b>速度快</b>（纯硬件、无存储访问），但指令一改电路就得重做、<b>修改扩展难</b>（Q406、Q418 2009）。</li><li><b>微程序控制器</b>：把每条指令的执行步骤预先编成<b>微程序存入控制存储器（CS/ROM）</b>，执行指令 = 逐条读出微指令产生控制信号。<b>速度慢</b>（多一次读 CS），但改指令只需改微程序、<b>修改容易、灵活性高</b>。</li></ul></li><li><b>选型结论</b>：实时性要求高（如精简指令快速响应）→ 硬布线；可修改性/灵活性要求高（复杂指令集 CISC）→ 微程序。</li></ul>"""

NEW["4.1.4 扩展操作码指令格式"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么需要扩展操作码（原理）</b>：指令字长固定后，地址码越多，留给操作码的位数就越少。但不同指令的地址数需求不同（三地址/二地址/一地址/零地址）——于是操作码“<b>借用</b>”短指令多余的地址码位：地址数越少、操作码越长，<b>既保持字长不变又增加指令总数</b>（Q285）。</li><li><b>原理细节</b>：短操作码的编码<b>不能全部用完</b>，必须留出若干作“扩展前缀”；如 12 位指令、地址码 4 位：二地址最多 15 条（留 1 个编码扩展）、一地址最多 15 条、零地址 16 条 → 共 15+15+16=<b>46 条</b>（Q289）。</li></ul>"""

NEW["5.1.3 CPU中的寄存器"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么分“可见/不可见”（原理）</b>：<b>汇编程序员要显式使用</b>的寄存器必须在指令中直接指名（可见）：PC（程序计数器，下条指令地址）、通用寄存器（存数据/地址）、基址/变址寄存器、状态/标志寄存器（PSW，条件转移依据）（Q366 2010、Q423 2021）。</li><li><b>透明寄存器</b>（程序员不感知、汇编不触及）：<b>IR</b>（当前指令，供译码）、<b>MAR/MDR</b>（访存中转，见 3.1.2）、微指令寄存器——它们由控制器内部使用，程序无需也无法直接访问（Q350、Q423）。</li></ul>"""

NEW["3.5.1 程序访问的局部性原理"] = """<h4>📌 题库考点补充</h4><ul><li><b>两类局部性（通俗版）</b>：<ul><li><b>时间局部性</b>：刚访问过的数据<b>很快还会再访问</b>——循环变量 i、累加器 sum 每轮都用（Q229、Q255 2017）。</li><li><b>空间局部性</b>：访问了某个地址，<b>附近的地址很快也会被访问</b>——数组按序访问、指令顺序执行。</li></ul></li><li><b>块过大为什么反而降低命中率（反直觉点）</b>：Cache 容量固定时，块越大块数越少；把一个“大块”装进来会挤掉更多有用的块，而大块里<b>大量数据可能根本用不到</b>（浪费）→ 命中率先升后降（Q232、Q248）。</li><li>指令空间局部性优于数据空间局部性（顺序取指）；Cache 设计正是利用这两条局部性。</li></ul>"""

NEW["5.7.4 共享内存多处理器的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>Flynn 分类（按指令流/数据流）</b>：<ul><li><b>SISD</b>：单指令单数据——常规单核处理器（含标量流水线）</li><li><b>SIMD</b>：单指令多数据——<b>一个控制部件</b>发出同一条指令，驱动<b>多个处理单元</b>对各自的数同时操作（阵列机、向量机、GPU）</li><li><b>MIMD</b>：多指令多数据——多处理机、多核、SMP（各处理器独立指令流）</li><li><b>MISD</b>：多指令单数据——理论存在、几乎无实际系统</li></ul>（Q472、Q473、Q474、Q475）。</li></ul>"""

NEW["7.3.1 程序查询方式"] = """<h4>📌 题库考点补充</h4><ul><li><b>程序查询的原理与代价</b>：CPU 执行<b>查询程序</b>，不断读设备状态寄存器判断“数据是否就绪”，就绪才传一个数据，如此反复直到全部传完——CPU 与外设<b>串行工作</b>（设备等待期间 CPU 空转“忙等”，Q568）。</li><li><b>优点/缺点</b>：硬件最简单（不用中断/DMA 控制器）、控制容易；缺点 = CPU 利用率极低（大部分时间在查状态）。</li><li><b>实现细节</b>：每次传送后 <b>主存地址寄存器 +1、计数器 −1</b>（维护传输指针）；查询开销计算真题：50MHz、每秒查 200 次×500 周期 = 100000 周期 → 占 <b>0.20%</b>（Q584 2011）。</li></ul>"""

NEW["5.1.2 CPU的基本结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>PC（程序计数器）的三个细节</b>：①<b>总是存放指令地址</b>（不是数据地址）；②取指完成后 PC <b>自动加“当前指令长度”</b>（变长指令加对应长度，不是固定 +1）；③<b>转移指令执行时才改 PC</b>——无条件转移<b>转移成功</b>才改为目标地址，失败仍 = 顺序下一条（Q351、Q353、Q354、Q355）。</li><li><b>PC 位数怎么定</b>：要能容纳主存所有指令地址——按字编址时取决于<b>存储器容量和指令字长</b>（如 4GB/字长 32 位 → PC=30 位字地址，Q367 2016）。</li></ul>"""

NEW["§6.1 总线概述"] = """<h4>📌 题库考点补充</h4><ul><li><b>三类总线各传什么（为什么方向不同）</b>：<ul><li><b>数据线（双向）</b>：CPU 与主存/外设互传数据（都能发/收）</li><li><b>地址线（单向）</b>：只能 CPU → 主存/外设（地址由 CPU 给出，用于选择单元/端口）</li><li><b>控制线</b>：定时、读写命令、请求/回答等控制信息</li></ul>（Q484、Q485、Q487、Q496）。</li><li><b>总线复用（原理）</b>：同一条信号线<b>分时传送不同信号</b>（如地址/数据复用：先传地址、再传数据）→ 省引脚/省线，但每个周期只能干一件事、<b>降低速度</b>（Q490、Q503）。</li><li>系统总线连接 CPU、主存和外设（Q485）。</li></ul>"""

NEW["§7.3 I/O方式"] = """<h4>📌 题库考点补充</h4><ul><li><b>中断隐指令（硬件自动完成，为什么）</b>：CPU 检测到中断请求后，要立刻进入中断服务程序——这 3 步<b>来不及等软件安排、必须硬件自动</b>做（Q559、Q563、Q575、Q585 2012、Q600 2024）：<ul><li>①<b>关中断</b>：先屏蔽新中断，防止保存断点时被打断（现场破坏）</li><li>②<b>保存断点（PC）</b>：记住“回到哪继续执行”（压栈/存专用寄存器）</li><li>③<b>形成中断向量地址/引出中断服务程序</b>：按中断类型找到服务程序入口</li></ul></li><li><b>为什么保存通用寄存器不是硬件的活</b>：寄存器很多、保存到哪块内存由 OS 决定——放在<b>中断服务程序开头由软件</b>完成；硬件只保最关键、最少的状态 = <b>PC 和 PSW</b>（Q566）。</li></ul>"""

NEW["6.1.2 常见的总线标准"] = """<h4>📌 题库考点补充</h4><ul><li><b>USB 为什么叫“通用串行总线”（原理）</b>：<ul><li><b>串行</b> = 数据按位在一根线上传输，<b>不能同时传 2 位</b>（与并行的 ISA/PCI 相对，Q501 2012）</li><li><b>即插即用/热插拔</b>：设备接入时自动被主机识别配置（枚举），无需断电重装</li><li><b>菊花链拓扑</b>：设备一个接一个串下去，最多可扩至 127 台（Q502 2013）</li><li>它是<b>设备与设备控制器之间互连</b>的接口标准（不是 CPU-主存总线）</li></ul></li><li>常见总线标准：ISA、EISA、PCI、PCI-Express 等（Q499 2010）。</li></ul>"""

NEW["§1.2 计算机系统层次结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>完整计算机系统 = 硬件 + 软件</b>：只有主机/运算器/存储器都不完整（缺操作系统和应用软件就只是裸机），Q1。</li><li><b>ALU 不只做算术运算</b>：还做<b>逻辑运算</b>（与/或/非/异或/移位）——名字叫“算术逻辑单元”（Q4）。</li><li><b>层次结构（自上而下抽象层）</b>：<b>高级语言虚拟机 → 汇编语言虚拟机 → 操作系统虚拟机 → 机器语言机器</b>——上层屏蔽下层细节、向下层翻译（Q5）。</li></ul>"""

NEW["5.6.4 流水线的冒险与处理"] = """<h4>📌 题库考点补充</h4><ul><li><b>结构冒险（为什么）</b>：多条指令在同一周期<b>争用同一个硬件资源</b>（典型：都在访存）。对策：<b>指令 Cache 与数据 Cache 分离</b>（取指与取数各用各的存储）、资源重复设置、部件按序使用（Q446、Q460 2014）。</li><li><b>控制冒险（为什么）</b>：<b>转移/中断/异常</b>改变了指令流向，流水线里已预取的指令可能作废。对策：<b>空操作</b>（插入气泡）、<b>分支预测</b>（静态每次猜同一个方向、动态按历史更准；<b>预测错则冲刷已取指令</b>重取）（Q451、Q452）。</li><li>数据冒险 RAW 的处理见 §5.6（转发/阻塞）。</li></ul>"""

NEW["§7.2 I/O接口"] = """<h4>📌 题库考点补充</h4><ul><li><b>I/O 接口要干四件事</b>：<b>选址</b>（译码出设备地址，只响应发给自己的）、<b>传送命令</b>（CPU 的命令转给设备）、<b>传送数据</b>（缓冲+格式转换）、<b>反映设备状态</b>（就绪/忙/错给 CPU 查）（Q533）。</li><li><b>三大类寄存器</b>：<b>数据缓冲寄存器</b>（数据中转）、<b>控制寄存器</b>（存命令）、<b>状态寄存器</b>（存状态）（Q535）。</li><li><b>I/O 指令</b>：是机器指令的一类（执行 I/O 操作），但<b>格式与通用指令不同</b>（含设备地址/功能码而不含通用操作数，Q537）；数据传送发生在<b>通用寄存器与 I/O 端口</b>之间（Q547 2017）。</li></ul>"""

NEW["5.3.3 数据通路的组织与分类"] = """<h4>📌 题库考点补充</h4><ul><li><b>单周期 vs 多周期的本质区别</b>：<ul><li><b>单周期</b>：每条指令 <b>1 个时钟周期</b>内完成（CPI=1）；时钟必须按<b>最慢指令</b>（如乘法）的时延定长 → 时钟周期长；每个部件<b>一条指令只用一次</b>（不能复用）；<b>不能用单总线结构</b>且需<b>分离的指令/数据存储器</b>（一个周期只够做一件事）；硬件冗余大、成本高（Q388、Q389、Q390、Q394 2016）。</li><li><b>多周期</b>：指令拆多步、<b>CPI&gt;1</b>；部件<b>分时复用</b>（一个 ALU 先后算地址、算数据）；每一步一个短时钟；控制信号随阶段<b>变化</b>（Q389、Q390）。</li></ul>单周期简单但慢，多周期复杂但快（时钟短部件省）。</li></ul>"""

NEW["3.2.3 多模块存储器"] = """<h4>📌 题库考点补充</h4><ul><li><b>低位交叉为什么快（原理）</b>：把地址<b>低位做模块号</b>（连续地址分到不同模块），各模块有<b>独立的读/写电路</b>，可对其<b>轮流启动、重叠工作</b>——像流水线一样，前一个仍在读、后一个已开始 → 平均访存时间缩短、带宽提高（Q186、Q181）。</li><li><b>启动间隔条件</b>：轮流启动的间隔 = 总线周期，要求 <b>模块数 ≥ 存取周期 ÷ 总线周期</b>，否则来不及轮完。</li><li><b>计算真题</b>：四体低位交叉、存取周期 200ns、总线周期 50ns、字 32 位：200ns 内可读出 <b>128 位</b>（4 字，Q182）；连续取 m 字耗时 <code>t = T + (m−1)·r</code>（T=存取周期、r=总线周期，Q183）。</li></ul>"""

NEW["1.2.2 计算机硬件（冯·诺依曼机）"] = """<h4>📌 题库考点补充</h4><ul><li><b>冯·诺依曼机核心特征</b>：<b>存储程序</b>（程序和数据都以二进制存在同一主存，Q3）、<b>按地址访问</b>、自动按序执行——因此叫<b>控制流驱动</b>（按指令序列流来驱动运算，与数据流驱动相对，Q2）。</li><li><b>数据在哪（易错）</b>：指令中给出的是<b>操作数的地址码</b>，操作数本身在主存/寄存器里，<b>不是直接写在指令里</b>；“数据都在指令中直接给出”是错误说法（Q13 2019 真题）——那只有<b>立即寻址</b>才是。</li></ul>"""

NEW["5.5.2 异常和中断的分类"] = """<h4>📌 题库考点补充</h4><ul><li><b>自陷（陷阱，人为异常）</b>：由<b>陷阱/访管指令</b>主动触发（人预先设计好），用途 = <b>程序调试断点、单步跟踪、系统调用入口</b>；因其是“主动请求”，处理完后返回到陷阱指令的<b>下一条</b>（Q425、Q436 2020）。</li><li><b>内部异常不一定能回断点</b>：<b>非法指令、除 0</b> 等严重故障程序已无法继续，只能<b>终止</b>——所以内部异常处理后“可能回不到原断点继续执行”（Q434 2015）。</li><li><b>谁来做（分工）</b>：异常/中断<b>响应（检测、保护断点、转入口）由硬件</b>完成；<b>进程上下文切换（换页表、状态）由 OS 软件</b>完成（Q432）。</li></ul>"""

NEW["2.2.3 定点数的加减运算"] = """<h4>📌 题库考点补充</h4><ul><li><b>模 4 补码（双符号位）为什么这样用</b>：正常存储只占<b>1 个符号位</b>；送入 ALU 运算时把符号位<b>复制成两位</b>（00 正、11 负）——加法后<b>最高两位不同（01/10）即溢出</b>，结果仍截成单符号位。即“<b>存储单符号、运算双符号</b>”（Q79）。</li><li><b>C 语言符号扩展真题</b>：short→int 时<b>负数高位补 1</b>（符号扩展）：x=127→7F、y=−9→FFF7（short→int）→ 运算得 z=00000076H（Q93 2009）；<code>[x]=FFFF FFDH</code>（即 −33，注意 FFDH 是 16 位补码 −3 符号扩展到 32 位）、<code>[y]=41H</code>（=65）→ x−y = <b>FFFF FF9EH</b>（Q97 2018）。</li></ul>"""

NEW["4.1.1 指令集体系结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>ISA 是什么（通俗版）</b>：<b>软件/硬件交界面</b>的“契约”——软件开发者（编译器、汇编）能看到、依赖的机器属性：指令字格式与类型、寻址方式、通用寄存器个数与位数、存储空间与编址方式、大端/小端等（Q278、Q279、Q291 2022、Q293 2025）。</li><li><b>ISA 不管什么（易错）</b>：<b>不规定实现细节</b>——执行指令用的控制信号、CPU 时钟周期、加法器进位方式、是否阵列乘法器、是否微程序控制器、是否单总线数据通路（这些都是微体系结构的事）。同一 ISA 可以有多种不同速度的实现（对应 1.2.4 的 ISA vs 微体系结构）。</li></ul>"""

NEW["7.2.2 I/O接口的基本结构"] = """<h4>📌 题库考点补充</h4><ul><li><b>连接通路</b>：<b>CPU和主存 → I/O 总线 → I/O 接口 → 通信总线（电缆）→ 外设</b>（Q543）。</li><li><b>为什么两侧数据宽度可不同</b>：主机侧接高速并行总线（32/64 位），设备侧可能是串行/窄并行——接口负责<b>宽度与格式转换</b>（Q544）。</li><li><b>I/O 总线数据线上传的三种信息</b>：命令字、状态字、<b>中断类型号</b>（Q545 2012）——注意中断类型号也走数据线（不是地址线）。</li><li><b>易错</b>：磁盘向磁道写数据用<b>串行</b>方（磁道是比特流，Q541）；磁盘驱动器本身<b>不是 I/O 接口</b>（它是设备，接口在磁盘控制器，Q548 2021）。</li></ul>"""

NEW["7.1.2 外部设备"] = """<h4>📌 题库考点补充</h4><ul><li><b>点阵字库容量</b>：16×16 点阵 = 16×16 位 = 32B/字；7500 字 × 32B = <b>240KB</b>（Q529）。</li><li><b>显存（视频存储器）容量</b> = 分辨率 × 颜色深度：1024×1024 × 8bit = <b>1MB</b>（Q530）。</li><li><b>刷新带宽 = 分辨率 × 颜色深度 × 帧频</b>（每秒要重绘整屏多少次）：1600×1200×24×85 ≈ 3916.8Mb/s，占显存带宽 50% → 显存总带宽 = <b>7834Mb/s</b>（Q531 2010）。</li></ul>"""

NEW["5.2.3 指令周期的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>几个“周期”从大到小</b>：<b>指令周期</b> = 取出并执行一条指令的总时间（含取指+执行）；<b>机器周期</b> = 一次总线/访存操作的时间；<b>时钟周期</b> = 最小时间单位（节拍），机器/指令周期都是时钟周期的整数倍（Q369、Q370）。</li><li><b>无条件转移为何 PC 改两次</b>：取指阶段 PC 先<b>加指令长度</b>（指向下一条）；执行阶段再把 PC <b>改为转移目标</b>（Q371、Q372）。</li><li>指令周期第一阶段<b>必为取指</b>；乘法指令周期 &gt; 加法（多周期 CPU）；<b>单周期 CPU 的指令周期 = 1 个时钟周期</b>（Q375）。</li></ul>"""

NEW["§5.7 多处理器的基本概念"] = """<h4>📌 题库考点补充</h4><ul><li><b>双核是什么</b>：一个 CPU 芯片上集成<b>两个执行核心</b>（各自有取指/译码/执行部件），核心之间可<b>独立或共享 Cache</b>（Q478、Q480）。</li><li><b>多核的局限（为什么单线程不加速）</b>：<b>单线程程序只用一个核</b>，多核帮不上忙（且单核频率往往更低→可能更慢）；<b>能耗通常更高</b>（Q479、Q481）。</li><li><b>SMP（对称多处理）</b>：所有处理器<b>共享单一物理地址空间</b>（哪块内存都能访问，Q482 2022）；多核属于 <b>MIMD</b> 类；硬件多线程技术<b>不只可用于多核</b>（单核超线程也可，Q482）。</li></ul>"""

NEW["5.2.1 指令执行的一般流程"] = """<h4>📌 题库考点补充</h4><ul><li><b>为什么取指不需要操作码控制（原理）</b>：取指是<b>所有指令的共同第一步、固定流程</b>（都从 PC 指向的主存取指令送 IR、PC 自增）——它是控制器的<b>固有功能</b>，不依赖被取指令的操作码，因此自动进行；但<b>不同长度指令的取指操作不同</b>（要取 2 字节还是 4 字节由当前指令长度决定，Q373、Q374、Q398）。</li><li><b>CPU 怎么区分指令和数据</b>：按<b>指令周期的阶段</b>——<b>取指阶段取出的是指令</b>、执行阶段访存取的是数据（Q378 2009）。</li><li>开中断时<b>每条指令执行结束</b>都可能被外部中断打断；空操作指令取指后 PC 同样 +1（Q379 2011）。</li></ul>"""

NEW["7.2.4 I/O端口及其编址"] = """<h4>📌 题库考点补充</h4><ul><li><b>统一编址（存储器映射 I/O）</b>：I/O 端口与主存单元<b>共享同一个地址空间</b>，用地址码区分（高位段给 I/O）——CPU 用<b>访存指令</b>即可访问端口，<b>不需要专门的 IN/OUT 指令</b>；端口地址必须<b>固定在地址空间某区域</b>（不能随意乱放，否则与主存地址冲突，Q532、Q536、Q538、Q539、Q540、Q546 2014）。</li><li><b>独立编址（端口 I/O）</b>：端口与主存<b>各占独立地址空间</b>，靠指令类型区分 → CPU 需<b>专门的输入/输出指令（IN/OUT）</b>访问端口（Q534、Q538）。</li><li>两者对比：统一编址指令丰富但占地址空间；独立编址需专用指令但地址空间不冲突。</li></ul>"""

NEW["4.2.1 指令寻址和数据寻址"] = """<h4>📌 题库考点补充</h4><ul><li><b>取操作数速度排序（为什么）</b>：<b>立即 &gt; 寄存器 &gt; 直接 &gt; 间接</b>——立即数在指令里（0 次访存）、寄存器也在 CPU 内（0 次）、直接寻址访存 1 次、间接寻址先取地址再取数共 2 次（Q298）。</li><li><b>地址字段长度</b>：<b>寄存器寻址</b>的地址段最短（只用 log₂寄存器数 位）；<b>隐含寻址</b>（操作数隐含在专用寄存器如累加器 A）进一步简化地址结构（Q296、Q297）。</li><li><b>各寻址语义</b>：直接寻址形式地址<b>即有效地址 EA</b>（Q299）；间接寻址 <code>EA=(A)</code> 需访存 ≥2 次；寄存器间接 <code>EA=(R)</code>（Q304、Q310）；<b>转移指令把地址码送 PC</b>（Q295）；跳跃寻址实现条件/无条件转移（Q303）。</li></ul>"""

# ============ 主逻辑 ============
def main():
    dry = "--dry" in sys.argv
    raw = open(PATH, "rb").read()
    d = json.loads(raw.decode("utf-8"))
    done, appended, missing = [], [], []
    for c in d["chapters"]:
        for s in c["sections"]:
            sk = "§" + s["section"].strip()
            if sk in NEW:
                h = s.get("html") or ""
                j = h.find("📌")
                if j >= 0:
                    k = h.rfind("<h4>", 0, j)
                    s["html"] = h[: (k if k >= 0 else j)] + NEW[sk]
                    done.append(sk)
                else:
                    s["html"] = h + NEW[sk]
                    appended.append(sk)
            for ss in s.get("subsections", []):
                sec = ss["section"].strip()
                if sec not in NEW:
                    continue
                h = ss.get("html") or ""
                j = h.find("📌")
                if j >= 0:
                    k = h.rfind("<h4>", 0, j)
                    ss["html"] = h[: (k if k >= 0 else j)] + NEW[sec]
                    done.append(sec)
                else:
                    ss["html"] = h + NEW[sec]
                    appended.append(sec)
    for k in NEW:
        if k not in done and k not in appended:
            missing.append(k)
    print(f"替换 {len(done)} 节, 追加 {len(appended)} 节, 未命中 {len(missing)}")
    if missing:
        for m in missing:
            print("  [未命中]", m)
        if not dry:
            sys.exit(1)
    if dry:
        print("dry-run 结束, 未落盘")
        return
    out = (json.dumps(d, ensure_ascii=False, indent=2) + "\n").encode("utf-8")
    open(PATH, "wb").write(out)
    d2 = json.load(open(PATH, encoding="utf-8"))
    n = 0
    for c in d2["chapters"]:
        for s in c["sections"]:
            if "📌 题库考点补充" in (s.get("html") or ""):
                n += 1
            for ss in s.get("subsections", []):
                if "📌 题库考点补充" in (ss.get("html") or ""):
                    n += 1
    print(f"写入完成, 含补充块的 节+小节 总数 = {n}")

if __name__ == "__main__":
    main()