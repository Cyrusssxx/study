# 408 刷题应用 — 缺点评估报告

> 评估对象：`D:\ai code\408-quiz-app`（Flask + PWA 双端的考研 408 刷题应用）
> 评估日期：2026-07-25 ｜ 依据：源码实证（含文件/行号）
> 说明：按严重度分层，每条均标注代码依据；这是"健康度体检"，不是否定功能完成度。

---

## 一、架构层：最大的结构性缺陷

### 1. 全套「双端镜像」= 一份功能写两遍，维护成本翻倍且必然漂移 🔴最严重
同一套逻辑在两处平行实现：

| 维度 | Flask 端 | PWA 端 |
|---|---|---|
| 业务逻辑 | `app.py`(827 行) + `database.py`(660 行) | `pwa/js/backend.js`(762 行) |
| 页面 | `templates/*.html` | `pwa/*.html` |
| 样式 | `static/css/style.css`(2268 行) | `pwa/css/style.css`(2268 行，**逐字节相同**) |
| 题库数据 | `data/questions/*.json`(~2.2MB) | `pwa/data/*.json`(**同样 2.2MB 复制**) |

**后果**：
- 每加一个功能都要写两遍、再靠 `git hash-object` + node 逐行 diff 保证不漂 —— 这个固定流程本身就是缺陷的证据，不是优点。
- 两份实现随时可能**静默分叉**（改了一边忘同步另一边），且无任何自动化拦截。
- 题库 JSON 三份物理副本（本项目 `data/` + `pwa/data/` + `study` 仓库），改一次同步三处。

> 正解方向：抽出唯一数据源与逻辑层，Flask 仅做静态托管或两端共享同一份 JS 逻辑；CSS 至少构建期复制而非手工维护两份。

### 2. 两套互不相通的数据存储，无跨设备同步 🔴
- exe 版进度存 **SQLite**（`database.py`），网页版存 **IndexedDB**（`backend.js`）。
- 桌面 exe 刷的题/错题/收藏/笔记，**在网页端完全看不到**，反之亦然。备份/迁移全靠手动。
- 网页端数据只活在**那一个浏览器**里——换浏览器、清缓存、无痕模式即丢。

---

## 二、内容与数据质量

### 3. 题库源自 PDF OCR，答案/解析质量是持续隐患 🔴
工作区一大堆纠错脚本即证据：`fix_exponents*.py`、`fix_pua.py`、`fix_gaps.py`、`clean_long_expl.py`、`extract_*_ocr.py`、`fill_missing_answers.py`，以及 `explanation_report.json`(200KB)、`missing_questions.json`(36KB)、`exp_scan_report.txt`。
- 说明**部分题目仍可能有 OCR 残留错误、缺答案、解析错位** —— 对刷题 App 这是最致命的，用户会背错。
- 缺少系统化的「题目质量状态」标记与纠错闭环，全靠零散脚本人肉扫。

---

## 三、健壮性与安全

### 4. API 参数无防御，坏输入直接 500 🟡
`app.py` L184-185：
```python
page = int(request.args.get('page', 1))
per_page = int(request.args.get('per_page', 1))
```
`?page=abc` 直接抛 `ValueError` → 500。章节筛选、`random.sample` 也无空集保护。个人单机风险低，但属明显缺口。

### 5. 静默失败吞掉数据丢失 🟡
笔记高亮保存处 `catch (e) { /* 高亮保存失败静默，不阻断阅读 */ }` —— IndexedDB 配额满或写失败时，用户**以为存上了实际没存**，毫无提示。base64 压缩图（1200px JPEG）入库无配额管理，长期有撑爆 IndexedDB 风险。

### 6. XSS 面（当前可控，但脆弱）🟡
`templates/notes.html` L33 `{{ sec.html | safe }}` 直出原始 HTML。目前笔记内容自产、可信，暂无风险；但只要将来有「用户可编辑内容」进入这条渲染路径，即成注入漏洞。（批注用 `textContent` 防注入，这点正确。）

---

## 四、工程化

### 7. 无应用级自动化测试 🟠
`test_extract_os.py` / `test_ocr.py` 是 OCR 抽取探针，**不是**路由/业务逻辑回归测试。每次改动的安全网完全依赖手工冒烟 + 临时 node/hash 校验，功能越多回归风险越大。

### 8. 仓库卫生差，构建工具与应用混杂 🟡
根目录塞满一次性脚本与调试产物：`_pdf_pages.txt`、`_preview_daka.txt`(136KB)、`_xlsx_dump.txt`、`nat_page.png`(488KB)、`exp_dump.txt`…… 无 `tools/` 与应用代码的清晰隔离，难以上手维护。

### 9. PWA 缓存版本纯手工维护，易出错 🟡
每次改动手动递增 `CACHE_VER`（已 v18→v21），`PRECACHE` 列表手工维护。**漏一次**用户即拿到新旧混用缓存。应由构建脚本按内容哈希自动生成。

### 10. 2268 行单体 CSS，无组件化 🟡
样式全堆一个文件（×2 份）。z-index 已需人肉排账本（3000/2400/2300/2000…）避免层级打架，是可维护性恶化的信号。

---

## 五、体验细节（较轻）

- **性能**：每科题库客户端全量 parse + 每次进页面重建内存检索索引；笔记页一次性渲染整科、无虚拟化（长到需专门加"回到顶部"按钮）。4 科规模尚可，但无扩展余量。
- **无障碍**：图标/按钮多用 emoji、高亮仅靠颜色区分、缺 ARIA、键盘导航有限。个人自用影响小。

---

## 总结：优先级排序

| 优先级 | 问题 | 建议动作 |
|---|---|---|
| 🔴 P0 | 双端镜像重复实现 | 收敛为单一逻辑/数据源，消灭手工同步 |
| 🔴 P0 | 题库 OCR 质量 | 建质量标记 + 纠错闭环，比加功能更重要 |
| 🟠 P1 | 两套数据不互通 | 统一导出格式，做一键跨端迁移 |
| 🟠 P1 | 无应用级测试 | 给路由/核心逻辑补最小回归测试 |
| 🟡 P2 | 参数无防御、静默失败 | 加 try/except 与失败可见提示 |
| 🟡 P2 | 仓库卫生 / 缓存版本手工 | 清理产物、构建期自动化 |

**一句话**：作为个人自用的考研刷题工具，功能完成度已很高、体验也做得细；但健康度瓶颈不在"缺功能"，而在**双端镜像带来的维护熵**与**OCR 题库的内容可信度**——这两块不治理，之后每加一个功能都在加重债务。
