# 408 刷题应用 — 项目评估（增量版）

> 评估对象：`D:\ai code\408-quiz`（Flask + PWA 双端考研 408 刷题应用）
> 评估日期：2026-08-14 ｜ 基线：`缺点评估报告.md`（2026-07-25）
> 方法：源码实证 + `git`/`diff`/脚本实测，非主观判断
> 说明：本版**只更新变化项**，未变项沿用基线报告结论，避免重复。

---

## 〇、当前工作树真实状态（先澄清，否则会误判）

`git status` 显示近 200 个文件为 `M`，但 `git diff`（内容差异）**只有 1 个文件真改**：

- **唯一真实改动**：`pwa/dati.html`（35 增 / 41 删）= 大题专项导航改为「向右收起」抽屉（上一轮已完成，未提交）。
- **其余近 200 个 `M` 全是 CRLF 噪声**：`core.autocrlf=true` 且无 `.gitattributes`，git 在「下次触碰」时要把 LF 统一成 CRLF，于是把整库标记为已改。

> 含义：本仓库 `git status` 当前**不可信**，任何「全量提交」都会把整库行尾改一遍。详见第六节第 4 项。

---

## 一、与基线（7-25）对比：哪些变了

| # | 基线问题 | 当前状态 | 判定 |
|---|---|---|---|
| 1 | 双端镜像重复实现（P0） | **已实际分叉**（见第二节） | 🔴 恶化 |
| 2 | 题库 OCR 质量隐患（P0） | 纠错脚本仍在，无闭环标记 | 🔴 未变 |
| 3 | 双存储不互通（P1） | SQLite / IndexedDB 仍分离 | 🟠 未变 |
| 4 | 无应用级测试（P1） | 仍只有 OCR 探针 `test_*.py` | 🟠 未变 |
| 5 | API 参数无防御（P2） | `app.py` L184-185/199/604/608 仍裸 `int()`/`random.sample` | 🟡 未变 |
| 6 | 静默失败吞数据（P2） | 未触及 | 🟡 未变 |
| 7 | 仓库卫生差（P2） | 新增 CRLF 噪声 + `NUL` 保留名文件 | 🔴 恶化 |
| 8 | PWA 缓存版本手工（P2） | `sw.js` 已到 `quiz408-v71`，仍手工递增 | 🟡 未变 |
| — | OS 笔记超长段（基线后新增任务） | **已彻底解决**（见第三节） | ✅ 已解决 |
| — | 优化脚本 tokenizer 死循环/内存爆炸 | **已根治**（见第四节） | ✅ 已解决 |

---

## 二、🔴 最关键新发现：双端镜像从「风险」变成「实际分叉」

基线报告把「双端镜像」列为 P0，并警告「随时可能静默分叉，无自动化拦截」。**这条预警现已应验**：

| 文件 | pwa（网页版） | static/templates（桌面 exe 版） | 结论 |
|---|---|---|---|
| `css/style.css` | **3654 行** | **2708 行**（差 **1132 行**） | 网页版多 946 行样式，exe 版缺失 |
| `index.html` | 与 `templates/index.html` **已 DIFFER** | 已 DIFFER | 两端 HTML 也已漂移 |

- 两端 CSS 自基线（当时均 2268 行、逐字节相同）起**各自增长且未同步**：pwa 涨了 ~1386 行、static 涨了 ~440 行，净分叉 946 行。
- 后果：用**桌面 exe** 跑的用户，看到的是缺了近千行样式、且页面结构与网页版不一致的老界面；两边改一处不互相同步，且无任何校验。
- 这比「写两遍」更糟——现在是「写两遍且已写歪」。

**建议（P0）**：短期把 pwa 当作唯一真源，用脚本把 `pwa/css/style.css`、`pwa/*.html` 同步回 `static/`、`templates/`（或反之定一个真源），并加 `.gitattributes` + 一个 `sync` 校验脚本卡住漂移；长期收敛为单一逻辑/数据源（基线已给方向）。

---

## 三、✅ 数据质量：OS 笔记排版已达标

对 `data/notes/os_notes.json`（698KB）实测：

- 段落总数 **2237**，`<p class="paragraph">` 纯文本长度
- **最大段落：149 字符**；**≥150 字符段落：0 个**；**≥200 字符段落：0 个**
- ≥80 字符段落占 15%（分布健康）

→ 基线后用户要求的「≥150 全拆、超长段手拆」**已彻底完成**，无残留超长段。pwa 与 data 两版均已更新。

---

## 四、✅ 健壮性：优化脚本死循环/内存爆炸已根治

`opt_os_format.py` 当前实现：

- `tokenize()`：**纯线性扫描，无正则回溯**（L74-106），注释明写「绝不回溯，避免灾难性回溯」。
- `_balanced_split_html()` 递归仅在段仍 ≥`SPLIT_LEN` 时触发，每次在中点切分，**严格缩小**，必然收敛；单句/长串段落走「中点附近文字字符硬切」兜底，不再卡死。
- `transform_section_html()` 用 `html.find('</p>')` 且 `end_idx==-1` 有保护（L349），不会死循环。
- `strip_empty_inline()` 用 `while prev != html` 收敛循环。

→ 基线提到的「上一 AI 的 tokenizer 死循环/内存爆炸」**已修复**，脚本可安全重跑。

---

## 五、🔴 后端 API 仍脆弱（P2，3 周未修）

`app.py` 实测（与基线同位置）：

- L184-185：`page = int(request.args.get('page', 1))` / `per_page = int(...)` —— 无 `try/except`，`?page=abc` 直接 `ValueError → 500`。
- L199：`random.sample(questions, len(questions))` —— 空集合直接 `ValueError`。
- L604 / L608：`random.sample(pool, min(...))` —— `pool` 为空直接抛错。

个人单机风险低，但属明显缺口且改动成本低，建议一次性加 `try/except` + 空集保护。

---

## 六、🔴 仓库卫生：新增两项具体隐患

1. **CRLF 噪声污染 `git status`**（见第〇节）：根因 `core.autocrlf=true` 且无 `.gitattributes`。
   - 修复：加 `.gitattributes`（`* text=auto eol=lf`）+ 提交一次归一化，之后 `git status` 才可信。
2. **`NUL` 保留名空文件**：根目录存在 0 字节文件 `NUL`（Windows 设备保留名）。Windows 下对该文件无法安全 `open`/`del`，git 跟踪它存在跨平台/工具风险。建议 `git rm --cached` 并从工作树删除（用 `del \\.\NUL` 或安全删除工具）。
3. 一次性脚本泛滥：根目录 **119 个 `.py`**（含大量 `fix_*`/`extract_*`/`check_*`/`_*.py` 探针），**32 个 `_*.txt` 调试产物共 1.7MB**，与业务代码（`app.py`/`database.py` 1485 行）混杂，上手成本高。建议迁到 `tools/` 或归档。
4. 题库 JSON 仍 **3 份物理副本**（`data/questions/*.json` + `pwa/data/*.json` + `data/questions/backup_*`），改一次同步多处。

---

## 七、其他现状

- **无 CI**：无 `.github/`，无任何自动化测试/构建门禁。
- 工程规模：`app.py` 826 行 + `database.py` 659 行（Flask 端）；`pwa/js/backend.js` 782 行（网页端）。
- 工作树 80MB，`.git` 47MB（此前 `git gc --aggressive` 已回收约 455MB，当前仅微量增长，正常）。

---

## 八、更新后的优先级建议

| 优先级 | 问题 | 建议动作 | 状态 |
|---|---|---|---|
| 🔴 P0 | 双端**实际分叉**（CSS 差 946 行、HTML 已 DIFFER） | 定唯一真源 + 同步脚本 + 漂移校验 | 新恶化 |
| 🔴 P0 | 题库 OCR 质量无闭环 | 建质量标记 + 纠错流程 | 仍未变 |
| 🔴 P1 | CRLF 噪声 / `NUL` 文件 | 加 `.gitattributes` 归一化 + 删保留名文件 | 新增 |
| 🟠 P1 | 双存储不互通 | 统一导出格式 + 一键跨端迁移 | 未变 |
| 🟠 P1 | 无应用级测试/CI | 给路由补最小回归 + 加 CI | 未变 |
| 🟡 P2 | API 参数无防御 | `app.py` 加 `try/except` + 空集保护 | 未变 |
| 🟡 P2 | 一次性脚本/产物污染 | 迁 `tools/` 或归档 | 未变 |
| 🟡 P2 | PWA 缓存版本手工 | 构建期按内容哈希自动生成 | 未变 |

---

## 九、一句话结论

功能完成度很高、体验也细（导航右抽屉等交互打磨到位），**但健康度瓶颈已从左版「缺功能/有风险」转为「债务正在兑现」**：双端镜像已实际分叉、CRLF 让 `git status` 失真、保留名文件藏雷。下一阶段最该做的不是加功能，而是**先给仓库做三次小手术**（定双端真源并同步、加 `.gitattributes`、删 `NUL`），否则每次改动都在加重维护熵、且极易误提交整库行尾。

> 附：当前唯一未提交的真实改动是 `pwa/dati.html`（右抽屉），如需落库请告知，我可单独提交该文件（不要 `git add -A`，以免带入 CRLF 噪声）。
