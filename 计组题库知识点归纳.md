# 计组题库知识点归纳

> 数据来源：`pwa/data/co.json` 全部 603 题（第1章 34、第2章 114、第3章 129、第4章 67、第5章 138、第6章 45、第7章 76），按"章→节"组织，标注题号（Q1–Q603）与统考真题年份。
> 本文件为**过渡稿**，最终投递进线上「计算机组成原理 · 考点笔记」(`co_notes.json`)。

## 第1章 计算机系统概述（34 题，Q1–Q34）

### 1.2 计算机系统层次结构

**核心知识点**
- 完整计算机系统 = **配套的硬件设备 + 软件系统**（选项"主机/运算器存储器控制器"都不完整，Q1）。
- **冯·诺依曼机基本工作方式 = 控制流驱动**：按指令执行序列依次取指、据控制信息调用数据（Q2）。核心特征：**存储程序**、程序和数据统一存储、**按地址访问**、**自动按序执行**（Q3）。
- **运算器 ALU 不只做算术运算，还做逻辑运算**（与/或/非/移位）（Q4）。
- 层次结构自上而下：**高级语言虚拟机 → 汇编语言虚拟机 → 操作系统虚拟机 → 机器语言机器**（Q5）。
- **ISA vs 微体系结构**：ISA 是软/硬件抽象接口，定义软件可见行为（指令/寄存器/寻址方式）；**同一 ISA 可由不同微体系结构（流水线/缓存设计）实现，软件无需修改即兼容**；高级语言与机器指令**不一一对应**，只有汇编语言与 ISA 指令基本一一对应（Q6）。
- **编译 vs 解释**：编译=一次全部翻译生成目标代码文件、运行快（C/C++）；解释=逐句翻译边译边执行、运行慢（Python/JS），不生成目标代码文件（Q7、Q8、Q12 2016）。
- 汇编语言与**特定机器指令集一一对应、不可移植**（Q9）；**硬件只能直接执行机器语言**，汇编需经汇编程序转换（Q10、Q11 2015 真题：仅 I）。
- **冯·诺依曼：数据由指令的地址码指出**（除立即寻址外数据都存在主存中），"数据都在指令中直接给出"是错的（Q13 2019 真题）。
- 源程序→可执行文件：**预处理 → 编译 → 汇编 → 链接**（Q14 2022 真题）。

### 1.3 计算机的性能指标

**核心知识点**
- **主频** = CPU 使用的时钟频率；**CPI** = 执行一条指令平均使用的时钟周期数（Q15）。
- **CPI 与时钟频率无关**（主频高只加快执行，不改变 CPI），受系统结构、指令集、计算机组织影响（Q18）。
- 指标归属：**MFLOPS** = 每秒百万次浮点运算（科学计算/向量机用）；**MIPS** = 每秒百万条指令（标量机用）；**IPC = 1/CPI** = 每时钟周期指令数；MIPS/CPI/IPC/**字长**都是性能指标（Q16、Q21、Q27 2011）。
- **机器字长 ↔ 运算精度**最相关（字长越长精度越高，与运算速度/存取速度/内存容量关系不大，Q22）。
- 速度比较：**速度与运行时间成反比**（20s vs 16s → B 的速度是 A 的 20/16=1.25 倍，Q19）。

**常考计算（高频，掌握公式）**
- `CPU执行时间 = 指令条数 × CPI × 时钟周期 = 总时钟周期数 ÷ 主频`
- `平均 CPI = Σ(指令比例 × 该指令 CPI)`（Q24、Q29、Q33）
- `MIPS = 主频 ÷ (CPI × 10⁶)`；`GIPS = 主频 ÷ CPI`（Q34 2023）
- 例 1：A 主频 800MHz 跑 12s → 周期数 9600M；B 周期数 ×1.5=14.4G、要求 8s → 主频 14.4G/8 = **1.8GHz**（Q20）。
- 例 2：平均 CPI=2.24，T=CPI×(1/40MHz)×100 = **5.6×10⁻⁶s**（Q24）。
- 例 3：1200MHz、CPI=3 → MIPS=**400**（Q29 2013）。
- 例 4：1GHz、10000 条、80%×1 周期+20%×10 周期 → CPI=2.8、T=10000×2.8/10⁹=**28μs**（Q33 2022）。
- 例 5：编译优化后指令数×0.7、CPI×1.2 → 时间 ×0.84 = **16.8s**（Q30 2014）。
- 例 6：同 ISA，M1 1.5GHz/CPI2，M2 1.2GHz/CPI1 → 时间比=(2/1.5):(1/1.2)=**1.6**（Q31 2017）。
- 例 7：CPU 提速 50%：90s CPU 变 90/1.5=60 + 10s I/O = **70s**（I/O 时间不变，Q28 2012）。
- **PFLOPS = 10¹⁵ 次浮点/秒**（93.0146PFLOPS ≈ 9.3 亿亿次/秒，Q32 2021）。

**易错点**
- CPU 时间 ≠ 程序总时间：总时间 = CPU 时间 + I/O 时间等，提速 CPU 只缩 CPU 部分（Q28）。
- 平均 CPI 相等 ≠ 执行速度相等：还取决于指令条数与时钟周期（Q17）。
- 编译优化常"指令数↓ 但 CPI↑"，用乘积比时间（Q30）。

## 第2章 数据的表示和运算（114 题，Q35–Q148）

### 2.1 数制与编码

**核心知识点**
- 进制转换：整数**除基取余**、小数**乘基取整**（137.5→八进制 **211.4**，Q35）；**任何十进制小数不一定能精确转二进制**（二进制小数只能表示 1/2、1/4…1/2ⁿ 的组合，Q37）。
- 范围：n 位无符号 0~2ⁿ−1（16 位 → 0~**65535**，Q36）；n+1 位原码整数 `-(2ⁿ−1) ~ +(2ⁿ−1)`（Q42）；n+1 位补码最小负数 **−2ⁿ**、32 位补码 `−2³¹~2³¹−1`（Q43、Q67 2022）。
- **0 的表示唯一性**：**补码和移码**对 0 唯一；原码/反码有 ±0（Q38、Q50）。8 位原码/反码有 **255** 个不同数据，补码/移码 256 个（Q41）。
- 码间转换：补码→原码 = 符号位不变、数值位取反、末位+1（Q39）；`[-x]补` = **连同符号位一起取反再末位+1**（Q40）；**移码 = 补码符号位取反**（数值部分相同、符号相反，Q44、Q51）；0 的补码 ≠ 0 的移码（Q44）。
- **负数补码比较大小**：数值位**越小 → 绝对值越大**（负得越多）（Q45、Q46、Q62）。例：`[x]补=1,x₁x₂…x₆`，要 x>−32 → x₁=1 且 x₂~x₆ 至少一个为 1（Q45）。
- 补码特点：0 唯一、**符号位与数值一起参加运算**、**减法用加法实现**；但真值对应关系**不如原码直观**（Q58）。
- 主存地址用**无符号数**表示（Q59）。
- 寄存器内容判编码：`11111111`=+127 → **移码**（Q51）；=−1 → 补码（Q52）；`00000000`=−128 → 移码（Q53）；`10000000`=−0 → 原码（Q50）；8 位移码 `[-x]移` 对 `00000000`（=−128）溢出（Q55）。
- **符号扩展**：16→32 位，负数高位补 **1**、正数补 0（Q60）；移码偏置值 127/128 时最值排列（Q56）。
- **C 语言类型转换（高频真题）**：
  - 无符号→更长无符号：**高位填 0**（`unsigned short 65530→unsigned int` = 0000FFFAH，Q61 2012）。
  - 有符号→等长无符号：**符号位变成数据位**（`short −32767 → unsigned short 32769`，Q63 2016；`65535→short = −1`，Q65 2019）。
  - short→int：**符号扩展**（`si=32777→8009H→−32759`，Q101 2024；`si=−32767→unsigned int` = 2³²−2¹⁵+1，Q68 2025）。
  - 机器数比较：有符号/无符号解释不同（FFFDH、FFDFH、7FFCH：无符号 z<x<y；有符号 y<x<z，Q66 2021）。
- 二进制编码原因：**运算规则简单 + 两稳态器件易造 + 便于逻辑门实现**（Q64 2018）。
- 混合运算类型提升：**char→int→long→double**，最终结果 double（Q104）。

**常考计算**
- −66 补码 8 位 = **BEH**（Q48）；9BH 补码真值 = **−101**（Q49）；真值 −0.1101 机器数 1.0010 = **反码**（Q54）。
- 由 2 个"1"+6 个"0"组成 8 位补码：最大 01100000=96、最小 10000001=−127，差 **223**（Q57）；3 个"1"+5 个"0"最小 = 10000011 = **−125**（Q62 2015）。

### 2.2 运算方法和运算电路

**核心知识点**
- **ALU 核心部件 = 加法器**（减法用补码、乘除用移位+加法，Q69）；功能 = **算术 + 逻辑**运算（Q70）。
- **算术移位**：左移**低位补 0**、移出的高位移到符号位前后比较——**左移前后符号位不同 → 溢出**；右移**高位补符号位**（负数右移补 1）（Q71、Q72、Q73、Q75、Q83、Q85、Q98）。**逻辑左移 = 算术左移结果相同**（最高位移出 1 即溢出，Q85）。
- **溢出判断三法**：①双符号位（模 4 补码）：`01`=正溢出、`10`=负溢出、相同则无溢出（Q80、Q82）；②进位法：最高位进位 C₁ ≠ 符号位进位 C₀ 则溢出（Q81）；③用**异或门**实现（Q76）。
- 溢出**根本原因 = 机器字长有限**（Q77）。
- 模 4 补码：**存储只占一个符号位**，仅送入 ALU 运算时才扩展成双符号位（Q79）。
- 减法电路：`Sub=1` → Y 各位取反 + 低位进位 1（用多路选择器+反向器实现）（Q86、Q87）。
- 乘法：**阵列乘法器可 1 个时钟周期完成**；ALU+移位器串行需多周期；**乘常数可优化为移位+加减**；两变量相乘可用移位+加法**循环实现**（Q84、Q102 2024）。
- 补码一位乘法：乘积寄存器 P 与乘数寄存器 Y 的右移是**算术右移**；溢出判据是 P 的各位是否都等于 Y 的符号位（Q91）。
- 除法：若**|被除数| < |除数| 直接置商 0、余数=被除数**，跳过迭代最快（Q92）。

**常考计算与真题**
- 溢出判定：r₁=F5H(−11)、r₂=EEH(−18) → **r₁×r₂=198 溢出**（Q78、Q94 2010 同型）；x=103、y=−25 → **x−y=128 溢出**（Q96 2014）。
- 移位运算：x=11110100、y=10110000，z=2x+y/2 → 11101000+11011000=**11000000**（Q95 2013）；11011000 逻辑右移 1 位=01101100、算术右移=11101100（Q98 2018）。
- **CF 与 OF 的语义**：ALU 只算不管有/无符号——**CF=当作无符号数运算时溢出（借位）、OF=当作有符号数运算时溢出**；x−y 时借位标志 CF 看无符号解释（Q99 2018 sub 指令、Q100 2023、Q103 2025）。
- 混合类型强制转换：`int i=32777 → short 8009H → int FFFF8009H`，真值 −32759（Q101 2024）；`x−y`：A3H−75H=2EH 但 OF=1（符号位 1+1→结果 0，Q103 2025）；`if(i ≤ j−1)` 无符号解释时用 **CF** 判断、输出"王道"（Q90）。
- C 语言运算：x=127、y=−9（short→int 符号扩展）→ z=00000076H（Q93 2009）；`[x]=FFFF FFDH`（=−33）、`[y]=41H`（=65）→ x−y=**FFFF FF9EH**（Q97 2018）。

### 2.3 浮点数的表示与运算

**核心知识点**
- 总位数固定时：**阶码越长 → 表示范围越大、精度越低**（数变稀疏）（Q105）。
- **IEEE 754**：**尾数用原码**（含隐含的最高位 1）、**阶码用移码**（单精度偏置 **127**、双精度 1023）；**基数隐含不出现**（Q106、Q109）。
- **规格化**：把尾数写成 1.xxx × 2ᵉ 形式，**主要目的是增加表示精度**（Q111）；隐含位在**对阶右移前要先恢复到尾数高位**、再整体逻辑右移（高位补 0）（Q119）。
- 非规格化数：**阶码全 0、尾数非 0**，用于处理**阶码下溢**（比最小规格化数还小的数）（Q118、Q145）；阶码全 1 尾数全 0=无穷大、尾数非 0=NaN（Q145）。
- **下溢** = 运算结果绝对值小于机器能表示的最小绝对值（Q113）；**浮点溢出判断取决于阶码是否上溢**（尾数上/下溢可经左/右规调整，Q114）。
- 表示范围**正负对称**：最大正数与最小负数绝对值相等（Q112）。
- 对阶：**小阶向大阶看齐**，小阶数尾数右移（Q116）；单精度有效数字 24 位，**阶差 ≥ 25 时小阶操作数对结果无影响**（直接取大阶数，Q120）。
- 浮点加减步骤：**对阶 → 尾数运算 → 规格化 → 舍入 → 判断溢出**（Q129 2009、Q136 2015）：对阶不会引起阶码溢出；**右规与舍入可能引起阶码上溢、左规可能阶码下溢**；尾数溢出时结果不一定溢出。
- C 语言浮点：int→float 可能**舍入**、不会溢出；float→double 保精度；float/double→int 截断小数且可能溢出（Q104、Q127、Q130）；`1.2` **不能用 IEEE 754 精确表示**（Q142 2021）；`i==(int)(double)i` 恒真、`f==(float)(int)f` 为假（Q127）；整数/整数=整数、实数/整数=实数（Q128）。

**常考计算（IEEE 754 换算，真题大户）**
- 真值→机器数：−8.25 → **C1040000H**（Q131 2011）；−0.4375 → **BEE00000H**（Q143 2022）；short −8190 → **E002H**（Q144 2023）。
- 机器数→真值：41A4C000H=+20.59375（Q108）；45100000H=(+1.125)×2¹¹（Q115）；C6400000H=**−1.5×2¹³**（Q134 2013）；80200000H=**−2⁻¹²⁸**（非规格化，Q145 2023）；47300000H=1.375×2¹⁵（Q147 2025）；C8000000H 按 int 解释=−7×2²⁷、按 float 解释=−2¹⁷（Q141 2020）。
- 两个 float 比较：CC900000H(−1.001×2²⁶) < B0C00000H(−1.1×2⁻³⁰)，同负（Q135 2014）。
- **float 能表示的最大正整数 = 2¹²⁸−2¹⁰⁴**（尾数全 1×2¹²⁷，Q132 2012）；**最小规格化正数 = 1.0×2⁻¹²⁶**（Q138 2018）；64 位最小规格化负数 = −(2−2⁻⁵²)×2¹⁰²³（Q107）。
- 整数 vs 浮点精度：α∈[−2²⁰,2²⁰] 用 **32 位整数**、β∈[−2⁴⁰,2⁴⁰] 用**双精度浮点**（Q146 2024）。

**易错点**
- 偏置值是 127 不是 128；别忘隐藏最高位 1（Q131 易错）。
- 浮点加减对阶时先恢复隐含位再整体右移（Q119）；右移是逻辑右移高位补 0（Q119）。

### 2.4 数据的宽度和存储（部分并入 2.3，此处列综合）
- **机器字长**：CPU 定点运算数据通路宽度，通常与通用寄存器位数一致；决定定点数范围与精度，**影响硬件造价**（Q121）。
- **边界对齐**：数据存储地址是其**字节长度的整数倍**（char 1B、short 2B、int/float 4B、double 8B 对齐），以空间换时间（Q122、Q126、Q133、Q140、Q148）。
- **大端/小端**：大端=高位字节存低地址；**小端=低位字节存低地址**（int 01234567H → 首字节 67H，Q125；double 1122 3344 5566 7788H → 00008046H 存 22H，Q137 2016；-64 → C0 FF FF FF，Q139 2018）。
- 结构体对齐计算：`{int a; char b; short c}` → 8B，record.c 地址 = 0xC00E（Q133 2012）；`{short x1; int x2}` → 34H 在 2020FE06H（Q140 2020）；`{int id; char name[10]; int salary}` → 20B，56H 在 0000A0C5H（Q148 2025）。

## 第3章 存储系统（129 题，Q149–Q277）

### 3.1 存储器概述

**核心知识点**
- 分类：磁盘 = **直接存取存储器 DAM**（介于随机与顺序之间，Q149）；U 盘/Flash = **ROM 类**（Q170）；CD-ROM = **串行（直接）存取**、非随机存取（Q189 2011）。
- **存取周期** = 连续两次访问之间允许的**最短时间间隔**（≠ 一次读/写时间，Q150）；**相联存储器按内容寻址**、并行匹配，用于 TLB/Cache 标记（Q151）。
- CPU 不能直接访问硬盘（需先调入主存，Q152）；**多级存储目的 = 解决容量/速度/价格三者矛盾**（Q153）；存储系统 = 寄存器 + Cache + 主存 + 外存（Q154）；速度排序：**寄存器 > Cache > 主存 > 辅存**（Q157）。
- **MAR 位数 = 地址总线宽度**（须覆盖全部可寻址单元）；**MDR 位数 = 数据总线宽度**（Q155）。
- 多级存储：主存-辅存调动对**应用级程序员透明**；CPU 与 Cache 交换、也可与主存直接交换（Q158）。
- 数据传输率计算：存取周期 250ns、每次读出 16bit=2B → **8×10⁶ B/s**（Q159）。

### 3.2 主存储器

**SRAM vs DRAM（高频对比）**
- **SRAM**：双稳态电路（触发器）存储 0/1，**不需刷新**、速度快、**集成度低、功耗大、单位价格高**（Q167、Q163）。
- **DRAM**：**电容存储电荷**（有电荷=1），需**按行刷新**、速度慢、**集成度高、功耗小、成本低**（Q162、Q163、Q167）。
- DRAM 刷新：以**行**为单位；通过"**读但不输出数据**"（假读）重写电容实现；**一次刷新占一个存取周期**、会与 CPU 访存冲突（存在访存"死时间"）（Q162、Q173、Q176）；SDRAM 与 CPU 同步交换、**同样需刷新**、行缓冲器用 SRAM 实现（Q177）。
- RAM 易失、ROM 非易失，都采用**随机存取**；ROM **不能用作 Cache**；DRAM 需刷新、ROM 不需要（Q164、Q165、Q188 2010）。
- 主存 = RAM + ROM（统一编址）；**系统主存并非全部用 DRAM**（BIOS 用 ROM/Flash）（Q165、Q171）；EPROM 可多次改写但不能作 RAM（Q172）。
- **Flash（闪存）**：非易失、MOS 管浮栅存储电荷；**写/擦除必须先擦块、写速明显慢于读速**（读近 RAM、写近 ROM，Q175、Q190 2012）；读写不对称。
- 随机存取 = 存取时间与物理位置无关（Q189）。

**芯片引脚/容量计算（常考）**
- SRAM：地址引脚 = log₂(单元数)、数据引脚 = 位宽（1024×8 位 → 10+8=**18** 根，Q160；32K×16 → 15+16 根，Q161）。
- **DRAM 地址复用**：行/列地址分时传送 → **地址引脚 = log₂(容量位) / 2**，数据引脚不变（1024×8 位 → 5+8=**13** 根，Q168；4M×8 → 11+8=**19** 根，Q191 2014；4M×4 → 11 地址+4 数据，行缓冲 8Kb，Q180）。
- 内存条容量：芯片数 × 位平面 × 行 × 列（16 芯片 × 4 位平面 × 4096×4096 → **128MB**，Q179）。
- 每增加 1 根地址线 → 行、列各增 1 位 → **容量 ×4**（Q178）。

**多模块 / 单体多字**
- 多模块存储（低位交叉编址）靠**各模块独立读/写电路并行工作**提速（Q186）；轮流启动如流水线，要求模块数 ≥ 存取周期/总线周期（Q181）。
- 四体低位交叉、存取周期 200ns、总线 50ns：**200ns 内可提供 128 位**（4 字×32 位，Q182）；连续取 m 字耗时 `t=T+(m−1)r`（Q183）。
- **单体多字**：每单元存多字，解决**访存速度**（非容量）；指令连续存放、跳转少时效率高（Q185）。
- 总线宽 64 位 + 8 个 16M×8 DRAM → **必须多模块交叉编址**；跨模块的 int 需两个存取周期（Q187）。

### 3.3 主存储器与 CPU 的连接

**核心计算**
- 字扩展/位扩展：16K×1 组成 64K×8 → 字扩 **4** 倍、位扩 **8** 倍（Q197）；位扩展只需增加数据位（80386DX 32 位系统用 8K×8 组 32KB 存储体 → 只位扩展，Q198）。
- 芯片数 = 总容量 ÷ 芯片容量（Q199、Q200、Q201、Q203、Q204）；片选译码输入 = 高位地址线（Q202）。
- **MAR 位数取决于主存地址空间**（64MB → **26 位**，与已装主存容量 32MB 无关，Q208 2011）。
- 片选与地址范围：片选 111 → 首末地址 38000H~3FFFFH（Q205）。
- 真题：64KB 主存、ROM 4KB（2K×8 ROM 2 片）+ RAM 60KB（4K×4 RAM 30 片，Q206 2009）；2K×4 组 8K×8 → 0B1FH 在 0800H 组（Q207 2010）；ROM 4000H~5FFF=8KB、RAM 56KB → 8K×4 SRAM **14** 片（Q209 2016）；24 位地址线按字编址 RAM 2²²×32bit ÷ 512K×8 → **32** 片（Q210 2021）；30 根地址线、RAM:ROM=3:1 → ROM 地址范围 30000000H~3FFFFFFFH（Q211 2023）。

### 3.4 外部存储器

- 磁盘地址 = **磁道号（柱面）+ 磁头号（盘面）+ 扇区号**（盘面号=磁头号，Q213）；读写成批、**DMA 方式**、与主存直接交换信息（Q214）。
- **转速提高一倍 → 平均旋转等待时间减半**（寻道时间不变，Q215）。
- 格式化容量 < 非格式化容量（扇区写入控制信息+空隙，Q212、Q223 2019）；最小读/写单位 = **扇区**（Q223）；RAID 提高的是可靠性（镜像+奇偶校验），**条带化提速度不提高冗余**（Q221 2013）；SSD：读写以**页**、擦除以**块**、写慢于读、磨损均衡延长寿命（Q216、Q217）。
- 磁盘计算：`传输率 = 转速(转/秒) × 每磁道扇区数 × 扇区大小`（7200 转/分 → **9600KB/s**，Q218）；`存取时间 = 寻道 + 旋转半圈 + 传输`（10000 转/分 → 6ms 寻道 + 3ms 旋转 + 0.2ms 传输 + 0.2ms 控制器 = **9.4ms**，Q220 2013；7200 转/分、8ms 寻道、1000 扇区 → **12.2ms**，Q222 2015）。

### 3.5 高速缓冲存储器（Cache）—— 计算真题大户

**核心知识点**
- 存储系统总容量**不是简单相加**（Cache 是主存副本，Q224）；缺失时：主存→CPU 传**字**、主存→Cache 传**块**（Q225）。
- **Cache 全由硬件实现**；替换单位是**块**；与主存**独立编址**（地址映射）；回写法下 Cache 与主存**可暂时不一致**（Q231）。
- 写策略：命中时用**回写法/全写法**，失效时用**写分配法/非写分配法**；**回写配写分配、全写配非写分配**（Q227）；写密集→回写法快；要求主存一致性→全写法（Q228）。
- 局部性：**时间局部性**（i、sum 反复使用）；**空间局部性**（数组顺序访问，Q229、Q255 2017）；指令 Cache 空间局部性优于数据 Cache；**块过大反而降低命中率**（Q232、Q248）。
- 映射方式：**直接映射**（K=1，每组 1 块，无需替换策略）、全相联（K=M）、组相联（每组 K 块，K 路）（Q235、Q247）；**LRU 与局部性原理相关**、FIFO 需时间戳、直接映射无需替换（Q236、Q247）。
- **TLB 和 Cache 都由 SRAM 组成**、命中率与局部性有关、缺失后都要访问主存（Q273 2020）。

**地址结构 & 计算（背公式）**
- 地址三段：`标记 tag + 组号 + 块内地址`；块内地址位数 = log₂(块大小)；组号位数 = log₂(组数)（Q238、Q239、Q244、Q251 2009、Q257 2022）；组相联比较器个数 = **路数**、位数 = 标记位数（Q249、Q257）。
- 平均访问时间：`h×t_cache + (1−h)×(t_cache + t_mem)`（2ns/40ns 求 h=97.5%，Q226；L1 94% + L2 局部 85% → 全局 99.1%，Q234）。
- **Cache 行总位数 = 数据位 + 标记位 + 脏位(回写) + 有效位 + 替换位(视算法)**：全相联 32K 字 → 30 标记+1 修改+1 有效+32 数据 = **2048K 位**（Q243）；直接映射 4K 字回写 → **148K 位**（Q253 2015）；32KB/32B 直接映射回写 → **275 位/行**（Q256 2021）。
- 标记阵列 = 行数 × (标记 + 有效位 + 脏位…)：直接映射 64 块、4096 倍 → **64×13bit**（Q237）。
- 缺失率计算：2 路组相联 LRU 模拟 20 次访问缺失 8 次 → **40%**（Q233）；`a[k]=a[k]+32` 直接映射 → **12.5%**（Q254 2016）；访问 1000 次缺失 50 → 命中率 95%（Q250 2009）。
- 缺失损失：非突发 `8×(1+8+1)=80` 周期（Q245）；突发传送 64B 一次 → **17** 周期（Q246）。

### 3.6 虚拟存储器

- 基础 = **局部性原理**（程序对主存访问不均匀，Q258、Q259）；目的 = **扩大主存空间**（Q263）；对**应用程序员透明、对系统程序员不透明**（Q261）；地址映射由**操作系统**完成（Q262）。
- 管理方式：页式按**页**、段式按**段**（可变长）、段页式先分段再分页、与主存**以页交换**（Q260）；**逻辑地址位数 > 物理地址位数**（Q264）。
- 页表：每进程一个、每表项对应一虚页、含**有效位**、**只能 OS/硬件访问**（Q265）；**快表 TLB = 相联存储器按内容查找**（Q268）。
- **缺页 = 内部异常**（非外部中断）；由 OS 缺页程序处理，据页故障地址从外存读页，处理完**重新执行发生缺页的指令**（Q266、Q272 2019）。
- 分段对**低级语言程序员和编译器不透明**（需用段号编程/链接，Q267）。
- **TLB/Cache/Page 命中关系**：Cache 命中 ⇒ Page 必然命中（内容在主存）；TLB 命中 ⇒ Page 命中；反之未必 → "TLB 命中、Cache 命中、Page 未命中"不可能（Q269 2010）。
- 虚实转换计算：虚页号查 TLB/页表 → 页框号 + 页内地址拼接（03FFF180H → **0153180H**，Q270 2013；00082840H → **018840H**，Q274 2022）。
- Cache 直写 + TLB 的访存次数：取数可能命中 Cache、写回直写要写主存 → 至少 **1 次**（Q271 2015）。
- **Cache-主存 vs 主存-外存层次**：交换单位=主存块 vs 页；替换算法=硬件 vs 软件；主存-外存通常**回写法+全相联**（Q275 2024）。
- TLB 标记位数：32 位虚地址、页 1KB（10 位页内）、TLB 32 项 4 路（3 位组号）→ 标记 **19 位**（Q276 2024）。
- **MMU 地址转换中检测**：访问越权、页面缺失、TLB 缺失；**Cache 缺失不是**（在取到物理地址后才访 Cache，Q277 2024）。

## 第4章 指令系统（67 题，Q278–Q344）

### 4.1 指令系统

**核心知识点**
- **ISA（指令集体系结构）** = 软件/硬件交界面，规定：指令字格式与类型、寻址方式、通用寄存器个数与位数、存储空间/编址方式、大端小端等；**不规定**：执行指令的控制信号、CPU 时钟周期、加法器进位方式、是否阵列乘法器/微程序控制器/单总线数据通路（Q278、Q279、Q291 2022、Q293 2025）。
- 指令分类：运算型指令寻址**操作数**、转移型指令寻址**下一条指令地址**（Q280）；程序控制指令 = 无条件/条件转移、子程序调用与返回、循环（Q281、Q282）；**中断隐指令由硬件实现、不属于指令系统**（Q282）。
- 指令长度与地址个数**无必然联系**（单地址指令也可能因寻址方式不同长度不同，Q283）。
- 定长操作码：200 条指令 → 至少 **8 位**（⌈log₂200⌉，Q284）。
- **扩展操作码目的 = 保持指令字长不变、增加指令数量**（操作码长度随地址码减少而增加，Q285）。

**扩展操作码计算（高频真题）**
- 16 位、地址码 4 位：三地址 15 条 → 余 1 个 4 位编码扩展 → 二地址 8 条 → 余 (4096−2048)−2032=16 编码 → **零地址最多 16 条**（Q287）。
- 16 位、地址码 6 位：二地址 12 条 → 余 4 编码；一地址 254 条 → 余 `4×64−254=2` 编码 → 零地址 `2×2⁶=128` 条（Q292 2022）。
- 12 位、地址码 4 位：二地址最多 15、一地址最多 15、零地址 16 → 共 **46 条**（Q289）。
- 三地址 29 条 + 二地址 107 条、每地址 6 位 → 操作码 5 位（余 3 编码×2⁶=192 ≥ 107）→ 指令字长 23 位 → 按字节编址取整 **24 位**（Q290 2017）。

### 4.2 指令的寻址方式

**核心知识点**
- 采用多种寻址方式的**目的**：缩短指令字长、扩大寻址空间、提高编程灵活性（Q294）。
- **取操作数速度**：立即 > 寄存器 > 直接 > 间接（Q298）；**寄存器寻址**地址段最短（log₂寄存器数）、**隐含寻址**简化地址结构（Q296、Q297）。
- 直接寻址：形式地址即操作数**有效地址**（Q299）；间接寻址 EA=(A)、需访存两次以上；寄存器间接寻址 EA=(R)（Q304、Q310）。
- 直接转移指令把地址码送入 **PC**（Q295）；跳跃寻址实现条件/无条件转移（Q303）。
- 各寻址方式 EA：
  - 基址 `EA=(BR)+A`（BR 由 OS 确定、不可变）——利于**多道程序与浮动程序**
  - 变址 `EA=(IX)+A`（IX 用户设定、可变）——便于**处理数组/循环**（Q301、Q322 2017）
  - 相对 `EA=(PC)+A`——**以下条指令地址为基准的偏移量**（Q302、Q316 2009）
  - 变址间址 `EA=((IX)+A)`（Q300、Q321 2016）
  - 基址/变址/相对统称**偏移寻址**，间接寻址**不属于**（Q317 2011）。
- 寄存器中存"操作数"还是"操作数地址"由**寻址方式**区分（Q326 2023）。

**常考计算与真题**
- 相对转移目标：`目标 = 取指后PC + 位移量`。注意取指后 PC 已 +2（双字节指令按字节编址）或 +1（按字编址）（Q305、Q307、Q316 2009：2000H+2+06H=**2008H**）。
- 位移量补码范围：操作码 7 位 + 寻址特征 3 位 + 地址 6 位 → 相对寻址偏移 `−32~+31`（Q308）；32 位定长、8 位操作码、16 寄存器 → 偏移 16 位补码 `−32768~+32767`（Q320 2014）；48 条指令 4 种寻址 → 直接寻址范围 0~255（主存地址非负，Q325 2020）。
- 变址计算：`(IX)=EA−A`，EA=2100H、A=2000H → (IX)=100H=256，double 占 8B → 下标 **32**（Q323 2018）；变址+形式地址（Q319 2013）。
- 基址+大端 LSB 地址：`EA=(BR)+A`（补码形式地址按符号扩展），大端下 LSB 在最高地址 `EA+3`（Q312、Q324 2019：F0000000H+FF12H=**EFFFFF12H** → LSB 在 EFFFFF15H）。
- **条件转移标志**：无符号大于 bgt = **CF+ZF=0**（不发生借位且不相等；含 SF/OF 的选项都是错的，Q318 2011、Q314 2025 同型）；有符号大于 = `ZF+(SF⊕OF)=0`（Q314）。

### 4.3 程序的机器级代码表示

- **add/sub 标志位**：add ax,bx 符号不同 → OF=0；结果 7FCEH → SF=0；有进位 → CF=1（Q327）；sub bx,ax（Intel 格式目的在前）：FFE8H−7FE6H=8002H，无借位 CF=0、无溢出 OF=0、SF=1（Q328）。
- **小端机器码**：`sub ax,imm`、imm=−3=FFFDH → 小端先存低字节 → 机器码 **2DFDFFH**、OF=0（Q329）。
- Intel 格式变址内存寻址：`add eax,[edx+esi*4]`（int 数组 4B，b[i][j] 地址 = edx + esi×4，Q330）；`imul eax,[eax+ebx*4],-16` → 结果送 eax 不写内存（Q331）；`mov eax,[ecx*8+80498c0H]`（double 数组 8B，Q333）。
- 条件转移机器码：jle 偏移 0d，指令 2 字节 → 目标 = 804846cH+2+0dH=**804847bH**（Q332）。
- **子程序调用（转子）指令**：必须①**保存返回地址**（压栈）②**入口地址送 PC**；嵌套调用必须压栈（Q334、Q337）。
- if-else 结构：一定含条件转移 + 无条件转移；statement_B 不一定在 statement_C 前（Q335）；循环结构：至少一条条件转移（Q336）。
- 过程调用步骤：**实参传递 → 保存返回地址并跳转 → 保存现场分配空间 → 执行函数体 → 恢复现场释放空间 → 取返回地址返回**（②③①⑥⑤④，Q338）。

### 4.4 CISC 和 RISC 的基本概念

- **RISC 特点**：指令数少、长度固定、格式与寻址种类少、**硬布线（硬连线）控制器**、**普遍采用流水线**、**通用寄存器多**、**LOAD/STORE 访存**、大部分指令 1 个时钟周期完成（Q339、Q340、Q342、Q343 2009、Q344 2025）。
- **CISC 特点**：指令功能强、寻址方式多、便于汇编程序员编程；常配**微程序控制器**；指令格式种类多**不利于编译优化**（Q342）。
- 易错：RISC **不是**微程序控制（Q343 2009）；"RISC 难以流水线"是错的（Q344 2025）；RISC 不兼容 CISC、不是选 CISC 指令子集（Q341）。

## 第5章 中央处理器（138 题，Q345–Q482）

### 5.1 CPU 的功能和基本结构

- CPU 基本功能：**时序控制、异常与中断处理、算术逻辑运算**；数据存储**不属于**（由主存/Cache 承担，Q345）。
- 寄存器功能速记：**IR** = 当前正在执行的指令（位数=指令字长，Q347、Q356）；**PC** = 下一条指令地址（有自增功能，Q348、Q352）；**通用寄存器** = 可编程指定多种功能、可存数据也可存地址、位数=机器字长（Q346、Q357、Q358、Q368 2020）；**PSW/标志寄存器** = 表征程序与机器运行状态、存算术逻辑及测试结果状态、条件转移的依据（Q349、Q359、Q360）；MAR 位数=地址总线、MDR 位数=数据总线（Q155）。
- **对汇编语言程序员可见**：PC、通用寄存器、基址寄存器、状态/标志寄存器（Q366 2010、Q423 2021）；**不可见（透明）**：IR、MAR、MDR、微指令寄存器（Q350、Q423）。
- **PC 细节**：总是存放指令地址；无条件转移**转移成功**才改为目标地址（失败仍取 PC+指令长）；取指后 PC 自动 +指令长度（不是一定 +1）；字地址时 PC 位数取决于**存储器容量和指令字长**（Q351、Q353、Q354、Q355）。
- 标志寄存器**不能**通过指令直接访问修改（Q361）；控制器 = 操作码译码 + 产生操作控制信号（Q362）；**CPU 不包括地址译码器**（在主存，Q363、Q364）；间址周期结束 MDR 中为**操作数地址**（Q365）。
- 真题：主存 4GB、字长 32 位、按字边界对齐 → **PC=30 位（字地址）、IR=32 位**（Q367 2016）；机器字长=ALU 与通用寄存器宽度（Q368 2020）。

### 5.2 指令执行过程

- **时钟周期** = 最小时间单位；**指令周期** = 从主存取出一条指令 + 执行该指令（Q369、Q370）。
- 无条件转移指令周期内 **PC 被修改 2 次**（取指后 +1、执行时改为目标，Q371）；取指后 PC = 下一条指令地址（Q372）。
- 取指是**控制器固有功能、不需要操作码控制**、自动进行（Q373、Q398）；但不同长度指令取指操作不同（Q374）。
- 指令周期第一阶段必为取指；**乘法指令周期 > 加法**（多周期 CPU）；单周期 CPU 指令周期=1 个时钟周期（Q375）。
- 多周期 CPU：各指令**时钟周期数可变、各时钟周期长度均匀**（Q376）；取指和译码是每条指令必经，取数/写回**不一定访存**（Q377）。
- **CPU 区分指令与数据的依据 = 指令周期的不同阶段**（取指阶段取出的是指令、执行阶段取出的是数据，Q378 2009）。
- 开中断状态下**每条指令执行结束时都可能被外部中断打断**；空操作指令取指后 PC 也 +1（Q379 2011）。

### 5.3 数据通路的功能和基本结构

- 数据通路结构：**单总线、多总线、专用数据通路**；"部件内总线"不是（Q380）。数据通路 = 组合逻辑（操作）元件 + 时序逻辑（状态）元件，功能由**控制信号**决定（Q381、Q396 2021）。
- **操作元件**（输出仅由当前输入决定、无存储）：ALU、加法器、译码器、多路选择器、三态门（Q382、Q397 2023）；**状态元件**（有存储功能）：寄存器、PC、移位寄存器、MDR 等（Q382）。
- **单总线结构 ALU**：一个输入端直连总线、**另一输入端需通过暂存器**；**输出端也要经暂存器**（防反馈影响运算，Q383）。
- 部件与总线之间用**三态门**控制通断（Q384）；总线信号原则：**每时刻只有一个器件发出、可多个器件接收**（Q385）。
- 时钟：边沿触发状态元件；时钟周期以**相邻状态元件间最长组合逻辑延迟**为基准（Q386、Q395 2019）；**并非每来一个时钟信号就执行一条新指令**（多周期/流水线例外，Q386、Q395）。
- **单周期 vs 多周期**：单周期 CPI=1、时钟周期长（以最慢指令为准）、控制信号不变、每个部件只用一次、**不能用单总线结构**（需分离指令/数据存储器）、硬件冗余大成本高（Q388、Q389、Q390、Q394 2016）；多周期 CPI>1、部件复用、控制信号分阶段变化（Q389、Q390）。
- 数据通路**不含控制部件本身**（Q463 2017）；读/写控制信号决定数据流方向与存储器操作类型（Q392）。

### 5.4 控制器的功能和工作原理

- **硬布线（组合逻辑）控制器**：速度快、修改扩展难；**微程序控制器**：速度慢、修改容易、灵活性高（实时性要求高→硬布线；可修改性要求高→微程序，Q406、Q418 2009）。
- **控制存储器 CS**：存放微指令、**ROM 实现**、在 **CPU 内**、按微指令地址访问；主存 MM 在 CPU 外存指令和数据（Q404、Q405、Q421 2017）。
- 层次：**微命令**（一个控制信号）→ 微操作（执行的动作）→ **微指令**（微命令集合）→ **微程序**（微指令有序序列，对应一条机器指令）（Q407、Q408、Q414）。
- **水平型微指令**：一条可并行执行多个基本操作、充分利用并行结构；**垂直型微指令**：一次一个基本操作、有**微操作码**、类似机器指令格式（Q409、Q410、Q412）。
- 相容微命令（可同时出现）vs 互斥微命令（不可同时出现）（Q411）；直接编码效率最高但位数多（Q403、Q412）。
- 下条微指令地址形成：**断定法**（微指令下地址字段/转移控制字段指定）或**增量法**（μPC 自增，Q416、Q417）；微程序入口地址由**机器指令操作码字段**指出（Q400）。
- **字段直接编码**：互斥微命令分同一字段、相容分不同字段、**每字段留 1 个"不发任何微命令"状态**；33 个微命令分 5 互斥类（7、3、12、5、6）→ 需 3+2+4+3+3=**15 位**（Q419 2012）。
- 下地址字段位数：32 条指令 ×4 条 + 公共取指 2 条 = 130 条微指令 → **8 位**（Q420 2014）。
- 真题：`R[r2]←M[r1]+M[r0]` 取数及执行需 GPRs、Memory、ALU，**不需指令译码器**（Q422 2019）；CPU 能直接执行机器指令和微指令（Q424 2021）。

### 5.5 异常和中断机制

- 分类：**异常 = 内中断**（与当前指令相关，CPU 内部检测、不能屏蔽）：故障（fault）/自陷（trap）/终止（abort）；**中断 = 外中断**（CPU 外部请求，与当前指令无关）（Q426、Q435 2016）。
- **自陷（陷阱）**：人为预先设定（陷阱/访管指令进入），可用于**程序调试断点与单步**；处理完返回到陷阱指令**下一条**（Q425、Q436 2020）。
- **故障**：如缺页、地址越界、除数为 0、溢出——处理完**必须回到当前（发生故障的）指令重新执行**（Q437 2021）；**终止**：严重错误（存储器校验错等）程序无法继续（Q427）。
- 断点：陷阱=下一条指令；故障=当前指令；外部中断=**刚执行完指令的下一条**（Q433）。
- **Cache 缺失由硬件处理、无须异常处理程序**（Q429）；无效操作码=内部异常，定时到/打印机缺纸/键盘缓冲满=外部中断（Q431）。
- CPU **响应中断的时机 = 一条指令执行结束**（Q430）；中断响应需"中断回答"确定最高优先级中断，异常不需要（Q428）。
- 内部异常处理后**不一定能回到原断点继续执行**（非法指令/除 0 只能终止，Q434 2015）；进程上下文切换由 OS 软件实现，异常/中断响应由**硬件**实现（Q432）。

### 5.6 指令流水线 —— 计算与冒险（真题大户）

**基本概念与计算**
- 流水线 = **时间并行**（分时复用功能部件）；**超标量 = 空间并行**（复制部件/多条流水线，每周期发射多条指令，CPI<1）（Q438、Q454、Q462 2017）。
- 流水段时间 = **最长功能段时间**（+流水段寄存器延时）：90/80/70/60ns → 时钟周期 **90ns**（Q456 2009）；80/50/50/70/50ps + 20ps 寄存器 → **100ps**（Q464 2018）。
- 执行 n 条 k 段指令：`(k+n−1)×Δt`；首条 kΔt、之后每 Δt 一条（Q440、Q442、Q443）；超标量度为 m：`(k+(n−m)/m)Δt`（度 4 执行 20 条 → **7Δt**，Q441）。
- 吞吐率：1.03GHz 4 级流水执行 100 条（103 周期）→ `1.03G×100/103 = 1.0×10⁹ 条/秒`（Q459 2013）。
- 理想 CPI：单周期=1、基本流水线=1、多周期>1、超标量<1（Q466 2020）；**CPI 与 Cache 缺失率有关**（缺失插入等待周期，Q469 2025）。

**冒险与处理（高频）**
- **结构冒险**：多指令争用同一硬件资源；对策=资源有序使用、重复设置部件、**指令/数据 Cache 分离**（Q446、Q460 2014）。
- **数据冒险（RAW）**：转发（旁路）技术可解决部分；**load-use 数据冒险无法用转发解决**，必须阻塞/插入空操作（Q447、Q448、Q449、Q450、Q461 2016、Q465 2019、Q468 2024）。
- **控制冒险**：由转移/中断/异常引起；对策=空操作、分支预测（静态每次相同、动态更准；预测错则弃掉已取指令）（Q451、Q452）。
- 按序流动流水线只可能出现 RAW（Q455）；流水段数不是越多越好（增加寄存器开销、控制复杂，Q453）。
- 利于流水线的指令特征：**格式规整长度一致、按边界对齐、仅 LOAD/STORE 访存**（RISC，Q458 2011）。
- 数据冒险判定真题：5 段流水线判断相关对（Q461 2016、Q465 2019）；转发+硬件阻塞时 I3（load-use）与 I4（控制冒险）被阻塞（Q467 2023）。

### 5.7 多处理器的基本概念

- **Flynn 分类**：SISD（单处理器、标量流水线）、**SIMD**（阵列/向量/并行处理机，一个控制部件+多个处理单元）、MIMD（多处理机、多核、SMP）、MISD（Q472、Q473、Q474、Q475）。
- **硬件多线程（超线程）**：单核内复制必要的线程资源并行执行两个线程；**需芯片组/OS/应用支持**；性能≠双核；资源冲突时一线程挂起（Q476、Q477）。
- 双核 = 一个 CPU 上集成**两个执行核心**（Q478）；各核心可有独立或共享 Cache（Q480）；**多核不能让单线程程序变快**、能耗通常更高（Q479、Q481）；多核属于 MIMD（Q481、Q482 2022）。
- **SMP**：所有处理器共享单一物理地址空间（Q482 2022）；硬件多线程技术**不只可用于多核**（可用于单核，Q482）。

## 第6章 总线（45 题，Q483–Q527）

### 6.1 总线概述

**核心知识点**
- 总线分**数据线（双向）、地址线（单向）、控制线**，分别传数据信息、地址信息、控制信息（Q484、Q496）；系统总线连接 **CPU、主存和外设**（Q485）。
- 总线使用原则：**多个部件只能分时发送、可同时接收**（Q483）；地址总线**单向**（CPU→主存/外设），指定主存单元与 I/O 端口地址；数据总线**双向**；控制总线传定时/操作命令/请求-回答信号（Q487、Q488、Q489、Q497）。
- 主存靠**总线类型**识别地址还是数据（地址走地址线、数据走数据线，Q491）；间址寻址第一次访存所得有效地址**经数据总线**传回 CPU（Q487）。
- 采用总线结构的优点：**减少信息传输线条数、便于增减外设**；代价：降低并行性与传输速度（Q486）。
- **总线复用** = 不同信号在同一条信号线上分时传输（如地址/数据复用，Q490、Q503）。
- 总线标准：ISA、EISA、PCI、PCI-Express 等（Q499 2010）；**USB** = 串行总线（非并行，不能同时传 2 位）、即插即用/热插拔、菊花链可扩至 127 台外设、设备与设备控制器之间互连的接口标准（Q501 2012、Q502 2013）。

**带宽计算（高频真题，公式：带宽 = 总线宽度 × 每秒传输次数）**
- 基础式：32 位总线、500MHz、5 周期传 1 字 → `4B × (500M/5) = 400MB/s`（Q492）；16 位、100MHz、5 周期 → **40MB/s**（Q495）；32 根数据线、33MHz → **132MB/s**（Q494）；4B/2 时钟周期、10MHz → **20MB/s**（Q498 2009）。
- 每周期传 2 次：32 根地址/数据复用线、66MHz ×2 → **528MB/s**（Q503 2014）；3 通道 DDR3-1333、64 位 → `3×8×1333M ≈ 32GB/s`（Q504 2019）；QPI 2.4GHz ×2 次 ×2B×2 → **19.2GB/s**（Q505 2020）；420MHz ×8B ×2 → **6.72GB/s**（突发条件为干扰项，Q506 2024）；quad-pumped 1333M 次/秒 ×8B → **10.66GB/s**（Q507 2025）。
- 图片传输时间：640×480×16 位 ÷ 56kb/s ≈ **87.77s**（Q493）。

### 6.2 总线事务和定时

**同步 vs 异步（高频）**
- **同步通信**：采用**统一公共时钟**、总线周期长度固定；适合**总线长度短、各部件的存取时间接近**；速度快（Q510、Q511、Q512）；同步时钟由**系统统一提供**，不是各设备提供（Q522 2015）。
- **异步通信**：无公共时钟、靠**握手（应答）信号**定时，传送双方按需分配时间；适合**速度差异大**的设备（如 I/O 接口与打印机，Q513、Q514、Q516）。
- 异步握手三方式速度：**不互锁 > 半互锁 > 全互锁**（全互锁最慢、非互锁可靠性最差，Q515、Q522 2015）。

**突发传输 / 事务**
- **突发（猝发）传输**：一次总线事务**只给首地址**、连续传输多个地址连续的数据单元（Q508、Q521 2014）；可提高数据传输速率（Q525 2018）。
- **分离事务**：解耦请求与响应、等待期间可处理其他事务 → **提高总线利用率**（Q519、Q523 2016）。
- 现代高速总线（PCIe/USB/SATA）普遍**串行差分**传输，高频下串行比并行快（线间干扰易控制）（Q519、Q523 2016）；多总线结构之间**用桥接器相连**、靠近 CPU 的总线速度快、PCIe 是串行（Q524 2017）。
- 提高同步总线传输速率的措施：**增加总线宽度、提高工作频率、支持突发传输**；地址/数据复用只省线不加速（Q525 2018）。
- 时钟频率 ≠ 总线工作频率（一次事务可占多周期，Q526 2021）；一次握手交换多位数据（Q526）。

**定时计算**
- 突发写 128 位：100MHz（10ns/周期）、32 位宽 → 地址 10ns + 4×10ns = **50ns**（Q520 2012）。
- 读主存块 32B：64 位宽、1GHz、备数 6ns、非突发 → 每次 1+6+1=8ns ×4 次 = **32ns**（Q527 2023）。
- 突发读 8 字（32B）：50MHz、1+3+8=12 周期 → 32B ÷ (12×20ns) = **133.3MB/s**（Q517）。
- 异步串行字符帧：起始 1 + 数据 7 + 奇校验 1 + 停止 1 = **10 位/字符**，480 字符/秒 → **4800 b/s**（Q518）。

## 第7章 输入/输出系统（76 题，Q528–Q603）

### 7.1 I/O 系统基本概念

- I/O 设备**不能直接连系统总线**，必须通过**设备控制器**（I/O 接口）连接（Q528）。
- 外部设备计算：16×16 点阵汉字 32B/字 → 7500 字 = **240KB**（Q529）；显存容量 = 分辨率×颜色深度（1024×1024×8bit=**1MB**，Q530）；**刷新带宽 = 分辨率 × 颜色深度 × 帧频**，1600×1200×24×85=3916.8Mb/s，占显存带宽 50% → 总带宽 **7834Mb/s**（Q531 2010）。

### 7.2 I/O 接口

- I/O 接口功能：**选址、传送命令、传送数据、反映设备状态**（Q533）；接口寄存器主要有**数据缓冲、控制、状态寄存器**（Q535）。
- **统一编址**：I/O 端口与主存同一地址空间，**靠不同的地址码区分**，用**访存指令**完成 I/O，**不需要专门的 IN/OUT 指令**；I/O 地址应固定在地址的某区域（不能随意）（Q532、Q536、Q538、Q539、Q540、Q546 2014）。
- **独立编址**：I/O 端口与主存不同地址空间，CPU 需**专门的输入/输出指令**（IN/OUT）访问（Q534、Q538）。
- I/O 指令是机器指令的一类，但**格式与通用指令不同**（Q537）；I/O 指令传送发生在**通用寄存器与 I/O 端口**之间（Q547 2017）。
- 连接通路：**CPU和主存 → I/O 总线 → I/O 接口 → 通信总线（电缆）→ 外设**（Q543）；接口主机侧与设备侧**数据宽度不一定相同**（Q544）。
- I/O 总线**数据线**上传送：命令字、状态字、**中断类型号**（Q545 2012）；磁盘向磁道写数据用**串行**方式（Q541）；磁盘驱动器**本身不是 I/O 接口**（Q548 2021）。

### 7.3 I/O 方式 —— 中断与 DMA（真题大户）

**三种方式对比**
- **程序查询**：CPU 与外设**串行**工作；CPU 执行查询程序完成 I/O；每次传送后**主存地址+1、计数值−1**（Q568）。
- **中断方式**：CPU 与外设**并行**、但数据传送与主程序**串行**（Q567）；请求的是 **CPU 处理时间**（Q586 2013）。
- **DMA**：CPU、外设、数据传送**全部并行**；请求的是**总线使用权**；传送在**设备接口与主存之间**由 DMA 控制器控制、**不经过 CPU**（Q567、Q569、Q577、Q586 2013、Q601 2024）。

**中断响应与处理（高频）**
- 响应时机：**一条指令执行结束**（Q555、Q560、Q596 2021）；响应条件 = **有请求 + 开中断 + 指令结束**（Q555、Q594 2020）。
- **中断隐指令（硬件自动）**：①**关中断** ②**保存断点（PC）** ③形成中断向量地址/引出中断服务程序；**保存通用寄存器由中断服务程序（软件）完成**（Q559、Q563、Q575、Q585 2012、Q600 2024）；保护的关键状态 = **PC 和 PSW**（Q566）。
- 中断向量 = 中断服务程序**入口地址**；**中断向量地址 = 入口地址的地址**（Q550、Q554）；最先完成的步骤 = **关中断**（Q564）。
- **屏蔽字**：1=屏蔽；**屏蔽字决定处理优先级，响应优先级由硬件/查询顺序决定**（Q557、Q565、Q583 2011、Q600 2024）；"1"越多优先级越高（Q557）。
- 多重中断：保存现场时**关中断**、执行中断服务程序时**开中断**（Q589 2017）；单级中断服务程序顺序 = **保护现场→开中断→关中断→保存断点→中断事件处理→恢复现场→中断返回**（Q582 2010）。
- 外部中断（键盘/定时器/网络包/DMA 结束）；内部异常（缺页/除 0/断点指令/浮点下溢按 0 处理不中断）（Q581 2009、Q593 2020、Q603 2025）；**NMI 关中断也能响应、优先级高于可屏蔽中断**（Q594 2020）；中断服务程序最后一条是**中断返回指令**（Q552）。

**DMA（高频）**
- 过程三阶段：**预处理（CPU 设置传送参数）→ 数据传送（DMA 控制器直接控制总线）→ 后处理（中断服务程序）**（Q571、Q592 2019、Q599 2023、Q601 2024）；初始化信息 = 主存起始地址/传送方向/字数，**不含通用寄存器编号**（Q580）。
- 响应时机：**总线（存取）周期结束**即可响应（非指令结束，Q572、Q586）；周期挪用每次传一个数据占**一个存取周期**、单字传送后 CPU 即可访存（Q578、Q595 2020）；DMA 请求总线优先级**高于中断**（Q573、Q574）；传送不破坏 PC 与寄存器（Q576）；一次 DMA 传送一个**数据块**（Q579）。
- 只有**具有 DMA 接口**的设备才能产生 DMA 请求（Q553）；中断方式两处请求目的不同：程序中断为传数据、DMA 结束中断为结束处理（Q570、Q551）。

**中断/DMA 开销计算**
- 查询方式：50MHz、每秒查 200 次 × 500 周期 = 100000 周期 → 占 **0.20%**（Q584 2011）。
- 中断方式：每 400ns 中断一次、处理 100ns → **25%**（Q587 2014）；50kB/s ÷ 4B = 12.5k 次/秒 × 1000 周期 ÷ 1GHz → **1.25%**（Q591 2019）。
- 适合 DMA 的设备：**网卡、固态硬盘**等高速块设备；键盘/针式打印机用中断/查询（Q602 2025）；中断 I/O 方式要求外设准备数据时间**大于**中断处理时间（否则丢数据，Q597 2022）。
