# 悬浮「回到开头」按钮 — 开发全流程总结

> 功能：考点笔记页右下角悬浮 ↑ 圆钮，下滑超过 400px 出现，点击平滑滚回页面顶部。
> 日期：2026-07-25 ｜ 项目：408 刷题应用（`D:\ai code\408-quiz-app`）｜ 状态：已完成并部署

---

## 一、需求

- **用户原话**：「笔记右下角做个悬浮的跳到开头的按钮」
- **拆解**：
  1. 位置：笔记页（notes.html）右下角，悬浮固定（不随滚动移动）
  2. 行为：点击后回到页面开头
  3. 隐含体验要求：刚打开页面（已在开头）时不该出现，否则白白挡内容——滚下去一段才浮现

## 二、方案设计

### 2.1 交互设计
| 决策点 | 方案 | 理由 |
|---|---|---|
| 出现时机 | `window.scrollY >= 400` 才显示 | 开头就显示没意义还挡内容；400px ≈ 半屏多，刚好是「想回头」的起点 |
| 回顶方式 | `window.scrollTo({ top: 0, behavior: 'smooth' })` | 平滑滚动有方位感，用户知道自己被带回了顶部（对比 `instant` 会有瞬移迷失感） |
| 显隐实现 | 切换 `hidden` 属性 | 最简单可靠，无需维护 class；初始 HTML 直接带 `hidden`，无闪烁 |
| 滚动监听 | `addEventListener('scroll', fn, { passive: true })` | passive 告知浏览器回调不会 preventDefault，滚动不被阻塞，性能最优 |
| 形态 | 44×44 圆形按钮，↑ 箭头 | 44px 是移动端最小舒适触控尺寸；圆形悬浮钮是通用「回顶」范式 |

### 2.2 层级（z-index）选择
笔记页已有多个浮层，新按钮取 **2300**，低于所有交互型浮层、高于内容：

```
3000  .hl-toolbar        划词工具条（临时交互，必须最顶）
2400  .notes-search-results  搜索结果下拉
2300  .notes-top-btn     ★ 本次新增（常驻辅助钮，不该压过任何弹层）
 100  .navbar            顶部导航
```

### 2.3 双端镜像约束（本项目固定架构）
- **PWA 版**（`pwa/notes.html`，GitHub Pages 线上 + 本地 exe 均用它）与 **Flask 版**（`templates/notes.html`）必须逐行同功能
- CSS 两份物理文件必须 **byte-identical**：`pwa/css/style.css` ↔ `static/css/style.css`
- PWA 内联 JS 缩进 8 空格、Flask 4 空格，模块内容去缩进后须一致

## 三、实现

### 3.1 改动文件清单（6 个）
| 文件 | 改动 |
|---|---|
| `pwa/notes.html` | +HTML 按钮（`</main>` 前）+JS 显隐模块（`loadNotes()` 前），+11 行 |
| `templates/notes.html` | 镜像同款（按钮在 `{% endblock %}` 前、JS 在 `initAnnoAndHl()` 前），+11 行 |
| `pwa/css/style.css` | 文件尾 +28 行按钮样式 |
| `static/css/style.css` | 同上（byte-identical） |
| `pwa/sw.js` | `CACHE_VER` v20 → **v21**（触发离线缓存换新） |
| `CHANGELOG.md` | 顶部新增「笔记页悬浮回顶按钮」节 |

### 3.2 HTML（两版仅缩进不同）
```html
<button class="notes-top-btn" id="notesTopBtn" title="回到开头" hidden
    onclick="window.scrollTo({ top: 0, behavior: 'smooth' })">↑</button>
```
要点：初始自带 `hidden`；点击逻辑极短所以直接内联 onclick，不必抽函数。

### 3.3 JS 显隐模块（两版内容一致）
```js
// 悬浮回顶按钮：下滑超过 400px 才出现，避免开头就挡内容
(function () {
    const topBtn = document.getElementById('notesTopBtn');
    if (!topBtn) return;
    window.addEventListener('scroll', () => {
        topBtn.hidden = window.scrollY < 400;
    }, { passive: true });
})();
```
要点：IIFE 不污染全局；`if (!topBtn) return` 防御按钮缺失；回调只做一次属性赋值，无需节流（浏览器对 `hidden` 重复同值赋值开销可忽略）。

### 3.4 CSS（两份文件相同追加）
```css
/* 笔记页悬浮回顶按钮 */
.notes-top-btn {
    position: fixed;
    right: 22px;
    bottom: 26px;
    z-index: 2300;
    width: 44px;
    height: 44px;
    border-radius: 50%;
    border: 1px solid var(--border);
    background: var(--card-bg);
    color: var(--primary);
    font-size: 1.3rem;
    line-height: 1;
    cursor: pointer;
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.18);
    transition: transform 0.15s, box-shadow 0.15s;
}

.notes-top-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.25);
}

.dark .notes-top-btn {
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.5);
}
```
要点：全部用主题 CSS 变量（`--border/--card-bg/--primary`），夜间模式自动换色，只需单独加深阴影；不写 `display`，避免覆盖 `hidden` 属性的 UA 样式（这是 `hidden` 显隐方案能成立的前提）。

## 四、验证（全绿）

| 项 | 方法 | 结果 |
|---|---|---|
| CSS 双端一致 | `git hash-object` 两文件 | 同 hash `c076edf4` ✅ |
| JS 语法 | node `vm.Script` 编译双端全部内联脚本 | PWA/Flask 均 OK ✅ |
| JS 模块镜像 | 临时脚本提取回顶模块、去缩进逐行比对 | 8 行 vs 8 行，MIRROR OK ✅ |
| Flask 冒烟 | `app.test_client().get('/notes/os')` | 200；`notesTopBtn`/`notes-top-btn`/`scrollY < 400` 均注入，且与既有 `notesSearchBox`/`initAnnoAndHl` 共存 ✅ |

临时验证脚本 `_check_topbtn.js` 用完即删，不留在仓库。

## 五、部署（固定流程：线上优先，exe 最后）

1. **同步 study 仓库**：6 个改动文件 copy 到 `D:\ai code\study`（`pwa/*` → 仓库 `pwa/`，Flask 侧 → 仓库 `408-quiz-app/`）
2. **commit**：`285e0dc`「笔记页右下角悬浮回到开头按钮：下滑400px出现，点击平滑回顶（双端，sw v21）」，6 files +83/-1 ✅
3. **push**：⚠️ 代理 7890 与直连均失败（VPN 未开），**commit 已就绪待补推**
4. **exe 重打包**：PyInstaller spec 打包，`Build complete!` → `dist\408刷题应用.exe` ✅

> 补推命令：`cd "D:\ai code\study"; git -c http.proxy=http://127.0.0.1:7890 -c https.proxy=http://127.0.0.1:7890 push`

## 六、经验沉淀

1. **`hidden` 属性显隐是最短路径**——前提是自己的 CSS 不写 `display`，否则会覆盖 UA 的 `display:none` 导致 hidden 失效。
2. **常驻悬浮钮的 z-index 应低于一切临时弹层**：按钮是背景级辅助设施，压住搜索下拉/工具条就是 bug。
3. **scroll 监听务必 `{ passive: true }`**：长笔记页滚动频繁，非 passive 监听会强制浏览器等回调，掉帧。
4. **双端镜像项目的固定验证三件套**：CSS `git hash-object`、JS `vm.Script` 语法、模块去缩进逐行 diff——每次改动跑一遍，几秒钟换镜像不漂移。
5. **部署顺序纪律**：GitHub 推送优先、exe 打包最后；推送失败不阻塞后续步骤，commit 就绪等网络补推即可。
6. **PowerShell + git 的红字陷阱**：git 把进度写 stderr，PowerShell 渲染成红色异常且 ExitCode 非 0——判断成败只看输出里有没有 `旧hash..新hash  branch -> branch`。
