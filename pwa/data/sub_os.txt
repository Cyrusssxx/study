=== os_0177 ans B
C: 现在有三个同时到达的作业J ，J ，J ，它们的执行时间分别是T ，T ，T ，且T <T <T 。系统按单 1 2 3 1 2 3 1 2 3 道方式运行且采用短作业优先调度算法，则平均周转时间是( )
=== os_0187 ans C
C: 使用抢占式最短剩余时间优先调度算法对下列进程进行调度，总周转时间是( )<br>进程名 到达时间 运行时间<br>P₀ 3 1<br>P₁ 1 2<br>P₂ 4 3<br>P₃ 5 4<br>P₄ 2 5
=== os_0204 ans D
C: 【2017统考真题】假设4个作业到达系统的时刻和运行时间如下表所示。系统在t=2时开始作业调度。若分别采用先来先服务和短作业优先调度算法，则选中的作业分别是( )<br>作业 到达时刻t 运行时间<br>J₁ 0 3<br>J₂ 1 3<br>J₃ 1 2<br>J₄ 3 1
=== os_0207 ans C
C: 【2019统考真题】系统采用二级反馈队列调度算法进行进程调度。就绪队列Q 采用时间片轮转 1 调度算法，时间片为10ms；就绪队列Q 采用短进程优先调度算法；系统优先调度Q 队列中的进程， 2 1 当Q 为空时系统才会调度Q 中的进程；新创建的进程首先进入Q ；Q 中的进程执行一个时间片后， 1 2 1 1 若未结束，则转入Q 。若当前Q ，Q 为空，系统依次创建进程P ，P 后即开始进程调度，P ，P 需要的 2 1 2 1 2 1 2 CPU时间分别为30ms和20ms，则进程P，P 在系统中的平均等待时间为( ) 1 2
=== os_0211 ans C
C: 【2022统考真题】进程P₀、P₁、P₂和P₃进入就绪队列的时刻、优先级(值越小优先权越高)及CPU执行时间如下表所示。若系统采用基于优先权的抢占式进程调度算法，则从0ms时刻开始调度，到4个进程都运行结束为止，发生进程调度的总次数为( )<br>进程名 进入就绪队列的时刻 优先级 CPU执行时间<br>P₀ 0ms 15 100ms<br>P₁ 10ms 20 60ms<br>P₂ 10ms 10 20ms<br>P₃ 15ms 6 10ms
=== os_0212 ans B
C: 【2023统考真题】进程P₁、P₂和P₃进入就绪队列的时刻、优先级(值越大优先权越高)和CPU执行时间如下表所示。若系统采用基于优先权的抢占式CPU调度算法，从0ms时刻开始进行调度，则P₁、P₂和P₃的平均周转时间为( )<br>进程名 进入就绪队列的时刻 优先级 CPU执行时间<br>P₁ 0ms 1 60ms<br>P₂ 20ms 10 42ms<br>P₃ 30ms 100 13ms
=== os_0251 ans C
C: 两个进程P0和P1互斥的Peterson算法描述如下：
<pre class="code-block">// 进程P0                    // 进程P1
flag[0]=1;                   flag[1]=1;
(1);                         (2);
while(flag[1]&amp;&amp;turn==1);     while(flag[0]&amp;&amp;turn==0);
临界区;                       临界区;
flag[0]=0;                   flag[1]=0;
其余代码;                     其余代码;</pre>
其中，(1)和(2)处的代码分别为( )
=== os_0261 ans C
C: 有两个优先级相同的并发程序P 和P ，它们的执行过程如下所示。假设当前信号量s1=0，s2= 1 2 0。当前的z=2，进程运行结束后，x，y和z的值分别是( )进程P₁ 进程P₂ ... ... y：=1； x：=1； y：=y+2； x：=x+1； z：=y+1； P(s1)； V(s1)； x：=x+y； P(s2)； z：=x+z； y：=z+y； V(s2)； ... ...
=== os_0264 ans C
C: 【2011统考真题】有两个并发执行的进程P 和进程P ，共享初值为1的变量x。P 对x加1，P 对 1 2 1 2 x减1。加1和减1操作的指令序列分别如下： //加1操作 //减1操作 load R1，x //取x到寄存器R1 load R2，x //取x到寄存器R2 inc R1 dec R2 store x，R1 //将R1的内容存入x store x，R2 //将R2的内容存入x 两个操作完成后，x的值( )
=== os_0297 ans C
C: 假设具有5个进程的进程集合P={P，P，P，P，P } 0 1 2 3 4 ，系统中有三类资源A，B，C，假设在某时刻有 如下状态，见下表。系统是处于安全状态的，则x，y，z的取值可能是( ) I.1，4，0 II.0，6，2 III.1，1，1 IV.0，4，7
=== os_0307 ans D
C: 【2012统考真题】假设5个进程P，P，P，P，P 共享三类资源R ，R ，R ，这些资源总数分别为18，6， 0 1 2 3 4 1 2 3 22。T 时刻的资源分配情况如下表所示，此时存在的一个安全序列是( ) 0
=== os_0310 ans B
C: 【2015统考真题】若系统S 采用死锁避免方法，S 采用死锁检测方法。下列叙述中，正确的是( ) 1 2 I.S 会限制用户申请资源的顺序，而S 不会 1 2 II.S 需要进程运行所需的资源总量信息，而S 不需要 1 2 III.S 不会给可能导致死锁的进程分配资源，而S 会 1 2
=== os_0311 ans C
C: 【2016统考真题】系统中有3个不同的临界资源R ，R 和R ，被4个进程P ，P ，P ，P 共享。各进 1 2 3 1 2 3 4 程对资源的需求为：P 申请R 和R ，P 申请R 和R ，P 申请R 和R ，P 申请R 。若系统出现死锁，则 1 1 2 2 2 3 3 1 3 4 2 处于死锁状态的进程数至少是( )
=== os_0312 ans A
C: 【2018统考真题】假设系统中有4个同类资源，进程P ，P 和P 需要的资源数分别为4，3和1，P ， 1 2 3 1 P 和P 已申请到的资源数分别为2，1和0，则执行安全性检测算法的结果是( ) 2 3
=== os_0316 ans B
C: 【2022统考真题】系统中有三个进程P ，P ，P 及三类资源A、B、C。若某时刻系统分配资源的 0 1 2 情况如下表所示，则此时系统中存在的安全序列的个数为( )
=== os_0383 ans B
C: 【2019统考真题】在分段存储管理系统中，用共享段表描述所有被共享的段。若进程P 和P 共 1 2 享段S，则下列叙述中，错误的是( )
=== os_0498 ans A
C: 设文件F 的当前引用计数值为1，先建立F 的硬链接文件F ，再建立F 的符号链接文件F ，现有 1 1 2 2 3 两个进程P 和P 分别打开了F 和F，则下列说法中正确的是( ) 1 2 1 2
=== os_0505 ans B
C: 【2009统考真题】设文件F 的当前引用计数值为1，先建立文件F 的符号链接(软链接)文件F ， 1 1 2 再建立文件F 的硬链接文件F，然后删除文件F。此时，文件F 和文件F 的引用计数值分别是( ) 1 3 1 2 3
=== os_0578 ans D
C: 某操作系统采用双缓冲区传送磁盘上的数据。设从磁盘将数据传送到缓冲区所用的时间为T ，将 1 缓冲区中的数据传送到用户区所用的时间为T ，CPU处理一块数据所用的时间为T ，假设一个磁盘 2 3 块和一个缓冲区的大小相等，某系统在一段时间内连续处理一大批数据，则平均处理一个磁盘块数据 的时间为( )