# -*- coding: utf-8 -*-
"""一次性脚本：补全 OS 笔记缺失的 5 个教材小节。
3.1.5 段页式存储管理 / 3.2.6 页框回收 / 3.2.7 内存映射文件 /
3.2.9 地址翻译的示例 / 5.2.6 I/O操作举例
内容依据王道2027 OS教材原文精编，含表格与排版。
"""
import json

SRC = 'pwa/data/notes/os_notes.json'
notes = json.load(open(SRC, encoding='utf-8'))

# ---------- 内容 ----------
sec315 = {
 'section': '3.1.5 段页式存储管理',
 'html': (
  '<p class="paragraph">分页存储管理能有效提高内存利用率，分段存储管理能反映程序的逻辑结构，并有利于段的共享与保护。将二者优势有机结合，便形成了<b>段页式存储管理</b>。</p>'
  '<p class="paragraph"><b>基本结构</b>：进程的地址空间先被划分为若干<b>逻辑段</b>（每段有唯一段号），随后每个段再被划分为若干大小固定的<b>页</b>；内存空间仍采用分页方式，划分为与页大小相同的<b>物理块</b>，内存分配以物理块为单位。</p>'
  '<p class="paragraph"><b>逻辑地址结构（三部分）</b>：<b>段号 S ＋ 页号 P ＋ 页内偏移量 W</b>。</p>'
  '<p class="paragraph"><b>段表与页表</b>：系统为每个进程建立<b>一张段表</b>，每个段对应一个段表项，记录该段的<b>页表始址</b>和<b>页表长度</b>（段号作为段表索引，无须显式存储）；每个段对应<b>一张页表</b>，页表项记录对应页的<b>物理块号</b>（页号作为页表索引，也无须存储）。此外设一个<b>段表寄存器</b>，存放当前进程段表的起始地址和段表长度，既用于段表寻址，也用于段号越界检查。</p>'
  '<p class="paragraph"><b>关键结论（常考）</b>：段页式存储管理中，<b>每个进程仅有一张段表，但可能有多张页表（每个段一张）</b>。</p>'
  '<p class="paragraph"><b>地址变换过程</b>：</p>'
  '<ol><li>根据逻辑地址中的<b>段号 S</b>，通过段表寄存器定位段表，查得该段对应<b>页表的起始地址</b>；</li>'
  '<li>利用<b>页号 P</b>访问该页表，获取<b>物理块号</b>；</li>'
  '<li>将物理块号与<b>页内偏移量 W</b>拼接，形成物理地址。</li></ol>'
  '<p class="paragraph">在<b>无快表（TLB）</b>的情况下，段页式每次访问内存需<b>三次访存</b>：第一次访段表、第二次访页表、第三次才访问真正的内存单元。为加速，可引入<b>快表（TLB）</b>，其表项包含关键字（段号、页号）及对应的物理块号和保护信息。</p>'
  '<p class="paragraph">三种存储管理方式对比：</p>'
  '<table><tr><th>方式</th><th>地址结构</th><th>访存次数（无快表）</th><th>优点</th><th>缺点</th></tr>'
  '<tr><td>分页</td><td>页号＋页内偏移</td><td>2 次（页表＋内存）</td><td>无外碎片、利用率高</td><td>不反映逻辑结构、共享保护差</td></tr>'
  '<tr><td>分段</td><td>段号＋段内偏移</td><td>2 次（段表＋内存）</td><td>反映逻辑结构、易共享保护</td><td>有外碎片</td></tr>'
  '<tr><td>段页式</td><td>段号＋页号＋页内偏移</td><td>3 次（段表＋页表＋内存）</td><td>兼有二者优点</td><td>访存次数多、结构最复杂</td></tr></table>'
 ),
}

sec326 = {
 'section': '3.2.6 页框回收',
 'html': (
  '<p class="paragraph">本节为 2025 年大纲新增考点。页框回收是指当系统可分配内存不足时，回收部分页框的机制，其核心是<b>页面缓冲算法</b>。</p>'
  '<p class="paragraph"><b>影响页面换入/换出效率的主要因素</b>：①页面置换算法的选择；②已修改页面（脏页）写回磁盘的频率；③磁盘内容读入内存的频率。</p>'
  '<p class="paragraph"><b>页面缓冲算法</b>：在原页面置换算法的基础上，增设两个专用链表，通过<b>延迟写回</b>显著降低换入/换出频率与磁盘 I/O 开销：</p>'
  '<ol><li><b>空闲页面链表（空闲页框链表）</b>：进程读入页面时从链表头部取一个页框装入目标页面；若某<b>未被修改</b>的页面被换出，不写回磁盘，直接将其物理页框挂在链表末尾（页框中仍保留原数据），后续若访问相同页面可直接取下复用，避免重新读盘。</li>'
  '<li><b>修改页面链表</b>：进程换出<b>已修改</b>的页面时，不立即写回磁盘，而是把其页框挂在链表末尾，待链表中积累足够数量的脏页后<b>批量写回磁盘</b>，降低写回频率。</li></ol>'
  '<p class="paragraph"><b>页面缓冲算法的优点</b>：①显著降低换入/换出频率、大幅减少磁盘 I/O；②即使采用简单的置换策略（如 FIFO）也能获得良好性能，且无须特殊硬件支持、实现简单。</p>'
  '<p class="paragraph"><b>可回收/不可回收的页框</b>：属于内核的大部分页框（内核栈、内核代码段、内核数据段等）<b>不可回收</b>；由进程使用的页框（进程代码段、数据段、堆栈、映射的文件页、共享内存页等）大多<b>可回收</b>。</p>'
  '<p class="paragraph"><b>kswapd 守护进程</b>：Linux 内核设置负责页面换出的守护进程 kswapd，定期检查内存使用情况，当空闲页框数低于阈值时主动发起回收。<b>不能等到空闲页框耗尽才回收</b>——因为释放脏页需先写回磁盘，而该 I/O 本身需要临时页框作缓冲区，若已无空闲页框，既无法分配缓冲区也无法释放页面，会导致内存分配死锁甚至系统崩溃。</p>'
  '<p class="paragraph"><b>伙伴算法逆操作</b>：Linux 用伙伴算法管理连续空闲页框（按大小分组为"空闲块"）。分配页框是"化整为零"；回收是逆过程——释放一个页框时检查是否存在大小相等的<b>伙伴空闲块</b>，存在则合并为大小翻倍的新空闲块，并继续向上尝试合并，直到无法再合并为止。</p>'
 ),
}

sec327 = {
 'section': '3.2.7 内存映射文件',
 'html': (
  '<p class="paragraph"><b>内存映射文件（Memory-Mapped Files）</b>是操作系统向应用程序提供的系统调用机制，它在磁盘文件与进程的虚拟地址空间之间建立<b>直接映射关系</b>，与虚拟内存机制紧密相关。</p>'
  '<p class="paragraph"><b>机制</b>：进程通过该系统调用把文件映射到自身虚拟地址空间的某区域，此后可<b>像访问内存一样读写文件</b>（把文件当作内存中的大字符数组），无须调用传统文件 I/O 接口；磁盘文件的实际读写由操作系统负责，对进程透明。</p>'
  '<p class="paragraph"><b>按需调入与延迟写回</b>：映射时并不立即加载文件内容，而是在进程<b>首次访问某页面时按需一页页调入内存</b>；当进程退出或显式解除映射时，所有被修改的页面才写回磁盘文件。</p>'
  '<p class="paragraph"><b>实现共享内存</b>：多个进程把<b>同一文件</b>映射到各自的虚拟地址空间，操作系统通过页表把它们对应的虚拟页映射到<b>相同的物理页</b>——尽管各进程虚拟地址空间相互独立，却访问同一块物理内存。因此一个进程写后，另一进程在其映射区域读能立即看到更新结果，数据必然一致，<b>无须额外复制或同步机制</b>。</p>'
  '<p class="paragraph"><b>好处</b>：①编程更简单（建立映射后可直接按内存方式读写文件）；②便于多个进程共享同一个磁盘文件。</p>'
 ),
}

sec329 = {
 'section': '3.2.9 地址翻译的示例',
 'html': (
  '<p class="paragraph">本节结合《计算机组成原理》Cache 的内容，分析虚实地址的变换过程（408 综合题典型考法）。设系统满足：配置一个 TLB 和一个 data Cache；以字节编址；<b>虚拟地址 14 位、物理地址 12 位、页面大小 64B</b>；TLB 采用<b>四路组相联</b>、共 16 个条目；data Cache 采用<b>物理寻址、直接映射</b>、行大小 4B、共 16 组。要求分析虚拟地址 <code>0x03d4、0x00f1、0x0229</code> 的访问过程。</p>'
  '<p class="paragraph"><b>地址结构划分</b>：页面 64B → 页内偏移占 <code>log₂64 = 6</code> 位；虚拟地址 14 位 → 虚拟页号 = 14 − 6 = <b>8 位</b>；物理地址 12 位 → 物理页号 = 12 − 6 = <b>6 位</b>。TLB 四路组相联、16 条目 → 组数 = 16 / 4 = 4，<b>组索引占 2 位</b>（虚拟页号低 2 位），<b>高 6 位作 TLB 标记</b>。data Cache 行 4B → 块内偏移 2 位；共 16 组 → <b>Cache 索引 4 位</b>，剩余高 6 位为 Cache 标记。</p>'
  '<table><tr><th>字段</th><th>位数</th><th>作用</th></tr>'
  '<tr><td>虚拟页号</td><td>8</td><td>其中低 2 位为 TLB 组索引、高 6 位为 TLB 标记</td></tr>'
  '<tr><td>页内偏移</td><td>6</td><td>定位页内字节</td></tr>'
  '<tr><td>物理页号</td><td>6</td><td>其中低 4 位为 Cache 索引、高 2 位并入 Cache 标记</td></tr>'
  '<tr><td>块内偏移</td><td>2</td><td>Cache 行内字节偏移</td></tr></table>'
  '<p class="paragraph"><b>三个虚拟地址的分析</b>：</p>'
  '<ol><li><b>0x03d4</b>（二进制 000011 010100，物理地址 0x354）：TLB 命中，得物理页号 0x0d，拼页内偏移得物理地址 <b>0x354</b>；查 Cache（索引 5、标记 0x0d、有效位 1）<b>命中</b>，偏移 0，数据为 <b>36H</b>。</li>'
  '<li><b>0x00f1</b>（物理地址 0x0b1）：TLB 未命中 → 查页表，虚拟页号 0x03 有效位 1，物理页号 0x02，拼页内偏移 110001 得物理地址 <b>0x0b1</b>；查 Cache（索引 0xc、有效位 0）<b>未命中</b>，需从主存读取。</li>'
  '<li><b>0x0229</b>：TLB 未命中 → 查页表，虚拟页号 0x08 对应页表项<b>有效位 0</b>，页面不在主存，触发<b>缺页中断</b>（需先从外存调入页面）。</li></ol>'
  '<p class="paragraph"><b>完整地址翻译与数据访问流程</b>：先查 TLB → 若未命中查页表完成虚实地址转换 → 若页面未调入（页表项无效）则触发缺页中断、从外存调入 → 再用物理地址访问 Cache → Cache 未命中则从主存读取。</p>'
 ),
}

sec526 = {
 'section': '5.2.6 I/O操作举例',
 'html': (
  '<p class="paragraph">以 C 语言库函数 <code>scanf()</code> 为例，介绍从执行 <code>scanf("%d", &amp;d)</code> 到键盘字符最终存入变量 d 的 I/O 执行过程。</p>'
  '<p class="paragraph"><b>两个缓冲区概念</b>：<b>内核缓冲区</b>（内核空间）与<b>用户缓冲区</b>（用户空间）相互隔离。系统调用在内核态执行，因此必须先把 I/O 设备的数据复制到内核缓冲区，在系统调用返回前再把数据从内核缓冲区复制到用户缓冲区。</p>'
  '<p class="paragraph"><b>scanf 执行的两个阶段</b>：</p>'
  '<p class="paragraph"><b>第一阶段（C 标准库，用户空间）</b>：①检查 scanf 关联的用户缓冲区 buf，有数据则直接读，为空则触发系统调用 read；②执行 read 系统调用，传入三个参数——<b>fd</b>（文件描述符，此处为标准输入）、<b>buf</b>（用户缓冲区地址）、<b>count</b>（期望读取的最大字节数）。</p>'
  '<p class="paragraph"><b>第二阶段（系统调用，内核态）</b>：read 通过含陷阱指令的代码使 CPU 从用户态陷入内核态，执行系统调用服务例程。在中断驱动方式下，大致过程如下：</p>'
  '<ol><li>设置 I/O 参数后，发起 I/O 的进程 P 进入<b>阻塞态</b>，CPU 调度其他进程；</li>'
  '<li>用户键盘输入字符，字符送入键盘 I/O 接口的数据端口；</li>'
  '<li>键盘 I/O 接口向 CPU 发出<b>中断请求</b>；</li>'
  '<li>CPU 响应中断，执行键盘中断处理程序，读取字符并从 I/O 端口送入<b>内核缓冲区</b>；</li>'
  '<li>进程 P 被唤醒并加入就绪队列等待调度；</li>'
  '<li>P 再次获得 CPU 后，系统调用服务例程把字符从内核缓冲区复制到<b>用户缓冲区</b>；</li>'
  '<li>P 从系统调用返回，scanf 解析缓冲区中的字符，最终存入变量 d。</li></ol>'
 ),
}

# ---------- 插入 ----------
def find(ch_title, sec_title):
    ch = next(c for c in notes['chapters'] if c['chapter'] == ch_title)
    return ch, next(s for s in ch['sections'] if s['section'] == sec_title)

# 3.1.5 插到 3.1 节 subsections（按编号，在 3.1.4 后）
ch, s = find('第3章 内存管理', '3.1 内存管理概念')
s['subsections'].append(sec315)

# 3.2.6 / 3.2.7 / 3.2.9 插到 3.2 节（按编号排序）
ch, s = find('第3章 内存管理', '3.2 虚拟内存管理')
s['subsections'].append(sec326)
s['subsections'].append(sec327)
s['subsections'].append(sec329)
# 按教材编号排序（3.2.6 < 3.2.7 < 3.2.9）
order = ['3.2.1', '3.2.2', '3.2.3', '3.2.4', '3.2.6', '3.2.7', '3.2.8', '3.2.9']
s['subsections'].sort(key=lambda x: order.index(x['section']) if x['section'] in order else 99)

# 5.2.6 插到 5.2 节
ch, s = find('第5章 输入/输出管理', '5.2 设备独立性软件')
s['subsections'].append(sec526)
order52 = ['5.2.1', '5.2.2', '5.2.3', '5.2.4', '5.2.5', '5.2.6']
s['subsections'].sort(key=lambda x: order52.index(x['section']) if x['section'] in order52 else 99)

with open(SRC, 'w', encoding='utf-8') as f:
    json.dump(notes, f, ensure_ascii=False, indent=2)
    f.write('\n')

print('已补 5 个小节')
for ch in notes['chapters']:
    if ch['chapter'] in ('第3章 内存管理', '第5章 输入/输出管理'):
        for s in ch['sections']:
            print(f'  {ch["chapter"][:8]} {s["section"]}:', [ss['section'] for ss in s.get('subsections', [])])
