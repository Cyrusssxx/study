# -*- coding: utf-8 -*-
"""计组笔记 · 第5章 中央处理器（王道2027结构）"""

CH5 = {
    "chapter": "第5章",
    "title": "第5章 中央处理器",
    "sections": [
        {
            "section": "5.1 CPU的功能和基本结构",
            "html": """<h4>5.1 CPU的功能和基本结构</h4>
<h5>5.1.1 CPU的功能</h5>
<p>CPU 的核心功能是执行程序，同时具备检测并响应各类异常与中断的能力。基本功能包括：</p>
<ul>
<li><b>取指令并译码</b>：从主存取出指令，解析操作码，生成相应控制信号。</li>
<li><b>更新程序计数器（PC）</b>：确定下一条指令地址，支持顺序执行或程序转移。</li>
<li><b>执行算术与逻辑运算</b>：通过 ALU 对操作数进行运算。</li>
<li><b>取操作数或写结果</b>：访问主存或 I/O 接口，读取操作数或将结果写回。</li>
<li><b>处理异常或中断</b>：检测异常/中断请求，必要时转入相应处理程序。</li>
<li><b>时序控制</b>：通过时钟信号协调各操作的执行顺序和持续时间。</li>
</ul>
<h5>5.1.2 CPU的基本结构</h5>
<p><b>关键部件</b>：</p>
<ul>
<li><b>程序计数器 PC</b>：存放即将执行指令的地址。顺序执行时自动递增（增量为当前指令所占字节数）；转移时更新为目标地址。位数由地址总线宽度决定，反映可直接寻址的内存空间大小。</li>
<li><b>指令寄存器 IR</b>：暂存当前正在执行的指令，位数等于指令字长。</li>
<li><b>指令译码器 ID</b>：分析 IR 中的操作码，识别指令类型，输出译码信号。</li>
<li><b>控制单元 CU</b>：综合译码结果、时序信号与状态标志，生成微操作控制信号序列。</li>
<li><b>通用寄存器组 GPRs</b>：暂存操作数、中间结果或地址指针，减少对主存的频繁访问。</li>
<li><b>标志寄存器 FR（程序状态字寄存器 PSWR）</b>：保存 ALU 运算产生的状态信息，用于条件判断与转移控制。</li>
<li><b>存储器地址寄存器 MAR</b>：存放当前要访问的主存地址，位数由地址总线宽度决定。</li>
<li><b>存储器数据寄存器 MDR</b>：暂存从主存读出或将要写入主存的数据，起缓冲与同步作用。</li>
<li><b>算术逻辑单元 ALU</b>：执行数据运算的核心部件。</li>
<li><b>时序信号产生部件</b>：以系统时钟为基础生成周期、节拍和工作脉冲。</li>
<li><b>操作控制信号形成部件</b>：综合译码、时序和状态标志生成微操作控制信号。</li>
<li><b>总线控制逻辑</b>：实现对总线传输的控制；<b>中断机构</b>：处理异常和外部中断。</li>
</ul>
<h5>5.1.3 CPU中的寄存器</h5>
<p>按汇编语言（机器语言）程序能否直接访问分为两类：</p>
<ul>
<li><b>用户可见寄存器</b>：可被用户程序直接读/写——通用寄存器组 GPRs、专用地址寄存器（基址寄存器、变址寄存器、堆栈指针）、程序计数器 PC。另有<b>只读型</b>标志寄存器 FR（内容由 ALU 自动生成，用户不能直接修改）。</li>
<li><b>用户不可见寄存器</b>：对用户程序完全透明，仅由 CPU 硬件或 OS 内核在特权模式下使用——指令寄存器 IR、MAR、MDR、页表基址寄存器等。</li>
</ul>
<p><b>易错点</b>：汇编程序员可见的是 PC（可通过转移/调用指令设置其值），IR、MAR、MDR 是 CPU 内部工作寄存器，对程序员不可见</p><p>机器字长 = ALU 位数 = 通用寄存器宽度（数据通路宽度）。</p>"""
        },
        {
            "section": "5.2 指令执行过程",
            "html": """<h4>5.2 指令执行过程</h4>
<h5>5.2.1 指令执行的一般流程</h5>
<p>CPU 持续循环执行"取指令"与"执行指令"两个基本操作，每条指令执行完毕后还要进行<b>中断与异常检测</b>。</p>
<ul>
<li>以 PC 当前值为地址取指令，同时 PC 自动更新为下一条顺序指令地址（增量为当前指令长度）。</li>
<li>译码后区分分支指令与非分支指令：分支指令在执行阶段判断分支条件，条件满足则 PC 更新为分支目标地址；非分支指令依次完成取操作数、执行运算、写回结果。</li>
<li>执行完成后检测中断/异常：未检测到则继续取指；检测到则进入中断响应阶段——屏蔽可屏蔽中断、保存返回地址（异常为当前指令地址、中断为下一条指令地址），将 PC 设为中断服务程序入口地址。</li>
</ul>
<p><b>指令与数据的区分</b>：指令和数据都以二进制存放于同一内存，CPU 从<b>时间上</b>（取指阶段取指令、执行阶段取数据）和<b>空间上</b>（取指送 IR、取数送通用寄存器/ALU）区分，不由内存决定。</p>
<h5>5.2.2 CPU的时序控制</h5>
<p>时钟信号由脉冲源（晶振）产生，经整形和分频后形成节拍信号</p><p><b>时钟周期长度由数据通路中相邻状态单元之间组合逻辑的最大传播延迟决定</b>。早期采用"机器周期-节拍-脉冲"三级时序系统：指令周期划分为若干机器周期（取指、取操作数、执行、中断响应等），每个机器周期细分为多个节拍，必要时节拍内插入工作脉冲。现代处理器已简化时序结构，一个时钟周期对应一个节拍，绝大多数指令在若干时钟周期内完成。</p>
<h5>5.2.3 指令周期的基本概念</h5>
<p><b>指令周期</b>：一条指令从主存读出到执行完成所经历的全部时间。典型划分为五个阶段：</p>
<ul>
<li><b>取指（IF）</b>：按 PC 从主存（或指令 Cache）读取指令送入 IR，同时更新 PC（变长指令需在取指阶段初步解析格式以确定长度）。</li>
<li><b>译码/读寄存器（ID）</b>：识别操作码和寻址方式，从寄存器堆读取操作数，组合基址寄存器与偏移量，提取立即数。</li>
<li><b>执行/计算地址（EX）</b>：ALU 运算；访存类指令计算有效地址；分支指令计算目标地址并判断是否转移。运算结果状态（零标志、进位等）在此阶段生成并暂存。</li>
<li><b>访存（MEM）</b>：需要访问主存的指令（加载/存储）在此完成读/写；寄存器-寄存器类指令不涉及。</li>
<li><b>写回（WB）</b>：将最终结果（ALU 输出或存储器读出的数据）写回寄存器堆。</li>
</ul>
<p><b>定长指令周期</b>（所有指令周期长度相同）vs <b>变长指令周期</b>（不同指令时钟周期数可变）</p><p>现代计算机普遍采用基于时钟信号定时的变长指令周期。</p>
<h5>5.2.4 处理器指令执行模型</h5>

<ul>
<li><b>单周期处理器</b>：所有指令在一个时钟周期内完成（CPI=1），指令严格串行；时钟周期由执行时间<b>最长的指令</b>决定，短指令也占用整个周期，硬件资源部分空闲。</li>
<li><b>多周期处理器</b>：按指令类型动态分配周期数（平均 CPI 通常大于 1），提高时钟频率和资源利用率；但指令仍串行执行，无法重叠处理。</li>
<li><b>流水线处理器</b>：指令级并行，每个时钟周期启动一条新指令，多条指令重叠执行；单条指令执行延迟未减少，但整体吞吐率显著提高（理想 CPI=1）。</li>
</ul>"""
        },
        {
            "section": "5.3 数据通路的功能和基本结构",
            "html": """<h4>5.3 数据通路的功能和基本结构</h4>
<h5>5.3.1 数据通路的功能</h5>
<p>CPU 由<b>数据通路</b>（Data Path）和<b>控制部件</b>（Control Unit）两大部分组成。数据在指令执行过程中所经过的路径及路径上的硬件部件称为数据通路（ALU、通用寄存器、状态寄存器等）</p><p>控制部件根据每条指令的功能生成控制信号，驱动数据通路完成指定操作。</p>
<h5>5.3.2 数据通路的组成</h5>
<p><b>组合逻辑元件（操作元件）</b>：任意时刻输出仅由当前输入决定，不依赖历史状态</p><p>内部不含记忆单元，不受时钟控制，无输出到输入的反馈通路。常用元件：加法器、ALU、译码器、多路选择器（MUX）、三态门等。</p>
<ul>
<li><b>译码器</b>：n 位输入产生 2ⁿ 个互斥输出，用于操作码译码或地址码译码。</li>
<li><b>多路选择器 MUX</b>：根据控制信号 Select 从多路输入中选择一路输出；输入路数为 N 时需 ⌈log₂N⌉ 位控制信号。</li>
<li><b>三态门</b>：受控的总线开关，EN=1 时输出等于输入（打开），EN=0 时输出端呈<b>高阻态</b>（隔断态），所连寄存器与总线断开。</li>
</ul>
<p><b>时序逻辑元件（状态元件）</b>：输出不仅取决于当前输入还依赖历史状态，内部包含记忆单元，必须在时钟同步控制下工作——通用寄存器组、PC、状态/暂存寄存器等。</p>
<p><b>最小时钟周期</b>：数据通路抽象为"状态元件-操作元件-状态元件"交替连接。寄存器输出延迟 T_clk_to_q + 组合逻辑关键路径延迟 T_max（所有输出信号达到稳定的最长时间）+ 寄存器建立时间 T_setup，即最小时钟周期 ≥ T_clk_to_q + T_max + T_setup。<b>缩短组合逻辑关键路径延迟是提升系统频率的关键手段</b>。</p>
<h5>5.3.3 数据通路的组织与分类</h5>
<p><b>按部件连接方式</b>：</p>
<p><b>表5.1 总线式与专用数据通路的特点对比</b>：</p>
<table>
<tr><th>对比维度</th><th>总线式数据通路</th><th>专用数据通路</th></tr>
<tr><td>结构复杂度</td><td>简单，部件共享总线，连线少</td><td>复杂，需要大量点对点专用连线</td></tr>
<tr><td>扩展灵活性</td><td>强，新增部件只需要挂接总线</td><td>弱，新增部件需要重新布线</td></tr>
<tr><td>数据传输效率</td><td>低，同一时刻仅支持一组数据传输</td><td>高，多组数据可并行传输</td></tr>
<tr><td>硬件成本</td><td>低</td><td>高</td></tr>
<tr><td>性能瓶颈</td><td>总线带宽受限，部件增多时延迟增加</td><td>无总线瓶颈，性能随并行度提升</td></tr>
<tr><td>典型应用场景</td><td>教学模型、嵌入式微控制器</td><td>高性能通用处理器（如 RISC）</td></tr>
</table>
<p><b>总线式</b>（片内总线，不同于系统总线）又分<b>单总线结构</b>（所有数据传输共享一条内部总线，ALU 输入输出分时传送）与<b>多总线结构</b>（多条独立总线支持更高并行度）</p><p><b>专用数据通路</b>采用点对点专用连线，可并行传输、延迟低，但布线复杂、扩展性差、成本高。</p>
<p><b>按时序组织方式</b>：</p>
<ul>
<li><b>单周期数据通路</b>：所有操作在一个时钟周期内完成，时钟周期由最慢指令（通常是访存指令）决定，资源利用率低。<b>单周期处理器（CPI=1）不能采用单总线结构</b>（一个周期内只允许一次操作，无法完成一条指令的所有操作）。</li>
<li><b>多周期数据通路</b>：指令执行划分为多个阶段，每阶段一个时钟周期，各阶段结果在时钟边沿被锁存到寄存器；提高了硬件复用率、缩短了时钟周期。</li>
<li><b>流水线数据通路</b>：指令执行分解为若干独立、可重叠的阶段，各阶段由专用功能部件完成，通过<b>流水段寄存器</b>隔离和传递中间结果；时钟周期由最长阶段决定，理想情况每周期完成一条指令。</li>
</ul>
<h5>5.3.4 单总线结构的数据通路</h5>
<p>所有可向内部总线输出数据的部件（寄存器、MDR、PC 等）均通过各自三态门与总线相连，避免多个输出同时驱动总线冲突。控制信号命名"部件名+in/out"：in 表示允许向该部件写入，out 表示允许该部件输出到总线。Y 和 Z 为暂存寄存器：<b>ALU 无存储能力，需先将一个操作数经总线送入暂存器 Y，下一周期再送另一操作数</p><p>ALU 输出不能直接连总线（会反馈干扰），需暂存于 Z</b>。</p>
<p><b>取指阶段数据流</b>（读主存 5 周期时共 7 周期）：① (PC)→MAR（PCout, MARin）</p><p>② MEM(MAR)→MDR（MDRin, read）</p><p>③ (MDR)→IR（MDRout, IRin）。</p>
<p><b>加法指令执行阶段</b>：① (rs)→Y（GPRout, Yin）</p><p>② (rs)+(rd)→Z（GPRout, ALUop）</p><p>③ (Z)→rd（Zout, GPRin）。</p>
<p><b>取数指令执行阶段</b>：① (rs)→MAR</p><p>② MEM(MAR)→MDR</p><p>③ (MDR)→rd。</p>
<h5>5.3.5 专用结构的数据通路</h5>
<p>以单周期数据通路为例：</p>
<ul>
<li><b>取指令部件</b>：指令存储器仅支持读，下地址逻辑计算并更新 PC（顺序执行 PC+4；分支/转移指令按类型计算转移目标）。</li>
<li><b>R 型指令</b>（add rd,rs,rt）：寄存器堆读地址端 Ra、Rb 接 Rs、Rt，写地址端 Rw 接 Rd；RegWr 为写使能，<b>RegWr 与溢出标志 Overflow 通过"与"门决定是否写入</b>；ALU 操作类型由 ALUctr 决定（N 种运算至少 ⌈log₂N⌉ 位）。<b>单周期处理器不设 IR</b>，指令字段直接用于生成控制信号。</li>
<li><b>I 型指令</b>（addi rt,rs,imm16）：三处改动——①目的寄存器选择 MUX（RegDst=1 选 Rd、=0 选 Rt）；<br>②立即数扩展器（ExtOp 控制：算术类符号扩展、逻辑类零扩展）；<br>③ALU 第二操作数 MUX（ALUSrc=0 选 Rt、=1 选扩展立即数）。</li>
<li><b>访存指令</b>（lw rt,imm16(rs) / sw rt,imm16(rs)）：地址计算为 R[rs]+SignExt(imm16)，通过 ALU 执行不判溢出的加法（addu）；lw 需 MemtoReg MUX（=0 选 ALU 结果、=1 选存储器读出数据），sw 将 busB（Rt 内容）接数据存储器 Dataln，MemWr 为写使能。</li>
</ul>
<p><b>单周期处理器特点</b>：指令存储器与数据存储器分离（取指与数据访存并行，避免冲突）；无 IR；时钟周期由最慢指令决定</p><p>关键功能单元需多组独立实例（硬件无法复用）；控制信号单周期有效。</p>"""
        },
        {
            "section": "5.4 控制器的功能和工作原理",
            "html": """<h4>5.4 控制器的功能和工作原理</h4>
<h5>5.4.1 控制器的结构和功能</h5>
<p>控制器是计算机的指挥中心，职责：①取出待执行指令并计算下一条指令位置（更新 PC）</p><p>②对操作码解码，产生对应的时序信号和控制信号</p><p>③管理 CPU、主存和 I/O 设备间的数据流向及时序。依据微操作控制信号的生成方式，控制器分为<b>硬布线控制器</b>和<b>微程序控制器</b>。</p>
<h5>5.4.2 硬布线控制器</h5>
<p>由复杂的组合逻辑电路和触发器构成（组合逻辑控制器）。<b>CU 的输入</b>：①操作译码器输出的指令信息（IR 中 n 位操作码经译码电路转换为 2ⁿ 个输出信号，每种操作码唯一激活一条信号线）</p><p>②时钟脉冲（频率即机器主频，划分节拍）</p><p>③执行单元反馈的状态标志。</p>
<p>特点：响应速度快（性能主要受电路延迟限制），但<b>灵活性差</b>——修改或新增指令需重新设计电路，微操作控制信号数量增加导致电路规模庞大、调试困难。</p>
<h5>5.4.3 微程序控制器</h5>
<p>采用<b>存储逻辑</b>实现：将微操作控制信号组织为微指令，每条机器指令对应的微指令序列（微程序）预先存入专用的高速存储器——<b>控制存储器（CM）</b>。执行一条机器指令 = 顺序执行其对应微程序。</p>
<p><b>基本术语</b>：</p>
<ul>
<li><b>微命令与微操作</b>：控制部件向执行部件发出的最基本控制信号是微命令（构成控制序列的最小单位），执行部件接收后执行的具体动作是微操作，二者一一对应。<b>相容性微命令</b>可同时有效；<b>互斥性微命令</b>同一时刻不允许同时有效。</li>
<li><b>微指令与微周期</b>：微指令是若干微命令的集合，含两个字段——①操作控制字段（微操作码字段）：生成微操作控制信号；<br>②顺序控制字段（下址字段）：确定下一条微指令地址。微周期是执行一条微指令所需的基本时间单位，通常为一个时钟周期。</li>
<li><b>主存储器 vs 控制存储器</b>：主存存放程序和数据，位于 CPU 外部，用 RAM 实现；控制存储器存放微程序，位于 CPU 内部，用 ROM 实现；CM 中每个存储单元的地址称为<b>微地址</b>。</li>
<li><b>程序 vs 微程序</b>：程序是指令的有序集合，由软件开发者在主存/辅存中；微程序是微指令的有序集合，由体系结构设计者编写，固化于控制存储器，是机器指令的硬件级解释逻辑，对程序员完全透明。</li>
<li><b>寄存器</b>：MAR（主存地址）、μPC/CMAR（微地址寄存器）、IR（机器指令）、μIR/CMDR（微指令寄存器）。</li>
</ul>
<p><b>工作过程</b>：①取指公共微程序（入口通常为 CM 的 0 号单元）→ IR</p><p>②根据 IR 操作码经微地址形成部件产生微程序入口地址 → μPC</p><p>③从 CM 逐条读微指令执行</p><p>④执行完自动返回取指微程序入口循环。<b>微程序总数 = 机器指令条数 + 公共微程序数</b>（取指、间址、中断等）。</p>
<p><b>微指令编码方式</b>：</p>
<ul>
<li><b>直接编码</b>：操作控制字段每一位直接对应一个微命令，无须译码，并行能力强、速度快；缺点：n 个微命令需 n 位，字长过长。</li>
<li><b>字段直接编码</b>：将操作控制字段划分为若干互斥段，互斥性微命令归入同一字段、相容性微命令分到不同字段；每字段独立编码经译码激活一个微命令。分段原则：互斥同段、相容异段；字段位数不宜过多；每字段需预留一个编码表示"无操作"（3 位字段最多表示 7 个互斥微命令）。显著缩短字长，但速度略低于直接编码。</li>
<li><b>字段间接编码</b>：某字段含义由另一字段编码共同决定，进一步缩短字长；译码更复杂、时间开销大。</li>
</ul>
<p>例：ALU 操作 4 种 + 存储器操作 4 种——直接编码 8 位</p><p>字段直接编码 2×3=6 位</p><p>字段间接编码仅 3+1=4 位。</p>
<p><b>微指令地址形成</b>：①<b>增量方式</b>（计数器方式）：μPC+1 自动生成，适用于顺序执行段</p><p>②<b>断定方式</b>（下址字段方式）：在当前微指令中显式指定下条微地址。典型情形：微程序入口地址（操作码生成）、顺序执行（μPC+1 或显式下址）、条件分支（下址字段 + 条件信号确定）。</p>
<p><b>水平型 vs 垂直型微指令</b>：水平型——直接/字段直接/字段间接编码均属之，一位（或一字段）对应一个微命令，可同时定义并执行多个并行微操作</p><p>并行能力强、效率高、微程序短，但字长长、编写困难。垂直型——类似机器指令结构，设微操作码字段经译码产生控制信号，一条微指令显式定义一个基本微操作</p><p>字长短、格式规整、编写简单，但微程序长、执行速度慢。</p>
<p><b>表5.2 微程序控制器与硬布线控制器的对比</b>：</p>
<table>
<tr><th rowspan="2">对比项</th><th colspan="2">类别</th></tr>
<tr><th>微程序控制器</th><th>硬布线控制器</th></tr>
<tr><td>工作原理</td><td>微操作控制信号以微程序的形式存放在控制存储器中，执行指令时读出即可</td><td>微操作控制信号由组合逻辑电路根据当前的指令码、状态和时序即时产生</td></tr>
<tr><td>执行速度</td><td>慢</td><td>快</td></tr>
<tr><td>规整性</td><td>较规整</td><td>烦琐、不规整</td></tr>
<tr><td>应用场合</td><td>CISC CPU</td><td>RISC CPU</td></tr>
<tr><td>易扩充性</td><td>易扩充修改</td><td>扩充修改困难</td></tr>
</table>"""
        },
        {
            "section": "5.5 异常和中断机制",
            "html": """<h4>5.5 异常和中断机制</h4>
<h5>5.5.1 异常和中断的基本概念</h5>
<p><b>异常（内中断）</b>：CPU 执行指令过程中由内部检测到的<b>同步事件</b>，如除零、非法操作码、页缺失等</p><p>由当前指令直接引发，具有确定性和可重现性。<b>中断（外中断）</b>：由外部设备（如 I/O 控制器）发起的<b>异步事件</b>，如数据就绪、传输完成</p><p>与当前指令无关，具有随机性。</p>
<p><b>响应时机</b>：指令触发异常时，CPU 通常在其执行过程中或完成时立即响应</p><p>外部中断仅在第 i 条指令<b>完整执行结束后</b>才被检测（指令执行期间不采样中断信号）。响应后保存现场并转移至服务程序</p><p>处理完成后：<b>异常通常重新执行第 i 条指令</b>（如缺页异常调入页面后重试访存），<b>中断从第 i+1 条指令继续执行</b></p><p>不可恢复的致命错误（如非法指令）将终止进程。</p>
<h5>5.5.2 异常和中断的分类</h5>
<p><b>异常分类</b>：硬故障中断（硬件逻辑功能异常：存储器校验错、总线错误）+ 程序性异常（软件中断，因执行指令引起：除零、溢出、断点、单步、非法指令、栈溢出、地址越界、缺页）。按发生原因和返回方式分：</p>
<ul>
<li><b>故障（Fault）</b>：指令启动之后、执行完成之前被检测到，CPU 未提交结果即响应，处理后<b>重新执行同一条指令</b>。典型：非法操作码、缺页、除零。可恢复的（缺页）处理后重执；不可恢复的（非法指令、除零）终止进程。</li>
<li><b>自陷（Trap）</b>：预先安排的主动转入 OS 内核的同步事件（陷阱/陷入），<b>指令正常执行完毕之后才触发</b>，CPU 可完整提交结果；处理后<b>从自陷指令的下一条指令继续执行</b>。典型：断点调试、单步跟踪、系统调用指令（syscall）、条件自陷（teq）。</li>
<li><b>终止（Abort）</b>：指令执行中发生严重硬件故障（控制器出错、存储器校验错、总线错误），系统无法继续运行，程序被迫终止；非特定指令引发、不可预测。</li>
</ul>
<p>故障和自陷属于程序性异常（软件中断）</p><p>终止和外中断属于硬件中断。</p>
<p><b>中断分类</b>：</p>
<ul>
<li><b>可屏蔽中断</b>：经可屏蔽中断请求线 INTR 发送，CPU 可通过中断控制器中的中断屏蔽寄存器选择性屏蔽/允许特定中断源。</li>
<li><b>不可屏蔽中断</b>：经专用请求线 NMI 发送，无法被软件屏蔽，用于高优先级事件（电源掉电、内存校验错）。</li>
</ul>
<p>异常与中断的两个重要差异：①触发时机——异常由当前指令直接引发（同步），中断由外部事件异步触发且不会打断当前指令执行</p><p>②检测机制——异常由 CPU 内部逻辑自动检测或软件显式触发，中断依赖外部硬件经中断请求线通知 CPU。</p>
<p><b>其他分类</b>：按服务程序入口地址获取方式——<b>向量中断</b>（硬件直接提供服务程序入口地址）vs <b>非向量中断</b>（软件轮询确定中断源）</p><p>按是否允许嵌套——<b>单重中断</b>（服务期间禁止新中断）vs <b>多重中断</b>（允许高优先级中断嵌套）。</p>
<h5>5.5.3 异常和中断响应过程</h5>
<p>从事件发生到转移至处理程序的硬件自动操作序列（高度原子性），包括三个阶段：</p>
<ul>
<li><b>关中断</b>：自动屏蔽可屏蔽中断（通常清零标志寄存器 IF 位，IF=0 关中断、IF=1 开中断），确保上下文保存的原子性；不可屏蔽中断和部分高优先级异常仍可能发生。</li>
<li><b>保存断点和程序状态</b>：①断点（返回地址）——异常为当前指令地址、中断为下一条指令地址；<br>②程序状态字 PSW（条件码、中断允许标志、特权级等）。通常压入<b>内核栈</b>，保障安全且天然支持嵌套处理。</li>
<li><b>识别事件类型并转移至处理程序</b>：现代处理器普遍采用<b>硬件向量机制</b>——每类事件分配唯一类型号，系统初始化时将服务程序入口地址（中断向量）按类型号填入中断向量表；事件发生时 CPU 自动获取类型号，以此为索引访问向量表获取目标地址并转移，全程无须软件介入。早期系统采用软件轮询（非向量中断），效率低，现代已极少使用。</li>
</ul>
<p>完整处理是软硬件协同：响应结束后 CPU 执行服务程序第一条指令，后续处理逻辑（缺页页面调入、系统调用分发等）由 OS 内核软件实现。</p>"""
        },
        {
            "section": "5.6 指令流水线",
            "html": """<h4>5.6 指令流水线</h4>
<h5>5.6.1 指令流水线的基本概念</h5>
<p>提升处理器并行性的两条途径：<b>时间上的并行</b>——任务分解为多个子阶段、各阶段由专用功能部件依次处理、多个任务在不同阶段同时推进（流水线技术）</p><p><b>空间上的并行</b>——配置多个相同功能部件并行工作（超标量处理器）。</p>
<p><b>典型五段流水线</b>：取指（IF）→ 译码/读寄存器（ID）→ 执行/计算地址（EX）→ 访存（MEM）→ 写回（WB）。理想情况下（无冒险、无停顿）每个时钟周期有一条指令进入并有一条完成，<b>CPI 趋近于 1</b></p><p>第 k 条指令译码时可启动第 k+1 条取指。</p>
<p><b>流水线对指令集的要求</b>：①指令长度统一（简化取指与译码）</p><p>②指令格式规整（源操作数寄存器字段位置固定，支持译码前预取操作数）</p><p>③采用 LOAD/STORE 架构（仅访存指令访问主存，利于流水段划分）</p><p>④数据与指令按边界对齐存放（单次访存获取完整操作数）。</p>
<h5>5.6.2 流水线的基本实现</h5>
<p><b>设计原则</b>：①流水段数量以最复杂指令所需的功能段数为准</p><p>②每个流水段时长以最耗时的操作为准。例：各段延迟 200/100/150/200/100ps，单周期总时间 750ps</p><p>流水线时钟周期取最大段 200ps，单条指令总延迟 1000ps——<b>流水线不能缩短单条指令的执行延迟</b>，但 N 条指令总耗时从 N×750ps 降为 (N+4)×200ps，吞吐率显著提升。</p>
<p><b>流水段寄存器</b>：每个流水段之后设置一个流水段寄存器（IF/ID、ID/EX、EX/MEM、MEM/WB），锁存该段输出供下一周期使用</p><p>所有寄存器统一时钟同步。满载时最多 5 条指令同时处于不同阶段。<b>流水段寄存器本身引入时延，考试中无明确说明可忽略</b>。</p>
<p><b>吞吐率分析</b>：k 段流水线、时钟周期 Δt，完成 n 个任务总时间 (k+n-1)Δt，<b>吞吐率 TP = n/[(k+n-1)Δt]</b></p><p>n→∞ 时达到理论最大值 1/Δt（启动阶段前 k-1 个周期影响可忽略）。</p>
<h5>5.6.3 MIPS指令集的流水段分析</h5>
<p>所有指令前两个功能段相同：IFetch（取指并计算 PC+4）、Reg/Dec（读寄存器并译码）。后续：</p>
<p><b>表5.3 不同类型指令在各流水段中的操作</b>：</p>
<table>
<tr><th rowspan="2">指令</th><th colspan="5">流水段</th></tr>
<tr><th>IF</th><th>ID</th><th>EX</th><th>MEM</th><th>WB</th></tr>
<tr><td>ALU</td><td>取指</td><td>译码，读寄存器堆</td><td>执行</td><td>一</td><td>结果写回寄存器堆</td></tr>
<tr><td>取/存</td><td>取指</td><td>译码，读寄存器堆</td><td>计算访存有效地址</td><td>访存（读/写）</td><td>将读出的数据写入寄存器堆/一</td></tr>
<tr><td>转移</td><td>取指</td><td>译码，读寄存器堆</td><td>计算转移目的地址，设置条件码</td><td>若条件成立，将转移目的地址送 PC</td><td>一</td></tr>
</table>
<ul>
<li><b>R 型指令</b>：Exec（ALU 运算）→ Write（结果写回 Rd），共 4 段。</li>
<li><b>I 型运算指令</b>：立即数符号/零扩展后与 Rs 运算，结果写回 Rt，分段与 R 型相同。</li>
<li><b>lw 指令</b>：Exec（计算地址 R[Rs]+SEXT(imm16)）→ Mem（读数据）→ Write（写回 Rt），共 5 段（最复杂）。</li>
<li><b>sw 指令</b>：Exec（计算地址）→ Mem（写数据），无 Write。</li>
<li><b>beq 指令</b>：Exec（比较 Rs 与 Rt、计算目标 PC+4+SEXT(imm16)×4）→ Mem（相等则将目标地址写 PC；Mem 段并非真正访存，仅为与 lw/sw 对齐）。</li>
<li><b>j 指令</b>：仅 Exec（计算目标地址并更新 PC）。</li>
</ul>
<p><b>插入空段对齐原则</b>：每条指令对任一功能部件至多使用一次</p><p>相同功能部件必须在固定阶段使用（如寄存器写回总在第 5 阶段）。R/I 型在 Write 前插空 Mem</p><p>beq 后插空 Write</p><p>j 插空 Mem 和 Write。</p>
<h5>5.6.4 流水线的冒险与处理</h5>
<p>三种冒险：</p>
<ul>
<li><b>结构冒险（资源冲突）</b>：不同指令在同一时刻争用同一功能部件。例：指令与数据共享同一存储器时，LOAD 的 MEM 段与后续指令的 IF 段同时访存。解决：①遵循功能部件使用原则（每指令至多使用一次、总在固定阶段使用）；<br>②增加硬件资源（寄存器堆读写口分离支持前半拍写后半拍读；<b>指令 Cache 与数据 Cache 分离</b>从根本上消除取指与数据访存竞争）；也可暂停后续指令取指一个周期（<b>表5.4</b>）。</li>
</ul>
<p><b>表5.4 用暂停后续指令的方法解决访存冲突</b>：</p>
<table>
<tr><th rowspan="2">指令</th><th colspan="9">时钟周期</th></tr>
<tr><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>6</th><th>7</th><th>8</th><th>9</th></tr>
<tr><td>LOAD 指令</td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td><td></td><td></td><td></td><td></td></tr>
<tr><td>指令 i+1</td><td></td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td><td></td><td></td><td></td></tr>
<tr><td>指令 i+2</td><td></td><td></td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td><td></td><td></td></tr>
<tr><td>指令 i+3</td><td></td><td></td><td></td><td>停顿</td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td></tr>
<tr><td>指令 i+4</td><td></td><td></td><td></td><td></td><td></td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td></tr>
</table>
<ul>
<li><b>数据冒险（数据相关）</b>：后面指令用到前面指令的结果时结果还未产生或写回。按序发射、按序完成的流水线（统考常考）只可能出现 <b>RAW（写后读）冲突</b>——前一条指令写结果之前后面指令就要读取（如 add 写 R1 在 WB 段，sub 在 ID 段就读 R1）。解决：</li>
</ul>
<p><b>表5.5 add和sub指令发生写后读（RAW）冲突</b>：</p>
<table>
<tr><th rowspan="2">指令</th><th colspan="6">时钟周期</th></tr>
<tr><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>6</th></tr>
<tr><td>add</td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td><td></td></tr>
<tr><td>sub</td><td></td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td></tr>
</table>
<ol>
<li><b>延迟执行相关指令</b>：软件插入 nop 或硬件自动插入气泡（阻塞），将 sub 延迟 3 个周期使其 ID 段在 add 的 WB 段之后；若寄存器堆支持前半个周期写、后半个周期读，可只延迟 2 个周期。</li>
</ol>
<p><b>表5.6 用延迟相关指令的办法来解决 RAW 冲突</b>：</p>
<table>
<tr><th rowspan="2">指令</th><th colspan="9">时钟周期</th></tr>
<tr><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>6</th><th>7</th><th>8</th><th>9</th></tr>
<tr><td>add</td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td><td></td><td></td><td></td><td></td></tr>
<tr><td>sub</td><td></td><td>阻塞</td><td>阻塞</td><td>阻塞</td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td></tr>
</table>
<ol start="2">
<li><b>转发（旁路）技术</b>：设置相关转发通路，将 EX 段结束时的中间结果直接转发至 ALU 输入端（add 结果暂存于 EX/MEM 流水段寄存器，sub 进入 EX 段时直接取用）。相邻运算类指令间、相隔一条无关指令的运算类指令间的 RAW 冲突均可消除。</li>
</ol>
<p><b>表5.7 用转发技术来解决 RAW 冲突</b>：</p>
<table>
<tr><th rowspan="2">指令</th><th colspan="6">时钟周期</th></tr>
<tr><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>6</th></tr>
<tr><td>add</td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td><td></td></tr>
<tr><td>sub</td><td></td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td></tr>
</table>
<ol start="3">
<li><b>load-use 数据冒险</b>：load 指令在 MEM 段结束时才读出数据，其后的运算指令无法用转发解决，需插入 1 个 nop（或硬件阻塞）使 add 的 EX 段与 load 的 MEM 段错开，再从 MEM/WB 寄存器转发；最好由编译器调整指令顺序避免。</li>
</ol>
<p><b>表5.8 用延迟加转发技术来解决 load-use 冲突</b>：</p>
<table>
<tr><th rowspan="2">指令</th><th colspan="7">时钟周期</th></tr>
<tr><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>6</th><th>7</th></tr>
<tr><td>load</td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td><td></td><td></td></tr>
<tr><td>add</td><td></td><td>阻塞</td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td></tr>
</table>
<ul>
<li><b>控制冒险（控制冲突）</b>：转移、返回、中断或异常导致 PC 被修改、流水线断流。分支指令从进入流水线到确定是否转移产生延迟损失时间片 C（bne 在 EX 段完成条件计算、MEM 段结束才确定是否更新 PC → C=3）。解决：①<b>延迟分支处理</b>——分支指令后插入 C 条 nop 或硬件自动阻塞；<br>②<b>分支预测</b>——静态预测（总是预测转移/不转移）和动态预测（根据转移历史调整，准确率更高）；预测错误需清空错误路径指令并从正确目标重新取指（分支延迟周期数为 3 时损失 3 个周期）。<b>Cache 缺失、中断或异常也会引起流水线阻塞</b>。</li>
</ul>
<p><b>表5.9 用插入空操作的办法解决控制冲突</b>：</p>
<table>
<tr><th rowspan="2">指令</th><th colspan="10">时钟周期</th></tr>
<tr><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>6</th><th>7</th><th>8</th><th>9</th><th>10</th></tr>
<tr><td>add1</td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td><td></td><td></td><td></td><td></td><td></td></tr>
<tr><td>bne</td><td></td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td><td></td><td></td><td></td><td></td></tr>
<tr><td>add2</td><td></td><td></td><td></td><td></td><td></td><td>IF</td><td>ID</td><td>EX</td><td>MEM</td><td>WB</td></tr>
</table>
<h5>5.6.5 高级流水线技术</h5>
<p>提升指令级并行的两种策略：<b>多发射技术</b>（配置多个内部功能部件，每周期同时处理多条指令）、<b>超流水线技术</b>（增加流水线级数使更多指令重叠执行）。</p>
<ul>
<li><b>超标量流水线（动态多发射）</b>：每个时钟周期可并发发射多条独立指令，需配置多个功能部件；简单超标量按序发射，多数现代超标量结合动态调度（动态分支预测）支持<b>乱序执行</b>。</li>
<li><b>超长指令字 VLIW（静态多发射）</b>：由<b>编译器</b>挖掘指令间并行性，将多条可并行指令打包成一条超长指令字（含多个操作码字段分别控制不同处理部件）；并行性由软件静态确定，控制简单。</li>
<li><b>超流水线</b>：进一步细分流水段缩短时钟周期，提高主频和吞吐率；但流水段寄存器开销和控制复杂度增加，深度并非越多越好。</li>
</ul>
<p>理想情况下：超流水线充满后每周期完成一条指令 <b>CPI=1</b>（主频更高）</p><p>多发射每周期完成多条指令 <b>CPI&lt;1</b>（硬件成本更高、控制更复杂）。</p>
<p><b>易错点</b>：流水段越多并不一定越快——流水段寄存器延迟限制时钟周期缩短、单条指令所需周期数增加、冒险停顿损失更显著、控制逻辑急剧复杂化。</p>"""
        },
        {
            "section": "5.7 多处理器的基本概念",
            "html": """<h4>5.7 多处理器的基本概念</h4>
<p>根据指令流和数据流的数量（Flynn 分类法），计算机体系结构分为 SISD、SIMD、MISD、MIMD 四类：常规单处理器属 SISD，常规多处理器属 MIMD。</p>
<h5>5.7.1 SISD、SIMD、MIMD的基本概念</h5>
<ul>
<li><b>SISD（单指令流单数据流）</b>：传统串行计算机结构，一个处理器一个存储器，任一时刻仅执行一条指令；部分 SISD 采用指令流水线、多模块交叉存储器提升性能。</li>
<li><b>SIMD（单指令流多数据流）</b>：数据级并行，一个指令控制单元 + 多个处理单元，所有处理单元同时执行同一条指令但各自操作不同数据（一条 SIMD 指令可在 16 个 ALU 中并行运算 16 对数据）。处理含条件分支的代码时效率显著降低。<b>向量处理器</b>是 SIMD 的典型实现：专用指令直接操作一维数组（向量），数据加载到向量寄存器、流水化批量处理，在数值模拟等规则计算中优势显著。</li>
<li><b>MISD（多指令流单数据流）</b>：理论上存在，通用计算机中极为罕见，几乎无实际应用。</li>
<li><b>MIMD（多指令流多数据流）</b>：主流并行计算模型，分两类——①<b>多计算机系统</b>：每节点有私有存储器和独立地址空间，无法直接访存其他节点内存，需消息传递通信（消息传递型 MIMD）；<br>②<b>多处理器系统</b>：通常指共享存储多处理器系统，统一全局地址空间，所有处理器可经普通访存指令访问任意存储单元（共享存储型 MIMD）。</li>
</ul>
<p>SIMD 属数据级并行，适用于规则、同构的数据处理</p><p>MIMD 支持线程级或任务级并行，并行程度更高、适用范围更广。</p>
<h5>5.7.2 硬件多线程的基本概念</h5>
<p>传统 CPU 线程切换需保存/恢复寄存器上下文，开销显著。硬件多线程为每个线程配备<b>独立的通用寄存器组、PC 等关键状态部件</b>，切换时只需激活对应线程的寄存器组，无须将上下文写入或读出内存。三种实现方式：</p>
<ul>
<li><b>细粒度多线程</b>：每个时钟周期切换线程，不同线程指令在连续周期交错执行（周期 i 执行线程 A、周期 i+1 执行线程 B），提升功能部件利用率。</li>
<li><b>粗粒度多线程</b>：连续执行同一线程指令序列，仅当该线程因高延迟事件（如 Cache 缺失）导致流水线阻塞时才切换；需清空被阻塞流水线并重新填充，切换开销通常大于细粒度。</li>
<li><b>同时多线程（SMT）</b>：单个时钟周期内同时发射并执行来自多个线程的多条指令，既利用指令级并行又实现线程级并行；通常构建于支持超标量和乱序执行的现代微架构上，共享执行单元、缓存等资源。Intel 的<b>超线程（Hyper-Threading）</b>是 SMT 典型代表：一个物理核心维护两套完整线程状态（寄存器组、PC 等），缓存、ALU 等执行资源由两个逻辑核心共享，单核实现接近双核的并发能力（实际提升 10%~30%）。</li>
</ul>
<p>细粒度和粗粒度在同一时刻仅执行一个线程的指令，核心目标是通过线程切换隐藏长延迟操作、提升资源利用率。</p>
<h5>5.7.3 多核处理器的基本概念</h5>
<p><b>多核处理器（片上多处理器）</b>：将多个独立处理单元（核 core）集成到单个 CPU 芯片。每个核通常配备私有 L1 Cache（有时含 L2），多个核共享更高层级 Cache（如 L3），所有核通过互连网络共享主存储器。</p>
<p><b>要点</b>：要发挥多核并行能力必须采用多线程（多进程）模型，确保每个核运行时均有可执行线程</p><p>多核架构支持多线程在物理上同时执行（真正并行），单核系统多线程以时间交错方式执行（任一时刻仅一个线程运行，除非支持 SMT）。多核 CPU 各核心可各有 Cache 也可共享 Cache</p><p>双核是指两个 CPU 核心集成到一个封装（主板两个 CPU 属于多处理器，超线程是模拟双核）。单线程程序在多核上不会加速；多核属 MIMD。</p>
<h5>5.7.4 共享内存多处理器的基本概念</h5>
<p><b>SMP（共享内存多处理器）</b>：共享单一物理地址空间的多处理器系统，所有 CPU 均可通过常规访存指令访问内存任意位置，并通过读/写共享变量通信。各 CPU 仍可在各自独立的虚拟地址空间中运行程序，OS 负责将虚拟地址映射到统一物理地址空间。按内存访问延迟分两类：</p>
<ul>
<li><b>UMA（统一存储访问）</b>：所有 CPU 访问任意内存单元延迟基本相同，与请求 CPU 及目标地址无关。</li>
<li><b>NUMA（非统一存储访问）</b>：延迟取决于请求 CPU 与目标内存的物理位置关系。早期北桥架构中内存控制器集成于北桥，CPU 经前端总线（FSB）访问内存，多 CPU 争用使 FSB 成为瓶颈；NUMA 将内存控制器集成到各 CPU 内部，每个 CPU 直接连接一部分物理内存（本地内存），经高速互连总线（如 Intel QPI）互连可访问远程内存。访问本地内存延迟显著低于远程，程序性能对数据布局敏感。</li>
</ul>
<p><b>同步机制</b>：多个 CPU 可能同时访问同一共享变量，必须引入同步机制确保原子性与数据一致性，常用方法是对共享变量使用<b>互斥锁</b>（任一时刻仅一个 CPU 持有，其余等待释放）。</p>
<p><b>Cache 一致性</b>：单处理器系统关注 Cache 与主存的数据同步</p><p>SMP 中多个 CPU 的 Cache 可能同时缓存同一物理地址的副本，一致性要求更严格——任意时刻所有 CPU 对该地址的 Cache 副本必须保持一致，由硬件一致性协议保障（及时传播写操作或无效化其他 CPU 中的副本）。</p>"""
        }
    ]
}
