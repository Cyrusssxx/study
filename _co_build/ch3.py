# -*- coding: utf-8 -*-
"""计组笔记 · 第3章 存储系统（王道2027结构，合并袁书补充）"""

CH3 = {
    "chapter": "第3章",
    "title": "第3章 存储系统",
    "sections": [
        {
            "section": "3.1 存储器概述",
            "html": """<h4>3.1 存储器概述</h4>
<h5>3.1.1 存储器的分类</h5>
<p><b>按存储元件分类</b>：半导体器件（电荷/电压状态，构成 DRAM、SRAM）</p><p>磁性材料（磁化方向，磁表面存储器如硬盘、磁带）</p><p>光介质（反射率/相变特性，光盘）。</p>
<p><b>按存取方式分类</b>：</p>
<ul>
<li><b>随机存取（RAM）</b>：可对任意单元读/写，存取时间与物理位置无关（半导体存储器，用于主存/Cache）。</li>
<li><b>顺序存取</b>：按顺序存放和读出，存取时间取决于位置，以记录块为单位编址（磁带，容量大速度慢）。</li>
<li><b>直接存取</b>：先直接定位到目标区域再按顺序读取（传统机械磁盘）。</li>
<li><b>相联存取</b>：按内容而非地址存取，查找快、与位置无关，但成本高容量小（快表 TLB、路由表）。</li>
</ul>
<p><b>按信息的可更改性</b>：可读可写（RAM）与只读（ROM）</p><p>E²PROM、Flash 支持特定条件下重写。<b>按信息的可保存性</b>：<b>易失性</b>（RAM 断电丢失）与<b>非易失性</b>（ROM、Flash、磁盘、光盘）。<b>破坏性读出</b>：读出破坏原信息需立即再生（DRAM）</p><p><b>非破坏性读出</b>：无须再生（SRAM、磁盘）。</p>
<h5>3.1.2 主存储器的组成和基本操作</h5>
<p>主存由<b>存储体</b>（存储矩阵，由记忆单元/存储元组成）、地址译码器、读/写控制电路、地址寄存器和数据寄存器（MDR）组成。现代计算机普遍<b>字节编址</b>（每地址对应 1 字节）。</p>
<p><b>寻址过程</b>：CPU 将地址送 MAR → 经地址线到主存地址寄存器 → 译码选中存储单元 → 控制线发读/写信号 → 数据经数据线在 MDR 与主存间传输。要点：</p>
<ul>
<li><b>MAR 位数 = 地址线位数</b>，决定最大可寻址范围（36 位地址 → 2³⁶B = 64GB）。</li>
<li><b>MDR 位数 = 数据线宽度</b>，数据线 64 位时每次可并行存取 8 字节。</li>
<li>主存三大总线：地址线（单向，CPU 输出）、数据线（双向）、控制线（读/写命令）。</li>
</ul>
<h5>3.1.3 存储器的层次化结构</h5>
<p><b>多级存储结构</b>：寄存器 → Cache → 主存 → 磁盘 → 磁带/光盘。自上而下：速度变慢、容量增大、单位价格降低、CPU 访问频率降低。核心是两个层次：</p>
<ul>
<li><b>Cache-主存层</b>：缓解 <b>CPU 与主存速度不匹配</b>；数据调度由<b>硬件</b>自动完成，对所有程序员透明。</li>
<li><b>主存-辅存层</b>：解决<b>存储容量不足</b>；调度由<b>硬件与操作系统</b>协同完成，对应用程序员透明；发展形成<b>虚拟存储系统</b>。</li>
<li>上一层内容始终是下一层内容的<b>子集副本</b>（Cache 数据来自主存、主存数据来自辅存）。</li>
<li>主存是枢纽：能与 CPU、Cache、辅存双向交换信息。</li>
</ul>
<h5>3.1.4 存储器的主要性能指标</h5>
<ul>
<li><b>存取时间</b>：完成一次读/写所需时间（读出时间=主存收到有效地址到数据有效输出；写入时间=收到地址到数据写入）。</li>
<li><b>存储周期</b>：连续两次独立读/写所需的最小时间间隔。<b>存储周期 &gt; 存取时间</b>（需恢复内部状态）；DRAM 破坏性读出需再生，存储周期可达存取时间的 2 倍。</li>
<li><b>存储器带宽</b>：每秒可传输的最大数据量（如周期 50ns、每周期传 64 位 → 64b/50ns = 1.28Gb/s）；多模块并行可将总带宽提升数倍。</li>
</ul>"""
        },
        {
            "section": "3.2 主存储器",
            "html": """<h4>3.2 主存储器</h4>
<h5>3.2.1 半导体随机存取存储器</h5>
<p><b>SRAM</b>：存储元基于<b>双稳态触发器</b>（六晶体管 MOS），读操作为<b>非破坏性读出</b>、无须再生</p><p>速度快、集成度低、功耗大、成本高 → 用于<b>Cache</b>。</p>
<p><b>DRAM</b>：利用<b>栅极电容上的电荷</b>存储信息（有电荷=1、无电荷=0），存储元仅 1 管 1 电容，集成度远高于 SRAM</p><p>位价低、功耗小、容量大，但速度慢、<b>电荷漏电需定时刷新</b>、<b>破坏性读出需再生</b> → 用于<b>主存</b>。</p>
<p><b>存储芯片组成</b>：存储体（行选择线 X + 列选择线 Y 交叉选中）、地址译码器（<b>单译码法</b>一维译码输出线过多</p><p><b>双译码法</b> X/Y 二维译码，当前 DRAM 普遍采用）、I/O 读/写电路（放大信号）、<b>片选线 CS</b>（多芯片扩展时选中目标芯片）、读/写控制线 WE。</p>
<p><b>DRAM 关键技术</b>：</p>
<ul>
<li><b>地址引脚复用</b>：行地址与列地址经<b>相同引脚</b>分两次输入（RAS 行选通、CAS 列选通），地址引脚数<b>减少一半</b>。4M×4 位芯片 11 根地址引脚（2¹¹×2¹¹×4 = 4M×4）。</li>
<li><b>刷新机制</b>：仅提供行地址 + RAS 即可刷新整行（无须列地址）；刷新按行进行，2048×2048 阵列需 2048 次刷新；刷新计数器自动产生行地址；<b>刷新周期</b>=对某行两次刷新之间的时间间隔。</li>
<li><b>阵列设计</b>：容量 2ⁿ×b 位、行 r 列 c（r×c=2ⁿ）；引脚数由行列地址较大者决定 → 应使 r≈c；按行刷新 → 行数越少刷新开销越低（r≤c）；取行数略小于列数的近似正方形结构。</li>
<li><b>行缓冲器与突发传输</b>：行缓冲器容量 = 列数 × 每单元位数（如 4 列×8 位=32 位）；选中行全部数据一次载入行缓冲器，后续每时钟周期连续输出一个单元 → 突发传输显著提升带宽。</li>
<li><b>SDRAM（同步 DRAM）</b>：与系统时钟同步，连续访问同一行（页）数据可突发传输、避免等待状态；异步 DRAM 需 CPU 轮询等待不确定延迟。</li>
</ul>
<p><b>表3.1 SRAM 和 DRAM 各自的特点</b>：</p>
<table>
<tr><th rowspan="2">特点</th><th colspan="2">类型</th></tr>
<tr><th>SRAM</th><th>DRAM</th></tr>
<tr><td>存储信息</td><td>触发器</td><td>电容</td></tr>
<tr><td>破坏性读出</td><td>非</td><td>是</td></tr>
<tr><td>需要刷新</td><td>不需要</td><td>需要</td></tr>
<tr><td>送行列地址</td><td>同时送</td><td>分两次送（复用）</td></tr>
<tr><td>运行速度</td><td>快</td><td>慢</td></tr>
<tr><td>集成度</td><td>低</td><td>高</td></tr>
<tr><td>存储成本</td><td>高</td><td>低</td></tr>
<tr><td>主要用途</td><td>高速缓存</td><td>主机内存</td></tr>
</table>
<h5>3.2.2 非易失性存储器</h5>
<p><b>ROM</b>：与 RAM 均支持随机访问</p><p>ROM 非易失、结构简单位密度高、断电数据不丢。类型：MROM（掩模式，厂商固化）、PROM（一次可编程）、EPROM（紫外线整片擦除、可多次编程但慢，无法替代 RAM）。</p>
<p><b>Flash 存储器</b>：由 EPROM 发展而来，断电信息长期保存</p><p>支持<b>电擦除与在线重写</b></p><p>读取速度接近 RAM、写入明显较慢（读/写性能不对称）</p><p>用于 BIOS（现代主板）、U 盘、SSD。</p>
<p><b>固态硬盘（SSD）</b>：基于 Flash 构建，控制单元 + 存储单元（Flash 芯片阵列）</p><p>非易失、无机械部件、读写快、功耗低、抗震强</p><p>缺点：价格较高、写入寿命受擦写次数限制。</p>
<h5>3.2.3 多模块存储器</h5>
<p><b>目的</b>：空间并行技术，通过多个相同存储模块并行工作提高吞吐率。两种编址方式：</p>
<ul>
<li><b>连续编址（高位交叉）</b>：高位地址为体号、低位为体内地址。连续主存块集中在一个模块内，各模块<b>不能并行访问</b>，无法提高吞吐率（本质仍是顺序存取）。</li>
<li><b>交叉编址（低位交叉）</b>：低位地址为体号（模块号 = 地址 mod m）、高位为体内地址；连续地址分布到不同模块，可并行 → <b>交叉存储器</b>。</li>
</ul>
<p><b>轮流启动方式</b>：模块数 m = 存取周期 T ÷ 总线周期 r（取整）</p><p>每隔 1/m 存取周期启动一个模块，连续存取 m 个字时间 = <b>T + (m-1)r</b>（顺序方式为 mT），速度提高近 m 倍。<b>访存冲突</b>：相邻 m 次访问落在同一模块则冲突，需延迟处理（如 4 体交叉中地址 17 和 9 冲突）。</p>
<p><b>同时启动方式</b>：各模块并行读/写总位数 = 数据总线宽度（8 个 16M×8 位芯片同时提供 64 位数据匹配 64 位总线）</p><p>并行访问以连续 8 字节为单位，起始地址未对齐（横跨两个访问块）需两次访问——<b>这是内存访问要求数据对齐的根本原因</b>。</p>"""
        },
        {
            "section": "3.3 主存储器与CPU的连接",
            "html": """<h4>3.3 主存储器与CPU的连接</h4>
<h5>3.3.1 连接原理</h5>
<p>主存通过<b>数据总线</b>（位数 × 工作频率决定传输速率）、<b>地址总线</b>（位数决定可寻址最大空间）、<b>控制总线</b>与 CPU 相连。控制线因芯片而异：SRAM 含片选 + 读/写控制线；ROM 仅需片选线</p><p>DRAM 一般不设独立片选线（由行/列选通或外部译码实现）。</p>
<h5>3.3.2 主存容量的扩展</h5>
<p><b>1. 位扩展法</b>（增加字长）：多片<b>并联</b>，地址线、片选线、读/写控制线并联</p><p>数据线分别接到数据总线的不同位</p><p><b>所有芯片同时工作</b>共同提供一个完整字。例：8 片 8K×1 位 → 8K×8 位。</p>
<p><b>2. 字扩展法</b>（增加字数）：地址线接系统地址总线<b>低位</b></p><p>数据线、读/写控制线并联</p><p>地址总线<b>高位经译码器生成片选信号</b>分时选中不同芯片（同一时刻只选一片）。例：4 片 16K×8 位 → 64K×8 位，A₁₅A₁₄=00/01/10/11 分别选中 1~4 号芯片。<b>芯片地址范围</b>：由高位片选信号确定，各片连续分配（0000H~3FFFH、4000H~7FFFH...）。</p>
<p><b>3. 字位同时扩展</b>：先位扩展成组（满足字长），再对组进行字扩展。例：8 片 16K×4 位 → 64K×8 位：每 2 片位扩展为 16K×8 位一组，共 4 组字扩展，A₁₅A₁₄ 译码选中各组。</p>
<p><b>扩展题计算要点</b>：所需芯片数 = 目标容量 ÷ 单片容量（按位计算统一换算）</p><p>地址线根数 = log₂(字数)</p><p>片选线根数 = log₂(组数)</p><p>芯片内地址线 = log₂(芯片字数)。</p>"""
        },
        {
            "section": "3.4 外部存储器",
            "html": """<h4>3.4 外部存储器</h4>
<h5>3.4.1 磁盘存储器</h5>
<p><b>磁盘存储器的组成</b>：磁盘驱动器（驱动盘片旋转 + 磁头读写）、磁盘控制器（驱动器与主机的接口，解析 CPU 命令、监控状态）、盘片。</p>
<p><b>存储区域</b>：盘片由多个记录面组成（记录面数 = 磁头数），每面含若干同心<b>磁道</b>，每条磁道划分若干<b>扇区</b>（扇区是磁盘读/写的最小单位）</p><p>所有记录面上相同编号的磁道构成一个<b>柱面</b>（柱面数 = 单面磁道数）。磁道、扇区间有间隙</p><p>扇区按固定圆心角度划分 → 从外到内位密度逐渐增加，容量受限于最内圈最大记录密度。</p>
<p><b>磁盘高速缓存（Disk Cache）</b>：在内存开辟空间暂存待写数据</p><p>磁盘以"簇"（若干连续扇区）为单位写操作，缓存减少频繁小块写入</p><p>中间结果若再次使用可直接读缓存。</p>
<p><b>性能指标</b>：</p>
<ul>
<li><b>记录密度</b>：道密度（半径方向单位长度磁道数）、位密度（单磁道单位长度二进制位数）、面密度 = 位密度 × 道密度。</li>
<li><b>容量</b>：非格式化容量 = 记录面数×柱面数×每磁道磁化单元数；格式化容量 = 记录面数×柱面数×每道扇区数×每扇区字节数。<b>非格式化 &gt; 格式化</b>（有扇区间隙等格式开销）。</li>
<li><b>存取时间</b>：响应时间 = 排队延迟 + 控制器时间 + <b>寻道时间</b> + <b>旋转等待时间</b> + <b>数据传输时间</b>；后三者合称存取时间。平均寻道时间 ≈ 最大寻道时间一半；<b>平均旋转等待时间 = 磁盘旋转半周时间</b>；数据传输时间取决于转速和密度。例：6000 转/分 → 转一周 10ms → 半周 5ms；12 扇区/道 → 扫过一个扇区 10/12 ≈ 0.83ms。</li>
<li><b>最大数据传输速率</b> = 转速 r × 单磁道容量 N。</li>
</ul>
<p><b>磁盘地址</b>：柱面（磁道）号 + 盘面（磁头）号 + 扇区号。16 盘面、256 磁道/面、16 扇区/道 → 地址 16 位：柱面号 8 位 + 盘面号 4 位 + 扇区号 4 位。磁盘操作（寻址、读盘、写盘）为<b>串行执行</b>（机械部件）。</p>
<p><b>RAID（独立冗余磁盘阵列）</b>：多物理盘组合成逻辑盘，数据交叉分割并行访问。分级：RAID0 无冗余无校验（条带化，速度快但无容错）</p><p>RAID1 <b>镜像</b>（两盘互为备份，有效容量减半）；RAID2 海明码纠错</p><p>RAID3 位交叉奇偶校验</p><p>RAID4 块交叉奇偶校验</p><p>RAID5 分布式奇偶校验（无独立校验盘）。</p>
<h5>3.4.2 固态硬盘（SSD）</h5>
<p><b>结构</b>：闪存芯片 + 闪存翻译层（将逻辑块请求转换为物理闪存读/写控制信号，相当于磁盘控制器）。一个闪存芯片由 B 个块组成，每块含 P 页（页 512B~4KB，每块 32~128 页，块 16KB~512KB）。</p>
<p><b>读写特性</b>：<b>读/写以页为单位，擦除以块为单位</b>（整块擦除后才能写入其中的页）</p><p>每块擦写次数有限，磨损后失效。随机写入慢的原因：①擦除操作耗时约比页访问慢一个数量级</p><p>②修改已含有效数据的页时必须先复制该块所有有效页到新块再写入。</p>
<p><b>磨损均衡</b>：SSD 擦写寿命仅几百至几千次，读写集中在少数块会迅速磨损报废。两类技术：<b>动态磨损均衡</b>（写数据时优先选擦写次数少的空闲块）</p><p><b>静态磨损均衡</b>（无写入时控制器定期扫描迁移数据，高磨损块转为以读为主）。256GB、500 次擦写寿命 → 理论总写入量 125TB。</p>"""
        },
        {
            "section": "3.5 高速缓冲存储器",
            "html": """<h4>3.5 高速缓冲存储器</h4>
<h5>3.5.1 程序访问的局部性原理</h5>
<p>Cache 的设计基础：</p>
<ul>
<li><b>时间局部性</b>：某指令/数据当前被访问，不久的将来很可能再次被访问（循环、重复调用子程序、同一数据多次操作）。</li>
<li><b>空间局部性</b>：某单元被访问，其邻近单元很可能也被访问（指令顺序存放顺序执行、数组连续存储）。</li>
</ul>
<p><b>经典考点</b>：</p>
<ul>
<li>按行访问数组（与存放顺序一致）空间局部性好；按列访问每次跨越整行（如 8192 字节），若交换单位小于行大小则空间局部性极差。</li>
<li>数组元素每个仅访问一次 → 时间局部性差；循环体指令反复执行 → 时间局部性好。</li>
</ul>
<h5>3.5.2 Cache的基本工作原理</h5>
<p>Cache 与主存划分为<b>大小相等的块</b>（Cache 块 = Cache 行，块长/行长）</p><p>Cache 容量远小于主存，只保存最活跃的若干块副本。<b>访问流程</b>：先查 Cache，命中直接从 Cache 读</p><p>缺失则从主存将所在主存块整体调入 Cache（满则替换），再从中取数据。整个过程<b>完全由硬件实现、对程序员透明</b>（须在单条指令周期内完成）。</p>
<p><b>命中率与平均访问时间</b>：命中率 H = 命中次数 ÷ 总访问次数</p><p>平均访问时间 T = H·T_c + (1-H)·(T_m + T_c) = <b>T_c + (1-H)·T_m</b>（T_c 命中时间，T_m 缺失损失=从主存调入一块的时间）。例：Cache 为主存 5 倍速、命中率 95% → T = 0.95t + 0.05×6t = 1.25t，性能提升 5t/1.25t = <b>4 倍</b>。</p>
<p><b>实现需解决四大问题</b>：数据查找（快速判断是否命中）、地址映射（主存块装入何处）、替换策略（满时换哪行）、写入策略（保持一致性）。</p>
<h5>3.5.3 Cache和主存的映射方式</h5>
<p>每行设<b>标记位</b>（记录对应主存块号）+ <b>有效位</b>（指示数据是否有效，启动/复位时全无效）。三种映射：</p>
<p><b>1. 直接映射</b>：每块只能装入 Cache 唯一位置，<b>Cache 行号 = 主存块号 mod Cache 总行数</b>。地址结构：标记 | Cache 行号 | 块内地址（标记位数 t = 主存块号位数 m - 行号位数 c）。优点：实现简单、只需 1 个比较器、判断最快</p><p>缺点：块冲突概率最高、空间利用率最低、即使其他行空闲也不能用。</p>
<p><b>2. 全相联映射</b>：每块可装入任何位置。地址结构：标记 | 块内地址（标记 = 完整主存块号）。访存需与<b>所有行</b>的标记比较（每个 Cache 行一个比较器 → 相联存储器、按内容访问）。优点：冲突概率最低、空间利用率高、命中率高</p><p>缺点：比较速度慢、硬件开销大，不适合大容量 Cache。</p>
<p><b>3. 组相联映射</b>：Cache 分 Q 个组，<b>组间直接映射、组内全相联</b></p><p><b>Cache 组号 = 主存块号 mod Q</b></p><p>每组 r 行称 r 路组相联。地址结构：标记 | 组号 | 块内地址。Q=1 退化为全相联，Q=总行数退化为直接映射。路数 r 越大冲突越低、比较器越多（<b>r 路需 r 个比较器</b>）</p><p>访存时先按组号定位、再组内并行比较标记。</p>
<p><b>三方式对比</b>（容量、块大小固定）：命中率 全相联 &gt; 组相联 &gt; 直接</p><p>判断开销与时间 直接最小最快、全相联最大最慢</p><p>标记存储开销 全相联最多、直接最少。</p>
<h5>3.5.4 Cache中主存块的替换算法</h5>
<p>仅全相联/组相联需要替换算法（直接映射唯一行直接覆盖）。常用算法：</p>
<ul>
<li><b>随机（RAND）</b>：随机选一行，未利用局部性，命中率低。</li>
<li><b>先进先出（FIFO）</b>：替换最早装入的行，未考虑局部性，命中率不高。</li>
<li><b>最近最少使用（LRU）</b>：替换最近最久未访问的行，平均命中率高于 FIFO，考查重点。<b>LRU 位位数</b>：2 路 1 位、4 路 2 位（= log₂路数）。计数器更新：命中→所命中行清零、比它低的加 1；装入新行→新行置 0、其他非空闲全加 1；替换→替换计数值最大行、新行置 0、其余全加 1。访问序列循环超过组行数（如 5 个块访问 4 行组）→ <b>抖动</b>（命中率趋 0）。</li>
<li><b>最不经常使用（LFU）</b>：替换累计访问次数最少的行（新行计 0、每访问 +1）。LRU 关注"最近是否用过"，LFU 关注"总共用了多少次"。</li>
</ul>
<h5>3.5.5 Cache的一致性问题</h5>
<p><b>写命中策略</b>：</p>
<ul>
<li><b>全写法（直写，Write Through）</b>：数据同时写入 Cache 和主存；主存始终同步、替换时无须写回（<b>无须脏位</b>）；实现简单但每次写都访主存，性能低。改进：增设<b>写缓冲</b>（Write Buffer）异步写主存，缓解速度差异，但高频写时可能饱和溢出。</li>
<li><b>回写法（Write Back）</b>：仅写 Cache，被替换时才写回主存；减少主存访问、效率高，但有不一致风险；每行设<b>脏位（修改位）</b>：为 1 表示已修改、替换时必须写回；为 0 可直接覆盖。<b>回写必须设置脏位</b>。</li>
</ul>
<p><b>写不命中策略</b>：<b>写分配法</b>（先将主存块调入 Cache 再更新，利用空间局部性）与<b>非写分配法</b>（直接写主存、不调入 Cache）。典型组合：写回法常配写分配法，直写法常配非写分配法。</p>
<h5>3.5.6 Cache容量的计算</h5>
<p><b>Cache 总容量 = (每行数据位数 + 每行标记信息位数) × 总行数</b>。标记信息 = 有效位(1) + 标记位 + 脏位(仅回写) + LRU 位(仅 LRU 算法，log₂组内行数)。<b>例</b>：256MB 主存（28 位地址）、8 行 Cache、行长 64B（块内 6 位）、直接映射（行号 3 位）→ 标记 28-6-3=19 位</p><p>总容量 = 8×(512+1+19) = 4256 位。地址 3200 → 块号 50 → 直接映射行号 50 mod 8 = 2</p><p>2 路组相联组号 50 mod 4 = 2、行号 4 或 5。</p>
<h5>3.5.7 Cache的应用</h5>
<ul>
<li><b>分离 Cache</b>：指令 Cache 与数据 Cache 分开，消除取指/执行部件同时访问统一 Cache 的冲突，并针对指令与数据的局部性特征分别优化。</li>
<li><b>多级 Cache</b>：L1 离 CPU 最近（最快、最小），L2 较远（较慢、较大）；L1 常分离设计，数据 Cache 采用<b>写分配 + 回写</b>策略；L1、L2 均回写，L1 脏块替换时写回 L2、L2 替换时写回主存，避免写缓冲饱和。</li>
</ul>"""
        },
        {
            "section": "3.6 虚拟存储器",
            "html": """<h4>3.6 虚拟存储器</h4>
<h5>3.6.1 虚拟存储器的基本概念</h5>
<p>由<b>硬件与系统软件协同</b>实现的存储管理机制，利用主存 + 辅存构建逻辑上连续、容量巨大的地址空间，对应用程序员透明。用户程序使用的地址称<b>虚地址（逻辑地址）</b>，实际主存单元地址称<b>实地址（物理地址）</b></p><p>虚地址空间远大于实地址空间。</p>
<p><b>访问流程</b>：CPU 用虚地址访问 → 判断数据是否驻留主存：已驻留 → 地址变换为实地址直接访问</p><p>未驻留 → 触发<b>缺页（缺段）异常</b>，OS 将整个页/段从辅存调入主存，主存满则按替换算法置换。</p>
<p><b>虚拟存储器只能采用回写法</b>：辅存访问延迟极高，每次写都同步更新辅存不可行</p><p>页面修改时标记<b>脏页</b>，仅置换出主存时若为脏页才写回辅存。分页机制允许任一虚页装入任意可用物理页框（类似全相联映射），提高主存利用率。</p>
<h5>3.6.2 页式虚拟存储器</h5>
<p>以页为基本单位：主存中的页称<b>物理页（页框）</b>，虚地址空间中的页称<b>虚拟页</b>。<b>页表</b>记录每个虚页在主存中的映射位置，常驻内存。页表项字段：</p>
<ul>
<li><b>有效位（装入位）</b>：为 1 表示已调入主存、存放物理页号；为 0 表示未调入、存放外存地址。</li>
<li><b>脏位（修改位）</b>：页面是否被修改过，置换时决定是否写回磁盘。</li>
<li><b>引用位（使用位）</b>：是否被访问过，用于 Clock/LRU 等替换算法。</li>
</ul>
<p><b>地址转换</b>：虚地址 = 虚页号 | 页内偏移</p><p>物理地址 = 物理页号 | 页内偏移</p><p><b>页面大小相同 → 页内偏移完全一致</b>。转换过程：页表基址寄存器 → 取虚页号作索引查页表 → 有效位为 1 则取物理页号与页内偏移拼接；为 0 则缺页异常。</p>
<p><b>优点</b>：页面大小固定、页表简单、调入方便。<b>缺点</b>：最后一页产生<b>内部碎片</b></p><p>页是物理划分单位、缺乏逻辑意义，模块化/保护/共享不如段式灵活。</p>
<p><b>快表（TLB）</b>：由高速 SRAM 构成的页表项缓存，避免每次转换都访问主存页表</p><p>主存中的页表称<b>慢表</b>。TLB 工作原理类似 Cache，通常<b>全相联或组相联</b>映射</p><p>表项含虚拟页号（作标记）+ 物理页号 + 控制位</p><p>全相联下标记为完整虚页号、组相联下虚页号高位为标记低位为组索引。TLB 命中即获得实页号完成转换</p><p>缺失则查慢表并装入 TLB（满则替换）。</p>
<p><b>具有 TLB 和 Cache 的系统</b>：CPU 给出虚地址 → 虚页号与所有 TLB 项并行比较 → TLB 命中取实页号得物理地址 → Cache 按映射方式划分标记/组号/块内地址 → 组号定位、标记比较 → 命中取数据。</p>
<p><b>三种缺失</b>：</p>
<ul>
<li><b>TLB 缺失</b>：虚页号不在 TLB</li>
<li><b>Cache 缺失</b>：主存块不在 Cache</li>
<li><b>Page 缺失</b>：页面不在主存</li>
</ul>
<p><b>Page 缺失则 TLB 必缺失、Cache 必缺失</b></p><p>TLB 命中则 Page 必命中。</p>
<p><b>表3.3 TLB、Page、Cache 三种缺失的可能组合情况</b>：</p>
<table>
<tr><th>序号</th><th>TLB</th><th>Page</th><th>Cache</th><th>说明</th></tr>
<tr><td>1</td><td>命中</td><td>命中</td><td>命中</td><td>TLB 命中则 Page 一定命中，信息在主存，就可能在 Cache 中</td></tr>
<tr><td>2</td><td>命中</td><td>命中</td><td>缺失</td><td>TLB 命中则 Page 一定命中，信息在主存，也可能不在 Cache 中</td></tr>
<tr><td>3</td><td>缺失</td><td>命中</td><td>命中</td><td>TLB 缺失但 Page 可能命中，信息在主存，就可能在 Cache 中</td></tr>
<tr><td>4</td><td>缺失</td><td>命中</td><td>缺失</td><td>TLB 缺失但 Page 可能命中，信息在主存，也可能不在 Cache 中</td></tr>
<tr><td>5</td><td>缺失</td><td>缺失</td><td>缺失</td><td>TLB 缺失则 Page 也可能缺失，信息不在主存，也一定不在 Cache</td></tr>
</table>
<p><b>组合开销</b>：</p>
<ul>
<li>全命中免访主存</li>
<li>TLB 缺失但 Page 命中 + Cache 命中/缺失需访主存 1 次</li>
<li>TLB、Cache 均缺失需 2 次</li>
<li>Page 缺失触发缺页异常（访磁盘 + 至少 2 次主存）</li>
</ul>
<p>Cache 缺失处理由硬件完成</p><p>缺页由 OS 缺页异常处理程序完成</p><p>TLB 缺失软/硬件均可。</p>
<h5>3.6.3 段式虚拟存储器</h5>
<p>段按<b>程序的逻辑结构</b>划分，长度因程序而异。虚地址 = 段号 | 段内地址</p><p>变换由<b>段表</b>实现（记录段号、有效位、段起点、段长——段长可变所以必须给出起始地址与段长）。<b>分段对程序员不透明</b>（分页透明）。优点：段边界与自然逻辑边界一致、便于编译/管理/修改/保护、易于段共享</p><p>缺点：段长度可变、主存分配困难、段间产生<b>外部碎片</b>。</p>
<h5>3.6.4 段页式虚拟存储器</h5>
<p>先按逻辑分段、每段再划分固定大小的页，调入调出以页为单位。每个程序一个段表、每段一个页表</p><p>虚地址 = 段号 | 段内页号 | 页内地址。转换：段号查段表得页表起始地址 → 段内页号查页表得实页号 → 与页内地址拼接。优点：兼具页式与段式优点（按段共享保护、无外部碎片）</p><p>缺点：<b>地址变换需两次查表</b>，系统开销大。（非统考重点，掌握原理即可）</p>
<h5>3.6.5 虚拟存储器与Cache的比较</h5>
<p><b>相同</b>：目标都是提高系统性能、体现容量-速度-价格梯度</p><p>都以小信息块为基本交换单位（页比 Cache 块大得多）</p><p>都涉及地址映射、替换算法、更新策略</p><p>都基于局部性原理。 <b>不同</b>：①Cache 解决速度差异、虚拟存储器解决容量不足</p><p>②Cache 全硬件实现、对所有程序员透明，虚拟存储器软硬件协同、仅对应用程序员透明（对 OS 开发者不透明）</p><p>③不命中代价不同——Cache 缺失访主存延迟增数十倍，<b>缺页访磁盘延迟增可达十万倍</b></p><p>④Cache 缺失硬件自动从主存取数据装入，缺页必须先从辅存调入主存、CPU 才能访问（辅存与 CPU 无直接通路）。</p>"""
        }
    ]
}
