# -*- coding: utf-8 -*-
"""计组笔记 · 第6章 总线（王道2027结构）"""

CH6 = {
    "chapter": "第6章",
    "title": "第6章 总线",
    "sections": [
        {
            "section": "6.1 总线概述",
            "html": """<h4>6.1 总线概述</h4>
<p><b>总线</b>：一组可供多个部件<b>分时共享</b>的公共信息传输线路。核心特征：<b>分时性</b>（任一时刻仅允许一个部件向总线发送信息，多个发送方分时使用）</p><p><b>共享性</b>（多个部件可同时挂接在总线上，并能同时接收总线上传输的信息）。引入总线的好处：简化系统结构、减少连线数量、统一接口标准、支持模块化灵活扩展、简化软件编程、便于故障诊断与成本控制。引入总线的问题：多个主设备同时请求使用总线会引发<b>总线竞争</b>，需配置<b>总线仲裁部件</b>选择一个主设备获得总线控制权。</p>
<h5>6.1.1 总线的分类</h5>

<ul>
<li><b>内部总线（片内总线）</b>：芯片内部的总线，用于 CPU 芯片内部各寄存器之间以及寄存器与 ALU 之间的连接。</li>
<li><b>系统总线</b>：计算机系统内各功能部件（CPU、主存、I/O 接口）之间相互连接的总线，分三类：</li>
</ul>

<ul>
<li><b>数据总线</b>：在各部件之间<b>双向</b>传输数据信息（指令、操作数、状态字、中断类型号等）；位数反映 CPU 一次能并行传送的数据位数。</li>
<li><b>地址总线</b>：指定要访问的主存单元或 I/O 端口地址，是<b>单向总线</b>；位数决定系统最大可寻址空间。部分架构采用<b>地址线与数据线复用</b>（分时在同一组物理线路传输）以减少引脚。</li>
<li><b>控制总线</b>：传输各种协调与控制信号（时钟、复位、总线请求/允许、中断请求/响应、存储器读/写、I/O 读/写、传输确认等）；由若干单向或双向信号线组成。</li>
</ul>
<ul>
<li><b>I/O 总线</b>：连接主机与内部各类 I/O 控制器（显卡、网卡等），采用标准化内部总线协议（PCI、AGP、PCI Express 等）。</li>
<li><b>通信总线</b>：用于主机与外部 I/O 设备之间或不同计算机系统之间通信，需适应不同传输距离、速率和电气规范（RS-232、USB 等）。</li>
</ul>
<h5>6.1.2 常见的总线标准</h5>
<p>总线标准主要区别：总线宽度、带宽、时钟频率、寻址能力、是否支持突发传送等。</p>
<ul>
<li><b>ISA</b>（工业标准体系结构）：最早的系统总线标准，传输速率低、CPU 占用率高；系统总线、并行总线。</li>
<li><b>EISA</b>（扩展的 ISA）：支持多主控器和突发传送，完全兼容 ISA；系统总线、并行总线。</li>
<li><b>VESA</b>（视频电子标准协会）：32 位局部总线标准，为多媒体 PC 高速图像传输设计；局部总线、并行总线。<b>局部总线</b>是位于 CPU 与 ISA 总线之间的高速扩展总线，将高速外设（显卡、磁盘控制器）从低速 ISA 总线迁移至更接近 CPU 的数据通路。</li>
<li><b>PCI</b>（外部设备互连）：高性能 32/64 位总线，与处理器时钟频率无关，支持即插即用；局部总线、并行总线。</li>
<li><b>AGP</b>（加速图形接口）：专为图形卡设计，允许显卡直接高效访问系统主存；局部总线、并行总线。</li>
<li><b>PCI-E</b>（PCI-Express）：高速<b>串行点对点</b>连接，由多条通道组成（×1、×16），支持<b>全双工</b>，速率远超 PCI 和 AGP；局部总线、串行总线。</li>
<li><b>RS-232C</b>：DTE 与 DCE 之间的串行二进制通信标准接口；设备总线、串行总线。</li>
<li><b>USB</b>（通用串行总线）：连接外部 I/O 设备，支持即插即用、热插拔、多设备级联；设备总线、串行总线。</li>
<li><b>PCMCIA</b>：笔记本扩展卡标准，支持即插即用；设备总线、并行总线。</li>
<li><b>IDE（ATA）</b>：连接主板与硬盘/光驱的传统并行接口；设备总线、并行总线。</li>
<li><b>SCSI</b>（小型计算机系统接口）：高性能系统级设备接口，常用于服务器硬盘；设备总线、并行总线。</li>
<li><b>SATA</b>（串行高级技术附件）：IDE/ATA 的串行替代标准，高速串行传输；设备总线、串行总线。</li>
</ul>
<h5>6.1.3 总线的性能指标</h5>
<ul>
<li><b>总线传输周期</b>：完成一次完整总线事务（如一次读或写）的总时间，通常由若干总线时钟周期组成（简称总线周期）。</li>
<li><b>总线时钟频率</b>：总线基础时钟信号的频率，是总线时钟周期的倒数；早期与 CPU 时钟同频，现代通常独立于 CPU 时钟。</li>
<li><b>总线工作频率</b>：总线每秒能进行的<b>有效数据传输次数</b>；早期每时钟周期传一次（工作频率=时钟频率），现代一个时钟周期可传 2 次、4 次甚至更多。</li>
<li><b>总线宽度</b>：总线中数据线的条数，决定每次能并行传输的数据位数。</li>
<li><b>总线带宽</b>：单位时间内所能传输的最大数据量。<b>总线带宽 = 总线宽度 × 总线工作频率</b>。例：时钟频率 11MHz、每周期传 2 次（工作频率 22MHz）、宽度 16 位（2 字节）→ 带宽 = 2×22×10⁶ = 44MB/s。<b>注意</b>：计算带宽应依据物理参数（宽度、时钟频率、每周期传输次数）反映理论峰值，无须考虑地址/命令开销或突发传输细节；仅在求平均数据传输率时才需分析事务时序；切勿把"两个设备间一次通信的有效速率"当作总线带宽。</li>
<li><b>总线复用</b>：同一组信号线在不同时间段传输不同类型的信息（如地址/数据线复用），减少引脚数量、节省硬件成本。</li>
<li><b>总线寻址能力</b>：由地址线位数决定，表示 CPU 能访问的最大地址空间（16 位地址线 → 2¹⁶=65536 个单元，每单元 1B 则最大寻址空间 64KB）。</li>
</ul>
<p>最主要的性能指标：<b>总线宽度、总线工作频率、总线带宽</b>。</p>
<h5>6.1.4 总线的结构</h5>
<p><b>1. 早期共享总线结构</b>：单总线结构——CPU、主存、各类 I/O 设备均挂接在同一条系统总线上</p><p>实现简单、成本低，但所有设备分时竞争总线、频繁传输冲突、效率低。双总线结构增设专用存储总线，但 I/O 与主存交换数据仍需 CPU 中转。三总线结构增加 DMA 总线，允许高速 I/O 设备经 DMA 控制器直接与主存通信、无须 CPU 干预，显著提升 I/O 吞吐能力。</p>
<p><b>2. 传统分层总线：南北桥结构</b>（Pentium 4 及早期 Core 系列）：<b>北桥芯片 MCH</b>（存储器控制中心）连接 CPU、主存和显卡，负责高带宽数据传输</p><p><b>南桥芯片 ICH</b>（I/O 控制器集线器）管理 USB、SATA、以太网等低速 I/O 接口并提供扩展槽。CPU 与北桥之间为<b>前端总线 FSB</b>（也称系统总线）。局限：所有数据（内存访问、显卡通信、I/O 传输）最终都需经 FSB 汇聚至 CPU，FSB 成为唯一高性能通道，共享式特性演变为新的性能瓶颈。</p>
<p><b>3. 现代集成化总线结构</b>（自 Intel Core i7 起）：内存控制器集成到 CPU 内部，传统北桥消失，转向片上集成与点对点互连。三大优势：①<b>内存访问路径缩短</b>——CPU 经片上存储器控制器直接访问主存，支持多通道 DDR（双通道、三通道），理论带宽近似为单通道整数倍</p><p>②<b>处理器间高速互连</b>——多核间、多 CPU 间通过 QPI 等点对点高速串行链路交换数据，QPI 也用于连接 CPU 与 IOH（类似南桥）</p><p>③<b>I/O 通路直连</b>——高速外设（显卡、SSD）经 PCIe 通道直接接入 CPU 绕过南桥，低速设备由集成度更高的 PCH（新一代南桥）统一管理。</p>"""
        },
        {
            "section": "6.2 总线事务和定时",
            "html": """<h4>6.2 总线事务和定时</h4>
<h5>6.2.1 总线事务</h5>
<p><b>总线事务</b>：总线上主设备（如 CPU、DMA 控制器）与从设备（如主存、I/O 设备）之间完成一次完整信息交换的过程。类型由操作性质决定：存储器读、存储器写、I/O 读/写、中断响应等。每个总线事务通常包含三个基本阶段：</p>
<ul>
<li><b>地址传送阶段</b>：主设备将目标地址和操作类型（读/写）经总线传送给从设备。</li>
<li><b>从设备响应阶段（数据准备阶段）</b>：从设备根据地址准备数据（耗时由设备特性决定，题中未明确提及通常可忽略）。</li>
<li><b>数据传送阶段</b>：完成实际数据在总线上的传输。</li>
</ul>
<p><b>非突发传输与突发传输</b>：</p>
<ul>
<li><b>非突发传输</b>：每次仅传输一个数据单元，每次传输都必须独立经历完整三阶段（发送地址 → 等待准备 → 传输数据）；连续读取多个相邻数据也要重复发送地址，地址开销大、效率低。</li>
<li><b>突发传输（猝发传输）</b>：用于高效传输连续成块数据。事务开始时仅发送数据块<b>首地址</b>，随后在不释放总线的前提下连续传送多个数据单元，后续地址由硬件自动递增生成（首地址+1、+2...），无须重复使用地址线。显著提升有效带宽，广泛应用于高速存储器访问（SDRAM 行读取、Cache 块填充）。<b>非突发="一次地址，一次数据"；突发="一次地址，多次数据"</b>。</li>
</ul>
<p><b>串行传输与并行传输</b>：</p>
<ul>
<li><b>串行传输</b>：数据按比特位依次顺序传输，通常用一条双向线路或两条单向线路；引脚少、布线简单、抗干扰能力强，适合长距离通信（USB、PCIe、SATA）。分两种：<b>同步串行通信</b>——发送方时钟直接控制接收方时钟实现位同步，收发时钟严格一致，仅数据块首尾添加开始/结束标记，效率高但硬件复杂成本高；<b>异步串行通信</b>——收发双方使用独立时钟无须严格同步，每个字符独立传输，用<b>起始位（逻辑 0）</b>标识开始、<b>停止位（逻辑 1）</b>标识结束，线路空闲保持逻辑 1，数据位从最低位开始逐位发送，可选择发送一位奇偶校验位。</li>
<li><b>并行传输</b>：利用多条数据线同时传输多个比特位，理论上单周期完成一个字传输，短距离延迟低、吞吐高；但频率提高时信号串扰和时序偏移加剧，限制工作频率提升，适合板级/芯片级短距离通信（早期 PCI、内存总线）。<b>注意：并行并不总比串行快</b>——并行总线工作频率难以持续提高，串行可通过提升频率实现更高总带宽，现代高速接口多采用串行化设计。</li>
</ul>
<h5>6.2.2 总线定时</h5>
<p>总线定时是协调主/从设备交换数据时序的控制协议，有同步、异步、半同步和分离式四种：</p>
<ul>
<li><b>同步定时方式</b>：采用系统统一的时钟信号协调传送定时，每次数据传送在一个总线周期内完成，总线周期连续进行。<ul>
<li><b>优点</b>：传送速度快、总线控制逻辑简单</li>
<li><b>缺点</b>：主从设备强制性同步，缺乏应答/握手机制，无法根据从设备实际状态动态调整时序，可靠性较差</li>
<li><b>适用</b>：总线长度较短、各部件存取时间比较接近的系统</li>
</ul></li>
<li><b>异步定时方式</b>：不依赖统一时钟，通过主从设备之间的<b>握手信号</b>实现定时控制——主设备发"请求"，从设备就绪后发"回答"。按请求/回答信号撤销是否互锁分三类：<ul>
<li><b>不互锁</b>（请求在预设时延后自行撤销，回答在时延后自动撤销，双方无互锁）</li>
<li><b>半互锁</b>（主设备收到回答后才撤销请求，但从设备发出回答后不确认请求是否撤销、在时延后自动撤销）</li>
<li><b>全互锁</b>（主设备收到回答才撤销请求，从设备确认请求已撤销才撤销回答，可靠性最高）</li>
<li><b>评价</b>：优点——总线周期长度可变，能可靠连接速度差异大的设备；缺点——控制逻辑复杂、传输速率较低。适用：连接速度差异大、对可靠性要求高而对速率要求不苛刻的系统</li>
</ul></li>
<li><b>半同步定时方式</b>：结合同步与异步优点——地址、命令、数据的发送严格参照系统时钟前沿（上升沿），接收方通常在时钟后沿（下降沿）识别；增设一条 <b>Wait 信号线</b>允许慢速从设备反馈准备状态：主设备在时钟上升沿检测 Wait，Wait 无效（高电平）表示数据未就绪、插入等待周期，直到 Wait 有效（低电平）才读取数据。<ul>
<li><b>优点</b>：统一时钟下控制比异步简单、可靠性较高</li>
<li><b>缺点</b>：系统时钟频率受限于最慢设备，整体速度不高</li>
<li><b>适用</b>：包含多种速度差异较大设备、但性能要求不高的简单系统</li>
</ul></li>
<li><b>分离式定时方式</b>：将一个总线事务拆分为两个独立子阶段——<b>请求阶段</b>（主设备 A 获得总线使用权，发送地址和命令后立即释放总线）与<b>应答阶段</b>（从设备 B 准备好数据后主动申请总线，以主设备身份将数据发回给 A），两阶段之间释放总线，总线在准备期间可被其他事务使用。<ul>
<li><b>优点</b>：显著减少总线空闲等待时间、提高总线利用率</li>
<li><b>缺点</b>：控制逻辑复杂、协议开销大、对总线仲裁和事务管理要求高</li>
<li><b>适用</b>：多主设备竞争激烈且从设备响应延迟较大的高性能系统</li>
</ul></li>
</ul>
<p><b>易错点</b>：同步、异步、半同步三种定时方式均采用"独占式事务模型"——从主设备发起请求到传送结束，总线全程被该事务占用，从设备准备数据阶段总线被占用却空闲，造成资源浪费（分离式正是为解决此问题）。任一总线周期内总线上只能有一个主设备控制总线，并选择一个从设备一对一通信或向所有从设备广播，<b>同一时刻不能存在多对并发的主从通信</b>。</p>"""
        }
    ]
}
