# -*- coding: utf-8 -*-
"""计组笔记 · 第7章 输入/输出系统（王道2027结构）"""

CH7 = {
    "chapter": "第7章",
    "title": "第7章 输入/输出系统",
    "sections": [
        {
            "section": "7.1 I/O系统基本概念",
            "html": """<h4>7.1 I/O系统基本概念</h4>
<p>（本节内容 2021 年已从统考大纲删除，仅供学习参考）<b>输入</b>是指将信息从外部设备传送到主机，<b>输出</b>则是将信息从主机传送到外部设备。I/O 系统的核心任务是对各类信息的输入与输出过程进行有效控制。</p>
<h5>7.1.1 I/O系统的组成</h5>
<p><b>基本概念</b>：</p>
<ul>
<li><b>外部设备</b>：包括输入设备、输出设备，以及需通过 I/O 接口访问的外部存储设备。</li>
<li><b>接口</b>：位于外设与主机之间的逻辑部件，负责协调数据传输过程中的速度匹配、电平转换和格式适配等工作。</li>
<li><b>输入设备</b>：用于向计算机输入命令、文本或数据的装置（键盘、鼠标等）；<b>输出设备</b>：用于将处理结果输出到外部介质的装置（显示器、打印机等）；<b>外存设备</b>：除内存和 CPU 缓存以外的存储器（硬磁盘、光盘、固态硬盘等）。</li>
</ul>
<p><b>I/O 系统构成</b>：</p>
<ul>
<li><b>I/O 软件</b>：主要包括设备驱动程序、I/O 管理程序以及用户应用程序，负责实现 CPU 与 I/O 设备之间的信息交换。</li>
<li><b>I/O 硬件</b>：包括外部设备、设备控制器、I/O 接口以及 I/O 总线等；设备控制器负责控制外设的具体操作，I/O 接口实现与主机总线的连接。</li>
</ul>
<h5>7.1.2 外部设备</h5>
<p><b>输入设备</b>：键盘（输入字符、数字及各种控制命令）、鼠标（定位输入设备，将用户操作映射为屏幕光标位置，实现人机交互）。</p>
<p><b>输出设备——显示器</b>：按类型分为阴极射线管（CRT）、液晶显示器（LCD）和发光二极管（LED）等，以点阵方式工作。主要性能参数：</p>
<ul>
<li><b>屏幕尺寸</b>：以对角线长度表示，单位为英寸，常见规格 12～32 英寸。</li>
<li><b>分辨率</b>：屏幕上可显示的像素总数，常表示为"宽度×高度"，如 1920×1080。</li>
<li><b>色彩深度</b>：每个像素所能呈现的颜色数量；黑白显示器体现为灰度级（8 位对应 256 级），彩色显示器通常采用 24 位真彩色（约 1677 万种颜色）或更高。</li>
<li><b>刷新机制</b>：像素发光持续时间极短，必须周期性重绘整屏图像以维持画面稳定。</li>
<li><b>刷新频率</b>：每秒刷新屏幕的次数；人眼在刷新频率高于 30Hz 时才不会感知闪烁，现代显示器一般支持 60～120Hz。</li>
</ul>
<p><b>显示存储器（VRAM）计算</b>：VRAM 用于存储一帧完整的图像数据，容量与带宽公式：<b>VRAM 容量 = 分辨率×色彩深度（位数）</b></p><p><b>VRAM 带宽 = 分辨率×色彩深度（位数）×帧频</b>。例（2010 真题）：1600×1200、24 位、85Hz，刷新占显存总带宽 50% → 刷新所需带宽 = 1600×1200×24×85 = 3916.8Mb/s，显存总带宽至少为 3916.8/0.5 ≈ 7834Mb/s。</p>
<p><b>输出设备——打印机</b>：</p>
<ul>
<li><b>针式打印机</b>：点阵式击打打印机，通过打印针撞击色带在纸张上形成字符；优点是多层复写（如发票）、耗材便宜，缺点是噪声大、分辨率低、速度慢。</li>
<li><b>喷墨式打印机</b>：通过精确喷射微小墨滴形成图像，基于青、品红、黄三基色混合实现高质量彩色打印，广泛应用于家庭和办公场景。</li>
<li><b>激光打印机</b>：结合激光扫描与静电成像技术，数字信号调制激光束在感光鼓上形成静电潜像，经显影、转印、定影等步骤呈现在纸上；打印速度快、质量高、处理能力强。</li>
</ul>
<p><b>外部存储器（辅存）</b>：</p>
<ul>
<li><b>磁表面存储器</b>：利用磁性材料涂覆在金属或塑料基片上存储信息，典型代表包括硬盘、软盘、磁带和磁鼓等。</li>
<li><b>固态硬盘（SSD）</b>：采用 Flash 存储器作为存储介质，无机械运动部件，读/写速度快、抗震性强、功耗低。</li>
<li><b>光盘存储器</b>：利用聚焦激光束以非接触方式在盘片上读/写信息，完整系统由光盘片、光盘驱动器和光盘控制器组成，常见类型包括 CD、DVD 等。</li>
</ul>"""
        },
        {
            "section": "7.2 I/O接口",
            "html": """<h4>7.2 I/O接口</h4>
<p><b>I/O 接口（也称 I/O 控制器）</b>：主机与外部设备之间的交接界面，用于实现二者之间的信息交换。由于外设种类繁多，在工作方式、数据格式和工作速度等方面存在显著差异，接口正是为弥合这些异构性而设置的。</p>
<h5>7.2.1 I/O接口的功能</h5>
<p>主要功能：</p>
<ul>
<li><b>地址译码与设备选择</b>：CPU 发出 I/O 操作请求时同时提供目标外设的地址码，接口对地址译码产生设备选择信号，确保主机仅与指定外设通信。</li>
<li><b>通信联络与时序协调</b>：主机与外设工作速度不匹配，接口通过握手信号或状态轮询机制动态协调双方操作节奏，确保数据传输可靠有序。</li>
<li><b>数据缓冲</b>：接口设有数据缓冲寄存器，暂存传输数据，避免因速度不匹配导致数据丢失。</li>
<li><b>信号格式转换</b>：完成电平转换、并/串或串/并转换、模/数或数/模转换等，实现物理兼容。</li>
<li><b>控制命令与状态信息传递</b>：CPU 通过向控制寄存器写入命令字（如"启动"）控制外设；外设通过状态寄存器反馈状态（如"就绪"）；外设需主动通知 CPU（如数据到达）时，接口可向 CPU 发出中断请求。</li>
</ul>
<h5>7.2.2 I/O接口的基本结构</h5>
<p>I/O 接口在主机侧通过 I/O 总线与 CPU 和内存相连，内部主要包括三类寄存器：</p>
<ul>
<li><b>数据缓冲寄存器</b>：暂存 CPU 与外设之间传送的数据。</li>
<li><b>状态寄存器</b>：记录接口及外设的当前状态，仅被 CPU 读取。</li>
<li><b>控制寄存器</b>：保存 CPU 发给外设的控制命令，仅被 CPU 写入。</li>
</ul>
<p><b>端口复用</b>：状态寄存器与控制寄存器访问方向相反且访问时间错开，在某些设计中可复用同一端口地址：<b>读操作访问状态，写操作访问控制</b>。</p>
<p>I/O 接口与总线的连接：<b>数据线</b>传送读/写数据、状态信息、控制信息以及中断类型号（在向量中断中）</p><p><b>地址线</b>指定要访问的 I/O 接口内部寄存器的端口地址</p><p><b>控制线</b>传送读/写控制信号（用于区分寄存器访问方向）、中断请求与响应信号、总线仲裁信号和设备握手信号。</p>
<p><b>I/O 控制逻辑的功能</b>：①对控制寄存器中的命令字译码，并将生成的控制信号经外设界面控制逻辑送至外设</p><p>②输出时把数据缓冲寄存器的内容发送给外设，输入时把外设数据写入数据缓冲寄存器</p><p>③实时采集外设状态并更新至状态寄存器。</p>
<p><b>I/O 指令</b>：对接口内部寄存器的访问通过专用指令完成，这类指令称为 I/O 指令。在采用<b>独立 I/O 地址空间</b>的体系结构（如 x86）中，I/O 指令通常为<b>特权指令，仅限操作系统内核使用</b>。</p>
<h5>7.2.3 I/O接口的类型</h5>
<ul>
<li><b>按数据传送方式</b>（外设与接口一侧）：并行接口（一字节或一个字的所有位同时传送）和串行接口（一位一位有序传送），接口需完成相应的并/串或串/并格式转换。</li>
<li><b>按主机访问 I/O 设备的控制方式</b>：程序查询接口、中断接口和 DMA 接口等。</li>
<li><b>按功能灵活性</b>：可编程接口（通过编程改变接口功能）和不可编程接口。</li>
</ul>
<h5>7.2.4 I/O端口及其编址</h5>
<p><b>I/O 端口</b>是指 I/O 接口电路中<b>可被 CPU 直接访问的寄存器</b>，主要包括：</p>
<ul>
<li><b>数据端口</b>（支持读/写操作）</li>
<li><b>状态端口</b>（仅支持读操作）</li>
<li><b>控制端口</b>（仅支持写操作）</li>
</ul>
<p>注意：<b>端口与接口是两个不同概念——端口是接口内部可寻址的寄存器</b>。</p>
<p><b>编址方式</b>：为使 CPU 能够访问各个 I/O 端口，必须对端口进行编址，每个端口对应一个唯一的端口地址，常见两种：</p>
<ul>
<li><b>独立编址（I/O 映射方式）</b>：为 I/O 端口建立独立于主存的地址空间，端口地址与内存地址逻辑上完全分离，地址值可以相同但不会冲突；CPU 通过<b>专用 I/O 指令</b>（如 x86 的 IN 和 OUT）访问端口，指令中的地址字段指定端口号。<ul>
<li><b>优点</b>：端口数量远少于内存单元，所需地址线较少、译码电路简单、寻址速度快；使用专用指令使 I/O 操作清晰可辨，便于阅读与调试</li>
<li><b>缺点</b>：I/O 指令功能有限，通常仅支持简单的数据传输，程序设计灵活性较差；CPU 需同时提供存储器读/写和 I/O 读/写两组控制信号，控制逻辑复杂</li>
</ul></li>
<li><b>统一编址（存储器映射方式）</b>：将部分主存地址空间分配给 I/O 端口，与内存单元共享同一地址空间，通过地址范围区分访问目标（如高地址段映射到 I/O 设备），<b>无须专用 I/O 指令</b>，用普通访存指令（加载/存储）即可访问端口。<ul>
<li><b>优点</b>：编程灵活；端口可获得较大的编址空间；保护机制可由虚拟存储管理系统统一实现，无须额外硬件支持</li>
<li><b>缺点</b>：端口占用主存地址空间，减少可用内存容量；需根据完整地址判断是否为 I/O 区域，译码电路复杂，可能降低译码速度</li>
</ul></li>
</ul>
<p><b>易错点</b>：统一编址下区分存储单元和 I/O 设备靠<b>不同的地址码</b>（2014 真题）</p><p>I/O 指令实现的数据传送发生在<b>通用寄存器和 I/O 端口之间</b>（2017 真题）</p><p>磁盘驱动器（磁头、磁盘和读/写电路组成的磁盘本身）不属于 I/O 接口（2021 真题）</p><p>I/O 接口主机侧数据线宽度与设备侧电缆数据线宽度<b>不一定相同</b></p><p>主机侧通过 I/O 总线与主机相连，设备侧通过通信总线（电缆）与外设相连。</p>"""
        },
        {
            "section": "7.3 I/O方式",
            "html": """<h4>7.3 I/O方式</h4>
<p>输入/输出系统实现主机与 I/O 设备之间的数据传送，可采用不同的控制方式，在硬件代价、系统性能及适用场景等方面各有侧重。常用的 I/O 方式包括<b>程序查询、程序中断和 DMA</b>，其中前两种方式高度依赖 CPU 执行程序指令来完成控制。</p>
<h5>7.3.1 程序查询方式</h5>
<p>数据交换的控制完全由 CPU 通过执行程序实现。接口电路通常包含一个<b>数据缓冲寄存器（数据端口）</b>和一个<b>设备状态寄存器（状态端口）</b>。主机进行 I/O 操作时，首先读取设备状态，据此决定立即传送数据还是继续等待。</p>
<p><b>工作流程</b>：①CPU 执行初始化程序，预置传送参数（起始地址、数据量等）</p><p>②向 I/O 接口发送命令字，启动外设</p><p>③循环读取外设状态寄存器</p><p>④设备未就绪则继续查询，就绪则执行一次数据传送</p><p>⑤修改地址和计数器参数</p><p>⑥判断传送是否完成，未完成返回 ③，直至计数器归零。</p>
<p><b>两类查询策略</b>：</p>
<ul>
<li><b>独占查询</b>：一旦启动外设，CPU 便连续不断地查询其状态直至操作完成；期间 CPU 无法执行其他任务，处于忙等待状态，CPU 与外设完全串行工作。</li>
<li><b>定时查询</b>：CPU 以固定时间间隔周期性查询外设状态，每次查询时若设备已就绪则传送一个数据单元，随后返回用户程序；查询间隔需根据外设数据传输速率合理设置，以避免数据丢失。</li>
</ul>
<p><b>计算例题</b>（2011 真题类似）：主频 500MHz、CPI=4、外设速率 2MB/s、32 位缓冲寄存器、每次查询执行 10 条指令。外设填满（4B）缓冲区时间 = 4B÷2MB/s = 2μs，故最多每隔 2μs 查询一次，每秒至少查询 1s÷2μs = 5×10⁵ 次</p><p>每秒用于 I/O 的时钟周期数 = 5×10⁵×10×4 = 2×10⁷，主频 500MHz（5×10⁸ 周期/秒），I/O 时间占比 = 2×10⁷÷(5×10⁸) = 4%。</p>
<p><b>评价</b>：优点是设计简单、硬件开销小</p><p>缺点是 CPU 需耗费大量时间查询与等待，且同一时间段内只能与一台外设通信，CPU 与外设串行工作，效率很低。</p>
<h5>7.3.2 程序中断方式</h5>
<p><b>基本概念</b>：程序中断指程序执行过程中出现某些急需处理的外部事件或内部异常时，CPU 暂停当前程序，转去处理该事件</p><p>处理完毕后返回到原程序的断点处继续执行。中断技术最初用于实现主机与 I/O 设备之间的<b>异步通信</b>。其重要功能：①实现 CPU 与 I/O 设备并行工作</p><p>②处理硬件故障和软件异常</p><p>③支持人机交互</p><p>④支撑多道程序与分时操作系统的任务切换</p><p>⑤满足实时系统快速响应需求</p><p>⑥实现用户程序向操作系统的切换（软中断/系统调用指令）</p><p>⑦多处理器系统中协调处理器间通信与任务迁移。</p>
<p><b>工作思想</b>：当前进程发起 I/O 操作时启动相应外设并主动进入阻塞状态，CPU 转而执行另一进程</p><p>外设完成数据准备后主动向 CPU 发出中断请求，CPU 响应后暂停当前指令流、保存现场、转入中断服务程序完成数据传送</p><p>传送结束后恢复被中断进程的现场，返回断点继续执行。</p>
<p><b>中断请求</b>：中断源是能向 CPU 发出中断请求的设备或事件</p><p>中断系统为每个中断源设置一个<b>中断请求标记触发器</b>（为 1 表示有请求），这些触发器组成中断请求标记寄存器，可集中置于 CPU 内部，也可分散在各中断源中。</p>
<p><b>可屏蔽 vs 不可屏蔽</b>：通过 <b>INTR</b> 信号线发出的是<b>可屏蔽中断</b>（优先级较低，关中断状态下不被响应）</p><p>通过 <b>NMI</b> 信号线发出的是<b>不可屏蔽中断</b>（用于电源掉电、总线错误等紧急关键硬件事件，优先级最高，不受中断允许标志影响）。</p>
<p><b>中断响应判优</b>：多个中断源同时提出请求时，由硬件排队器（或中断查询程序）确定响应顺序。一般规则：①不可屏蔽中断 &gt; 可屏蔽中断</p><p>②I/O 传送类中，高速设备 &gt; 低速设备、输入设备 &gt; 输出设备、实时设备 &gt; 普通设备。<b>响应优先级</b>由硬件线路或查询顺序固定决定，不可动态更改</p><p><b>处理优先级</b>可通过中断屏蔽技术动态调整，以支持多重中断（中断嵌套）</p><p>只有未被屏蔽的中断请求才会被送入中断判优电路参与响应优先级的判定。</p>
<p><b>CPU 响应中断的条件</b>：①存在有效的中断请求</p><p>②CPU 处于开中断状态（中断允许标志为 1</p><p>异常和不可屏蔽中断不受此限制）</p><p>③当前指令已执行完毕（异常不受此限制），且无更高优先级任务待处理。I/O 设备的就绪时间是随机的，而 CPU 仅在每条指令执行结束时统一采样中断请求信号（前提是开中断），因此 <b>CPU 响应 I/O 中断的时机总是发生在某条指令执行完成之后</b>（此处中断特指 I/O 中断，不包括异常）。</p>
<p><b>中断响应过程——中断隐指令</b>：CPU 响应中断后由硬件自动完成一系列操作（统称<b>中断隐指令</b>，并非指令系统中的真实指令），主要包括：①<b>关中断</b>——关闭中断允许标志，防止保存断点和现场时被新中断打断（否则现场信息不完整，中断返回后无法正确恢复）</p><p>②<b>保存断点</b>——将 PC 和程序状态字 PSW 等关键现场信息保存至栈或专用寄存器（x86 机器保存 PC 和 PSW 到内存栈，MIPS 无 PSW 只保存 PC 到特定寄存器</p><p>中断发生于指令执行完成后，断点为下一条指令地址</p><p>故障类异常如缺页、除零因指令未完成，断点为当前指令地址，处理后需重新执行当前指令</p><p>陷阱类异常如系统调用在指令成功执行后触发，断点为下一条指令地址）</p><p>③<b>引出中断服务程序</b>——通过识别中断源，将对应中断服务程序的入口地址送入 PC，识别方法主要有硬件向量法和软件查询法。</p>
<p><b>中断向量</b>：中断识别分为向量中断和非向量中断两类，非向量中断采用软件查询法。向量中断中，每个中断源被分配一个唯一的中断类型号，对应中断服务程序入口地址，该地址称为<b>中断向量</b></p><p>所有中断向量集中存放在内存特定区域，称为<b>中断向量表</b>。CPU 响应后在中断响应阶段从<b>数据总线</b>获取中断类型号，据此计算出对应中断向量在表中的地址并读出送入 PC，从而转入对应中断服务程序。</p>
<p><b>中断处理过程</b>（支持中断嵌套的典型流程）：①关中断</p><p>②保存断点（①~③由中断隐指令硬件完成）</p><p>③中断服务程序寻址（中断类型号获取入口地址送入 PC）</p><p>④保存现场和中断屏蔽字（软件指令压栈</p><p>现场信息指 CPU 中可能被中断服务程序修改且需恢复的寄存器内容</p><p>现场信息用指令可直接访问故由软件显式保存，断点由硬件自动保存，两者均不可被中断服务程序破坏）</p><p>⑤开中断（允许更高优先级中断被响应，实现中断嵌套）</p><p>⑥执行中断服务程序</p><p>⑦关中断（确保恢复现场和屏蔽字过程不被中断）</p><p>⑧恢复现场和中断屏蔽字</p><p>⑨开中断、中断返回（中断服务程序最后一条指令通常为中断返回指令）。</p><p>④~⑨由中断服务程序（软件）完成。<b>单重中断</b>不支持嵌套，省略 ⑤ 和 ⑦ 即可。</p>
<p><b>多重中断和中断屏蔽技术</b>：执行中断服务程序过程中出现新的更高优先级请求而不响应，称为<b>单重中断</b></p><p>暂停当前服务程序转去处理新中断，称为<b>多重中断（中断嵌套）</b>。处理优先级可通过中断屏蔽技术动态调整：可编程中断控制器中，中断请求信号先记录在<b>中断请求寄存器</b>，<b>中断屏蔽字寄存器</b>每一位对应一个中断源（置 1 屏蔽、置 0 允许），请求信号与其对应屏蔽位的取反值相与后才送入判优电路。中断屏蔽字通常在进入中断服务程序时由软件加载，因此屏蔽仅在 CPU 执行中断服务程序期间生效</p><p>主程序运行阶段响应由主程序设置的屏蔽状态决定（通常全开放）。</p>
<p><b>屏蔽字计算例题</b>（2017 真题风格）：4 个中断源 A、B、C、D，硬件响应优先级 A&gt;B&gt;C&gt;D，要求处理优先级调整为 A&gt;D&gt;C&gt;B。A 最高屏蔽所有中断（含自身）：1111</p><p>D 次高只允许被 A 中断：0111</p><p>C 允许被 A 和 D 中断，屏蔽 B 和自身：0110</p><p>B 最低仅屏蔽自身：0100。若 A、C、D 同时请求，按响应优先级先 A（屏蔽字 1111 独占完成），再 C（屏蔽字 0110，允许 D 打断），D 完成后返回 C 继续</p><p>C 期间 B 请求被屏蔽（C 的屏蔽字 0110 中 B 位为 1），待 C 完成后才响应 B。</p>
<p><b>表7.1 中断源对应的中断屏蔽字</b>：</p>
<table>
<tr><th rowspan="2">中断源</th><th colspan="4">屏蔽字</th></tr>
<tr><th>A</th><th>B</th><th>C</th><th>D</th></tr>
<tr><td>A</td><td>1</td><td>1</td><td>1</td><td>1</td></tr>
<tr><td>B</td><td>0</td><td>1</td><td>0</td><td>0</td></tr>
<tr><td>C</td><td>0</td><td>1</td><td>1</td><td>0</td></tr>
<tr><td>D</td><td>0</td><td>1</td><td>1</td><td>1</td></tr>
</table>
<p><b>评价</b>：程序中断方式克服了程序查询方式中 CPU 的忙等待现象，显著提高 CPU 利用率</p><p>但从微观操作分析，CPU 处理中断时仍需暂停原程序运行，高速设备频繁、成批地与主存交换数据时会不断打断现行程序，造成较大开销。</p>
<h5>7.3.3 DMA方式</h5>
<p><b>DMA（直接存储器存取）方式</b>是一种完全由硬件控制的数据传输方法，具备中断方式"数据准备阶段 CPU 与外设并行工作"的优点，并在外设与内存之间建立了一条<b>直接数据通路</b>，信息传送无须经过 CPU，避免了保存与恢复 CPU 现场等复杂操作。特别适用于<b>磁盘、显卡、声卡、网卡等高速设备的大批量数据传输</b>，但硬件实现开销较高</p><p>DMA 方式中，中断的作用仅限于处理故障和正常传输完成的通知。</p>
<p><b>DMA 方式的特点</b>：①打破主存与 CPU 的固定关联，主存既可被 CPU 访问，又可被外设直接访问</p><p>②块数据传输过程中，主存地址的生成与传送字数的计数均由硬件电路自动完成</p><p>③主存中需设置专用缓冲区以支持高效数据交换</p><p>④支持高速数据传输，CPU 与外设可并行工作</p><p>⑤传输开始前需由软件预处理，结束后通过中断机制完成后处理。</p>
<p><b>DMA 控制器的组成</b>：DMA 控制器（DMA 接口）对数据传送过程进行控制，I/O 设备需要传送时通过 DMA 控制器向 CPU 提出 DMA 请求，CPU 响应后让出系统总线，由 DMA 控制器接管总线执行数据传送。主要部件：</p>
<ul>
<li><b>主存地址计数器</b>：存放待传送数据的主存地址；传送前保存整批数据起始地址，每传送一个字自动加 1，直至该批数据传送完毕。</li>
<li><b>传送长度计数器</b>：记录待传送数据的总长度；传送前存入总字数，每传送一个字计数值减 1，为 0 时表示传送结束。</li>
<li><b>数据缓冲寄存器</b>：暂存每次传送的数据；与主存之间以字为单位传送，与外设之间可能以字节或字为单位。</li>
<li><b>DMA 请求触发器</b>：I/O 设备准备好数据时发出 DMA 请求信号使其置位。</li>
<li><b>控制/状态逻辑</b>：设定传送方向、更新传送参数，协调 DMA 请求与 CPU 响应信号。</li>
<li><b>中断机构</b>：一批数据传送完毕后触发中断并向 CPU 发出中断请求。</li>
</ul>
<p>DMA 传送过程中 DMA 控制器接管系统总线，传送结束后将总线控制权交还 CPU，因此 DMA 控制器必须具备控制系统总线的能力。</p>
<p><b>DMA 的三种传送方式</b>（主存与 I/O 设备交换信息不经过 CPU，但 I/O 设备与 CPU 同时访问主存可能冲突）：</p>
<ul>
<li><b>停止 CPU 访存</b>：I/O 设备发出 DMA 请求时，DMA 控制器向 CPU 发送停止信号，使 CPU 放弃总线控制权并暂停访存，直至 DMA 完成整块数据传送后再交还总线。<ul>
<li><b>优点</b>：控制逻辑简单，适用于数据传输速率很高的 I/O 设备进行成组数据传送</li>
<li><b>缺点</b>：DMA 访存期间 CPU 基本处于空闲状态，资源利用率较低</li>
</ul></li>
<li><b>周期挪用</b>：高速设备不及时访存可能丢失数据，其访存请求优先级较高；周期挪用允许 I/O 设备挪用一个主存存储周期，传送完一个数据字后立即释放总线，属于<b>单字传送方式</b>。三种情况：<ul>
<li>① CPU 当前不在访存，无冲突可直接使用总线</li>
<li>② CPU 正在访存，需等待当前存储周期结束再释放总线控制权</li>
<li>③ I/O 与 CPU 同时请求访存发生冲突，CPU 暂时放弃总线控制权</li>
<li><b>评价</b>：优点——既满足 I/O 传送需求，又较好发挥 CPU 与主存效率；缺点——每次挪用均需申请并释放总线控制权，带来一定开销</li>
</ul></li>
<li><b>DMA 与 CPU 交替访存</b>：将 CPU 工作周期划分为两个子周期，一个供 CPU 访存、另一个供 DMA 访存，每个 CPU 周期内两者轮流访问主存；适用于 CPU 工作周期长于主存存储周期的场景（如 CPU 周期 1.2μs、主存存储周期小于 0.6μs）。<ul>
<li><b>优点</b>：无须总线控制权的申请与释放，数据传送速率高</li>
<li><b>缺点</b>：硬件控制逻辑较为复杂</li>
</ul></li>
</ul>
<p><b>DMA 的传送过程</b>，分为三阶段：</p>
<ul>
<li><b>预处理</b>：由 CPU 完成初始化——设置 DMA 控制器中的主存起始地址、传送方向、传送数据个数，并启动 I/O 设备；随后 CPU 继续执行原程序。设备准备好数据时向 DMA 控制器发出 DMA 请求，DMA 控制器随即向 CPU 发出总线请求，申请总线控制权。</li>
<li><b>数据传送</b>：DMA 以<b>数据块</b>为单位传送；获得总线控制权后，DMA 控制器通过硬件循环自动完成数据的输入或输出操作，整个过程无须 CPU 干预。</li>
<li><b>后处理</b>：数据块传送完成后，DMA 控制器向 CPU 发出中断请求；CPU 响应后执行中断服务程序进行后处理（如校验数据完整性，出错则转入诊断程序等）。</li>
</ul>
<p><b>效率计算例题</b>：主频 500MHz、CPI=4、外设数据率 40MB/s、数据端口 32 位、DMA 每次传送块 1000B、预处理和后处理总时钟周期数 500。DMA 每秒次数 = 40MB/s÷1000B = 40000 次</p><p>只有预处理和后处理需要 CPU（数据传送全程由 DMA 控制），CPU 用于外设 I/O 总时间 = 40000×500 = 2×10⁷ 个时钟周期，占 CPU 总时间百分比 = 2×10⁷÷(5×10⁸) = 4%。对比：同样 40MB/s 外设若用中断方式，中断响应与处理 400 周期（0.8μs），而外设填满 4B 缓冲区仅需 0.1μs，数据准备时间远小于中断处理时间，新数据会在处理完前次中断前到达导致缓冲区覆盖丢失，因此高速外设不适合单字中断方式。</p>
<p><b>DMA 方式和中断方式的区别</b>：①中断方式涉及程序切换，需保存和恢复 CPU 现场</p><p>DMA 方式不中断现行程序，无须保存现场，除预处理和后处理外完全不占用 CPU 资源。</p><p>②中断请求只能在每条指令执行结束后被响应</p><p>DMA 请求可在当前<b>总线周期</b>结束后立即获得响应，无须等待指令完成。</p><p>③中断方式的数据传送依赖 CPU 执行指令完成</p><p>DMA 方式由专用硬件直接在主存与外设间传送数据，传输速率高，适用于高速外设的成组数据传输。</p><p>④CPU 和 DMA 控制器同时访问主存时，DMA 请求的优先级通常更高。</p><p>⑤中断方式具备处理异常事件的能力，DMA 方式仅用于大批量数据的高效传输。</p><p>⑥从数据传送看，中断方式由软件控制传送，DMA 方式由硬件直接完成。</p>"""
        },
    ],
}
