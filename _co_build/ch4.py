# -*- coding: utf-8 -*-
"""计组笔记 · 第4章 指令系统（王道2027结构）"""

CH4 = {
    "chapter": "第4章",
    "title": "第4章 指令系统",
    "sections": [
        {
            "section": "4.1 指令系统",
            "html": """<h4>4.1 指令系统</h4>
<h5>4.1.1 指令集体系结构</h5>
<p><b>基本概念</b>：机器指令是指示计算机执行某种操作的命令，所有指令的集合构成<b>指令系统（指令集）</b>，是 ISA 中最核心的部分。ISA 完整定义了软件与硬件之间的接口，是机器语言/汇编语言程序员必须掌握的基础。</p>
<p><b>ISA 规定的内容</b>：</p>
<ul>
<li>指令格式、寻址方式、操作类型，以及各操作所需操作数的个数、类型和寻址约束。</li>
<li>操作数的数据类型，以及按大端还是小端方式存放。</li>
<li>程序可访问的寄存器编号、数量和位数，存储空间的大小及编址方式。</li>
<li>程序员可见的控制状态，如程序计数器（PC）、条件码的定义与行为等。</li>
</ul>
<p><b>意义</b>：ISA 规定了机器级程序的格式</p><p>高级语言抽象层较高，隐藏机器级细节，难以利用与硬件相关的优化手段提升性能，理解 ISA 及底层硬件特性可开发出性能更优的程序。</p>
<h5>4.1.2 指令的基本格式</h5>
<p><b>指令构成</b>：一条指令由操作码和地址码组成。操作码指明指令应执行的操作及功能，是识别指令、确定地址码含义的关键</p><p>地址码给出被操作信息（指令或数据）的地址，包括源操作数地址、目的操作数地址、转移目标地址或子程序入口地址等。</p>
<p><b>指令字长</b>：一条指令包含的二进制位数，取决于操作码长度、地址码长度及地址码个数。<b>定长指令字结构</b>：所有指令字长相等，取指和译码简单，有利于流水线实现</p><p><b>变长指令字结构</b>：指令字长随功能而异。主存通常按字节编址，指令字长通常为字节的整数倍。</p>
<p><b>按地址码数目分类</b>：</p>
<ul>
<li><b>零地址指令</b>：仅含操作码无显式地址。两种情况：①无须操作数的指令（空操作、停机、关中断）；<br>②堆栈计算机的运算指令，操作数隐含从栈顶和次栈顶弹出，结果压回栈顶。</li>
<li><b>一地址指令</b>：①单操作数指令 OP(A₁)→A₁（如加1、减1、求反、移位）；<br>②累加器型双操作数指令 (ACC)OP(A₁)→ACC。若地址码为主存地址，完成一条需 <b>3 次访存</b>（取指1、取数1、存结果1）。</li>
<li><b>二地址指令</b>：(A₁)OP(A₂)→A₁，A₁ 为目的操作数地址（兼结果地址）。若地址码均为主存地址，需 <b>4 次访存</b>（取指1、取两操作数2、存结果1）。</li>
<li><b>三地址指令</b>：(A₁)OP(A₂)→A₃，A₁、A₂ 为源操作数地址，A₃ 为结果地址。若地址码均为主存地址，需 <b>4 次访存</b>。</li>
</ul>
<h5>4.1.3 定长操作码指令格式</h5>
<p><b>定长操作码</b>：在指令字最高位部分分配固定位数表示操作码。n 位操作码字段的指令系统<b>最多可表示 2ⁿ 条指令</b>。有利于简化硬件设计、提高译码速度，在字长 32 位或更长的系统中是常规用法。</p>
<h5>4.1.4 扩展操作码指令格式</h5>
<p><b>扩展操作码</b>：为在有限指令字长内支持丰富指令种类，采用可变长度操作码（位数不固定、分散在不同位置）。扩展操作码使<b>操作码长度随地址码减少而增加</b>，有效缩短指令字长。</p>
<p><b>典型安排</b>：指令字长 16 位，4 位基本操作码 + 3 个 4 位地址字段 → 15 条三地址指令（保留 1111 作扩展）+ 15 条二地址（保留 1111 1111）+ 15 条一地址（保留 1111 1111 1111）+ 16 条零地址。也可有其他组合（如 15/12/63/16 共 106 条）。</p>
<p><b>设计注意两点</b>：①不允许<b>短操作码成为长操作码的前缀</b>（短操作码不能与长操作码的前面部分相同）</p><p>②各指令的操作码一定不能重复。对使用频率高的指令分配较短操作码，频率低的分配较长操作码，减少译码和分析时间。</p>
<h5>4.1.5 指令的类型</h5>
<ul>
<li><b>数据传送指令</b>：MOV（寄存器间传送）、LOAD（内存读入 CPU）、STORE（CPU 写入内存）、PUSH、POP。</li>
<li><b>算术和逻辑运算指令</b>：ADD、SUB、MUL、DIV、INC、DEC、AND、OR、NOT、XOR 等。</li>
<li><b>移位操作指令</b>：算术移位、逻辑移位、循环移位。</li>
<li><b>顺序控制指令</b>：无条件转移 JMP、条件转移 BRANCH、调用 CALL、返回 RET、陷阱 TRAP。<b>JMP 任何情况都转移，条件转移仅在特定条件满足时转移</b>（转移条件通常是标志位的值或其组合）。<b>CALL 与 JMP 的区别：CALL 会保存下一条指令的地址（返回地址），子程序结束后能返回主程序；JMP 不返回</b>。</li>
<li><b>输入输出指令</b>：完成 CPU 与外部设备交换数据或传送控制命令及状态信息。</li>
<li><b>CPU 控制指令</b>：停机、开/关中断、系统模式切换、进入特殊处理程序等；只能在操作系统内核代码中使用，防止用户误用危害系统。</li>
</ul>"""
        },
        {
            "section": "4.2 指令的寻址方式",
            "html": """<h4>4.2 指令的寻址方式</h4>
<h5>4.2.1 指令寻址和数据寻址</h5>
<p><b>寻址方式</b>：确定指令或操作数有效地址的方法，分为<b>指令寻址</b>（确定下一条待执行指令的地址）和<b>数据寻址</b>（确定本条指令操作数的地址）两大类。</p>
<p><b>指令寻址</b>：</p>
<ul>
<li><b>顺序寻址</b>：通过程序计数器 PC 自动加 1（1 = 一条指令长度）给出下一条指令地址，PC 自增量与指令字长、编址方式有关（字节编址时每取一条指令 PC 加指令字长的字节数）。</li>
<li><b>跳跃寻址</b>：由本条指令给出下条指令地址（转移指令），分为绝对转移（给出目标地址）和相对转移（给出偏移量，目标 = (PC) + 偏移量）。</li>
</ul>
<p><b>数据寻址</b>：指令中地址码字段通常不直接给出操作数地址，而是给出<b>形式地址 A</b></p><p>A 与寻址特征结合经计算得到<b>有效地址 EA</b>，EA 对应主存中操作数的实际地址。寻址特征字段用来指出采用何种寻址方式。</p>
<h5>4.2.2 数据寻址方式</h5>
<p>常用寻址方式（有效地址与访存次数，不含取本条指令）：</p>
<p><b>表4.1 寻址方式、有效地址及访存次数</b>：</p>
<table>
<tr><th>寻址方式</th><th>有效地址</th><th>访存次数</th></tr>
<tr><td>立即寻址</td><td>A 即是操作数</td><td>0</td></tr>
<tr><td>直接寻址</td><td>EA=A</td><td>1</td></tr>
<tr><td>一次间接寻址</td><td>EA=(A)</td><td>2</td></tr>
<tr><td>寄存器寻址</td><td>EA=R</td><td>0</td></tr>
<tr><td>寄存器间接一次寻址</td><td>EA=(R)</td><td>1</td></tr>
<tr><td>相对寻址</td><td>EA=(PC)+A</td><td>1</td></tr>
<tr><td>基址寻址</td><td>EA=(BR)+A</td><td>1</td></tr>
<tr><td>变址寻址</td><td>EA=(IX)+A</td><td>1</td></tr>
</table>
<ul>
<li><b>隐含寻址</b>：操作数地址隐含在指令操作码或某个专用寄存器中（如累加器 ACC），指令字中少一个地址字段。</li>
<li><b>立即寻址</b>：形式地址 A 就是操作数本身（补码表示），<b>无须访存，执行速度最快</b>，但 A 位数限制了立即数范围。</li>
<li><b>直接寻址</b>：EA = A，只需 1 次访存取操作数；但<b>寻址范围受 A 位数限制</b>。</li>
<li><b>间接寻址</b>：EA = (A)（A 的内容是操作数地址），需 <b>2 次访存</b>（先取 EA 再取操作数）；扩大了寻址范围，便于编制程序，但速度慢。</li>
<li><b>寄存器寻址</b>：EA = R（操作数在寄存器中），指令中给出寄存器编号；不访存、速度快，指令字短。</li>
<li><b>寄存器间接寻址</b>：EA = (R)（寄存器内容为操作数地址），只需 <b>1 次访存</b>；比间接寻址少 1 次访存，比寄存器寻址多 1 次。</li>
<li><b>相对寻址</b>：EA = (PC) + A，A 为相对偏移量（补码，可正可负）；转移目标随 PC 浮动，<b>便于程序浮动（重定位）</b>，广泛用于转移指令。</li>
<li><b>基址寻址</b>：EA = (BR) + A，基址寄存器 BR 内容<b>由操作系统设定、运行时不可变</b>，面向系统，支持多道程序和存储分配。</li>
<li><b>变址寻址</b>：EA = (IX) + A，变址寄存器 IX 内容<b>由用户动态修改</b>，A 保持不变；特别适用于<b>数组遍历和循环程序</b>（例：数组 B 首址 1000H 存于 A，IX 置 0CH 即可访问 B[3]）。</li>
<li><b>堆栈寻址</b>：操作数地址由堆栈指针 SP 隐含指定，SP 自动更新；硬堆栈（寄存器构成，成本高容量小）vs 软堆栈（主存划分，经济实用）。</li>
</ul>
<p><b>偏移寻址范畴</b>：<b>相对寻址、基址寻址、变址寻址均属偏移寻址</b>，共同点是通过某个寄存器的值与形式地址相加确定有效地址。</p>
<p><b>基址 vs 变址</b>：地址形成相同（寄存器内容 + 形式地址），但基址寄存器由操作系统管理、偏移量 A 可变</p><p>变址寄存器由程序员控制、偏移量 A 固定。</p>
<p><b>访存次数总结</b>（取操作数，不含取指令）：立即 0 次、寄存器 0 次、直接 1 次、寄存器间接 1 次、间接 2 次</p><p>相对/基址/变址各 1 次。寻址方式丰富则程序简洁高效，但增加译码逻辑复杂度</p><p>过少则简化硬件但限制程序表达能力。</p>"""
        },
        {
            "section": "4.3 程序的机器级代码表示",
            "html": """<h4>4.3 程序的机器级代码表示</h4>
<p>本节为 2022 年新增考点。统考大纲未指定指令集，历年真题主要考查 <b>x86 和 MIPS</b> 汇编指令：MIPS 通常在试题中附带功能说明，x86 更常作为默认考查对象，本节重点介绍 x86 汇编。</p>
<h5>4.3.1 常用汇编指令介绍</h5>
<p><b>1. 相关寄存器</b>：x86 有 8 个 32 位通用寄存器——EAX（累加器）、EBX（基地址）、ECX（计数）、EDX（数据）、ESI/EDI（变址）、EBP（堆栈基指针）、ESP（堆栈顶指针）。EAX/EBX/ECX/EDX 的低 16 位可独立使用（AX/BX/CX/DX），每个 16 位寄存器又可拆为两个 8 位寄存器（AX → AH/AL）。除 EBP、ESP 外，其余通用寄存器用途较灵活。</p>
<p><b>2. 操作数标记</b>：&lt;reg&gt; 寄存器、&lt;mem&gt; 内存地址（[eax]、[var+4]、dword ptr [eax+ebx]）、&lt;con&gt; 常数。</p>
<p><b>3. 数据传送指令</b>：x86 采用<b>变长编码</b>，操作码通常 1 字节，同一指令（如 mov）因操作数类型不同对应多种机器码（mov ax,&lt;con16&gt; → B8H、mov al,&lt;con8&gt; → B0H 等）。</p>
<ul>
<li><b>mov</b>：复制第二个操作数到第一个操作数；<b>两个操作数不能同时为内存</b>（内存间复制需经寄存器中转）。</li>
<li><b>push</b>：入栈前先将 ESP 减 4（栈向低地址方向增长），再将操作数压入 ESP 所指地址；栈中元素固定为 32 位。</li>
<li><b>pop</b>：先读出 ESP 所指内容，再将 ESP 加 4。</li>
</ul>
<p><b>4. 算术和逻辑运算指令</b>：</p>
<ul>
<li><b>add/sub</b>：结果保存在第一个操作数；<b>inc/dec</b>：自增/自减 1。</li>
<li><b>imul</b>：有符号乘法，双操作数/三操作数格式，结果存入第一个操作数（必须为寄存器）。</li>
<li><b>mul</b>：无符号乘法，仅单操作数，被乘数隐含在 eax，结果存 <b>edx:eax</b>；edx≠0 时置 CF=1、OF=1。<b>无符号乘法以 CF 判断溢出，有符号（imul）以 OF 为准</b>。</li>
<li><b>idiv/div</b>：除法指令仅指定除数，被除数为 <b>edx:eax</b> 组成的 64 位数，执行后<b>商存 eax、余数存 edx</b>。</li>
<li><b>and/or/xor</b>：按位与/或/异或，结果存入第一个操作数（xor edx,edx 常用于清零）；<b>not</b>：按位取反；<b>neg</b>：取负（求补码 -x）。</li>
<li><b>shl/shr</b>：逻辑左移/右移，第一个操作数为被移位对象，第二个为移位位数（常数或 cl）。</li>
</ul>
<p><b>5. 控制流指令</b>：EIP（指令指针寄存器，相当于 PC）指示当前指令地址，<b>不能直接访问</b>，只能通过控制流指令修改</p><p>程序用标签（label）标记指令地址。</p>
<ul>
<li><b>jmp</b>：无条件转移到 label。</li>
<li><b>jcondition</b>：条件转移，根据标志位决定是否转移——je/jz（相等/为零）、jne（不等）、jg/jge（大于/大于等于）、jl/jle（小于/小于等于）等。</li>
<li><b>cmp/test</b>：cmp 执行减法、test 执行按位与，<b>均不保存结果、仅设置标志位</b>，常与 jcondition 配合（cmp var,10; jne loop；test eax,eax; jz xxxx）。</li>
<li><b>call/ret</b>：call 将<b>下一条指令地址（返回地址）压栈</b>后转移到 label；ret 从栈顶弹出返回地址并跳转。</li>
</ul>
<p><b>6. AT&T 格式 vs Intel 格式</b>（统考常涉及 Intel 格式）六点区别：</p>
<ul>
<li>AT&T 指令名必须小写，Intel 大小写不敏感。</li>
<li>操作数顺序：AT&T 为"源，目的"，Intel 为"目的，源"。</li>
<li>AT&T 寄存器前加 %、立即数前加 $；Intel 均无前缀。</li>
<li>内存寻址：AT&T 用圆括号 ()，Intel 用方括号 []。</li>
<li>复杂寻址：AT&T "disp(base,index,scale)"（如 8(%edx,%eax,2) 表示 R[edx]+R[eax]×2+8）；Intel "[edx+eax*2+8]"。</li>
<li>长度指定：AT&T 在助记符后加后缀 b/w/l；Intel 用 byte ptr/word ptr/dword ptr 前缀。</li>
</ul>
<p><b>注意</b>：x86 中 32/64 位均由 16 位扩展而来，<b>word（字）恒为 16 位</b></p><p>lea 指令将有效地址（而非内存内容）加载到寄存器。</p>
<p><b>表4.2 AT&T 格式指令和 Intel 格式指令的对比</b>：</p>
<table>
<tr><th>AT&T 格式</th><th>Intel 格式</th><th>含义</th></tr>
<tr><td>mov $100,%eax</td><td>mov eax,100</td><td>100→R[eax]</td></tr>
<tr><td>mov %ebx,%eax</td><td>mov eax,ebx</td><td>R[eax]→R[ebx]</td></tr>
<tr><td>mov %eax,(%ebx)</td><td>mov [ebx],eax</td><td>R[eax]→M[R[ebx]]</td></tr>
<tr><td>mov %eax,-8(%ebp)</td><td>mov [ebp-8],eax</td><td>R[eax]→M[R[ebp]-8]</td></tr>
<tr><td>lea (%edx,%eax,2),%eax</td><td>lea eax,[edx+eax*2+8]</td><td>R[edx]+R[eax]*2+8→R[eax]</td></tr>
<tr><td>movl %eax,%ebx</td><td>mov dword ptr [ebx],eax</td><td>32 位 R[eax]→M[R[ebx]]</td></tr>
</table>
<p>注：R[r] 表示寄存器 r 的内容，M[adr] 表示主存单元 addr 的内容，→ 或 ← 表示信息传送方向。</p>
<h5>4.3.2 选择语句的机器级表示</h5>
<p><b>条件码</b>：描述最近算术/逻辑运算结果属性的标志位，常用 <b>CF、ZF、SF、OF</b>。add/sub/imul/or/and/shl/inc/dec/not 等指令执行时自动设置条件码</p><p>cmp（相当于 sub）、test（相当于 and）专门设置条件码但不保存结果。C 语言中 test_expr 值为 0 表示假、非 0（含负数）为真。</p>
<p><b>if-else 翻译模板</b>：条件为假则 goto false 进入假分支</p><p>真分支执行后 goto done 跳过假分支。例：get_cont 函数比较指针 p1、p2——先加载参数到寄存器，cmp 比较，<b>指针比较在 C 中按无符号整数处理，故用 jbe（无符号小于等于）</b>转移；返回值放 eax。</p>
<h5>4.3.3 循环语句的机器级表示</h5>
<p>x86 没有直接对应高级循环结构的指令，编译器通过条件测试（cmp/test）和条件转移（je/jne/jg/jle）组合实现，大多数编译器将三种循环统一转换为 <b>do-while 形式</b>。</p>
<ul>
<li><b>do-while</b>（后测试）：循环体必执行一次——loop: 循环体；t=条件；if(t) goto loop。</li>
<li><b>while</b>（前测试）：可能一次都不执行——先 t=初始条件，if(!t) goto done；再转为带前置判断的 do-while。</li>
<li><b>for</b>：完全等价于 while——init_expr; while(test_expr){ body; update_expr; }，再按 while 转 goto。</li>
</ul>
<p><b>例 nsum_for</b>：mov ecx,[ebp+8]（加载参数 n）</p><p>eax=0（result）、edx=1（i）</p><p>cmp edx,ecx; jg .L2 前置判断</p><p>.L1: add eax,edx（result+=i）、add edx,1（i++）、cmp edx,ecx; jle .L1。i、n 为 int 型，<b>有符号比较用 jle/jg</b></p><p>频繁使用的局部变量被优化到寄存器中（result 在 eax 兼返回值，i 在 edx）。</p>
<h5>4.3.4 过程调用的机器级表示</h5>
<p><b>调用-返回六步骤</b>：①调用者 P 将入口参数放到 Q 能访问的位置</p><p>②P 执行 call，把返回地址压栈并转移到 Q</p><p>③Q 建立栈帧、为局部变量分配空间、必要时保存寄存器</p><p>④Q 执行主体</p><p>⑤Q 将返回结果放约定位置、释放局部变量空间、恢复寄存器</p><p>⑥Q 执行 ret 弹出返回地址转回 P。</p>
<p><b>寄存器保存约定</b>：<b>EAX、ECX、EDX 为调用者保存寄存器</b>（P 若希望在调用后继续使用须在调用前自行压栈保存）</p><p><b>EBX、ESI、EDI 为被调用者保存寄存器</b>（Q 若使用须先压栈保存、返回前恢复）。</p>
<p><b>栈帧</b>：每个过程在运行时栈中分配专属区域。EBP（基址指针）指向当前栈帧基址（保存旧 EBP 的位置），ESP 始终指向栈顶</p><p><b>栈从高地址向低地址增长</b></p><p>执行期间 ESP 动态变化、EBP 保持不变，便于通过固定偏移访问参数和局部变量。</p>
<p><b>例 caller 调用 add</b>：caller 汇编——push ebp（保存旧 EBP）→ mov ebp,esp（建立新栈帧）→ sub esp,24（分配空间）</p><p>局部变量 temp1=125、temp2=80、sum 占 12 字节（[ebp-12]、[ebp-8]、[ebp-4]）</p><p><b>入口参数右参数在较高地址</b>：将 temp2 放 [esp+4]、temp1 放 [esp]</p><p>call add 后返回值在 eax，mov [ebp-4],eax 存入 sum</p><p>最后 <b>leave 等价于 mov esp,ebp + pop ebp</b>，再 ret。GCC 规定每个函数栈帧大小为 16 字节的倍数（实际用 28 字节 → 分配 32 字节，4 字节对齐填充）。</p>
<p><b>add 过程体</b>：push ebp; mov ebp,esp（准备）</p><p>mov eax,[ebp+12]（y）、mov edx,[ebp+8]（x）、lea eax,[edx+eax]（lea 执行加法 R[edx]+R[eax]=x+y 存回 eax 作返回值）</p><p>pop ebp; ret（结束）。参数 x、y 分别在 R[ebp]+8、R[ebp]+12。<b>一个过程的机器级代码分三部分：准备阶段、过程体、结束阶段</b>。add 无局部变量、未用被调用者保存寄存器、不再调用其他过程，栈帧只需保存 EBP。</p>"""
        },
        {
            "section": "4.4 CISC和RISC的基本概念",
            "html": """<h4>4.4 CISC和RISC的基本概念</h4>
<p>指令系统两个发展方向：<b>CISC</b>（复杂指令系统计算机，通过增强原有指令功能、引入更复杂指令，将部分软件功能固化到硬件，典型代表 x86）</p><p><b>RISC</b>（精简指令系统计算机，精简指令集、简化指令功能以提升执行效率，典型代表 ARM、MIPS）。</p>
<h5>4.4.1 CISC（复杂指令系统计算机）</h5>
<p><b>CISC 的特点</b>：</p>
<ul>
<li>指令系统庞大复杂，指令数量通常超过 200 条。</li>
<li>指令长度不固定，指令格式和寻址方式种类繁多。</li>
<li>多数指令均可直接访问内存。</li>
<li>各类指令的使用频率差异显著。</li>
<li>指令执行时间相差较大，大多数指令需多个时钟周期才能完成。</li>
<li>控制器多采用微程序控制，部分复杂指令难以用硬连线逻辑实现。</li>
<li>难以通过优化编译生成高效的目标代码。</li>
</ul>
<p><b>80/20 规律</b>：对传统 CISC 指令系统统计分析表明，约 80% 的程序执行仅依赖 20% 的简单指令，其余 80% 的复杂指令使用频率极低——由此催生 RISC 架构（仅保留高频简单指令，用组合实现复杂功能）。</p>
<h5>4.4.2 RISC（精简指令系统计算机）</h5>
<p><b>RISC 的特点</b>：</p>
<ul>
<li>仅选取使用频率最高的简单指令，复杂功能由多条简单指令组合实现。</li>
<li>指令长度固定，指令格式和寻址方式种类较少。</li>
<li><b>仅 LOAD/STORE（取数/存数）指令可访问内存</b>，其余运算均在寄存器之间进行（寄存器-寄存器操作）。</li>
<li>CPU 中配备大量通用寄存器。</li>
<li>普遍采用指令流水线技术，绝大多数指令在一个时钟周期内完成。</li>
<li>以硬布线（组合逻辑）控制为主，极少或完全不用微程序控制。</li>
<li>高度依赖编译器优化，以缩短程序执行时间。</li>
</ul>
<p><b>兼容性</b>：CISC 通常支持向后兼容（新机型包含旧机型全部指令并扩展）</p><p>RISC 大幅简化指令集、改变指令格式，通常无法与老机型兼容。现实中 x86 凭借庞大软件生态占据主流，现代 CISC 处理器已在内部融合大量 RISC 思想，两者性能差距日益缩小。</p>
<h5>4.4.3 CISC 和 RISC 的比较</h5>
<p><b>RISC 的优势</b>：①更高效利用芯片面积——CISC 微程序控制存储器占芯片面积 50% 以上，RISC 硬布线逻辑仅占约 10%</p><p>②更高的运算速度——指令少、格式统一、寻址简单，配合大量通用寄存器和流水线，多数指令单周期完成</p><p>③更易设计与维护——结构简单、设计周期短、逻辑清晰、出错概率低</p><p>④更利于编译优化——指令类型和寻址方式有限，编译器更易选择最优指令序列。</p>
<p><b>表4.3 CISC 与 RISC 的对比</b>：</p>
<table>
<tr><th rowspan="2">对比项目</th><th colspan="2">类别</th></tr>
<tr><th>CISC</th><th>RISC</th></tr>
<tr><td>指令系统</td><td>复杂，庞大</td><td>简单，精简</td></tr>
<tr><td>指令数目</td><td>一般大于 200 条</td><td>一般小于 100 条</td></tr>
<tr><td>指令字长</td><td>不固定</td><td>定长</td></tr>
<tr><td>可访存指令</td><td>不加限制</td><td>只有 LOAD/STORE 指令</td></tr>
<tr><td>各种指令执行时间</td><td>相差较大</td><td>绝大多数在一个周期内完成</td></tr>
<tr><td>各种指令使用频度</td><td>相差很大</td><td>都比较常用</td></tr>
<tr><td>通用寄存器数量</td><td>较少</td><td>多</td></tr>
<tr><td>目标代码</td><td>难以用优化编译生成高效的目标代码程序</td><td>采用优化的编译程序，生成代码较为高效</td></tr>
<tr><td>控制方式</td><td>绝大多数为微程序控制</td><td>绝大多数为组合逻辑控制</td></tr>
<tr><td>指令流水线</td><td>可以通过一定方式实现</td><td>必须实现</td></tr>
</table>
<p><b>LOAD/STORE 型指令特点</b>：访存与计算分离（只有 LOAD/STORE 能访问主存，运算指令仅操作寄存器）</p><p>指令格式规整（寄存器编号位数远少于内存地址，固定字段分配可实现定长指令，简化译码）</p><p>利于流水线执行（统一指令长度和访存边界，取指/译码/执行易于并行）</p><p>潜在缺点——频繁数据搬移使 LOAD/STORE 指令比例较高、增加指令条数，但编译器优化和高速缓存可缓解。</p>"""
        }
    ]
}
