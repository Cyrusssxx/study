# -*- coding: utf-8 -*-
"""计组笔记 · 第2章 数据的表示和运算（王道2027结构，合并袁书补充）"""

CH2 = {
    "chapter": "第2章",
    "title": "第2章 数据的表示和运算",
    "sections": [
        {
            "section": "2.1 数制与编码",
            "html": """<h4>2.1 数制与编码</h4>
<h5>2.1.1 进位计数制及其相互转换</h5>
<p><b>为什么采用二进制</b>：</p>
<ul>
<li>二进制只有 0/1 两个状态，只需具有两种稳定物理状态的器件即可表示一位（高/低电平），硬件实现成本低。</li>
<li>1 和 0 恰好对应逻辑值"真"与"假"，为逻辑运算和条件判断提供直接支持。</li>
<li>二进制运算规则极为简单，可通过基本逻辑门电路高效实现各类算术与逻辑操作。</li>
</ul>
<p><b>进位计数制三要素</b>：<b>基数</b>（每个数位可使用的不同数码个数，如十进制基数 10）、<b>位权</b>（某位的实际值 = 数码 × 位权）、<b>逢基进一</b>。一个 r 进制数的数值 = 各位数码与其位权乘积之和（按权展开）。</p>
<ul>
<li><b>二进制</b>：基数 2，数码 0/1，逢二进一，第 i 位位权 2ⁱ。</li>
<li><b>八进制</b>：基数 8，数码 0~7，每 3 位二进制对应 1 位八进制。</li>
<li><b>十六进制</b>：基数 16，数码 0~9 和 A~F（A~F 代表 10~15），每 4 位二进制对应 1 位十六进制。</li>
<li><b>后缀标识</b>：B=二进制、O=八进制、D=十进制（常省略）、H=十六进制；也常用前缀 0x 表示十六进制。</li>
</ul>
<p><b>进制转换方法</b>：</p>
<ul>
<li><b>二进制 ↔ 八/十六进制</b>：以小数点为界分组（整数部分向左、小数部分向右每 3/4 位一组），不足在高位/低位补 0。例：1111000010.01101 = (1702.32)₈ = (3C2.68)₁₆。</li>
<li><b>任意进制 → 十进制</b>：按权展开相加。例：(11011.1)₂ = 1×2⁴+1×2³+0×2²+1×2¹+1×2⁰+1×2⁻¹ = 27.5。</li>
<li><b>十进制 → 任意进制</b>：整数部分<b>除基取余</b>（先得余数为最低位），小数部分<b>乘基取整</b>（先得整数为最高位），两部分拼接。例：123.6875 = (1111011.1011)₂。</li>
</ul>
<p><b>精确表示条件</b>：一个十进制小数能用有限位二进制精确表示，当且仅当它可以表示成形如 k/2ⁿ 的分数。如 0.3 = 3/10，10 的质因数含 5，无法有限表示</p><p>任何有限位二进制小数都能精确转回十进制。</p>
<h5>2.1.2 定点数的编码表示</h5>
<p><b>真值与机器数</b>：带"+"-"-"号的数称为<b>真值</b></p><p>符号数字化的表示称为<b>机器数</b>（0 表示正、1 表示负）。机器内部没有小数点，定点小数/定点整数只是人为约定的小数点位置，编码与运算无需区分。</p>
<ul>
<li><b>定点小数</b>：约定小数点位于符号位之后、数值最高位之前。</li>
<li><b>定点整数</b>：约定小数点位于数值部分最低位之后。</li>
</ul>
<p><b>原码</b>：最高位为符号位，其余位表示绝对值。n+1 位原码整数范围 -(2ⁿ-1) ~ 2ⁿ-1（关于原点对称）</p><p><b>零有两种表示</b>：[+0]=0,0000000、[-0]=1,0000000。优点是与真值对应关系简单直观，缺点是加减运算复杂。</p>
<p><b>补码</b>：正数同原码</p><p>负数 = 原码数值位取反加 1（或按模 2ⁿ⁺¹ 定义 [x]补 = 2ⁿ⁺¹+x）。n+1 位补码范围 -2ⁿ ~ 2ⁿ-1，<b>比原码/反码多表示一个最小负数 -2ⁿ</b>（如 8 位补码最小 -128）。零的表示唯一（00000000）。<b>变形补码（双符号位）</b>：00 正、11 负、01/10 溢出，用于溢出检测。</p>
<p><b>反码</b>：负数 = 原码数值位全部取反，符号位不变。范围 -(2ⁿ-1) ~ 2ⁿ-1，零也有两种表示。考试以了解为主。</p>
<p><b>移码</b>：[x]移 = 偏置量 + x（n 位数值的偏置常取 2ⁿ，IEEE 754 阶码偏置取 2ᵏ⁻¹-1）。<b>全 0 表示最小值、全 1 表示最大值</b>，大小比较直观</p><p>与补码仅符号位相反（符号位取反即得补码）。</p>
<p><b>四码要点</b>：</p>
<ul>
<li>补码判断大小：正数按数值位直接比；<b>负数数值位部分越小（越接近全 0），其绝对值越大，即负得越多</b>。</li>
<li>补码转原码（求真值）：从右向左找到第一个 1，该位及其右边不变，左边（除符号位）全部取反。</li>
<li>真值范围速记：n 位补码最小 1,00...0（-2ⁿ⁻¹）、最大 0,11...1（2ⁿ⁻¹-1）；32 位补码范围 -2³¹ ~ 2³¹-1。</li>
</ul>
<h5>2.1.3 整数的表示</h5>
<p><b>无符号整数</b>：全部二进制位均用于表示数值。相同字长下能表示的最大值大于有符号整数</p><p>适用于地址运算、指针等非负场景。8 位无符号范围 0~255。</p>
<p><b>有符号整数</b>：现代计算机统一采用<b>补码</b>，原因：①零的表示唯一</p><p>②符号位可与数值位一同参与运算，<b>加减法统一为加法</b></p><p>③表示范围更大（多一个最小负数）。n 位补码范围 -2ⁿ⁻¹ ~ 2ⁿ⁻¹-1。</p>
<p><b>表2.1 y与x的位级表示对比</b>：</p>
<table>
<tr><th rowspan="2">变量</th><th rowspan="2">值</th><th colspan="16">二进制位（15..0）</th></tr>
<tr><th>15</th><th>14</th><th>13</th><th>12</th><th>11</th><th>10</th><th>9</th><th>8</th><th>7</th><th>6</th><th>5</th><th>4</th><th>3</th><th>2</th><th>1</th><th>0</th></tr>
<tr><td>x</td><td>-4321</td><td>1</td><td>1</td><td>1</td><td>0</td><td>1</td><td>1</td><td>1</td><td>1</td><td>0</td><td>0</td><td>0</td><td>1</td><td>1</td><td>1</td><td>1</td><td>1</td></tr>
<tr><td>y</td><td>61215</td><td>1</td><td>1</td><td>1</td><td>0</td><td>1</td><td>1</td><td>1</td><td>1</td><td>0</td><td>0</td><td>0</td><td>1</td><td>1</td><td>1</td><td>1</td><td>1</td></tr>
</table>
<h5>2.1.4 C语言中的整数类型及类型转换</h5>
<p><b>整型数据类型</b>：short 通常 16 位；int 通常 32 位</p><p>long 在 32 位系统为 32 位、64 位系统通常 64 位</p><p>char 通常 8 位（特殊整型）</p><p>加 unsigned 为无符号类型，未显式指定则默认为有符号。所有有符号整型以补码存储。</p>
<p><b>类型转换三种情形</b>：</p>
<ul>
<li><b>长 → 短（位截断）</b>：直接丢弃高位、保留低位，<b>不触发任何异常，隐蔽性强</b>。例：int x=165537（0x000286A1）→ short 得 0x86A1 = -31071。</li>
<li><b>相同字长（仅改变解释方式）</b>：位模式不变。例：short x=-4321（0xEF1F）→ unsigned short y = 61215。</li>
<li><b>短 → 长（位扩展）</b>：源为<b>无符号数 → 零扩展</b>（高位补 0）；源为<b>有符号补码 → 符号扩展</b>（高位重复符号位）。例：x=-4321（0xEF1F）→ int 得 0xFFFFEF1F 仍为 -4321；u=61215（0xEF1F）→ 0x0000EF1F 仍为 61215。</li>
</ul>
<p><b>易错点</b>：</p>
<ul>
<li>位截断产生的数值变化是<b>语义变化</b>，不是运算溢出，硬件不会报告。</li>
<li>signed 转等长 unsigned：符号位成为数据一部分，负数会变成大正数（如 -1 → 65535/4294967295）。</li>
<li>char 通常按无符号整数解释。</li>
</ul>
<h5>2.1.5 字符编码（袁书补充）</h5>
<ul>
<li><b>ASCII</b>：7 位编码，128 个字符。0-31 + 127 为控制字符；32-126 为可打印字符（数字 0-9：48-57；大写 A-Z：65-90；小写 a-z：97-122）。</li>
<li><b>Unicode</b>：统一字符集，包含各国文字。常用 UTF-8（1-4 字节变长）、UTF-16（2/4 字节）、UTF-32（4 字节）。</li>
<li><b>UTF-8 编码规则</b>：1 字节 0xxxxxxx（兼容 ASCII）；2 字节 110xxxxx 10xxxxxx；3 字节 1110xxxx 10xxxxxx 10xxxxxx（基本多文种平面 BMP）；4 字节 11110xxx 10xxxxxx 10xxxxxx 10xxxxxx。</li>
</ul>"""
        },
        {
            "section": "2.2 运算方法和运算电路",
            "html": """<h4>2.2 运算方法和运算电路</h4>
<h5>2.2.1 基本运算部件</h5>
<p><b>运算器组成</b>：算术逻辑单元（ALU）+ 移位器 + 程序状态字（PSW）+ 寄存器组。</p>
<p><b>一位全加器</b>：输入 A、B、低位进位 Cᵢₙ，输出和 S = A⊕B⊕Cᵢₙ，进位 Cₒᵤₜ = AB + (A⊕B)·Cᵢₙ = AB + ACᵢₙ + BCᵢₙ。</p>
<ul>
<li><b>串行进位加法器</b>：进位逐级传递，<b>延迟由进位链决定、与位数成正比</b>（位数越多越慢）。</li>
<li><b>并行进位（CLA，超前进位）</b>：并行生成各位进位，延迟与位数无关（*非考纲，了解）。</li>
</ul>
<p><b>带标志加法器</b>：在 n 位加法器基础上扩展标志位生成逻辑：</p>
<ul>
<li><b>OF（溢出）</b> = Cₙ⊕Cₙ₋₁（最高位进位 ⊕ 次高位进位），仅对有符号数有意义。</li>
<li><b>SF（符号）</b> = 运算结果最高位，仅对有符号数有意义。</li>
<li><b>ZF（零）</b> = 结果为全 0，对有符号/无符号数均有意义。</li>
<li><b>CF（进位/借位）</b> = Sub⊕Cₙ₊₁（做加法时 Cₙ₊₁ 为进位，做减法时为其取反表示借位），仅对无符号数有意义。</li>
</ul>
<p><b>ALU</b>：运算器核心部件，其功能选择端 ALUop 的位数决定可执行操作类型的数量（k 位可区分 2ᵏ 种操作）</p><p>核心是一个带标志加法器加移位器，可完成加、减、与、或、非、异或等运算。</p>
<h5>2.2.2 定点数的移位运算</h5>
<p><b>逻辑移位</b>：左移低位补 0，右移高位补 0。<b>左移时若最高位 1 被移出则发生溢出</b>（丢弃后结果错误）。逻辑左移 1 位 = 无符号数乘 2，右移 1 位 = 无符号数除 2。</p>
<p><b>算术移位</b>：左移低位补 0、<b>移出的最高位与原符号位不同则溢出</b></p><p>右移高位补<b>符号位</b>（保号）。算术左移 1 位 = 有符号数乘 2，算术右移 1 位 = 有符号数除 2（负数右移向 -∞ 方向取整，如 -7 右移 1 位得 -4）。</p>
<p><b>循环移位</b>：移出的位填入另一端空出的位（循环左移/右移）</p><p><b>带进位循环移位</b>中 CF 参与循环链。循环移位不改变数据位的内容，常用于密码等领域；不改变符号性。</p>
<p><b>易错点</b>：</p>
<ul>
<li>逻辑移位不分正负一律补 0；算术移位右移补符号位、左移补 0。</li>
<li>左移是否溢出：逻辑移位看最高位是否移出 1；算术移位看移出的位是否 ≠ 原符号位。</li>
<li>float 型<b>不能</b>通过左移实现乘 2（移的是尾数，破坏规格化形式），乘 2 应通过阶码加 1。</li>
</ul>
<h5>2.2.3 定点数的加减运算</h5>
<p><b>补码加减运算</b>：</p>
<ul>
<li>[A+B]补 = [A]补 + [B]补（mod 2ⁿ⁺¹）——<b>符号位与数值位一同参与运算</b>，结果自然正确。</li>
<li>[A-B]补 = [A]补 + [-B]补，其中 [-B]补 = [B]补按位取反末位加 1（连同符号位取反）。</li>
<li>"一套电路两种语义"：同一个加法器，符号数/无符号数解释不同，但位级结果一致。</li>
</ul>
<p><b>溢出判别三法</b>（溢出仅发生在<b>同号相加</b>或<b>异号相减</b>）：</p>
<ul>
<li><b>一位符号位法</b>：V = AₛBₛS̄ + ĀₛB̄ₛS（两个操作数同号而结果符号相反则溢出）。</li>
<li><b>进位法</b>：V = Cₙ⊕Cₙ₋₁（最高位进位与符号位进位不同则溢出）。</li>
<li><b>双符号位法（变形补码）</b>：结果双符号位 00/11 正常，<b>01 正溢出、10 负溢出</b>；正确符号位为第一符号位。</li>
</ul>
<p><b>标志位含义总结</b>：ZF 两种数均可用</p><p>OF、SF 仅对有符号数有意义</p><p>CF 仅对无符号数有意义。<b>无符号大小比较</b>：ZF=1 相等</p><p>ZF=0 且 CF=0 则 A&gt;B</p><p>CF=1 则 A&lt;B。<b>有符号大小比较</b>：ZF=0 且 OF=SF 则 A&gt;B</p><p>OF≠SF 则 A&lt;B。</p>
<p><b>原码加减</b>：现代计算机一般<b>不实现</b>原码加减运算（需先比较绝对值大小，符号由绝对值大的数决定，再做绝对值大减小）</p><p>IEEE 754 浮点尾数虽为原码，但符号独立，尾数按无符号整数送入 ALU 运算。</p>
<h5>2.2.4 定点数的乘除运算</h5>
<p><b>原码一位乘法</b>：乘积符号 = 两符号位<b>异或</b></p><p>数值 = 绝对值相乘。迭代式：部分积 Pᵢ = (Pᵢ₋₁ + X×yᵢ) 右移 1 位，共迭代 n 次，用 2n 位寄存器（X 存被乘数、P 存部分积、Y 存乘数）。</p>
<p><b>无符号乘法电路</b>：X/Y/P 三个寄存器 + 计数器控制迭代次数</p><p>每次根据 Y 的最低位决定是否加 X，然后 [P:Y] 联合右移</p><p>n 次后 [P:Y] 的 2n 位即为结果。若高 n 位（P）非 0，则超出 n 位表示范围，OF/CF 置 1。</p>
<p><b>补码一位乘法（Booth 算法）</b>：增加辅助位 y₋₁（初始 0），根据 <b>yᵢyᵢ₋₁ 判断</b>：01 → 加 X</p><p>10 → 减 X（加 -X 的补码）；00/11 → 空操作</p><p>每次做<b>算术右移</b>（高位补符号位）。若 P 的高位部分不是 Y 的符号扩展，则结果溢出。</p>
<p><b>三种乘法实现</b>：迭代式（2n 个时钟周期，硬件简单）、阵列式（一个周期完成，硬件量大）、移位-加减（顺序实现）。</p>
<p><b>除法运算</b>：</p>
<ul>
<li><b>无符号除法</b>：试商法，余数寄存器 R 与商寄存器 Q 联合左移，被除数 64 位放 [R:Q]（高位补 0），除 0 时产生<b>除法异常（中断）</b>。</li>
<li><b>补码除法</b>：R 中做<b>全符号位扩展</b>；商溢出仅发生在 -2ⁿ⁻¹ ÷ (-1)（结果为 2ⁿ⁻¹ 超出范围）。</li>
<li><b>两个 n 位无符号数相除不会溢出</b>（商一定 ≤ 被除数）；被除数为 0 则商为 0。</li>
</ul>"""
        },
        {
            "section": "2.3 浮点数的表示与运算",
            "html": """<h4>2.3 浮点数的表示与运算</h4>
<h5>2.3.1 IEEE 754标准的浮点数</h5>
<p><b>浮点表示</b>：N = (-1)ˢ × M × R^E。S 为符号位（0 正 1 负）</p><p><b>尾数 M 用原码小数</b>（定点小数）</p><p><b>阶码 E 用移码（偏置）</b>表示以简化比较</p><p>基数 R 通常为 2（也有 4、16）。</p>
<p><b>规格化</b>：尾数最高有效位为 1。IEEE 754 规定规格化数的尾数最高位 1 <b>隐含不存储</b>（隐藏位），因此单精度实际有效位为 24 位（1+23），双精度 53 位（1+52）。</p>
<p><b>IEEE 754 格式</b>：</p>
<ul>
<li><b>单精度</b>：1 位符号 + 8 位阶码 + 23 位尾数，偏置值 127，规格化真值 = (-1)ˢ × 1.f × 2^(e-127)。</li>
<li><b>双精度</b>：1 位符号 + 11 位阶码 + 52 位尾数，偏置值 1023，真值 = (-1)ˢ × 1.f × 2^(e-1023)。</li>
<li>规格化单精度阶码 e 取值范围 1~254（全 0、全 1 保留）；双精度 1~2046。</li>
<li><b>表示范围</b>：单精度最小规格化正数 = 1.0×2⁻¹²⁶，最大 = (2-2⁻²³)×2¹²⁷；双精度最小 2⁻¹⁰²²、最大 (2-2⁻⁵²)×2¹⁰²³。</li>
</ul>
<p><b>表2.2 IEEE 754 规格化浮点数的表示范围</b>：</p>
<table>
<tr><th>格式</th><th>最小值</th><th>最大值</th></tr>
<tr><td>单精度</td><td>1.0×2⁻¹²⁶（e=1，f=0）</td><td>2¹²⁷×(2-2⁻²³)（e=254，f=111…1）</td></tr>
<tr><td>双精度</td><td>1.0×2⁻¹⁰²²（e=1，f=0）</td><td>2¹⁰²³×(2-2⁻⁵²)（e=2046，f=111…1）</td></tr>
</table>
<p><b>上溢与下溢</b>：</p>
<ul>
<li><b>上溢</b>：结果绝对值超过最大规格化数（正上溢/负上溢）→ 结果置为 +∞/-∞ 并置溢出标志，默认不中断程序。</li>
<li><b>下溢</b>：结果绝对值小于最小规格化正数但不为零 → 采用<b>渐进下溢</b>：若在非规格化数范围内则以非规格化形式存储（保留部分精度），更小则舍入为 ±0，置下溢标志。</li>
</ul>
<p><b>特殊值</b>：</p>
<ul>
<li><b>阶码全 0、尾数全 0</b>：±0（符号位决定正负，通常等效）。</li>
<li><b>阶码全 1、尾数全 0</b>：±∞（溢出等异常时程序可继续执行）。</li>
<li><b>阶码全 1、尾数非 0</b>：NaN（非数，如 0/0、0×∞ 等无效运算结果）。</li>
<li><b>阶码全 0、尾数非 0</b>：非规格化数——不使用隐藏位，指数固定为 -126（单精度）/-1022（双精度），用于渐进下溢、填补 0 与最小规格化数之间的间隙。</li>
</ul>
<p><b>表2.3 阶码全为 0 或全为 1 时 IEEE 754 浮点数的解释</b>：</p>
<table>
<tr><th rowspan="2">值的类型</th><th colspan="4">单精度（32 位）</th><th colspan="4">双精度（64 位）</th></tr>
<tr><th>符号</th><th>阶码</th><th>尾数</th><th>值</th><th>符号</th><th>阶码</th><th>尾数</th><th>值</th></tr>
<tr><td>正零</td><td>0</td><td>0</td><td>0</td><td>0</td><td>0</td><td>0</td><td>0</td><td>0</td></tr>
<tr><td>负零</td><td>1</td><td>0</td><td>0</td><td>-0</td><td>1</td><td>0</td><td>0</td><td>-0</td></tr>
<tr><td>正无穷大</td><td>0</td><td>255（全 1）</td><td>0</td><td>+∞</td><td>0</td><td>2047（全 1）</td><td>0</td><td>+∞</td></tr>
<tr><td>负无穷大</td><td>1</td><td>255（全 1）</td><td>0</td><td>-∞</td><td>1</td><td>2047（全 1）</td><td>0</td><td>-∞</td></tr>
<tr><td>无定义数（非数）</td><td>0 或 1</td><td>255（全 1）</td><td>f≠0</td><td>NaN</td><td>0 或 1</td><td>2047（全 1）</td><td>f≠0</td><td>NaN</td></tr>
<tr><td>非规格化正数</td><td>0</td><td>0</td><td>f≠0</td><td>2⁻¹²⁶(0.f)</td><td>0</td><td>0</td><td>f≠0</td><td>2⁻¹⁰²²(0.f)</td></tr>
<tr><td>非规格化负数</td><td>1</td><td>0</td><td>f≠0</td><td>-2⁻¹²⁶(0.f)</td><td>1</td><td>0</td><td>f≠0</td><td>-2⁻¹⁰²²(0.f)</td></tr>
</table>
<p><b>实数 ↔ IEEE 754 转换</b>：</p>
<ul>
<li>十进制 → 单精度：转二进制 → 规格化 1.f×2ᵉ → 阶码 = 真值 + 127（如 -8.25 = -1.00001×2³，E=130=10000010）→ 1 位符号 + 8 位阶码 + 23 位尾数。得 -8.25 = 1;10000010;00001000000000000000000 = C1040000H。</li>
<li>单精度 → 十进制：按格式划分后，阶码真值 = 编码 - 127，尾数含隐藏位 1。如 C6400000H = 1;10001100;100...0，真值 = -1.5×2¹³。</li>
</ul>
<h5>2.3.2 浮点数的加减运算</h5>
<p><b>浮点加减五步骤</b>：<b>对阶 → 尾数加减 → 规格化 → 舍入 → 溢出判断</b>。阶码与尾数分开处理。</p>
<ul>
<li><b>1. 对阶</b>：<b>小阶向大阶看齐</b>——阶码小的数尾数右移（右移位 = 阶差）；隐藏位 1 随右移进入小数部分，空出高位补 0；移出的低位保留参与后续运算（不丢弃）。若大阶向小阶看齐则需左移，最高有效位移出造成不可逆精度损失。</li>
<li><b>2. 尾数加减</b>：还原隐藏位后按定点原码小数加减；保留的附加位也参与运算（不存储）。</li>
<li><b>3. 规格化</b>：结果形如 1.xxx×xxx（进位）→ <b>右规</b>：尾数右移 1 位、阶码 +1（只需一次）；结果形如 0.0...01xxx（借位）→ <b>左规</b>：尾数左移、阶码 -1（可能多次）。左规一次相当于乘 2，右规一次相当于除 2。</li>
<li><b>4. 舍入</b>：引入三个辅助位——<b>保护位</b>（紧邻最低有效位之后第一位）、<b>舍入位</b>、<b>粘滞位</b>（其后任一位为 1 则置 1）。四种舍入模式：①<b>就近舍入</b>（默认）：舍去或加 1 使结果最接近；恰在中间时向<b>偶数</b>舍入（末位为 0）；<br>②正向舍入（朝 +∞）；<br>③负向舍入（朝 -∞）；<br>④<b>截断法</b>（朝原点，最简单）。</li>
<li><b>5. 溢出判断</b>：浮点溢出<b>仅由阶码决定</b>（尾数溢出可右规修正）。右规时阶码 +1 若超出最大规格化阶码（如单精度 11111110 → 11111111）→ <b>指数上溢</b>；左规时阶码减至低于最小规格化值 → 进入非规格化范围，再小则 <b>指数下溢</b>置机器零。</li>
</ul>
<p><b>易错点</b>：对阶时小阶向大阶看齐丢失的是<b>低位</b>（可舍入修正）</p><p>若大阶向小阶看齐丢失<b>高位</b>，精度错误不可逆。</p>
<h5>2.3.3 C语言中的浮点数类型</h5>
<p><b>类型对应</b>：float 对应 IEEE 754 单精度、double 对应双精度、long double 通常为扩展双精度（依赖平台）。隐式类型转换序列：char→int→long→double、float→double</p><p>混合运算时<b>低类型自动提升为高类型</b>（如 int 与 long 运算结果为 long）。</p>
<p><b>转换注意事项</b>：</p>
<ul>
<li><b>int → float</b>：不会溢出，但 float 尾数（含隐藏位）仅 24 位有效，int 的二进制有效位超过 24 位时需舍入，<b>精度损失</b>。</li>
<li><b>int/float → double</b>：double 有效位更多，通常能精确表示原值。</li>
<li><b>double → float</b>：范围变小可能<b>溢出</b>；有效位变少可能<b>舍入误差</b>。</li>
<li><b>float/double → int</b>：小数部分<b>直接丢弃（向零截断）</b>；浮点值超出 int 范围则整数溢出。</li>
</ul>
<h5>2.3.4 数据的宽度和存储</h5>
<p><b>数据宽度与单位</b>：<b>比特 bit</b> 是最小信息单位</p><p><b>字节 B</b> = 8 位，是基本存储和寻址单位</p><p>容量前缀按 2 的幂定义（1KB = 2¹⁰B = 1024B）</p><p><b>字 word</b> 是由体系结构定义的逻辑单位（常见 2/4/8 字节）</p><p><b>字长</b> = CPU 内部整数运算数据通路宽度 = 通用寄存器宽度（"32 位机/64 位机"即指字长）。注意：x86 架构自 80386 起字长 32 位，但体系结构仍把 16 位定义为"字"、32 位叫"双字"——字是架构约定，字长是硬件能力。</p>
<p><b>大小端存储</b>：现代计算机普遍<b>字节编址</b>（每地址 1 字节），多字节数据占连续单元，排列方式有两种：</p>
<ul>
<li><b>大端方式</b>：MSB（最高有效字节）存低地址，字节顺序与十六进制书写顺序一致。</li>
<li><b>小端方式</b>：LSB（最低有效字节）存低地址，字节顺序与书写顺序相反。</li>
<li>例：int 0123 4567H 存入地址 0800H 起——大端为 01H,23H,45H,67H；小端为 67H,45H,23H,01H。</li>
<li>读小端机器码需按逆序组合：如指令中立即数 0x08049464 在内存按 64H,94H,04H,08H 存放。</li>
</ul>
<p><b>边界对齐</b>：32 位系统中，字节可位于任意地址、<b>半字地址须为 2 的倍数、字地址须为 4 的倍数</b>。对齐时 CPU 一次访存即可读取完整数据</p><p>不对齐则需两次访存并拼接，效率大降。编译器必要时填充空白字节（以空间换时间）。RISC 普遍采用边界对齐以支持高效流水线。</p>
<p><b>结构体对齐规则</b>：①每个成员的起始地址必须是其对齐值的整数倍（char=1、short=2、int=4）</p><p>②整个结构体大小必须是最大成员对齐值的整数倍（不足尾部填充）。例：struct A{int a; char b; short c;} 大小 8</p><p>struct B{char b; int a; short c;} 大小 12（成员顺序影响占用空间）。</p>"""
        }
    ]
}
