# -*- coding: utf-8 -*-
"""计组笔记 · 第1章 计算机系统概述（王道2027结构，合并袁书补充）"""

CH1 = {
    "chapter": "第1章",
    "title": "第1章 计算机系统概述",
    "sections": [
        {
            "section": "1.2 计算机系统层次结构",
            "html": """<h4>1.2 计算机系统层次结构</h4>
<h5>1.2.1 计算机系统的组成</h5>
<p>一个完整的计算机系统由<b>硬件 + 软件</b>组成。硬件是有形的物理装置</p><p>软件是在硬件上运行的程序及相关的数据与文档。系统设计必须合理划分软/硬件功能边界：使用频繁且硬件实现成本低的功能宜由硬件实现以提升效率。</p>
<h5>1.2.2 计算机硬件（冯·诺依曼机）</h5>
<p>冯·诺依曼在研究 EDVAC 时首次提出"存储程序"思想，基于该思想的计算机统称冯·诺依曼机，主要特点：</p>
<ul>
<li>采用<b>"存储程序"的工作方式</b>：程序和初始数据预先存入主存，启动后自动连续地取指执行，无须人工干预。</li>
<li>硬件系统由<b>运算器、控制器、存储器、输入设备、输出设备</b>五大部件组成。</li>
<li>指令和数据在存储器中以<b>相同形式</b>存放，仅凭内容无法区分，但计算机应能识别（通过取指/执行阶段区分）。</li>
<li>指令和数据均采用<b>二进制</b>编码表示。</li>
<li>指令由<b>操作码</b>（指明操作类型）和<b>地址码</b>（指出操作数地址）组成。</li>
</ul>
<p><b>现代计算机的功能部件</b>：CPU（运算器+控制器集成）、存储器、输入/输出控制器（I/O 接口）、外部设备、总线。</p>
<ul>
<li><b>CPU</b> = <b>数据通路</b>（ALU + 通用寄存器组 + 多路选择器、内部互连通路等，负责实际运算）+ <b>控制单元（CU）</b>（取指、译码、生成控制信号指挥数据通路）。</li>
<li><b>存储器</b>：现代内存 = 主存 + Cache（传统"内存"仅指主存）；外存分两类——可与主存交换数据的磁盘/固态硬盘，以及长期备份的海量存储设备（磁带、光盘）。</li>
<li><b>外设与设备控制器</b>：外设由物理功能部件 + 设备控制器组成（如键盘接口、显卡、网卡），统称 I/O 控制器或 I/O 接口。</li>
<li><b>总线</b>：部件间传输信息的公共通路。典型多总线系统：CPU 经处理器总线 → I/O 桥接器（北桥芯片）→ 存储器总线接主存、I/O 总线接各类设备控制器。</li>
</ul>
<p><b>易错点</b>：</p>
<ul>
<li>PC 存的是<b>地址</b>（下一条指令在哪），IR 存的是<b>指令本身</b>（当前要执行的指令）。</li>
<li><b>MAR 位数决定寻址空间</b>（2^MAR 个存储单元），MDR 位数 = 存储字长（不是寻址范围）。MAR、MDR 物理上在 CPU 内部，逻辑上属于主存接口。</li>
<li>现代计算机以<b>存储器</b>为中心（早期以运算器为中心）；I/O 通过 DMA 直接与存储器交换数据，不经过运算器。</li>
<li>硬件是基础、软件是扩充；没有软件的计算机称"裸机"。</li>
</ul>
<h5>1.2.3 计算机软件</h5>
<ul>
<li><b>系统软件</b>：保障系统高效正确运行的基础软件，包括操作系统（OS）、数据库管理系统（DBMS）、语言处理程序、网络与分布式软件系统、标准库程序、服务性程序等。</li>
<li><b>应用软件</b>：为解决特定应用问题而开发的程序（科学计算、工程设计、信息处理等）。</li>
<li><b>软/硬件逻辑功能等价性</b>：同一功能既可用硬件实现也可用软件实现，相同规范下逻辑功能等价（如浮点运算可硬件实现也可软件模拟），但硬件效率通常远高于软件。如何划分软/硬件边界是计算机体系结构研究的核心问题。</li>
</ul>
<h5>1.2.4 计算机系统的层次结构与编程语言</h5>
<p><b>多级层次结构（自下而上）</b>：应用（问题）→ 算法与编程 → 操作系统/虚拟机 → <b>ISA（指令集体系结构，软硬件分界线）</b> → 微体系结构 → 功能部件/RTL → 电路与器件。下层是上层的基础，上层是下层的功能扩展。</p>
<p><b>三类编程语言</b>：</p>
<ul>
<li><b>机器语言</b>：0/1 二进制指令序列，是计算机唯一能<b>直接识别和执行</b>的语言。</li>
<li><b>汇编语言</b>：用助记符（mov、add）代替二进制指令，需经<b>汇编程序</b>翻译成机器语言后才能执行。</li>
<li><b>高级语言</b>（C、C++、Java 等）：接近自然语言，通常经编译程序处理：或先编译为汇编再汇编成机器语言，或直接编译成机器语言。</li>
</ul>
<p><b>三类翻译程序</b>：</p>
<ul>
<li><b>汇编程序（汇编器）</b>：汇编语言源程序 → 机器语言目标程序（多为一一映射）。</li>
<li><b>编译程序（编译器）</b>：高级语言源程序<b>一次性</b>翻译为汇编语言或机器语言目标程序，先翻译后执行，生成目标文件（.o/.exe），后续执行不需源程序和编译器。</li>
<li><b>解释程序（解释器）</b>：高级语言源程序<b>逐条翻译逐条执行</b>，不生成独立目标程序，边翻译边执行，需源程序始终参与。</li>
</ul>
<p><b>ISA（指令集体系结构）</b>：计算机软/硬件之间的关键接口，从程序员和编译器视角定义软件可直接使用的硬件功能（指令格式、操作类型、寻址方式、可访问的寄存器等），是软件"感知"到的计算机功能视图，即<b>软件可见部分</b>。ISA 位于软硬件交界处，是软件执行的基础。</p>
<p><b>微体系结构（微架构）</b>：处理器内部实现 ISA 的硬件组织方式——ISA 定义"做什么"，微架构决定"怎么做"（数据通路组织、控制单元实现、流水线级数、Cache 层次、分支预测等）。相同 ISA 可对应多种微架构（如 Intel x86 的 Core、Skylake、Alder Lake 各代）。</p>
<h5>1.2.5 计算机系统的不同用户</h5>
<ul>
<li><b>最终用户</b>：直接操作应用程序（最上层）。</li>
<li><b>系统管理员</b>：配置、管理和维护系统（操作系统层面）。</li>
<li><b>应用程序员</b>：用高级语言开发应用软件（高级语言层面）。</li>
<li><b>系统程序员</b>：开发操作系统、编译器、DBMS 等核心系统软件（机器语言层面，直接面向 ISA）。</li>
</ul>
<p><b>透明性</b>：下层机器的结构特性对上层用户"透明"（看不到、无须关心，与日常"透明=可见"相反）。如 ISA 之下的硬件实现细节对高级语言程序员透明</p><p>IR、MAR、MDR 对所有程序员均透明。</p>
<h5>1.2.6 计算机系统的工作原理</h5>
<p><b>"存储程序"工作方式</b>：程序执行前将指令和数据预先装入主存</p><p>启动后自动逐条取指执行。每条指令执行包括：按 PC 取指令 → 指令译码，PC←(PC)+1 → 取操作数并执行 → 送结果。PC 更新规则：顺序指令下一条地址 = 当前 PC + 指令长度</p><p>跳转指令 = 指令中指定的目标地址。</p>
<p><b>从源程序到可执行文件</b>：以 GCC 编译 hello.c 为例，四个阶段：</p>
<ul>
<li><b>预处理（cpp）</b>：处理 # 开头的预处理指令（如 #include 展开），生成 hello.i。</li>
<li><b>编译（cc1）</b>：翻译为汇编程序 hello.s。</li>
<li><b>汇编（as）</b>：转换为机器语言，生成可重定位目标文件 hello.o（二进制）。</li>
<li><b>链接（ld）</b>：与标准库函数（如 printf）链接，解析外部符号，生成可执行文件 hello。</li>
</ul>
<p><b>指令执行三阶段循环</b>：可执行文件由机器指令构成，指令执行可建模为"取指—译码—执行"循环</p><p>具体硬件实现（控制器、数据通路、时序）详见第 5 章。</p>
<p><b>易错点</b>：</p>
<ul>
<li>编译 vs 解释：编译<b>先翻译后执行</b>、生成目标代码、速度快；解释<b>边翻译边执行</b>、不生成目标文件、跨平台性好。</li>
<li>ISA 是<b>软硬件界面</b>：软件程序员看到的是 ISA（指令系统），硬件工程师实现 ISA。</li>
<li>微程序对<b>所有程序员</b>透明——只有设计控制器的工程师才关心。</li>
</ul>"""
        },
        {
            "section": "1.3 计算机的性能指标",
            "html": """<h4>1.3 计算机的性能指标</h4>
<h5>1.3.1 计算机的主要性能指标</h5>
<p><b>1. 运算速度</b></p>
<ul>
<li><b>吞吐量</b>：系统在单位时间内处理请求的数量。受信息输入内存速度、CPU 取指速度、内存读写速率、结果输出效率等多环节影响，主存存取性能影响显著。</li>
<li><b>响应时间</b>：从用户发出请求到系统返回结果的总等待时间 = <b>CPU 时间</b>（程序实际运行时间）+ <b>等待时间</b>（磁盘/内存访问、I/O 操作等）。</li>
</ul>
<p><b>2. 主频和 CPU 时钟周期</b></p>
<ul>
<li><b>CPU 时钟周期</b>：机器内部主时钟脉冲的宽度，是 CPU 工作的<b>最小时间单位</b>。以相邻状态单元间组合逻辑电路的最大延迟时间为基准确定（流水线中取各流水段最大延迟）。</li>
<li><b>主频</b>（CPU 时钟频率）：主时钟的频率，= <b>1/时钟周期</b>，表示每秒包含的时钟周期数。同型号机器主频越高运算速度越快。</li>
</ul>
<p><b>3. CPI（Cycles Per Instruction）</b>：执行一条指令所需的时钟周期数。不同指令 CPI 不同，机器/程序的 CPI 指<b>平均 CPI</b>。<b>IPS</b>（每秒执行指令数）= 主频 ÷ 平均 CPI。</p>
<p><b>4. CPU 执行时间</b>：</p>
<p><b>CPU 执行时间 = CPU 时钟周期数 ÷ 主频 =（指令条数 × CPI）÷ 主频</b></p>
<p>CPU 性能取决于三个相互制约的要素：<b>指令条数、CPI、主频</b>。如 CISC 可减少指令条数但控制逻辑复杂、时钟周期长、限制主频</p><p>RISC 指令条数多但利于缩短时钟周期、提高主频。</p>
<p><b>5. MIPS（Million Instructions Per Second）</b>：<b>MIPS = 指令条数 ÷（CPU 执行时间 × 10⁶）= 主频 ÷（CPI × 10⁶）</b>。用于不同机器比较有缺陷：各机器指令集架构不同，指令功能强度不等，CPI 与时钟周期也不同。</p>
<p><b>6. FLOPS（每秒浮点运算次数）</b>：</p>
<ul>
<li>MFLOPS：百万（10⁶）次浮点运算/秒；GFLOPS：十亿（10⁹）；TFLOPS：万亿（10¹²）；PFLOPS：千万亿（10¹⁵）；EFLOPS：百亿亿（10¹⁸）；ZFLOPS：十万亿亿（10²¹）。</li>
</ul>
<p><b>2. 基准程序（Benchmarks）</b>：一组专门用于性能评测的典型程序，通过在不同机器上运行同一基准程序比较执行时间来评估性能。局限性：性能常依赖某些关键代码片段，硬件/编译器开发者可能针对性优化导致结果失真，应结合具体应用领域选择并辅以多种评测手段。</p>
<p><b>注意（易混点）</b>：描述<b>存储容量、文件大小</b>时 K/M/G/T 基于 2 的幂次（如 1KB = 2¹⁰B）</p><p>描述<b>速率、频率</b>时基于 10 的幂次（如 1kb/s = 10³b/s）。习惯上容量用大写 K，速率用小写 k，其余前缀 M/G/T 均为大写，需结合上下文判断。</p>
<p><b>常见疑问</b>：主频高的 CPU 不一定性能更高——实际性能还取决于微架构、流水线深度、Cache 容量与层级、指令集效率、位宽、并行能力等（如 IPC 更高的低主频处理器可能反超）。</p>"""
        }
    ]
}
