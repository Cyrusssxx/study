# 最近改动总结 2026-09-01

## 计网题库修复（第一阶段：补缺失题 + 题号 + id 迁移）

### 1. 全节"题库数 vs 教材数"审计（28 节全部核完）
用王道2027《计算机网络》PDF 的 `本节习题精选/答案与解析` 块边界精确统计每节题数，与 `cn.json` 对比：

| 结论 | 数据 |
|---|---|
| 教材单选总数 | 564 |
| 题库原总数 | 558 |
| **缺失题** | **6 道** |

23 节数量一致；5 节缺题：**1.1（缺1）、1.2（缺2）、4.2（缺1）、5.3（缺1）、6.5（缺1）**。

### 2. 定位并补入 6 道缺失题（均为"①-多空综合题"）
用题干+选项完整文本做贪心顺序对齐 + 关键词复核，确认缺的是：
1. **1.1-06**（教材第6题，你点名的）：四种数据交换方式性能比较（4 空），答案 A、A、A、C
2. **1.2-23**：OSI 各层功能（5 空），答案 B、D、C、A、B
3. **1.2-28**：差错发生在哪些层（3 空），答案 A、C、C（教材无解析）
4. **4.2-27**：CIDR 地址块（2 空），答案 D、A
5. **5.3-18**：TCP 三次握手序号（2 空），答案 A、C
6. **6.5-04**：DNS/HTTP 域名解析流程（2 空），答案 C、C

**根因**：当初题库解析器整批跳过了"每空独立选项"的多空综合题（其他科的①题只是题干里含①②场景、仍单组选项，所以没被跳过；计网这 6 道是每个空各带 A-D 选项的真题格式）。

### 3. 题号修复
- 6 题按教材顺序插入正确位置；`number` 重排为 **1..564 连续唯一**，`id` 同步顺延（`cn_0001..cn_0564`）。
- **5 个补齐节 `_pdf_qno` 按教材题号 1..N 赋值**（1.1=17、1.2=38、4.2=72、5.3=61、6.5=19）。
- 其余 23 节 `_pdf_qno` 仍是 None，留待第二阶段逐节核对。

### 4. 前端：多空题支持"每空独立选项"
`quiz.html` 的 `fmtMultiContent` 增强：
- 识别 `(①)`-`(⑩)` 括号空 + 原有 `( )` 空；
- 每空独立选项组走新字段 `q.blank_opts`（数组，每空一个 {A,B,...}）；
- 无 `blank_opts` 时回退 `q.options`（兼容 os/ds 旧共享选项数据）。

### 5. 本地数据 id 迁移（你已确认做）
- 部署 `pwa/data/cn_id_map.json`（558 条 旧 id → 新 id）。
- `backend.js` 新增 `migrateCnQuestionIds()`：页面加载时跑一次，把 IndexedDB 里
  `favorites/wrong/notes/progress` 的 `question_id` 从旧 id 重写为新 id；
  `localStorage['cn_id_migrated']` 标记幂等，失败静默不阻断。

### 6. 验证
| 测试 | 结果 |
|---|---|
| `_test_multi_blank_render.js`（新，每空选项/兼容/代码块防回归） | 10/10 |
| `_test_cn_numbering.js`（新，题号连续/多空结构/图片引用） | 24/24 |
| `_test_cn_id_migrate.js`（新，迁移幂等/范围） | 9/9 |
| 既有 `_test_fav_wrong_reset` / `_test_svg_hl` / `_test_bg_fab_shift` / `_test_daka_render` / `_test_algo_render` | 全过 |

### 7. SW 版本（已重建，commit `13a5afd` 已 push，远程 HEAD 一致）
| 层 | 值 |
|---|---|
| APP_VER | `quiz408-app-22669daa46`（quiz.html / backend.js） |
| DATA_VER | `quiz408-data-647f653b4f`（cn.json / cn_id_map.json） |
| LAZY_VER | `quiz408-lazy-b69d9f5707`（懒加载文件数 1002） |

> 用户本地需 `Ctrl+Shift+R` 强刷；首次进入会自动执行 id 迁移（收藏/错题/笔记/作答记录保留）。

### 8. 剩余阶段（下一步）
- **阶段2**：其余 23 节 `_pdf_qno` 逐节核对赋值（复用 `tools/_cn_pdf_audit.py`）。
- **阶段3**：47 张配图全部重新截取（26 张已挂图重截 + 13 张题面补图 + 8 张答案补图）。
- **阶段4**：答案/解析中"下图"引用的 8 处补图。

### 9. 本次工具产物（tools/，供阶段2复用）
`_cn_pdf_audit.py`（节题数审计）、`_cn_full_qs.json`（5 节完整题干）、`_cn_add_missing.py`（补题+重编号脚本）、`_cn_id_map.json`（id 映射源）。
备份：`archive/backups_questions/cn.json.bak_0901`（改动前 cn.json 全文）。

---

## 第二阶段：全库 `_pdf_qno` 补齐（commit `eba2574`，已 push）

### 做法
- 抽取**全部 28 节**完整题干（`tools/_cn_full_qs_all.json`，含题干+选项）。
- 逐位顺序校验：23 节（题库 vs 教材）**顺序完全一致**；唯一低置信位 4.4-25 是图内文字噪声，实为同题。
- 逐节按位置赋 `_pdf_qno`：**564 题全部非空、每节内 1..N 连续**（与教材题号一一对应）。

### 验证
- `_test_cn_numbering.js` 扩展为校验全部 28 节（21 项通过）；其余 6 套回归全过。

### SW 版本（重建）
| 层 | 值 |
|---|---|
| APP_VER | `quiz408-app-22669daa46`（未变） |
| DATA_VER | `quiz408-data-d7545c3b63`（cn.json 更新） |
| LAZY_VER | `quiz408-lazy-b69d9f5707`（未变） |

### 剩余阶段
- **阶段3**：47 张配图全部重新截取（26 张已挂图重截 + 13 张题面补图 + 8 张答案补图）——最重的一步。
- **阶段4**：答案/解析中"下图"引用的 8 处补图。

---

## 第三阶段：配图裁剪（v2 已挂图 15/26 张成功，commit `8ce5c9d`；下阶段基础设施 commit `1b0af7e`）

### 关键发现：PDF 是整页位图 + OCR 文本层
- `get_drawings()` = 0（无矢量），`get_image_info()` 仅一张全页大图 → 王道 PDF 是扫描版，每页 1 张 ~2254×3141 位图叠 OCR。
- 因此**图区域检测**只能靠 OCR 文本坐标（题干末行 y1 → 首选项 y0），**再渲染该带后按暗像素收紧 bbox**。

### v2 cropper（`tools/_cn_crop_figs_v2.py`）
- 区分同页图 / 跨页图（图在下一页页首）；marker 紧跟图也兜底；2× DPI 渲染，3× 易 OOM。
- 本批 15 张成功：cn_0012/0013/0015/0246/0254/0330/0335/0340/0342/0345/0348/0379/0408/0519/0541，目检样张图边界正确、标签完整。
- 11 张失败：选项子串在 PDF 难定位（选项跨行 / 含图中标记），待 v3 用题干题号 regex 重定位。

### 子进程隔离（防 xref 损坏页 segfault）
- 部分页 xref 损坏（`cannot find object in xref 3642`），pymupdf 原生代码 segfault，**Python try/except 抓不住**。
- 唯一可靠隔离 = `subprocess.run` + `returncode`（子进程崩溃父进程仅得非零 rc）。`tools/_cn_crop_one.py` 单题渲染 + 裁剪。

### 14 张缺失题面图本轮 0/14
- 关键障碍：**bank 文本与 PDF 文本不完全一致**（题库解析时被清洁过，如"若某分组交换网络"→"某分组交换网络"），`kw in page_text` 关键词定位多处失效。
- 解法方向：改用**题干题号** regex 定位（`page.search_for(r'^\s*16\.', regex=True)`）替代内容关键词，绕开 bank↔PDF 文本差异。
- 提交了子进程脚本 + 主调用脚本作为下阶段基础设施（commit `1b0af7e`），不提交任何 cn_figs/cn.json 改动。

### 待下阶段
- v3 cropper：用题号 regex 定位重跑 14 缺失题面图 + 8 答案图 + 11 v2 失败（共计 ~33 张）。
