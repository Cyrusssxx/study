# 最近改动总结 2026-08-29

## 刷题页：新增「清空收藏错题」按钮（commit 09a5e02）
- **背景**：收藏模式原本只有「清空重做 ⟳」，它把收藏题的**作答记录 + 错题记录一起清空**（重刷一遍）。缺一个中间档：把已经会了的收藏题从错题本移走，但正确率和作答记录不该跟着丢。
- **用法**：收藏 / 收藏+真题模式下，头部与「清空重做」并列出现「清空收藏错题 🗑」，带实时计数（如「清空收藏错题 5」）。
  - 0 条时按钮**置灰禁用**，不点进去才弹窗说"没有"。
  - 点下后会再取一次真实条数进确认框——如果用户刚在页面上又答错几题，按钮上的旧数字不会写进确认框。
  - 取数失败时确认框不带数字照常执行（宁可多问一句，别漏删）。

### 后端（pwa/js/backend.js）
| 接口 | 删哪些 | 保留 |
|---|---|---|
| `/api/favorites/reset/<科目>` | progress + wrong | 收藏本身 |
| `/api/favorites/reset-wrong/<科目>` | 只删 wrong | progress（作答/正确率）+ 收藏 |

- 两个接口共用同一段删除循环，`scope` 变量选 store 列表；**收藏 store 永远不动**。
- 返回值语义修正：`cleared` 从「收藏题数量」改为「**实际删除行数**」，并加 `scope`（all\|wrong）与 `favs`。原客户端没读 `cleared`，无兼容风险。

### 前端细节（pwa/quiz.html）
- 计数交集用 `quizAllQuestions` 而非 `questions`——**年份筛选后 `questions` 只是子集**，拿它算会少报。
- 计数**不加 `include_resolved`**：按错题本实际显示的来数（连对 2 次已移出的历史记录不计入），否则按钮写 5、错题本里只看得见 3 个。
  但真删时仍按「该题的全部错题记录」删（含已解决）——这些收藏题彻底移出错题本，不会因残留已解决记录又回到 30 天复盘。
- 全量「清空重做」后同步刷新该按钮计数（否则按钮上残留旧数字）。
- `MODE` 切换时的隐藏列表补上了新按钮（原本漏掉会导致切模式后按钮残留）。

### 样式（pwa/css/style.css）
- `.opt-shuffle-btn:disabled`：opacity .45 + not-allowed。
- `.quiz-info` 加 `flex-wrap: wrap`：头部现在 7 个控件，窄屏换行而不是横向溢出。

### 验证
新增 `_test_fav_wrong_reset.js`（**29 项全过**）：jsdom + `fake-indexeddb` **真实执行 backend.js**，用真实 API 造数据（收藏 os_1/os_3，答错 os_1/os_2，答对 os_3），断言作用范围：
- `reset-wrong` → 只删收藏题的 wrong 行；progress 一条不动；**未收藏的 os_2 错题记录不被误删**；收藏保留
- `reset` → 删 progress + wrong；os_2 的 progress 保留；收藏保留
- 边界：非法科目 404 / 无收藏科目 cleared=0 / 重复调用幂等

**为什么要这个测试**：两个接口共用删除循环，而 **key 路径不同**（progress 用 autoIncrement 的 `r.pk`，wrong 用 `r.question_id`）。改错一处是**静默删不掉或误删**，页面上看不出来。

**教训**：测试期望值我一开始写错了（以为 os_2 的错题行也会被清），跑出来才发现是期望错、代码对——这正是它的价值。

## 🎨 背景按钮撞位修复（commit 28f403b，同日）
- **现象**：笔记页 / 算法讲义页滚动过 400px 后，「回到开头」按钮出现在右下角，与 🎨 完全重叠；页面按钮 z-index 2300 > 🎨 200 → **🎨 被整个盖住点不到**。
- **根因**：旧避让逻辑是写死的选择器名单 + 固定偏移，笔记页改版后右下角按钮换成 `.notes-top-btn`，名单里没有它；且 `.bg-fab-shift` 只在 `≤900px` 生效，桌面端加了类没有任何 CSS 作用。
- **改为实测避让**：取 🎨 默认停靠位，与右下角 240px 区域内所有**可见** fixed 按钮做矩形相交测试；撞了就抬到最靠上那个之上留 12px，没撞就回默认位。滚动/resize 时 rAF 节流重测。
- **关键坑**：参考矩形**不能读 fab 当前的 bottom**——抬过一次再测会拿"已抬高"的位置判断，误判为无碰撞然后落回原位抖动。
- 回归测试 `_test_bg_fab_shift.js`（13 场景全过）。**新增右下角 fixed 按钮要补进 `FAB_SEL` 名单**。

## 版本号
| 层 | 值 | 变化原因 |
|---|---|---|
| APP_VER | `quiz408-app-9b9b9aab90` | 见下方示意图修复（notes.html / map.html 属外壳层） |
| DATA_VER | `quiz408-data-6c8b3e3b7d` | 未变 |
| LAZY_VER | `quiz408-lazy-32ab9cfdfe` | 未变 |

## 提交记录
| commit | 内容 |
|---|---|
| `1dd2d6f` | 修复示意图(SVG)高亮后文字消失 + 回归测试 `_test_svg_hl.js` + 重建 sw.js |
| `62eb514` | 「清空收藏错题」计数按错题本实际显示来算（去掉 include_resolved） |
| `ebd69de` | 本份总结入库 |
| `09a5e02` | 收藏模式新增「清空收藏错题」+ 回归测试 |
| `28f403b` | 🎨 背景按钮实测避让 + 回归测试 |

五笔均已 push，远程 HEAD 一致。

## 示意图(SVG)高亮后文字消失修复（commit 1dd2d6f）
- **现象**：笔记页 / 导图页对示意图（电路交换 vs 报文交换 vs 分组交换那张）里的文字划词高亮后，文字消失，强刷后依旧——用户原话"现在没了"。
- **根因**：划词高亮把选中文本节点用 `range.surroundContents(span)` 包进一个 HTML `<span>`。SVG 的 `<text>` 节点里是真实文本，但 **SVG 渲染器不渲染 `<text>` 内部的 HTML `<span>` 元素** → 文字不显示。更糟的是高亮是持久化的（`saveHl` / `loadHighlights`），每次加载都按偏移重新包裹，于是**永久消失**。笔记页 `notes.html` 与导图页 `map.html` 是同源逻辑，两处都中招。
- **修复**（`notes.html` + `map.html` 同步）：
  1. `hlWalkFilter` 新增 `inSvg()`，把 SVG 子树整体排除 → `offsetInRoot` / `cleanTextOf` / `wrapRange` 三处偏移计数与包裹口径一致地跳过 SVG 文本；
  2. `showHlToolbar` 选区触及 SVG 时直接隐藏工具条，**禁止对示意图发起高亮**（📝批注本就因无 `.anno-line` 锚点 no-op）；
  3. `wrapRange` 兜底：即便偏移落到 SVG 文本节点，也绝不把 HTML span 包进去；
  4. `loadHighlights`(笔记) / `reapplyHighlights`(导图) 丢弃无法定位的失效高亮（含旧 SVG 项），并 `saveHl()` 落盘清洗——**旧的坏高亮记录不再复现，文字恢复后不会反复消失**。
- **回归测试** `_test_svg_hl.js`（**6 项全过**）：从 `notes.html` **逐字提取真实函数源码**在 jsdom 执行，断言：
  - SVG `<text>` 文本保持原样、内部未插入 `.note-hl` span（文字不会消失）
  - 普通正文高亮仍正常生成 `.note-hl` 包裹、内容正确、整体文本不被破坏
  - `cleanTextOf` 跳过 SVG 文本，仅计正文（偏移口径一致）
- **用户侧动作**：本沙箱已强刷验证路径；**你本地需 `Ctrl+Shift+R` 强刷**一次，才能拉到新外壳（`APP_VER` 已变）。强刷后那张示意图文字会自动恢复。
