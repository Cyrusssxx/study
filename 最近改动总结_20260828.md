# 最近改动总结 2026-08-28

## CO 题库答案纠错（第4章 4.3 程序的机器级代码表示）
- 题目：**co_0328**（number 328，第4章 指令系统 / 4.3 程序的机器级代码表示）
  - 原题：`R[ax]=7FE6H，R[bx]=FFE8H`，执行 `sub bx, ax`
  - 错误：标记答案 B 把 **OF 错标为 1**；但原解析自身写的是"无溢出，OF=0"——**答案键与解析自相矛盾**。
  - 正确结论：**R[bx]=8002H, OF=0, SF=1, CF=0, ZF=0**
  - 修复内容：
    1. 选项 B 的 `OF=1` → `OF=0`，使唯一正确答案成立（答案键仍 B）；
    2. 补全被截断/错乱的 `explanation`（原在 `=====` 处截断），重写为分步骤、含 CF/OF/SF/ZF 推导的完整解析；
    3. 题干两处笔误：`寄存器的内存`→`寄存器的内容`、`subbx`→`sub bx`。
  - 计算核验（CPU 风格 16 位补码）：bx=FFE8H−7FE6H=8002H；CF=0（无符号被减数 65512 ≥ 减数 32742，不借位）；OF=0（减法转 bx+(~ax)+1=FFE8+8019+1=18002→取低16位 8002，两加数符号位均1、结果符号位1，符号位进位=最高位进位，无溢出）。
- 全 CO 标志位类题目扫描（选项含 OF/CF/SF/ZF 共 **6 道**），逐道核验：
  - co_0327（add ax,bx，4.3）→ ✅ 正确（ax=7FCEH, CF=1, OF=0, SF=0, ZF=0，答案 C）
  - num99（2.2 减法 FFFFFFFF−FFFFFFF0）→ ✅ 正确（CF=0, OF=0，答案 A）
  - num100（2.2 x=10,y=−20）→ ✅ 正确（OF=0, CF=1，答案 B）
  - num314 / num318（4.2 bgt 条件转移，概念题）→ 答案键与公认 408 真题一致，**未改动**（时）；num318 当时标注为观察项——见下方本轮第二项修复。

## CO 题库答案纠错（第4章 4.2 / 2011 统考真题 bgt）
- 题目：**co_0318**（number 318，第4章 指令系统 / 4.2 指令的寻址方式）【2011 统考真题】
  - 题干：条件转移指令 `bgt`（无符号整数比较大于时转移）的转移条件。
  - 错误：四个选项原存储为 `CF+OF=1 / SF+OF=1 / CF+ZF=1 / CF+SF=1`（即"=1"形式）。**"=1" 是印刷/转抄错误**：无符号 `A>B` 的真条件是 **CF=0 且 ZF=0**，即 `CF+ZF=0`；无横线的 `CF+ZF=1` 实际表示 A≤B，与原解析自述"CF和ZF均为0，选C"直接矛盾。
  - 查证：2011 年统考**原题标准选项即写作"=0"**（见真题详解：A.CF+OF=0 B.SF+ZF=0 C.CF+ZF=0 D.CF+SF=0，答案 C）；部分重印本误写成"=1"，正确等价写法是加反相横线 ¬(CF+ZF)=1。用户所问"取反标识没"即指此反相缺失。
  - 修复内容：
    1. 四个选项由 `=1` 还原为权威的 `=0` 形式（A.CF+OF=0 / B.SF+ZF=0 / C.CF+ZF=0 / D.CF+SF=0）；
    2. 答案键仍为 **C**（与官方一致，且逻辑现已自洽）；
    3. 重写 `explanation`：明确 `A>B ⟺ CF=0 ∧ ZF=0 ⟺ CF+ZF=0`，排除含 SF/OF 的 A/B/D，并注明本题源自 2011 原题、标准即"=0"、重印本"=1"为常见印刷错误。
  - 另一道 bgt 概念题 **co_0314**（"大于零转移"，选项本就用"=0"）经核查无此错误，**未改动**。
- 提交 **3d11f8c**，已 push，远程 `main` HEAD 一致。DATA_VER→`quiz408-data-2b21aa6b71`（需 Ctrl+Shift+R 强刷生效）。
- 结论：**第4章乃至全 CO 中，仅 co_0328 这一道实际有误**，其余标志位题均正确。若你在刷题时还发现其他具体题号有误，告诉我题号我逐道核算。
- 提交 **d3eab2a**，已 push，远程 `main` HEAD 一致。DATA_VER→`quiz408-data-506c0931a9`（需 Ctrl+Shift+R 强刷生效）。
