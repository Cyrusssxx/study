# 408-quiz 项目总结

> 离线 408 计算机考研（王道教材）刷题 + 笔记 PWA。本地优先、断网可用、数据全在设备端。
> 最后更新：2026-08-29｜当前提交 `a230395`｜远程 `git@github.com:Cyrusssxx/study.git` 分支 `main`

---

## 1. 项目定位

| 项 | 内容 |
|---|---|
| 用途 | 408 四科（OS / 计组 CO / 数据结构 DS / 计网 CN）题库刷题、错题收藏、统计、章节笔记、思维导图、算法讲义、代码真题、每日打卡 |
| 形态 | 纯前端 PWA（无后端），Service Worker 离线缓存 + IndexedDB 本地存储 |
| 运行环境 | 浏览器安装到桌面 / 手机"添加到主屏幕"；首屏后断网照常使用 |
| 唯一真源 | `pwa/`（桌面 exe 已废弃删除，见 §4） |
| 部署 | 静态文件托管（GitHub Pages / 任意静态服务器），`pre-commit` 钩子自动重建 SW 缓存哈希 |

**技术栈**：原生 HTML/CSS/JS，零框架；数据用 JSON 单源；存储用 IndexedDB；离线用 Service Worker（三层版本隔离）；回归测试用 Node + jsdom + fake-indexeddb。

---

## 2. 页面能力（13 个页面）

| 页面 | 能力 |
|---|---|
| `index.html` | 首页：四科入口、进度概览、背景自定义入口 |
| `quiz.html` | 刷题：四科题库、筛选（章节/年份/难度）、作答持久化、收藏/错题、XButton 侧键翻题 |
| `favorites.html` | 收藏夹（已收藏的题） |
| `wrong.html` | 错题本（含"已掌握"标记与 30 天复习重入机制） |
| `stats.html` | 统计：正确率、章节覆盖、趋势 |
| `notes.html` | 章节笔记：三级目录、可收起侧栏、知识点跳转、教材原图内嵌 |
| `map.html` | 思维导图（OS / 计组），卡片流 + markdown 表格 + 搜索定位 |
| `algo.html` | 一休算法讲义：章节卡 + 真题/例题卡 + 表格（纯教材原图） |
| `code.html` | 数据结构代码真题（18 道）+ 考点/易错点 |
| `daka.html` | 数据结构每日打卡（62 题，纯教材原图） |
| `dati.html` | 大题专项（页状态持久化） |
| `exam.html` | 真题套卷 |
| `search.html` | 全局搜索（仅索引刷题题库，不含打卡表/笔记） |

---

## 3. 数据架构（单源 + 三层缓存）

### 3.1 真源数据 `pwa/data/`

**题库（4 科 + 专项）**

| 文件 | 体积 | 条目 |
|---|---|---|
| `cn.json` 计网 | 510 KB | 558 题 |
| `co.json` 计组 | 556 KB | 603 题 |
| `ds.json` 数据结构 | 571 KB | 632 题 |
| `os.json` 操作系统 | 605 KB | 651 题 |
| `ds_daka.json` 打卡 | 36 KB | 62 题（纯图化，见 §5） |
| `ds_code.json` 代码真题 | 126 KB | 18 题 |
| `dati.json` 大题 | 76 KB | 0（内容在别处） |
| `algo_notes.json` 算法讲义 | 153 KB | 条目化笔记 |

**笔记（4 科单源）** `pwa/data/notes/`

| 文件 | 体积 | 章 | 节 |
|---|---|---|---|
| `cn_notes.json` | 140 KB | 6 | 28 |
| `co_notes.json` | 196 KB | 7 | 27 |
| `ds_notes.json` | 89 KB | 8 | 32 |
| `os_notes.json` | 773 KB | 5 | 19 |

**思维导图**：`co_map.json` / `os_map.json`；**元信息**：`meta.json`（各科目总题量）。

### 3.2 图片资源（懒加载层）

| 目录 | 文件数 | 体积 |
|---|---|---|
| `daka_figs/` | 162 | 6.9 MB |
| `ds_figs/` | 84 | 33.4 MB |
| `os_figs/` | 13 | 1.3 MB |
| `cn_figs/` | 27 | 11.2 MB |
| `dati_figs/` | 703 | 37.4 MB |

### 3.3 三层 Service Worker 缓存（`sw.js` + `tools/build_sw.py`）

由 `tools/build_sw.py` 按"内容 SHA-256 + 路径"自动算出版本后缀，**无需手改版本号**；`.githooks/pre-commit` 在每次提交前自动重建。

| 层 | 变量 | 来源清单 | 体积 | 频率 |
|---|---|---|---|---|
| 外壳 | `APP_VER=quiz408-app-abdb29cf55` | `APP_PRECACHE`（页面/CSS/JS/图标/meta） | 644 KB | 低 |
| 数据 | `DATA_VER=quiz408-data-d6d10f9e89` | `DATA_PRECACHE`（题库/笔记/导图/打卡表） | 5.2 MB | 中 |
| 懒加载 | `LAZY_VER=quiz408-lazy-9fdef590c3` | `LAZY_PREFIXES`（`algo_notes.json` + 5 个 figs 目录） | 90 MB | 按需 |

**第三层（懒加载）的必要性**：懒加载资源既不在 APP 也不在 DATA 清单里，改它时前两个哈希都不变；若运行时沿用外壳命名空间缓存优先，旧缓存会永久命中，修复永远到不了用户设备。fetch 路由对懒加载前缀用 `path.includes('/'+prefix)` **优先判定**（不能用 `endsWith`）。新增懒加载资源只需在 `LAZY_PREFIXES` 加一行，build 脚本自动展开目录并纳入哈希。

---

## 4. 关键约定与已落地决策

- **PWA 为唯一真源**：桌面 exe（PyWebView）已于 2026-08-14 废弃删除，`pwa/` 是 canonical。
- **题库/笔记单源**（2026-08-14 落地）：`pwa/data/*` 为真源；`data/questions/*` 改为 `tools/sync_questions.py` 生成的构建产物（.gitignore，不手工维护）。上游管线均写 `pwa/data`。
- **真源 JSON 序列化格式各文件不同**，改动前必须按字节回比确认再重写（否则整库 diff 爆炸）：`algo_notes.json` 用 `indent=1`；`ds_daka.json` 用 `indent=2` + **CRLF** + 无尾换行；分隔符统一 `separators=(',',':')` → 实际为 `separators=(',', ': ')`。
- **git 提交纪律**：因历史 `core.autocrlf` + 无 `.gitattributes` 闹过整库 M 噪声，已加 `.gitattributes`（`* text=auto eol=lf`）；**提交务必用显式路径 stage，严禁 `git add -A`**。远程改 SSH 后沙箱 `.git/refs` 只读，`git status` 会误报"领先 N"，推送成功与否以 `git ls-remote origin main` 实时查询为准。
- **题库 `number`/`id` 全局连续唯一**：插入/删除题后必须把插入点之后所有题的 `number` 与 `id` 同步顺延，否则与后续章节题号冲突（曾因只顺延单节导致 2.4 死锁节 272/273 撞号）。
- **教材核对资源**：王道 2027 四科 PDF 在 `D:\ai code\408教材`；PDF 文本/表格提取用托管 `pymupdf`（避免沙箱 venv 建不全，直接对托管 python 装包）。

---

## 5. 近期重点改动（2026-08-28 ~ 29）

1. **算法讲义重排版 + 表格修复**（`3d15d17` / `58b7a14`）：仿 os 笔记骨架，去掉目录蓝色链接；用讲义 PDF 还原 3 张损坏表格，并补 SW 第三缓存层（懒加载）。
2. **🎨 背景按钮撞位修复**（`28f403b`）：由死逻辑改为实测矩形相交避让，覆盖笔记/导图/刷题/打卡/大题各页右下角按钮。
3. **刷题页「清空收藏错题」**（`09a5e02` / `62eb514`）：新增只移出错题本、保留作答记录的清理接口；计数口径按错题本实际显示对齐。
4. **打卡表纯图化**（`6922ade`）：`ds_daka.json` 删全部 62 题文字字段（61273 字，150→36 KB，-75.6%），仅留元数据 + `figs` 图引用；`daka.html renderCard` 改为图+文字都缺才占位。

详见 `细节总结.md` 与 `最近改动总结_20260829.md`。

---

## 6. 回归测试（改动后必跑）

| 测试 | 覆盖 | 命令 |
|---|---|---|
| `_test_algo_render.js` | algo 讲义版式/表格左补/真题卡 | `node _test_algo_render.js` |
| `_test_daka_render.js` | 打卡表逐题渲染、图片命中、占位兜底 | `node _test_daka_render.js` |
| `_test_bg_fab_shift.js` | 右下角按钮避让几何 | `NODE_PATH=<workspace>/node_modules node _test_bg_fab_shift.js` |
| `_test_fav_wrong_reset.js` | 收藏清理两接口作用范围（29 断言） | `NODE_PATH=<workspace>/node_modules node _test_fav_wrong_reset.js` |

（托管 node workspace 已装 `jsdom` + `fake-indexeddb`，`--registry=https://registry.npmmirror.com`。）

---

## 7. 部署与生效流程

1. 改 `pwa/` 代码或数据 → 跑对应回归测试
2. `python tools/build_sw.py` 重建缓存哈希（或修改 `pwa/` 后靠 `pre-commit` 自动重建）
3. `git add <显式路径>` 仅 stage 改动文件 → `git commit` → `git push origin main`
4. 推送后用 `git ls-remote origin main` 核实远程 HEAD
5. **用户端 Ctrl+Shift+R 强刷** 触发 SW 更新与新缓存下载

> 注意：当前工作树除本次文档外，还有若干**其它会话遗留的未提交改动**（`CHANGELOG.md`、`add_chapters.py`、`archive/` 大批脚本删除等），本次文档提交均按显式路径 stage，互不牵连。
