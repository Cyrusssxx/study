# 408-quiz 数据生成管线依赖
#
# 项目已废弃 exe / Flask 桌面端，PWA（pwa/）为唯一前端。
# 因此无需 flask / werkzeug / pyinstaller。
#
# 以下仅为一次性数据脚本（parse_pdf.py、extract_*.py 等）所需的第三方库。
# 运行前：pip install -r requirements.txt

PyMuPDF              # fitz，PDF 解析与文本/图片提取
pdfplumber==0.10.3   # PDF 表格提取
Pillow               # PIL，图片压缩与处理
numpy                # 数值处理
rapidocr-onnxruntime # OCR：答案/解析提取
opencv-python-headless # cv2，图像处理（headless 版，无 GUI 依赖）
