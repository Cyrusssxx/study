# 最近改动总结（2026-08-21 ~ 08-22）

> 范围：自侧键拦截修复起，到计网示意图混合方案落地，再到 08-22 大题专项状态持久化、刷题页刷新恢复位置、OS 缺页处理笔记。
> 最新提交：`7fb59d7`（已推送，HEAD == origin/main）。
> 涉及四科笔记数据（`pwa/data/notes/*.json`）、思维导图（`pwa/data/os_map.json` / `pwa/map.html`）、前端样式（`pwa/css/style.css`）、页面（`pwa/quiz.html` / `pwa/notes.html` / `pwa/dati.html`）、Service Worker（`pwa/sw.js`）。

---

## 〇、思维导图：搜索后左侧目录自动定位命中模块（08-22，提交 7fb59d7）

**需求**：在左侧目录框输入关键词搜索后，目录树自动展开并滚动定位到命中模块所处节点，高亮显示，且不破坏原有浏览体验。

**改动**（`pwa/map.html` + `pwa/css/style.css`，APP_VER → `quiz408-app-c1ff204a0c`）：
- `renderNav()` 搜索模式：命中的节下补出**第三层卡片节点**（章 → 节 → 卡片），直观展示模块层级位置；无搜索时保持原两级目录
- 搜索有结果时：目录若处于收起状态自动展开；目录内滚动到第一个命中节点并**黄底高亮**（`.nav-flash`，1.8s 后消失，暗色适配）
- 卡片节点可点击，复用 `gotoNode()` 跳转主区对应卡片
- 清空搜索词 → 恢复原目录，零破坏

---

## 〇、OS 缺页处理：笔记 + 思维导图同步（08-22，提交 4f1b6cd）

**需求**：把「缺页处理完整流程」加入 OS 笔记（很重要），同步 os 思维导图，文本流程渲染成 SVG 图。

**改动**（`pwa/data/notes/os_notes.json` + `pwa/data/os_map.json`，DATA_VER → `quiz408-data-1541e23668`）：
- 笔记 `3.2.2 请求分页管理方式` 追加「4.缺页处理完整流程（重点）」：缺页定义 → 6 步流程详解（①合法性 ②找空闲页框/置换 ③磁盘读入 ④更新页表 ⑤刷新TLB ⑥恢复上下文重执行）→ 从进程角度看（映射建立答疑）→ 总结一句话 → 延伸「如何选牺牲页」指向 3.2.4
- 2 张 SVG 图（class 风格，暗色自适应）：
  - 完整流程图：CPU 访问 VA → MMU 查页表 P=0 → 缺页中断 → 六步处理 → 命中重执行
  - 整体示意图：虚拟地址空间 ↔ 物理内存 ↔ 磁盘 三栏 + 页表更新/TLB/重执行
- 思维导图 `os_map.json` 新增根节点「第3章 内存管理」→「3.2 虚拟内存管理·缺页处理（重点）」4 张卡片（含 2 图），标题改为「操作系统·考点思维导图」
- 顺手修复 3.1 历史遗留裸 `<` 未转义 3 处（`S=2<4` / `W=100<500` / `S=3<4`）
- 校验：os_notes 96 块 html + os_map 9 块 svg 全部通过 XML 校验

---

## 〇、页表本质「一个数组」一句话理解（08-22，提交 88a7e46）

**需求**：把「页表=以虚拟页号为下标、以页表项为元素的数组」这段话做成表格，同上（进笔记 + 同步思维导图）。

**改动**（`pwa/data/notes/os_notes.json` + `pwa/data/os_map.json`，DATA_VER → `quiz408-data-58378b71d6`）：
- 笔记 `3.2.2` 追加「5.页表本质：一个数组（一句话理解）」表格，5 行：连续存储 / 页表的本质 / MMU 做的事 / 缺页处理做的事 / 页表项内容
- 附推论：下标寻址 = O(1)（呼应页表项物理地址公式）；缺页处理本质 = 改一个数组元素（开销极小，慢在磁盘读入）；`[页号]` 越界 = 数组越界 → 越界中断
- 思维导图同节点加卡片「页表本质：一个数组」，d 字段用 `|` 表格行格式（首行表头、空行后接推论段落，map 渲染逻辑已模拟验证）
- 96 块 html 全过 XML 校验

---

## 〇、刷题页：刷新后自动回到上次做的题（08-22，提交 3fab6b6）

**需求**：刷新后自动跳转到之前在做的题目。

**改动**（`pwa/quiz.html`，APP_VER → `quiz408-app-7238aa5e2a`）：
- 复用已有的 `practiceHistory`（`trackPractice()` 每次翻题自动写入），无需新增存储
- `loadQuizQuestions()` 加载完成后：无显式 `chapter`/`goto` 定位时，按 `subject|mode|chapter|section` 匹配最近记录，用 `qid` 定位 `currentIndex`
- 定位优先级：URL 显式定位（chapter/goto）> 历史恢复 > 首题；年份过滤后 qid 不在列表则安全回退首题

---

## 〇、大题专项：页面状态持久化（08-22，提交 81e33d9）

**需求**：刷新后记住当前状态。

**改动**（`pwa/dati.html`，SW 缓存版本同步刷新，APP_VER → `quiz408-app-d2d55967a1`）：
- 持久化筛选（全部/统考真题/收藏）→ `localStorage['datiFilter']`
- 持久化年份筛选 → `localStorage['datiYear']`
- 持久化已展开的答案面板（含答案图填充 + 按钮文案"收起答案"）→ `datiAnsOpen`
- 持久化已打开的笔记面板（含按钮高亮）→ `datiNoteOpen`
- 原有持久化保留：当前科目 `datiSubject`、展开章节 `datiOpenCh`
- 恢复逻辑集中在 `afterRender()`（重新渲染后统一恢复答案/笔记状态），`loadDati()` 恢复筛选与年份 UI

---

## 一、交互层：鼠标侧键 & 侧栏折叠（8 个提交）

**问题链**：鼠标侧键（前进/后退）与浏览器导航冲突 → 折叠侧栏后"完全丢失看不见" → 用户要求改成"笔记目录式独立小按钮"。

**落地方案**：
- `pwa/quiz.html` 侧键**双层防护**：捕获阶段 `mousedown`/`mouseup`/`auxclick` 调 `preventDefault()` + `popstate` 事件兜底（Chrome 进程层仍会触发后退，靠 pushState 同文档锚点抵消）；新增 `sideGuard` 开关。
- 侧栏收起改为**笔记目录那种独立小按钮**（`chapterToggle`/`numberToggle`，`sticky top:82px`），收起态 `width:0 + opacity:0` 彻底消失不留长条，不再有折叠高度塌陷问题。
- 收起状态持久化（localStorage 换 key，修旧残留导致侧栏意外消失）。

---

## 二、笔记三级目录重构（全局推广）

**动机**：OS 第 4 章缺 4.2.7 文件的操作、4.2.8 文件共享等小节，用户要求"按王道教材原目录拆小节，二级目录可展开"。

**数据格式**：
```json
{ "chapters": [ { "chapter": "4",
  "sections": [ { "section": "4.2",
    "subsections": [ { "section": "4.2.7", "html": "..." } ] } ] } ] }
```

**落地**：
- OS 第 4 章先按教材目录拆三级（章→节→小节）。
- **全局推广**到 OS 全 5 章 + 计组（co）：OS 79 小节、co 99 小节（h5 切分）。
- `pwa/notes.html` 重做渲染：三级目录（章→节→小节）、**事件委托**（用 `tocBox.onclick` 替代内联 `onclick` 双引号嵌套截断 bug）、`gotoSpecific` 加 `strict` 参数（`quiz` 页 📖 链入时 strict=true，禁止全局兜底跳错节）、滚动高亮到小节级、`expandTocSec` 自动展开所属节。
- 题目 📖 精准跳到小章节。

---

## 三、内容补全（对照王道 2027 教材）

**修复 3.2 拆分丢内容 bug**（关键）：
- 3.2 虚拟内存用了「主标题 h4 + 数字子标题 h4」嵌套，原映射只认主标题，导致 17 个数字子标题（如 1.传统存储管理、2.局部性原理、1.页表机制、1.OPT…）全漏入节尾。
- 重拆后字数归位：3.2.1=1841 / 3.2.2=1617 / 3.2.3=2817 / 3.2.4=3237 / 3.2.8=1414。
- 修复 2.5.4 漏知识点（补"死锁临界条件"）。

**补全缺失教材小节**（OS 5 个 + 计网 8 个）：
- OS：3.1.5 段页式、3.2.6 页框回收、3.2.7 内存映射文件、3.2.9 地址翻译示例、5.2.6 I/O 操作举例（含表格）。
- 计网（cn）：1.1.2 组成、2.2.2 物理层接口四特性、3.2.3 零比特填充、3.2.4 违规编码、3.8.3 共享式以太网（对比表）、4.3.2 IPv6 基本首部、4.5.3 硬件多播、6.4.3 POP3 和 IMAP。
- 结果：OS 79 小节、cn 79→87 小节全部覆盖教材目录，共 51 个表格。

---

## 四、示意图：ASCII → 内嵌 SVG → 截图混合

**演进**：
1. 先以 ASCII 字符图还原 12 处 OS 核心示意图（`.ascii-fig` 等宽 pre + 浅蓝底虚线框，中文说明放图外）。
2. 用户反馈"这示意图是什么玩意，不能渲染成图片吗" → 全部重绘为**内嵌 SVG 矢量图**（`.struct-fig`/`.struct-svg`，`rect.node/bar` + 箭头 `marker` + `text/lbl`，CSS 变量自动适配亮/暗色）。
3. 用户问"直接用书上截图会不会更省 token" → **混合方案**：SVG 矢量图为主，教材截图仅作示范（计网图 2.3 数字调制 ASK/FSK/PSK 波形，用 pymupdf `page.search_for()` 定位真图标题后 `get_pixmap(dpi=150)` 裁切，输出 `pwa/img/cn_fig_2_3.png`，201KB）。

**覆盖范围**：
- OS：12 张 SVG（图1.1层次 / 2.1内存映像 / 2.2五状态机 / 3.17-18段页式 / 3.27共享内存 / 4.1文件系统层次 / 4.16两级打开表 / 4.17-19链接 / 4.20布局 / 4.21-22位示图成组链接 / 4.23 VFS / 5.16系统调用）。
- 计网：12 张 SVG（交换方式 / OSI vs TCP/IP / 零比特填充 / 滑动窗口 / 停止等待 / IPv6首部 / 距离向量 / UDP数据报 / UDP伪首部 / TCP三次握手 / TCP拥塞控制 / 域名空间树）+ 1 张截图。

**配套修复**：SVG 脚本幂等化（先清后加）、`T` 函数 class 重复、5.3.6 内 `cwnd<ssthresh` 裸 `<` 转 `&lt;`、`<img>` 自闭合。

---

## 五、数据卫生全量修复（四科）

修复 XML 合法性问题（全部小节通过 `ElementTree.fromstring` 校验）：
- `rowspan`/`colspan` 属性未加引号（co 卫生修复 101 处）。
- `<br>` 未自闭合、数学表达式裸 `<` 未转义（如 `dist[u]+w(u,v)<dist[v]`）。
- `AT&T;` 实体、错误标签 `</m>`、未闭合 `<b>`（co 5.3.4）。
- 修复后 os/co/cn/ds 四科数据全部 XML 合法。

---

## 六、Service Worker & 缓存

- `sw.js` 预缓存拆 APP / DATA 两层（`APP_PRECACHE` / `DATA_PRECACHE`）。
- 每次数据改动 `DATA_PRECACHE` 追加新资源、DATA_VER 哈希递增（如 `quiz408-data-54c5a9a1e4`）。
- 计网截图加入 `DATA_PRECACHE`（`'img/cn_fig_2_3.png'`），DATA_VER 17→18。

---

## 未做 / 待确认

- **数据结构化（ds）未拆三级**：ds 仍是节级平铺，若要按教材拆小节需重写，用户未要求。
- 教材整表为图片的 B 类表（如计组 6.1 总线标准对比表）未处理。
- 工作区存在大量未提交改动（`.workbuddy/`、`archive/scratch` 临时脚本、`*.md` 报告等），本次总结不涵盖、未提交。

---

## 提交清单（倒序）

| 提交 | 主题 |
|------|------|
| `7fb59d7` | 思维导图搜索后左侧目录自动定位+高亮命中模块 |
| `88a7e46` | 页表本质「一个数组」一句话理解表格（笔记+思维导图） |
| `4f1b6cd` | OS 缺页处理完整流程笔记 + 思维导图同步（2 SVG） |
| `3fab6b6` | 刷题页刷新后自动回到上次做的题 |
| `81e33d9` | 大题专项页面状态持久化（筛选/年份/答案/笔记面板） |
| `f4f7806` | 计网示意图混合方案：12 SVG + 1 教材截图 |
| `1132b6d` | 计网三级目录 + 内容补全，四科数据卫生修复 |
| `086931b` | 示意图 ASCII → 内嵌 SVG 矢量图 |
| `3c4d03e` | OS 12 处核心示意图 ASCII 还原 |
| `9c54fe5` | 修复 3.2 拆分丢内容 + 补全 5 缺失小节 |
| `1b1ca32` | 三级目录全局推广 + 修小节跳转 + 题目📖精准 |
| `f57bb7b` | OS 第 4 章三级目录（章→节→小节） |
| `1d528d5` | 修复 quiz 📖 跳转到奇怪地方（gotoSpecific 全局兜底） |
| `2f58ba3` | 侧键拦截升级双层防护（popstate 兜底） |
| `82c72a4` | 侧栏改笔记目录式小按钮，收起后完全消失 |
| `aadc32e` | 折叠侧栏高度塌陷修复 + 侧键拦截开关 |
| `9a11853` | 收起态窄条改主色高亮，修复视觉隐形 |
| `7c9fdba` | 上/下一题自定义快捷键，支持鼠标侧键 |
| `40128e3` | 侧栏收起状态存储 key 换新 |
| `86c8611` | 侧栏收起按钮内嵌式，收成 26px 窄条 |
| `f55619d` | 收起动画改抽屉收合，把手改小方块 |
| `0f87748` | 笔记目录收起动画 + 刷题侧栏左右整栏收起 |
| `62424b1` | 导图卡片正文支持 markdown 表格渲染 |
| `9ba4b04` | 移除顶部步骤条 + OS 导图溢出修复 |
| `2a7b688` | 思维导图页改卡片流 + highlight.js 高亮 |
| `589b22a` | 首页加载提速 + SW 预缓存拆层 |
| `7295c04` | OS 2.4 PV 总结新增哲学家多资源解法 |
| `818cf6b` | 笔记页左侧目录可向左收起，状态持久化 |
| `3242cf8` | OS 思维导图改 PV 操作总结版 |
| `53e0a12` | 新增 OS 思维导图，🗺️ 页面不再"制作中" |
