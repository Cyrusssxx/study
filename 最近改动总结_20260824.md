# 最近改动总结（2026-08-24）

> 本次：① 题目→笔记映射升级为**小节级精确定位**；② 「我的笔记」**输入自动保存**；③ **DS 笔记全科细化**（32 节 90 小节对齐王道 2027 目录）；④ 刷题题头改小节题号 + 📖 跳转逻辑重构；⑤ **刷题位置恢复加固**（独立存档键）；⑥ CO 笔记位扩展vs字扩展对比表。
> 提交：`b48f1aa`、`3e23f99`、`d3dd484`、`0846d6a`、`a6f85ed`、`b564e66`、`940132a`、`e488e0f`、`840afdd`（均已推送）。
> 涉及：`pwa/data/{cn,co,ds,os}.json`、`pwa/data/notes/{ds,co}_notes.json`、`pwa/quiz.html`、`pwa/wrong.html`、`pwa/notes.html`、`pwa/js/common.js`、`pwa/sw.js`（DATA_VER `quiz408-data-91fc0b0f55`）。

---

## 一、全量审计结论

四科题库（2444 题）的 `section` 与笔记节/小节名做了全量比对，模拟 notes.html 的匹配链路（精确→别名→编号前缀→关键词）：

| 科目 | 题数 | 节级匹配 | 失败 | 笔记小节数 |
|---|---|---|---|---|
| CN | 558 | 全部命中 | 0 | 87 |
| CO | 603 | 全部命中 | 0 | 99 |
| DS | 632 | 全部命中 | 0 | **0（无小节）** |
| OS | 651 | 命中 | 2（脏数据） | 80 |

## 二、小节级 subsection 字段（核心）

- 离线匹配器：题目题干（去噪）与「所属节内各小节」计算 n-gram 重叠，小节标题命中 ×6 加权 + IDF 抑制通用词；top1/top2 margin ≥ 1.6 才算高置信写入 `subsection` 字段。
- 写入量：CN 392 / CO 376 / OS 362 题；低置信不写、保持原节级跳转（无回归）。随机抽样人工核对正确率良好。

## 三、OS 题库脏 section 修复（63 处）

- `71.3 文件系统` → `4.3 文件系统的实现`（明显笔误）
- `2.4 死锁` → `2.5 死锁`（笔记因插入「PV操作大题总结」重编号）
- 同步修正 UI 上「知识点：… · 71.3 文件系统」这类错误显示。

## 四、前端链接升级

- `quiz.html` 📖 徽章、`wrong.html`「看知识点」按钮：`goto` 参数优先用 `q.subsection`，无字段回落 `q.section`；`q=题干片段` 行级细定位逻辑不变（strict 节内匹配 + TOC 高亮所在小节）。
- DS 题（无小节）行为不变。

## 五、验证

- JSON 合法性校验通过；build.py 刷新 SW 缓存。
- 部署后请 **Ctrl+Shift+R** 强刷验证：刷题页点章节徽章 → 应直接落到具体小节并高亮目录。

---

## 六、「我的笔记」输入自动保存（提交 3e23f99）

- **防抖 800ms**：停止输入 0.8 秒后自动入库，提示「已自动保存 ✓」。
- **输入即落内存 `q.note`**：切题 / 重渲染不丢字（textarea 从 q.note 回填）。
- **双保险冲刷**：切题前（此刻 DOM/currentIndex 仍属旧题，存的是旧题内容）和 `pagehide`（关页/跳转）时立即保存挂起内容。
- **失败静默**：自动保存失败只在提示条显示「请点保存重试」，不弹 alert 打断输入；手动「保存笔记」按钮保留、行为不变。
- 贴图（Ctrl+V）/删图仍即时保存；清空内容 = 删除笔记，语义不变。

---

## 七、DS 笔记全科细化（提交 d3dd484 + 0846d6a）

按王道 2027《数据结构》教材目录（pymupdf 提取 PDF），把 ds_notes.json 从「28 节 0 小节」重构为「**32 节 90 小节**」，与其他三科对齐：

| 章节 | 结果 |
|---|---|
| 第1章 绪论 | 2节4小节 |
| 第2章 线性表 | 3节10小节 |
| 第3章 栈队列数组 | 4节16小节 |
| 第4章 串 | **新增 4.1 节**，共2节6小节 |
| 第5章 树与二叉树 | 5节12小节 |
| 第6章 图 | 4节13小节 |
| 第7章 查找 | **新增缺失的 7.1/7.4 B树B+树/7.5 散列表**，5节12小节 |
| 第8章 排序 | 7节17小节 |

- 原有内容全部归并进对应小节并扩充：表格对比、key-callout 黄框、典型例子演算、易错点归纳，notebook 样式统一。
- 附带修复存量 html 中 2 处未闭合 `<br>` 标签。
- `ds.json` 632 题中 **389 题**写入 subsection 字段 → DS 刷题页 📖 徽章自动获得小节级定位。
- 构建工具 `tools/_ds_lib.py` 保留（load/save + XML 校验 + replace_section），后续 DS 笔记编辑复用。

## 八、练习设置面板：模式热切换 + 复习间隔调整（提交 22a8d07 / 3f6701d / 636adb9 / 833b478 / 8a52b49）

### 8.1 刷题模式页内热切换（8a52b49）
- **backend.js** 新增 `fav_real` 混合模式：既是收藏题又是统考真题的交集
- 原快捷键弹窗升级为**「练习设置」面板**（题头「设置 ⚙」）：上半区练习模式 chips（顺序/收藏/真题/收藏+真题，当前高亮禁点）+ 下半区快捷键设置，统一管理
- `MODE` 改 `let` 支持热切换：切换后题目列表即时重载（无需退出页面），`history.replaceState` 同步 URL（刷新保持），偏好按科目持久化 `quiz_mode_pref_<subject>`
- 进入默认值逻辑：无显式 `mode` 参数且无定位参数 → 沿用上次偏好；首页按钮等显式入口以参数为准（无冲突）
- 竞态防护 `_loadSeq` 序号：快速连续切换丢弃过期响应
- 视觉反馈：题头徽章按模式着色（`data-mode`）；混合模式自动启用「清空重做」与真题年份筛选；空态文案单独提示
- jsdom 渲染级回归 **11/11 通过**（偏好沿用/显式优先/chips渲染/高亮/切换刷新/URL同步/徽章/清空重做/年份筛选）

### 8.2 首页复习入口重构（3f6701d + 636adb9）
- 删除「今日复习」按钮，阶段 chips 升级为主入口：「全部到期·N」高亮 chip + 2/4/7/15 天期分阶段 chip + 即将到期预告；补齐全套 `.review-chip` 样式（含深色模式，此前 HTML 有但 CSS 全缺）
- **修复收藏模式退不出去**：根因是「继续刷题」恢复最近记录时含 favorite 模式；恢复逻辑现跳过 favorite 记录

### 8.3 艾宾浩斯间隔调整（636adb9）
- `REVIEW_INTERVALS [1,2,4,7,15] → [2,4,7,15]`；索引钳制与毕业阈值改为随数组长度自适应（连对 4 次走完全部间隔毕业）

### 8.4 计组笔记 3.6.6「CPU访存全流程」（872ab97 + 833b478）
- co_notes.json 新增小节：核心概念速查表（9概念）、六阶段宏观流程、TLB/PTE 表项构成、缺页中断六步、Cache 三种映射对比、关键判断节点、进程切换动作、易错点，118 处加粗
- 嵌入两张内联 SVG 流程图（主流程图 + VA/PA 地址结构与三映射拆分），着色走 style.css `.co-flow` 类（含深色模式）
- co_map.json 思维导图新增「地址转换全流程」根节点（8 大分支）
